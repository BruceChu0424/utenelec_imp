# 技术选型

> 本档记录 Uten IMP 的核心技术选型及理由。详细决策过程见 [99-决策记录-ADR/](../99-决策记录-ADR/)。
>
> 📍 **现状（2026-07-27）**：前后端全栈已落地，业务模块**全量接真实后端**（详见 §六）。安全为真实落地：Argon2id 密码、JWT 轮换、pgcrypto 字段加密、触发器审计、部门权限 + 个人覆盖 + 按权限点脱敏 + 加密导出 + 系统设置（[ADR-009](../99-决策记录-ADR/ADR-009-后端安全加固与功能补全.md) / [ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md) / [ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md) / [ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)）。

---

## 一、技术栈总览

| 层 | 选型 | 版本 | 选型理由 |
|---|---|---|---|
| 框架 | Flutter | 3.41+ | 一套代码六端（iOS/Android/Web/Windows/macOS/Linux），制造业 IT 团队维护成本低 |
| 语言 | Dart | 3.x | 强类型、空安全、AOT 编译性能好 |
| 状态管理 | Riverpod | 2.x | 类型安全、可测试、无 BuildContext 依赖、社区主流 |
| 路由 | go_router | 最新 | 官方推荐、声明式、Web 深链友好、支持守卫 |
| 设计系统 | Material 3 | 内置 | Flutter 官方支持，主题 token 化好 |
| 国际化 | intl + gen_l10n | 官方 | 不引入额外依赖，arb 文件可协作 |
| 偏好存储 | shared_preferences | 最新 | 轻量偏好（主题/语言/字号/性能档/工作台布局缓存） |
| 敏感存储 | flutter_secure_storage | 最新 | token / refresh（iOS Keychain / Android Keystore，已启用） |
| 设备检测 | device_info_plus | 最新 | 喂给性能分级自动检测 |
| 网络层 | dio | 最新 | 拦截器机制好；已接真实后端，AuthInterceptor 自动加 token + 401 静默刷新 |
| Lint | very_good_analysis | 最新 | 严格规则，团队风格统一 |
| 测试 | flutter_test + integration_test | 内置 | 单元 + Widget + 集成 |

---

## 二、为什么选 Flutter（而不是 React Native / 原生 / Web）

| 候选 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| **Flutter** | 一套代码六端、UI 一致、性能好、Dart 强类型 | 桌面端生态不如原生（但企业后台够用） | ✅ 选定 |
| React Native | Web 生态成熟 | 桌面端支持差（RN 桌面不成熟）、双端 UI 不一致 | ❌ |
| 原生（各端分别做） | 体验最佳 | 5 端 × N 个模块开发量爆炸 | ❌ |
| 纯 Web | 桌面/移动浏览器都跑 | 移动端体验差、离线弱、无法上架 | ❌ |

详见 [../99-决策记录-ADR/ADR-001-技术栈选型.md](../99-决策记录-ADR/ADR-001-技术栈选型.md)。

---

## 三、为什么选 Riverpod（而不是 Provider / Bloc / GetX）

| 候选 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| **Riverpod 2.x** | 类型安全、可测试、无 context 依赖、AutoDispose、AsyncNotifier 一站式 | 学习曲线略陡 | ✅ 选定 |
| Provider（官方旧） | 简单 | 无类型、依赖 BuildContext、复杂状态难管理 | ❌ 已被 Riverpod 超越 |
| Bloc | 强约束、事件流清晰 | 样板代码多、对中小项目过重 | ❌ |
| GetX | 简单 | 架构混乱、反模式多、社区争议大 | ❌ |

详见 [../99-决策记录-ADR/ADR-002-状态管理选Riverpod.md](../99-决策记录-ADR/ADR-002-状态管理选Riverpod.md)。

---

## 四、为什么选 go_router

- Flutter 官方 package（first-party）
- 声明式路由，配置集中
- 支持 Web 深链和浏览器地址栏
- 内置 redirect 钩子，做路由守卫（未登录跳登录页）简单
- 支持嵌套导航（ShellRoute 外壳 + 子页面）

---

## 五、为什么用 Material 3（而不是自建设计系统）

- Flutter 官方支持，ThemeData 直接配
- ColorScheme / TextTheme / ComponentTheme 全套 token
- 响应式断点有官方推荐（WindowSizeClass）
- 团队学习成本低（设计师也熟悉 Material）
- 不选自定义系统：避免重复造轮子，企业后台不需要"惊艳到独一无二"

在 Material 3 基础上用品牌色（深绿+青绿）覆盖默认蓝色。

---

## 六、现状：全栈已落地，业务模块全量接入（不再 Mock）

> 后端 Spring Boot 3 + PostgreSQL + Flyway（见 [ADR-006](../99-决策记录-ADR/ADR-006-后端栈选型SpringBoot+Postgres.md)）。
> **所有业务模块（销售/采购/委外/仓库/生产/钱流/基础资料/HR/鉴权/访客/通知/建议/报销/工资条）已全部走真实后端**。
> 实验室检测 / 空调控制（设备类，未在老库迁移范围）仍为前端实现，待后续接入。

| 维度 | 实现 |
|---|---|
| 数据 | 真实 PostgreSQL（pgcrypto 字段加密）+ Flyway 迁移已推进至 V126；老库十几年业务数据已按模块迁移校验 |
| 网络 | dio + Repository 接口（全部模块为 Dio 实现）；AuthInterceptor 自动加 token + 401 静默刷新 |
| 认证 | Argon2id + JWT（access 15min + refresh 7 天，TTL 可配 + 不透明轮换 + 重用检测）+ 路由守卫 + 首登强制改密 |
| 存储 | 偏好 shared_preferences；令牌 flutter_secure_storage（禁止混用） |
| 加密 | 身份证/手机/银行/薪资 pgcrypto 字段级加密（密钥版本化 `v<version>:`）+ DTO 按权限点脱敏 + 导出 Agile AES-256 |
| 权限 | **部门配置（含上级部门）+ 个人覆盖 + 按权限点脱敏**（角色体系 2026-07-24 下线，[ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md)） |
| 数据导出 | 独立 `*:export` 权限 + Bucket4j 限流 + Agile AES-256（密码入 body）+ 行数上限 + 审计（[ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md)） |
| 系统设置 | 运行时策略阈值可视化 + 二次密码确认 + 审计（[ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)） |
| 错误处理 | 统一错误码（ApiException），不泄露后端细节 |
| 架构 | Repository 接口模式，业务代码与数据实现解耦；报表统一走 `ReportTableResponse` 范式 |

---

## 七、依赖清单（pubspec.yaml 草稿）

```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 路由
  go_router: ^14.0.0

  # 状态管理
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0

  # 国际化
  intl: ^0.19.0

  # 存储
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0    # token / refresh（已启用）

  # 设备
  device_info_plus: ^10.0.0

  # 网络（已接真实后端）
  dio: ^5.4.0

  # 跨端文件下载（加密 Excel 导出：Web blob / 桌面移动写文件）
  path_provider: ^2.1.0

  # 工具
  uuid: ^4.3.0
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  very_good_analysis: ^6.0.0

  build_runner: ^2.4.0
  riverpod_generator: ^2.4.0
  flutter_gen_l10n: ^0.0.0  # 通过 flutter gen-l10n 命令
```

> 实际版本以 `flutter pub add` 时解析的最新 stable 为准。
> 完整依赖清单见仓库根 [`pubspec.yaml`](../../pubspec.yaml)。

---

## 八、版本与升级策略

- **Flutter SDK**：跟随 stable 频道，不追 beta
- **依赖**：每季度 review 一次，major 升级单独安排
- **Dart**：随 Flutter，不单独升级
- **lint 规则**：升级 very_good_analysis 后统一修复

---

## 九、待定项

| 项 | 状态 | 决策时机 |
|---|---|---|
| 图表库（fl_chart vs syncfusion_flutter_charts） | 待定（报表统一表格 `MasterDataTableView` 暂不依赖） | Dashboard 数据可视化阶段 |
| 是否引入 sentry / firebase_crashlytics | 待定 | 部署运维阶段 |
| 是否引入 flutter_dotenv（环境配置） | 倾向是（目前走 `server/.env`） | 多环境部署时定 |
| CI/CD 方案 | 待定 | 部署阶段 |
| prod HTTPS 终结（反向代理） | 待定 | 部署阶段（[ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md) 残留） |
| 导出限流多实例共享（Redis） | 待定 | 多实例部署时（单实例 Bucket4j 已够用） |

---

**最后更新**：2026-07-27（业务模块全量接真实后端；移除 hive Mock 依赖；新增加密导出 / 系统设置 / 部门权限模型）
