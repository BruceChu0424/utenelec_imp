# 业务链 · 续作指引（新话题接力）

<!-- PRODUCTION-PLANNING-V195-CURRENT -->
> **2026-08-02 续作基线**：V191–V195 候选已形成“预排草案、物料详情与可编辑执行分段、审核原子下达、MAKE/BUY/SUBCONTRACT/DRAW 单据回查、A4 生产执行工卡、车间可修改建议学习、MAKE 完工回供与反向”的新数据闭环。处置分为 `READY`、`AUTO_WAIT`、`DEFERRED`；`release-defer` 只允许 DEFERRED 单向人工放行并立即重做齐套。车间/班组/负责人/日期受服务端组织层级与 V194 数据库守卫约束；V195 只刷新迁移后未来写入的审计覆盖，不补历史业务或审计。旧数据零回填；目标库仍为 V190，候选未部署且未完成真实岗位 UAT，生产发布保持 **NO-GO**。不得重复实现、手工补表或把候选验证写成已上线；后续优先做目标库前后快照、真实岗位权限/UAT、实物交接、压力与恢复演练。
<!-- PROCUREMENT-FINANCE-V202-CURRENT -->
> **2026-08-02 采购/委外后置基线**：V196–V202 源码候选将申请改为计划链下达、采购/委外端只读；任务中心可跨申请选行和部分分解，但一个供应商/委外商、一个仓库一张订货单；订货保存即提交精确财务负责人，只有通过才 `status=1` 并形成预计到货。超量先不入库/AP，精确财务负责人全批/自定义/不批，批准量由仓库再审，未批量只交原下单人退回。V202 是全部 `public` 业务表审计 sweep。公司目标库仍为 V190，V196–V202 未部署，完整 IQC、真实岗位/实物 UAT 和发布签字未完成，生产 **NO-GO**。下文 7 月的申请新建预填、同单引入和业务部门审核描述仅为历史证据，不得用于现行操作。


> **用途**：跨会话接力。新话题先读本文件、同目录 01/02/04/05/06 和最新全链路安全复核报告，再按当前实现边界继续。
> **更新日期**：2026-08-02 · **状态**：V90–V100 销售成品预留/发货历史切片保留；V178 生命周期、V187–V190 最小安全链及生产子计划点击已补齐；端到端生产发布仍为 **NO-GO**

---

## 一、当前状态（不要重做）

**不要把“销售成品预留与最小仓库安全链已有实现”写成“销售→生产→仓库→采购/委外全链路已闭环”。** V90–V100 保留订单有效预留等历史切片；V187 已补订单级发运策略和单据级最小仓库状态，V188 已补不可变仓库事件，V189 已补销售退货质量冻结，V190 已补新增业务表后的审计 sweep。V194 候选已补 MAKE 子计划回供父需求的精确 peg、分摊、红冲和重新齐套；尚未完成的是完整 WMS 数量台账、采购/委外质量门、供应商持有库存守恒，以及目标库迁移、真实岗位/实物 UAT 和运维验收。

**2026-07-29 修复**：Flutter 销售/委外 repository 动作端点字符串插值 bug（`'$_doc(id)/approve'` → 请求 URL 拼成方法引用导致 404，前端审核/红冲/改量/取消/财务审核/报价转入/委外审核全哑火）——11 处全部改 `'${_doc(id)}/...'`，E2E 复测通过（建单→审核→预留→生产待排产→红冲复原，测试数据已清）。同日落地统一操作反馈 `lib/core/ui/action_feedback.dart`（`context.guardAction/guardLoad/guardRun`：一行调用自带成功/业务失败/网络失败顶部通知），清完最后 5 处底部 SnackBar 残留，`UtenToast` 改适配层转发 AppNotificationService，三对话框（master/category/department edit）加未捕获异常兜底通知——见 [UtenNotify.md §三](../02-组件库/UtenNotify.md)。

| 段 | 实现 |
|---|---|
| 订货审核/红冲 | 库存检查+**订单有效预留**（减少 ATP，不扣在手、不等于拣货；支持部分预留）/ 对称释放+三挡拦截 |
| 出货审核/红冲 | **V90–V100 历史兼容切片**：超发校验、FIFO 消耗预留、旧红冲重挂；**当前 V187+**：`PICKED → SHIPPED` 才同事务扣库存/消费预留，所有 `SHIPPED` 禁普通红冲，纠错走 V189 退货冻结 |
| 排产/计划红冲 | links+planned_qty 回写+防超排+BOM 缺料标 3 / links 软删+回退 |
| 报工/红冲 | planNo 解析计划行+超报校验+fqty/links.produced+自动生成成品入库单；只有用户显式勾选 is_final 且审核通过，完结缺额才 capped 封顶并自动生成补产草稿，不是发现任意不良就自动投产 |
| 成品入库/红冲 | iqty 分摊回写+入库即补预留+行状态 6/7 / 按源单释放（已发货拒绝） |
| 出货驳回（V96） | 草稿态仓库驳回→部分释放预留→订单行回退→缺口进入计划需求池并重新选路 |
| 改量/取消（V100） | 改量按增量预留/减量回退并在涉及计划量时校验生产权限；取消只允许无发货、无排产/在产/完工关联的订单，任一生产关联均 fail-closed，须先受控清理下游，不能凭生产权限点直接继续取消 |
| 权限 | V91 归属隔离（owner_employee_id + sales:view:all + 变更守卫）；sales_order:price:view 已建 |
| 前端 | 订货工作台（4 统计卡+⚠ 延期标红+链路口径列）；生产调度工作台（待排产+合并排产） |
| 通知接入 | `ChainNoticeService`（afterCommit + REQUIRES_NEW 旁路，逐人异常隔离）：排产/完工/部分完工/补产/发货/驳回/取消确认→订单归属销售，缺料→buyer+planner 角色；`DeliveryDueWarningScheduler` 每日 08:23 延期预警（chain>0 过滤历史单 + 当日去重） |
| 报价转入 | `POST /quotes/{id}/convert`：已审报价一键转订货草稿（行带入+主行 sourceDocNo=报价单号），订货详情回联 sourceQuoteId+行级 quotePrice 比价；Flutter 报价详情「转订货单」+ 订单列表「从报价引入」 |
| 价格脱敏 | `SalesPriceMasker`（V30 同型按权限点）：订单列表/详情/订货明细报表/导出/待交货全口径，无 `sales_order:price:view` 者价格族置 null+priceMasked 标记（数量族保留），Flutter 渲染 *** |
| 批量发货 | `GET /orders/shippable-lines`（可发行 reserved>0，归属同列表）+ `POST /shipments/batch`（按客户分组合并出货草稿，逐行校验）+ Flutter 订单列表「批量发货」右滑面板（勾选/改量/全选/选仓） |
| 守卫修复（V90–V100 历史） | 订货红冲/取消的消耗守卫改净发货量判定（Σ消耗−Σ出货红冲重挂行>0 才拒绝），修复当时“部分发货→红冲出货→红冲订货”被历史部分消耗行误拦；当前 `SHIPPED` 规则不再允许该普通红冲路径 |

**历史重启验证（2026-07-29，仅适用于 V90–V100 当时兼容链）**：Flyway 当时 103+ 迁移全 success；当时端到端复测包含“建订货审核→出货审核→红冲出货→红冲订货”。该记录不得用于证明当前 V187–V190 允许已出库红冲；当前规则以所有 `SHIPPED` 禁普通红冲、纠错进入退货质量冻结为准。同期通知、报价转入、价格脱敏和批量开单证据仍保留为对应历史能力快照。

**2026-07-29 UI 统一（引入按钮 + 上游引入弹窗表格化）**：现场反馈「引入」按钮看不清、弹窗卡片式看不懂，四模块统一重做——
- **按钮**：新建共享组件 `lib/components/buttons/uten_import_button.dart`（深绿底 teal800 + 白字白图标 + 加大 15 号字/20px 图标/44+ 高），销售/采购/委外/财务 4 个编辑页的 `TextButton.icon` 全部替换（财务文案「从应收应付引入」）。
- **弹窗**：四个「从上游/应收应付引入」弹窗统一为**右滑入大面板（840，compact 回退底部全屏弹层）+ Excel 表格**两步式：Step1 选上游单据用 `MasterDataTableView`（单据号搜索 + 客户/供应商/委外商下拉 + 表头排序 + 「表头设置」列显隐 + 分页，只列已审单）；Step2 勾选明细用 `UtenEditableGrid`（showAddRow:false，勾选 + 货品/颜色/单位/单价/上游数量/已收已发/本次数量，全选/反选/取消全选 + 底部「已选 N 行」）。销售 07-28 已做，本次重写采购 `doc_link_picker.dart`、委外 `subcontract_link_picker.dart`、财务 `ar_ap_picker_dialog.dart`（财务单步：搜索 + 台账表格，搜索过滤时勾选/金额不丢，状态按 ledgerId 持有）。返回模型（LinkedItem/SalesLinkedItem/AppliedArAp）与默认数量规则不变，编辑页零改动。`flutter analyze` 五个文件 0 issues。
- **工具**：新增 `flutter-check.sh`（仿 compile-check.sh，通过 `PATH`/`FLUTTER_HOME` 使用项目要求的
  Flutter 版本，可带参数只查指定文件）；README「如何运行」只记录版本约束，不记录个人 SDK 路径。

**2026-07-30 销售报表体系重做 + 单据体验加固**（现场反馈五连，全部落地并实测）：
- **明细行备注贯通**：5 类单据编辑网格末列「备注」录入 + 详情页明细末列展示 + 4 张明细报表末列「备注」（出货/退货原「摘要」列统一改名；订货/其它出货新补 `i.remark`）。后端实体/DTO/读写映射本就齐备，本次为前端接线 + 报表补列；表头设置/导出/打印自动跟随。
- **新建/编辑必填红框**：客户（报价本次起也必填）/仓库（出货类/退货）/币种/交货日期（订货）+ 明细行 货品必选/数量>0/单价必填（其它出货除外）。保存拦截时未过字段红框红字、问题明细行对应格描红边，改值即消；填了内容没选货品的行不再静默丢弃。`UtenDropdownField`/`UtenDateField` 新增 `errorText`。
- **明细报表客户检索**：关键字加客户名匹配（4 类明细报表 + 报价/出货/其它出货/退货单据列表，订货原本就有）；订货/出货/退货明细报表补客户表头 facet；全站 `MasterDataTableView` 表头筛选下拉 ≥6 选项带搜索框；「已选筛选」chip 改友好文案（列中文名 + 客户名，不再暴露 clientName/UUID）；表头选定即刷新（选定直接重查）。
- **汇总报表重做（一行一客户）**：4 张汇总 SQL 重写为按 `client_id` 聚合（客户/区域/单类 + 单据数/数量合计/金额合计〔订货总额/成交金额/实际金额按类型〕+ 隐藏 `__clientId`），单号/开单日期等单行字段全去，默认金额降序；**点客户弹钻取对话框**（该客户本期明细表：分页/排序/行跳源头单据/预览打印〔上限 2000 行〕）；空客户归「(未指定客户)」可钻取；订货总额走 `SalesPriceMasker` 同口径脱敏；导出/打印自动跟随。SQL 对开发库实测：订货 10661 单 → 243 客户行，4 类全过。
- **根因修复（路由）**：`/sales/report` 父路由无条件 `redirect→detail`——go_router 父 redirect 对子路由同样生效，**汇总入口自上线起从未真正到达**（两张卡都开明细）；改精确匹配父路径才跳。仓库报表路由同款 bug 一并修。
- **返回键**：全局 `UtenBackButton` 栈空时先读 `returnTo` 来源（hub 卡片 `goFrom` 写入）回模块 hub、再兜底工作台；销售/采购/委外/生产 8 个详情/编辑页显式 `popOrBackTo` 回各自 hub（覆盖保存后 `replace` 丢 returnTo 场景）。工作台→模块 hub→子页→返回=回模块 hub。
- 验证：后端 compile-check EXIT_OK；flutter analyze 0 error；架构测试通过；汇总/备注列 SQL 探针实测（探针用完已删）。

**2026-07-30（下午）新建预填全家桶 + 引入过滤 + 审核反馈加固**（现场反馈三条，全部落地）：
> **ADR-019 已取代本节部分历史行为**：下方“采购申请人新建预填”和采购申请直引订货仅描述 2026-07-30 当时实现。现行申请无新建/编辑/审核入口；采购/委外从任务中心跨申请部分分解订货并送精确财务负责人。

- **新建预填**（能预填的都预填，仅新建态、均可改、失败静默）：人员类默认当前登录人——销售业务员/发货人、采购申请人/采购员/收货人、委外采购员/经办人、钱流经办人、生产计划跟单员（对方侧人员：交货人/生产工不填）；仓库预填「本类型最近一张单」推广到采购/委外/仓库单据（原仅销售 D1）；销售选客户自动带出主档收货地址（shipAddress→address）+ 联系电话（phone→mobile→phone2，订货合同信息/出货发货信息都接）；出货类「件数」改只读=明细已选货品数量之和取整（选品/改量/增删行联动，保存按明细重算）。
- **客户/供应商/委外商双向同步**（三模块「从上游引入」）：表头已选→面板筛选默认锁定；表头未选→引入确认后回填表头（销售回填后联动地址/电话）；返回模型改 `*LinkPickResult`（items + clientId/supplierId）；采购订货上游=申请单（无供应商）自动跳过。
- **引入面板只列未完成明细**：Step2 统一过滤剩余可引量>0（出货←订货扣已发、收货←订货扣已收、退货扣已退、材料退/损耗←已审发料扣已退+已损耗），「本次数量」默认=剩余量；全部完成时空态提示「已全部完成，无剩余可引入」。**2026-08-01 安全更正**：委外发料←订货已禁用，不再用 legacy `issuedQty` 冒充权威剩余发料量；缺冻结 BOM 快照与子件台账时新增发料审核由前端禁用、服务端 409 拒绝，历史已审发料保留查看/红冲和下游来源兼容。
- **审核"没反应"排查**：E2E 实测链路完全正常（8080 逐级 401/403/404/400；XC26070063 审核被「发货数量超过可发货数量（预留 0）」拦——订单行已上链但 reserved=0，须先入库预留，非 bug）。前端两处真问题已修：处理中底栏消失改常驻 `_BusyBar`；`_busy` 期间点击静默吞改 `appInfo` 提示；出货单未财务审核时点审核的确认文案写明现金客户前置条件。详见 [48-新建预填-引入过滤-审核反馈](../数据迁移/48-新建预填-引入过滤-审核反馈.md)。

**详细实现进度**：见 [02-数据库设计](02-数据库设计-V90-预留台账与计划订单联动.md) §三维护矩阵；当前为 19 条业务行，包含历史 ✅、候选 🟡 与强制锁定 🔒，不得再概括成“13 行全 ✅”。

**2026-08-01 销售现货预留：生命周期 + 稀缺仲裁（V178）**（对标 SAP ATP/OMBN/backorder，详见 [05](05-销售现货预留生命周期与稀缺仲裁.md)）：收口用户提出的三个尾巴——
- **缺口 C 安全库存**：`globalAvailableBase` 扣 `goods.min_qty` + 钳位 ≥0，对齐生产侧 `MrpService` 口径（仅销售审核/改量调用，生产侧另扣不重复；dev 库存量零变更实测通过）。
- **缺口 A 持有逾期**：`stock_reservations.hold_until`（null=用默认交货日+7天宽限动态算，免回填）+ `ReservationHoldScheduler` 每日 08:37 **notify-only** 扫描通知归属销售（不自动释放=数据安全；补三大 ERP 都缺的过期提醒）。
- **缺口 B 稀缺让单**：`sales_order_items.priority` + `POST /items/{id}/priority`（`sales_order:priority`）+ `POST /items/{id}/yield-reservation`（`sales_order:reallocate`，复用 `releaseForOrderItem`+镜像出货驳回 chain_status 回退+防错账守卫+通知）+ `GET /reservations/scarce` 让单面板；**不自动抢占**。
- 新权限 `sales_order:priority`/`sales_order:reallocate` 不自动授予（超管恒有，管理员分配给主管）。Flutter 的订单行操作、急单标识、稀缺仲裁页和 repository 已完成；该结论仅是 V178 当时的定向实施快照，不代表当前候选版本全量验收。
- **未做（刻意）**：自动释放过期预留、自动抢占、7天倒计时、VIP/促销优先、MTO 专属库存（架构级，后续 ADR）、FEFO（无批次数据）——均见 doc05 §六。
- **V178 历史实施说明**：当时需要重启后端应用 V178，dev 库曾验证存量不变；当前候选已在隔离 PostgreSQL 16.14 由 Flyway 迁移/校验至 V190，仍不能据此认定公司目标库已升级。

**2026-08-01 生产子计划点击 + 全流程口径复核**：
- `subplan_links` 的真实自制件子计划进入 `/production/plans/{childPlanId}`；`production_execution_segments` 只是父计划的执行分段，在 `/production/plans/{parentPlanId}?executionSegmentId=...` 内打开详情。生产看板、计划详情和销售生产进度的点击域已统一，空 ID、无权限、加载失败均给反馈。
- MRP 预览与真实子计划列表分开加载，任一失败不拖死另一块；计划 ID 变化时清旧状态并重载。子计划交互定向 33 项属于子集；当前候选又完成 Flutter 全量 278/278、Java 默认套件 589 项（0 failure/error、54 skipped）、定向 JVM 56/56，以及隔离 PostgreSQL 16.14 的 171 个 Flyway 迁移至 V190 和真实 PG 54/54。仍未完成真实登录态浏览器 UAT、公司目标库历史对账和发布签字。
- 销售订单审核后形成的是**订单有效预留**。V187 已实现 `ALLOW_PARTIAL / REQUIRE_COMPLETE / CUSTOMER_CONFIRM` 最小策略，并由服务端在建任务/开始拣货时复核、保存客户确认。仍待发运组、最小分批量、运费/装车约束和公司默认策略配置；齐套远期单一般保留数量承诺，但不到发运时间栅栏不长期占具体库位/批次、不提前拣货。
- 预留释放或稀缺让单后的缺口进入计划需求池，计划员按货品来源策略选择 `MAKE / BUY / SUBCONTRACT / TRANSFER` 并优先挂接已有未来供给，禁止无条件重复建生产单。
- 生产 `READY` 只在所有必需 BOM 物料可原子锁定时成立，`WAITING` 不零散占料；DRAW 审核只是确认需求，仓库逐行 `issue` 才实际扣库存。
- **目标态**要求采购/委外到货先待检隔离、合格后才进入可用和唤醒生产；V201 当前只关闭超量数量授权、库存/AP 防越权和退回责任，不能冒充 IQC。完整 IQC、委外供应商期末结存守恒及真实实物验收仍是 P0。

---

## 二、剩余任务（按优先级）

### ① P0：发布阻断（必须先闭环）
- **发运策略后续完整化**：V187 三策略和客户确认最小闭环已落；仍须配置客户/产品默认策略、发运组、最小分批量、额外运费、装车/整箱约束及客户确认 SLA，并完成岗位 UAT。
- **完整 WMS 数量态**：V187/V188 已有 `PENDING_PICK → PICKING → PICKED → SHIPPED` 单据态及不可变事件；仍缺库位/批次/序列号、实分配/实拣/短拣、复核、装箱、称重、承运件、跨仓调拨与扫码离线。让单/取消继续禁止穿透已开始仓库作业或已出数量。
- 采购/委外质量门：待检库存隔离、合格转可用、不合格/特采/退回及反向；收货不再直接等价于可生产。
- 委外实物守恒：公司物料在供应商处仍是公司库存/WIP，满足“累计发出 = 合格产出对应耗用 + 良/不良退回 + 审批损耗 + 供应商期末结存”，损耗/赔付按合同配置，不写死比例。
- MAKE 父需求闭环：补集成证据证明 V176 子计划完工/入库按 supply peg 回供并唤醒父需求，改量、取消、红冲和重试全部守恒。
- 销售退货客户处置：补“退款结案/换货/补发/维修后返还”的权威选择、审批及对 outstanding/应收/排产的确定影响，未选择前不得默认自动补产。
- 质量处置纠错：`GOOD_RELEASE/SCRAP/REWORK` 登记后补双人复核或追加式补偿命令；禁止改写不可变事件或用整单红冲绕过。
- V90 历史开放订单：逐订单行核对库存、历史已发和旧排产后显式激活；不得批量伪造预留或客户确认。
- 历史数据/权限/实物/运维验收：存量订单、预留、流水不重写；先对账再启用新规则，并完成真实岗位、真实登录态、迁移、备份恢复和告警演练。

### ② 后续增强（不应遮蔽 P0）
- ~~订货工作台"可发货置顶"排序~~ ✅ 已做（`sort=shippable`：Σ行预留>0 排前 + 交货日升序 + 开单日期倒序；Flutter 筛选区「可发货置顶」开关；三单对照实测权重>日期、组内升序正确）
- 报工编辑页"关联计划号"手填 → 计划行选择器（弹窗选已审计划行，直填 planItemId）
- 生产主管有限产能/负荷视图、批次/序列号/FEFO、完整 IQC/IPQC/OQC、返工拆解、模具刀具和精细成本域。
- 通知补强项：出货单加物流单号字段后回填发货通知；延期预警首日 08:23 实发后复核去重口径

---

## 三、接力话术（新话题直接粘贴）

> 继续 uten_imp 项目（从仓库根目录操作）的业务链开发。先读
> `docs/07-业务链路/01-销售订货到发货全链路-SOP.md`、`02-数据库设计-V90-预留台账与计划订单联动.md`、
> `04-生产订单排产与执行全链路需求.md`、`05-销售现货预留生命周期与稀缺仲裁.md`、
> `06-销售退货质检冻结与处置.md` 和
> `docs/99-项目治理/2026-08-01-销售仓库生产采购委外全链路安全复核报告.md`。V90–V100 历史切片、
> V178、V187–V190 最小安全链及 2026-08-01 子计划点击修复不要重做；当前生产发布仍为 NO-GO，
> 优先处理本文件 P0，并保留历史事实。
> 每步做完：后端跑 `bash compile-check.sh`，Flutter 从 `PATH`/`FLUTTER_HOME` 跑
> `flutter analyze`，同步更新业务链文档和 README。数据库连接只从 `server/.env` 或受控环境变量读取，
> 不把开发凭据写入接力文档。当前老库迁移以破坏性全量重建为主，不能断点续跑，也不能假设失败后
> 幂等重放；必须从干净目标库按迁移手册重新执行并完成对账。

---

## 四、工程纪律（沿用 YTDQ.txt 要求）

- Flyway 结构迁移只允许向前演进；老库数据迁移当前是破坏性全量流程，不支持断点续跑或失败后
  原地重放。失败后按手册清理/重建目标库，从受控源快照重新执行并对账；不得用
  `IF NOT EXISTS` / `ON CONFLICT` 掩盖半迁移状态。
- 一次性测试/探针代码用完即删，注释不泄露敏感信息
- 并发：所有审核入口 PESSIMISTIC_WRITE 行锁；预留校验与建行同事务
- 跨模块不建真 FK（契约 §一），联动走 Service 层
- 通知是旁路：必须 afterCommit + 独立事务发送，失败不得影响主事务（参考 ChainNoticeService）
- 文档同步：本目录 01/02/03/04/05/06、对应页面/数据模型/迁移路线图、场景矩阵、治理复核报告和 README；历史测试快照与当前验收结论必须分开写

---

## 相关文档
- [01-销售订货到发货全链路-SOP](01-销售订货到发货全链路-SOP.md)
- [02-数据库设计-V90-预留台账与计划订单联动](02-数据库设计-V90-预留台账与计划订单联动.md)
- [04-生产订单排产与执行全链路需求](04-生产订单排产与执行全链路需求.md)
- [05-销售现货预留生命周期与稀缺仲裁](05-销售现货预留生命周期与稀缺仲裁.md)
- [06-销售退货质检冻结与处置](06-销售退货质检冻结与处置.md)
- [销售出货仓库作业页](../03-页面/销售出货仓库作业页.md)
- [销售退货质检冻结处置页](../03-页面/销售退货质检冻结处置页.md)
- [生产调度工作台](../03-页面/生产调度工作台.md)
- [生产执行分段与报工页](../03-页面/生产执行分段与报工页.md)
- [2026-08-01 销售—仓库—计划—生产/采购/委外全链路安全复核报告](../99-项目治理/2026-08-01-销售仓库生产采购委外全链路安全复核报告.md)
- [2026-07-31 生产全链路实现与复核报告（历史基线）](../99-项目治理/2026-07-31-生产全链路实现与复核报告.md)
