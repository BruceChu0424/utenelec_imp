# 剩余工作交接（2026-08-06）· 多窗口分头执行

> **用途**：全链路审计 + 修复主体已完成（见 [2026-08-05 全链路审计与修复报告](2026-08-05-全链路数据一致性与安全审计与修复.md)、[ADR-023 任务并发控制](../99-决策记录-ADR/ADR-023-统一任务并发控制与认领.md)、[钱流模块审计与修复](2026-08-05-钱流模块审计与修复.md)）。`mvn test` 804 单测 0 失败。本文把**剩余工作**拆成 3 个独立窗口的交接 prompt，供新开 Claude Code 窗口整段粘贴开干。
> **状态基线**：目标库 Flyway V190；V191–V218 候选/本轮新增**均未部署**；生产 **NO-GO**。

---

## 〇、每个窗口都先做的事（通用）

1. 在仓库根目录 `D:\Projects\uten_imp` 开新窗口。记忆库 `MEMORY.md` 会**自动加载**（含 `fullchain-audit-20260805-taskclaim` 总结），无需手动灌上下文。
2. 先读 `docs/99-项目治理/2026-08-05-全链路数据一致性与安全审计与修复.md`（已修什么、剩什么的权威清单）+ 本窗口相关设计文档。
3. **工程纪律**（铁律）：
   - 所有审核/写入口 `PESSIMISTIC_WRITE` 行锁；通知 `afterCommit + REQUIRES_NEW`（`ChainNoticeService` 范式）；**无跨模块 DB FK**（走 Service 层）；SQL 全参数化（排序/表名走白名单 `ReportSort`/`TableSort`）；金额服务端 4 位小数。
   - Flyway 前向、迁移自包含（建列/回填/种子/约束一步到位）；PG 坑：`COMMENT ON IS` 只接单字符串字面量、Java 文本块拼 SQL 注意尾空格、改已应用迁移要修 checksum。
   - 后端改完先 `mvn compile` 再 `mvn test`（或 `bash compile-check.sh` 跑完整 verify）；前端 `flutter analyze` 须 0。
   - **派子代理改后端 = prompt 顶部铁令禁一切 git + 禁 mvn/flutter，由主线程集中验证**。
   - 改完同步更新对应文档（业务链 SOP / 数据迁移 / 安全策略 / ADR / 治理报告 / README）。

## 协调（多窗口并发注意）

- 三个窗口**文件集基本不重叠**：W1=`features/common/taskclaim/*` + `features/expenseclaim/*` + `features/purchase/*` + `lib/`；W2=`features/finance/*`；W3=`features/sales/ret/*` + `features/subcontract/*` + `features/warehouse/inbound/*`。
- 但它们**共用一个 git 工作区**——并发跑若撞同文件会冲突。建议**串行**（一个保存再开下一个）；若并发，先确认不撞文件。
- 建议顺序：**W1（最快出可见成果，且是最初提问）→ W2（授权风险高，需确认矩阵）→ W3（三项大建设，最重）**。

---

## 窗口 1 · 任务并发控制 Phase 2（前端 + 逐面集成）

```
接续 uten_imp 项目（D:\Projects\uten_imp）。做任务并发控制 Phase 2。

后端基础设施已就绪：V215 task_claims 表 + features/common/taskclaim/{TaskClaim,TaskClaimRepository,TaskClaimPolicy,TaskClaimService,TaskClaimController}（show-as-locked，见 ADR-023）。
REST：POST /api/task-claims/{targetType}/{targetKey}/{claim|takeover|heartbeat}、DELETE /api/task-claims/{targetType}/{targetKey}、POST .../force-release。
TaskClaimPolicy 已登记 EXPENSE_APPROVE / PURCHASE_DECOMPOSE / SALES_ORDER_APPROVE / FULFILLMENT_TASK（lease 30/15/30/120 分，claim/manage 权限按类型）。

先读：docs/99-决策记录-ADR/ADR-023-统一任务并发控制与认领.md + 全链路审计报告 §七 + lib/features/hr_task/（HrTaskClaim 前端范式，复用）。

本窗口做：
1) Flutter 统一「XXX 处理中（自 HH:MM）」徽标组件 + 认领/释放/接管操作；审批/分解弹窗打开时每 30s 调 heartbeat 续租；他人动作按钮禁用 + tooltip 显示认领人；自己见「继续/释放」。
2) 后端逐面接线（claim/release + requireNoActiveClaimByOther 守卫）：
   - 费用报销审批：ExpenseClaimService.approve 顶部调 taskClaimService.requireNoActiveClaimByOther("EXPENSE_APPROVE", claimId)；列表/详情 DTO 带 claimView；前端审批弹窗进入 claim、提交/关闭 release。
   - 采购申请→订货分解：PurchaseRequestService 的 decompositionPreview/create 接 PURCHASE_DECOMPOSE（target_key 用 request_item id 或 supplier+warehouse 分组）。
   - （后续 T2/T3）销售订单审核 / 履约工作台按 ADR-023。
3) 验证：flutter analyze 0；mvn compile + 相关单测；新增 task-claim 集成单测。
4) 文档：更新 ADR-023 落地状态 + docs/02-组件库 组件总览 + 相关页面文档。

纪律：动作端点保留既有 PESSIMISTIC_WRITE + 状态守卫，认领只是 UX/防碰撞层（纵深互补）。派后端子代理禁 git/mvn。
```

## 窗口 2 · 财务对象级授权(SEC-MED-4=钱流 M31) + GL 实时过账(FIN-P1-4)

```
接续 uten_imp 项目（D:\Projects\uten_imp）。做 SEC-MED-4 + FIN-P1-4。

背景见 docs/99-项目治理/2026-08-05-全链路数据一致性与安全审计与修复.md（§四 SEC-MED-4）+ docs/99-项目治理/2026-08-05-钱流模块审计与修复.md（M31）。
现状：features/finance/{payment,receipt,expense,bank_transfer,other_income} 仅特性级 @PreAuthorize，无 per-object owner/部门范围过滤（含银行账户 PII）；Sales/Payroll/ExpenseClaim 已正确强制对象范围（SalesDocumentAccessPolicy.nativeReadScope 范式）。预期授权下不可利用，过度授权即 IDOR。

先读：features/sales/.../SalesDocumentAccessPolicy + docs/05-架构/安全策略.md §4 + DataAccessPolicy。

本窗口做：
1) SEC-MED-4：给上述 5 个 finance 服务的 detail/list/approve 加对象范围过滤（按 maker_employee_id / 部门数据范围，仿 SalesDocumentAccessPolicy）。【先评估运行时授权矩阵：哪些角色该看哪些范围】再实现；NOT_FOUND 兜底防存在性预言机。注意 ExpenseClaim（features/expenseclaim/）已有 assertCanRead/assertOwner，不在本范围。
2) FIN-P1-4：GL 现仅手动批 POST /gl/generate；改 AR_POST/AP_POST/RECEIPT/PAYMENT/COST_CARRY 实时（或 @Scheduled 定时）入 GL（afterCommit 钩子 vs 调度，权衡一致性 vs 性能），保证报表不再依赖手动批。
3) 验证：mvn compile + test；【越权测试矩阵】（有/无权限、跨部门、跨 maker）+ GL 实时入账测试。
4) 文档：更新安全策略 §4 + 钱流报告 + GL 文档。

纪律：对象范围改动风险高，必须有越权测试；不破坏现有 maker≠approver / M28 em.refresh。派后端子代理禁 git/mvn。
```

## 窗口 3 · 业务链 🔴 架构缺口（销售退货处置 + 委外物料守恒 + 采购/委外 IQC）

```
接续 uten_imp 项目（D:\Projects\uten_imp）。做 3 个文档已记的 🔴 架构缺口（审计报告 §五 + docs/07-业务链路/01/06）。每项独立提交 + 单独 PR + 真实岗位 UAT。

先读：销售 SOP docs/07-业务链路/01 + 06、委外迁移 docs/数据迁移/21-22、server/src/main/resources/db/migration/V53__subcontract_documents.sql、features/warehouse/inbound/ProcurementArrivalControlService。

本窗口做（分阶段，建议每项一个 PR）：
1) 销售退货客户处置（BUG-S1 属其子集）：加 disposition 权威字段（REFUND_CLOSED / EXCHANGE / RESHIP / REPAIR_RETURN）+ 审批 + 对 outstanding / 应收 / 排产的确定影响。SOP 规定【退货不自动补产、不自动加 reserved_qty】，须销售先定处置再重预留/排产。
2) 委外物料守恒：建冻结 BOM 版本 + 逐需求发料子账（at_supplier_qty / consumed_qty / supplier_ending_qty）；回厂 receipt 按冻结 BOM 消费发料 + 守恒恒等校验（累计发出 = 合格产出耗用 + 良/不良退回 + 审批损耗 + 供应商期末结存）；满足台账后放开当前无条件 409 新发料门禁（SubcontractMaterialIssueService.approve）。
3) 采购/委外 IQC：收货入待检隔离（inspection_status），合格才进可用库存 + 唤醒生产 WAITING；StockBalanceRepository 可用量查询 + globalAvailableBase 加 IQC 过滤；不合格/特采/退回路径。
4) 验证：mvn compile + test（含 Testcontainers/PG 数据库类）+ 守恒/IQC 端到端测试。
5) 文档：销售 SOP 06 + 委外迁移 21/22 + 仓库 + 新 ADR。

纪律：迁移自包含 + 前向；触发器/约束注意 PG 坑（COMMENT ON IS 字面量、文本块空格、改 Flyway 修 checksum）。这是 P0 大建设，每项须真实岗位 UAT。派后端子代理禁 git/mvn。
```

---

## 收尾（跨窗口，部分需人工）

无论哪个窗口，所有迁移/代码改动最终都要：
1. **重启后端**应用 V215–V218（及各窗口新加迁移）；
2. 跑 **Testcontainers/PG 数据库测试类**（主审计窗口因无 Docker 跳过，须补跑）+ 完整 `mvn verify`；
3. **重建前端**（Flutter web/desktop/app）+ `flutter analyze` 0；
4. **真实岗位 UAT**（销售/PMC/仓库/生产/采购/委外/品质/财税 八类账号正负权限矩阵 + 实物对账）；
5. 目标库备份恢复演练 + 迁移前后数量/金额/状态零差异对账。

全部完成后方可把生产结论从 NO-GO 改 GO（见 [2026-08-01 全链路安全复核报告](2026-08-01-销售仓库生产采购委外全链路安全复核报告.md) §七放行清单）。

---

## 相关文档导航

- [全链路审计与修复报告](2026-08-05-全链路数据一致性与安全审计与修复.md)（已修清单 + 剩余路线图）
- [钱流模块审计与修复](2026-08-05-钱流模块审计与修复.md)（M1–M31 + Phase 计划）
- [ADR-023 统一任务并发控制与认领](../99-决策记录-ADR/ADR-023-统一任务并发控制与认领.md)
- [销售订货到发货全链路 SOP](../07-业务链路/01-销售订货到发货全链路-SOP.md)
- [销售退货质检冻结与处置](../07-业务链路/06-销售退货质检冻结与处置.md)
- [安全策略](../05-架构/安全策略.md)
- 详细 file:line 证据：`_scratch/chain-audit-findings.md`（本机工作文件，可能未入库）
