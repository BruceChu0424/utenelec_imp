# ADR-028：计划部「自底向上、最深层优先」整树确认（历史候选）

- 状态：源码候选、feature flag 默认关；其“递归创建正式计划”写语义已由 ADR-029 取代
- 日期：2026-08-07
- 取代日期：2026-08-08
- 相关：ADR-019（计划需求分解）、[ADR-029](ADR-029-生产计划前需求与物料分析重构.md)、[04-生产单排产与执行全链路需求](../07-业务链路/04-生产订单排产与执行全链路需求.md)、V230

> **当前效力**：V230 与本 ADR 保留为历史实现证据，不删除、不修改已应用迁移。新生产链路只把“全树、最深层优先”用于计划前物料分析和 MAKE `MAKE_COMPONENT` child 备料排序；不得调用本 ADR 的 full-tree orchestrator 递归创建、自动审核或确认正式生产计划。完整套料后只能先生成计划草稿、ACTIVE planning draft 和 SUBMITTED plan link；随后由 `production_plan:approve` 原子形成 READY、完整预留、DRAW 和销售分摊。
>
> **2026-08-10 证据校准**：当前源码事实与验证结果统一见 [ADR-029](ADR-029-生产计划前需求与物料分析重构.md) 和 [迁移说明 56](../数据迁移/56-生产计划前需求与物料分析重构.md)。本页末尾的 29/29 是 2026-08-07 旧候选实现的历史测试记录，不证明该入口仍启用，也不得与当前 V247–V250、审批后 READY 的验证数字相加。
>
> **2026-08-11 交互与联动后置覆盖**：调度台只保留“新建物料分析 / 联合分析所选 N 项”一个主入口，进入分析不生成任何计划或下游单据。完整 BOM 节点仍在服务端分析快照中，但页面初始“只看缺料”，搜索/筛选命中子件时保留祖先上下文，并按 30 个产品组增量构建；只有路线已确认且当前可执行的节点显示真实复选框，其余节点显示“先确认路线 / 先保存路线 / 下层未齐 / 仅查看”。路线确认和下达超过单请求 500 条时，客户端按稳定任务身份分块；每个请求块自身原子，后一块使用上一块返回的最新版本/指纹，幂等键绑定请求前 CAS 与块内精确任务集合，不绑定会在重试时变化的批次序号。计划车间预填只读取 V192 `production_goods_workshop_preferences`，不得向 `goods` 增加第二份默认车间。采购/委外收货先待检；每次 IQC `PASS` 只按该数量入可用库存、立即刷新物料分析，并按累计合格差额推进正式供给、WAITING→READY、预留/DRAW；同单其它待检行不阻塞，整张收货终态只最终对账。上述现行口径由 ADR-029、ADR-033 和迁移说明 56 约束，本 ADR 下方“整树一次确认正式计划”的内容继续只作历史。

## 2026-08-08 替代说明

业务负责人已确认“物料分析在前、正式计划在后”。因此本 ADR 的以下内容停止作为新业务入口：

- 一次 confirm 递归建立整棵正式 MAKE 子计划树；
- 自动审核尚未齐套的子计划；
- 用 WAITING 分段承接未来 MAKE 供给；
- 把无 BOM 直接推断为自制叶子并自动审核。

新规则是：完整 BOM 节点可按筛选、搜索和增量加载随时查看，但不再要求首次进入把整棵深树一次铺满；每个节点保留 `analysis_item_id`、父节点、路径和层级。MAKE 缺口先形成 `PREPLAN_MAKE_TASK`、系统 `MAKE_COMPONENT` child item 与可追溯 supply action，子件完整套料后才生成子件计划草稿，随后由 `production_plan:approve` 原子批准为 READY。无 BOM 必须由产品主数据显式标识 `DIRECT_MAKE`，否则 fail-closed 或走专门越权审批。

`production.bottom-up-orchestrator.enabled` 必须继续默认关闭，新页面不得暴露其写入口。旧数据和旧候选代码仅用于审计、兼容和受控回退；是否最终删除代码需要在存量清零、调用量为零及专项回归后另行决策。

## 背景（为什么）

现状确认生产计划只展开 **一层 BOM**（`ProductionExecutionPlanningService.snapshot()` 单 join；`generateSelfMadeSubplansForPackage` 只建一张草稿子计划、显式禁止递归）。多层 BOM 必须计划员**逐层手动**：确认 A → 手动打开/审核/确认 B → 再确认 C。与业务要的「自底向上、最深层优先、下层完成自动释放上层、傻瓜式」相反。

## 关键发现（决定方案）

侦察实证后，大半基础设施已存在、无需重建：

1. **全树 BOM 递归已存在**：`MrpService.buildMrpSql` 的 `exp` CTE（≤10 层、path 防环），但 `lvl` 被聚合丢掉未投影。
2. **自底向上自动释放已工作**：`ProductionExecutionReadinessService.onFinishedInboundApproved`（FINISHED_IN 审核 → V194 `PRODUCTION_PLAN_ITEM` peg + `production_material_make_receipt_allocations`）已把父 WAITING 段在子件入库后升 READY，且**天然链式**（C 完工→B 就绪→B 完工→A 就绪）。
3. `subplan_links`（V126+V176）支持 N 层；V194 MAKE-peg 不变量已强制溯源。

**唯一缺口**：confirm 只展一层；子计划建为草稿（`status=0`），而 confirm 要求 `status=1`，故无法自动递归；无持久化深度。

## 决策

采用 **Option B：新增递归 orchestrator 复用逐层 confirm**（**不改** `confirm`、`snapshot`、`generateSelfMadeSubplansForPackage`、V194，仅组合）：

- 新增 `BottomUpPlanOrchestrator.confirmFullTree(planId, req)`：preview 校验 BOM 图 → 顶层 confirm → 递归下行（自动审核子计划 + 逐层 confirm），直到自制叶子件。**建造自顶向下；就绪/执行自底向上**（由既有 V194 钩子驱动）。
- 自制叶子件（无 BOM，如 C）**不**调 confirm（confirm 的 snapshot 内联 goods_bom_items，无 BOM 会得空产品行）；只自动审核使其可报工/成品入库——与当前人工流程一致。
- V230 给 `production_plans` 加 `bom_depth`（MAKE 树距根深度）+ `auto_generated`（orchestrator 标记），驱动傻瓜式 UI「最深层可开工优先」排序，并使级联回退只作用于自动子。
- **级联回退**（同发布必修）：`closeSubplans` 对 `auto_generated` 且自带 CONFIRMED 包的子计划，先递归 cancel/reverse 其包（下到叶）再关本子。仅自动子级联；人造嵌套包绝不静默反转。整树取消用 **reverse**（status→−1），非 cancel（cancel 要求子为草稿，但 orchestrator 自动审核了子）。
- 新端点 `POST /api/production/plans/{id}/mrp/generate-planning-package-full-tree`，`production_plan:edit` + 能力开关 `production.bottom-up-orchestrator.enabled`（**默认关**）。

## 后果

- **正向**：一次确认建出整棵 MAKE 子计划树，最深叶先就绪，下层完工自动释放上层；傻瓜式。既有逐层 confirm 与 25 段全链路测试不受影响（confirm 不变）。
- **可逆**：flag 默认关；出问题关 flag（无需重部署）+ 用新级联回退清掉 orchestrator 建的包，即回退到今日逐层手动流。不重蹈上次整流被回退的覆辙（本次是新增组合方法、不改 confirm、逐段执行语义不变）。
- **权衡/已知边界**：单仓跨层假设；不做跨层 MRP 合并（既有局限）；自动审核绕过子计划人工复核（子内容由父 BOM+MAKE 短缺完全决定，无人决策内容）；深树单事务（≤10×宽1，可监控切 `REQUIRES_NEW`）。

## 验证

以下仅为旧候选实现的历史证据，不能作为当前入口验收：`FullChainEndToEndTest` 当时 4 个 #13 场景全绿（29/29 总），覆盖整树 A→B→C、下层入库释放上层、整树级联红冲与幂等重放。当前入口默认关闭，现行物料分析、分批计划、正式审批、采购/委外需求与 IQC 唤醒应只引用 ADR-029/迁移说明 56 的当前证据和发布边界。
