# V703–V706：初始允许超产比例与前向修复

对应 [ADR-117](../99-决策记录-ADR/ADR-117-自制实际产出分账与公共超产.md)。本次是在已提交的 V694–V702 基础上复核，不能把前次提交视为最终验收。

- V703：生产计划明细保存初始允许超产比例，默认 10%；物料分析准备和各自制下达行通过同一请求传入，预览与执行一致。下达后车间修改仍须申请审批；合并追加不得用默认比例覆盖既有有效比例。
- V704：全量转补产的销售份额可以保留零量历史锚点，但必须有同事务完结封顶证据。已开工且未过账的追加批次只能凭自身撤销事实取消，普通工单生命周期保护保留。
- V705：恢复本机实际已应用的 V694、V700、V701 原件后，以新迁移补齐后续候选阶段的改动；既有 V702 材料增量规则及 V703/V704 保护继续有效。
- V706：在既有完结封顶事件中保存数据库生成的逐执行段超产授权，提前完结不会以缩小后的目标反悔本单合法产量。此事实仅证明原本单，后续报工不多出额度；存在其它未审草稿时，必须先处理再提前完结。

2026-09-24 本机 PostgreSQL 只读取证：已成功执行到 V701，V694、V700、V701 的源码与数据库校验不一致。三个原件从实际构建目录找回，Flyway 11.7.2 算法的校验值分别为 `-1248105708`、`-55642355`、`-1134610811`，与数据库逐项相同。此恢复的依据是已应用原件，不能用发布前候选副本改写数据库历史。

不执行 `flyway repair`，不更改 `flyway_schema_history`，不删除业务记录。验证须覆盖原 V701 非空库前向升级、新库完整回放、重复执行零迁移，以及超产、追加材料、返工、取消和反向后的数量与成本。

源码目录为 V706 / 635 个版本迁移。公司数据库安装、用户本机数据库安装、本地临时测试库和 GitHub CI 分别记录；本次不触发远端 CI、不部署公司服务。
