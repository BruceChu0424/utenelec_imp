# Internal-test onboarding intentionally has no authoritative backup authority.
# A later, separately approved backup commissioner must replace this exact file
# only after creating/checking a stanza for this cluster system identifier and
# completing a restore rehearsal.
archive_mode = off
archive_command = ''
