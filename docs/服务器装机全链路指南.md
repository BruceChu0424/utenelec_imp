# 公司服务器装机指南（已停用）

<!-- CURRENT-ERP-TEST-SERVER-SCOPE-20260812 -->
> **当前主机简化范围（2026-08-15）**：只建设内部 ERP 测试环境（PostgreSQL、Spring Boot、Flutter
> ERP Web/Nginx）；企业官网延期到独立云服务器。ERP `/data` 改为系统 NVMe 上独立 350 GiB LVM/ext4；
> 机械盘退出 ERP 路径但不擦除。旧 Phase 1 resume 被替换前禁止计划重启。最新状态、03:00 未来维护窗口和安全顺序见
> [`deploy/current-test-server-status.zh-CN.md`](deploy/current-test-server-status.zh-CN.md)。

> **禁止继续照旧版逐行执行。** 旧内容包含会直接创建/格式化 RAID、把 `/data` 设为 `nofail`、启用额外管理端口、接受未受信证书和以密码作为日常 SSH 方式等做法；它不适用于已经安装且可能含数据的服务器。

当前权威入口：

- [`deploy/operator-guide.zh-CN.md`](deploy/operator-guide.zh-CN.md)：推荐架构、首次安装、日常发版、外网访问、备份恢复和故障边界。
- [`deploy/release/README.md`](deploy/release/README.md)：签名发布、非特权 staging 和人工激活的安全契约。
- [`deploy/release/GITHUB_SIGNING_AND_PROTECTION_SETUP.zh-CN.md`](deploy/release/GITHUB_SIGNING_AND_PROTECTION_SETUP.zh-CN.md)：最终仓库/套餐、main/tag ruleset、人员 Git 签名、隔离 Environment、Release 制品签名和 OIDC 前置。
- [`deploy/target-host-oob-authority.zh-CN.md`](deploy/target-host-oob-authority.zh-CN.md)：首次连接前的 CMDB、带外 Host Key、项目 `known_hosts`、双管理员密钥、批准路由、控制台和口令轮换 H01–H12。
- [`deploy/server-handoff.example.md`](deploy/server-handoff.example.md)：不含秘密的交接模板。
- 私有 `server-handoff.production.private.md`：只记录当前服务器真实状态，必须存入受控 CMDB/加密文档库；本机 `服务器部署交接.md` 只是已被 `.gitignore` 排除的兼容别名，Git 忽略不等于安全存储。
- [`deploy/cloud/README-cloud.md`](deploy/cloud/README-cloud.md)：未验收的可选异地灾备设计草案，当前禁止照其中命令部署或启用服务。

对现有服务器必须先完成 OOB H01–H12，才允许通过项目专用 `known_hosts` 做首次只读识别：确认磁盘、挂载、
数据库身份、Flyway live history、systemd 和备份，再由审批过的变更单决定下一步。任何格式化、阵列创建、
cluster 初始化、恢复、角色重构、SSH 认证切换或防火墙变更都不得从通用文档直接复制执行。
