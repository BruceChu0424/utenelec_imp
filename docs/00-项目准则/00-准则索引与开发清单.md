# 00 - 准则索引与开发清单

> ⚠️ **这是本项目最高优先级的文档。**
> 每次开发任何新功能、修改任何 UI、新增任何组件之前，**必须先看一遍下面的对照清单**。
> README 第一链接就指向这里。

> 📍 **项目现状（2026-08-23）**
> - 后端 Spring Boot 3.5.16 + PostgreSQL + Flyway 已覆盖核心业务；工资与员工报销活动 Mock 已移除，
>   V133 表、真实 API 与固定状态机已有源码，但业务口径/生产 E2E 仍待验收；实验室及业务设备控制待接入，
>   审计设备声明与本机回执已接入但不构成硬件认证，不能描述为“全量生产就绪”。
> - 前端 Flutter + Riverpod + go_router + Dio + flutter_secure_storage。
> - 会话/权限补强已有自动化证据：员工 token 原子记录（generation/intentGeneration/sessionLineage）、
>   CAS/latest-intent、corrupt tombstone、logout fence、加密 refresh 撤销队列、安全读两次重试、
>   严格 health+jitter、最小 staff JWT/服务端权限解析 503 与有限请求头预算。真实多标签页、目标网关/
>   数据库/浏览器、故障注入、负载和运维演练仍未全部完成，生产结论为 **NO-GO**。
> - 本地/云端架构固定为公司本地唯一写主库 + 阿里云异步物理热备；员工默认仅公司网，按人授予
>   云端远程权限，断链时云端业务 503。当前共享工作树迁移头为 V392/354（354 个唯一版本、无重号）；V288/V289 已形成现货借用结构、终态守卫和审计覆盖，并接通源码候选在线 create/revoke、双趟生效计算与 Flutter UI。后端 1269 项、Flutter analyze 0 issue 和 test 594/594 是 V252 时点归档证据，不是当前冻结回归；目标库、真实岗位 UAT 与签名发布仍为 NO-GO。开发原库保持 V244/225，
>   一次性生产物料克隆升到 V250/231；公司目标库仍保留 V238 既有口径。真实 ECS/VPN/TLS/OSS/PITR/UAT 未验收。
> - **角色体系已下线**（[ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md)）：权限 = 全员基础 ∪ 部门配置（含上级部门）± 个人覆盖，按权限点脱敏；审计查看/导出是例外，除超管固有全权限外只能个人点名授权、不得部门继承。
> - **生产计划前重构**（[ADR-029](../99-决策记录-ADR/ADR-029-生产计划前需求与物料分析重构.md)）：现行序列为 V234/V237/V239 与 V247–V250。公司目标库 V238 只包含 V234 分析骨架和 V237 未来写入审计 sweep；V239 全量 claim 后剩余需求可到 0 的终态修复已在开发原库 V244，目标库仍待 V239–V250 正式迁移。V247–V250 的阶段/包装计量、精确正式需求、零物料证据和来源守卫目前只有源码、自动化及 disposable-clone 阶段链/HTTP 证据。新链路先分析/备料，再 generate 草稿/SUBMITTED link；只有 `production_plan:approve` 才可原子 READY/完整预留/DRAW/销售分摊。实际收货/IQC、MAKE 正式子计划、报工/完工/入库/组装/分批发货没有在同一 HTTP 链完成，生产仍为 **NO-GO**。
> - 统一导出：独立 `:export` 权限 + Bucket4j 限流 + 可选 Agile AES-256 + 审计（[ADR-032](../99-决策记录-ADR/ADR-032-导出文件可选密码保护.md)）；系统设置页使用运行时策略阈值 + 二次密码确认（[ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)）；报表行跳源头使用 `__srcId` 隐藏列，表格统一为 `MasterDataTableView`。
> - 安全强制规范见 [../05-架构/安全策略.md §十](../05-架构/安全策略.md)；当前全局状态与阻断项以
>   [2026-08-09 本地云端部署与生产就绪清单](../99-项目治理/2026-08-09-本地云端部署与生产就绪清单.md)
>   为准。旧日期报告保留为历史证据；资产与待摊仍按其[专项验收报告](../99-项目治理/2026-08-01-资产与待摊全链路实现与验收报告.md)的业务门禁执行。
> - 中国大陆为主要运行区域；同源资源、北京时间、国内弱网/NAT、短信、备案、个人信息和数据地域
>   要求见[中国大陆部署与兼容性](../99-项目治理/中国大陆部署与兼容性.md)。

---

## 🎯 项目七大核心准则

| # | 准则 | 一句话 | 详细文档 |
|---|---|---|---|
| 1 | 响应式多端适配 | 一套代码跑通手机/平板/桌面/Web，布局按断点自动切 | [02-响应式与多端适配.md](02-响应式与多端适配.md) |
| 2 | 自适应布局组件 | 用 LayoutBuilder + 断点，不用散落的 if/else | [03-自适应布局组件.md](03-自适应布局组件.md) |
| 3 | 字体可调 | 字号走 token，支持小/标准/大/超大/超超大五档，不硬编码 sp | [04-字体与字号可调.md](04-字体与字号可调.md) |
| 4 | 国际化多语言 | 所有文案走 i18n，中文 + English | [05-国际化与多语言.md](05-国际化与多语言.md) |
| 5 | 动画规范 | 中等强度，走统一动画 token，不临时拼 | [06-动画规范.md](06-动画规范.md) |
| 6 | 性能自适应 | 三档（lite/standard/rich）自动降级，照顾低端机 | [07-性能自适应.md](07-性能自适应.md) |
| 7 | 主题与配色 | 深绿+青绿，深浅双模式，颜色全 token 化 | [08-主题与配色.md](08-主题与配色.md) |
| 8 | **顶部通知** | 所有用户反馈用顶部 `AppNotification`（自建），禁止底部 SnackBar | [02-组件库/AppNotification.md](../02-组件库/AppNotification.md) |
| 9 | **防连点 + 等待回执** | 按钮点击 → 进入置忙 → 视觉 loading → 等待回执 → 解锁。默认用 `UtenActionButton` | [02-组件库/ClickGuard与UtenActionButton.md](../02-组件库/ClickGuard与UtenActionButton.md) |

加上工程纪律：

| # | 准则 | 详细文档 |
|---|---|---|
| 10 | 命名、文案与标点规范 | [01-命名规范.md](01-命名规范.md) |
| 11 | 组件库使用规范（必须用 Uten 组件，不直接堆 Container） | [09-组件库使用规范.md](09-组件库使用规范.md) |
| 12 | 安全准则 | [10-安全准则.md](10-安全准则.md) |
| 13 | 中国大陆部署与兼容 | [../99-项目治理/中国大陆部署与兼容性.md](../99-项目治理/中国大陆部署与兼容性.md) |
| 14 | 本地/云端部署与放行 | [../99-项目治理/2026-08-09-本地云端部署与生产就绪清单.md](../99-项目治理/2026-08-09-本地云端部署与生产就绪清单.md) |
| 15 | Git 提交与 PR 合并门禁 | [11-Git提交与合并规范.md](11-Git提交与合并规范.md) |
| 16 | 后端编码规范（不可变集合禁止 null 键查询等） | [12-后端编码规范.md](12-后端编码规范.md) |

> 📌 **架构层安全总览**：[../05-架构/安全策略.md](../05-架构/安全策略.md)（含 **§十 数据导出与高危配置强制规范**）。
> 决策记录：[ADR-006](../99-决策记录-ADR/ADR-006-后端栈选型SpringBoot+Postgres.md) / [ADR-009](../99-决策记录-ADR/ADR-009-后端安全加固与功能补全.md) / [ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md) / [ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md) / [ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md) / [ADR-014](../99-决策记录-ADR/ADR-014-会话超时与token刷新机制修复.md) / [ADR-018](../99-决策记录-ADR/ADR-018-资产与待摊专业子账及不可变过账.md) / [ADR-021](../99-决策记录-ADR/ADR-021-人事域完善-转正车辆多联系方式与任务软认领.md) / [ADR-029](../99-决策记录-ADR/ADR-029-生产计划前需求与物料分析重构.md) / [ADR-031](../99-决策记录-ADR/ADR-031-本地云端单主库部署架构.md)。

---

## ✅ 每次开发前的对照清单（Copy 到 PR 描述里）

> 实现一个新功能 / 修改一个页面时，对照下列每一项确认。

### 命名
- [ ] 所有类名带 `Uten` 前缀（仅 Uten 组件库），或符合 Feature 命名
- [ ] 文件名 snake_case，页面用 `<feature>_page.dart`
- [ ] 路由路径 kebab-case
- [ ] 不出现 `MyButton`、`Widget1`、`temp`、`aaa` 这类随意命名
- [ ] 新增或修改的 UI/ARB/错误提示/日志/注释/测试/Markdown 括号统一使用 ASCII 半角 `()`，没有新增 U+FF08/U+FF09
- [ ] 人员身份统一写成 `姓名(工号)`，部门放在选择项副行

### 响应式与自适应
- [ ] 在 **compact（<600dp）**、**medium（600–840dp）**、**expanded（>840dp）** 三档下都测过布局
- [ ] 用 `LayoutBuilder` 或 Uten 响应式组件，不写死宽度高度
- [ ] 导航按断点分化：compact 底部悬浮胶囊 / medium+ 左侧 Rail；页面内容在三档宽度下不溢出、不被胶囊/Rail 遮挡
- [ ] 主 Tab 转场：compact 左右滑动 / medium+ 即时切换（不在大屏整页横移）
- [ ] 登录后页面文字可选可复制（`app_router.dart` 的 `ShellRoute.builder` 已套 `SelectionArea`，须在 Navigator 内；别挪到 `MaterialApp.builder`——会崩）
- [ ] 列表在窄屏单列、宽屏多列自适应

### 字体
- [ ] 字号全部走 `Theme.of(context).textTheme` 或 Uten 字号 token
- [ ] **不硬编码** `fontSize: 14` 这类数字
- [ ] 在字号=超大模式下不被截断

### 国际化
- [ ] 所有用户可见文案走 `AppLocalizations.of(context).xxx`
- [ ] **不硬编码** 中文或英文字符串到 Widget 里
- [ ] arb 文件中英文都补全

### 动画
- [ ] 动画走 Uten 动画 token（duration / curve 统一）
- [ ] 不在性能档=lite 下还跑重动画
- [ ] 列表进场用统一 stagger，不各写各的

### 性能自适应
- [ ] 在 **lite 档**下能正常跑（无模糊、无重动画）
- [ ] 用了 BackdropFilter / 粒子 / 复杂着色器的，包一层性能档判断
- [ ] 大列表用 `ListView.builder`，不用 `Column + List<Widget>`

### 中国大陆运行
- [ ] 新增运行时依赖不需要境外 CDN/字体/脚本；服务域名、字段、地域和留存已登记
- [ ] 业务日期使用 `Asia/Shanghai` 集中工具，数据库/JSON 时间协议仍为 UTC
- [ ] 搜索列表有 300–500ms 防抖且只让最新 GET 回写；写请求不自动重放
- [ ] 公司 NAT/运营商 CGNAT 下不会因低阈值 IP 桶让正常用户互相 429
- [ ] 中文输入法 composing、中文文件名、11 位/`+86` 手机号和身份证非法日期有回归测试

### 主题
- [ ] 颜色走 `Theme.of(context).colorScheme` 或 Uten 色 token
- [ ] **不硬编码** `Color(0xFFxxxxxx)`
- [ ] 浅色 / 深色模式下都可读（对比度够）

### 组件库
- [ ] 已有同类能力时复用 Uten 组件；页面私有布局可组合 Material 原语，但必须使用主题/token，
      且不得复制公共组件
- [ ] 缺组件时先补组件库再写业务（参考 [02-组件库/组件总览.md](../02-组件库/组件总览.md)）
- [ ] 组件复用而非复制粘贴

### 顶部通知
- [ ] 所有用户反馈走 `context.appSuccess/Error/Warning/Info`，**禁止** `ScaffoldMessenger.showSnackBar(...)`
- [ ] API 错误用 `context.appApiError(e)` 自动提取 `fieldErrors`
- [ ] 通知位置为顶部（status bar 下方），不允许底部
- [ ] 同一页面同时存在的通知最多 3 条，超出按 FIFO 出队（系统自动）

### 防连点 + 等待回执
- [ ] 任何触发网络请求的按钮必须用 `UtenActionButton`（自带 loading） 或 `UtenButton(isLoading: ...)`
- [ ] Switch / Slider / SegmentedButton 等联动控件：动作前后用 `ClickGuard.run()` + `isBusy` 守卫
- [ ] `onAction` 回调里用 `context` 前必有 `context.mounted` 守卫（异步回执后页面可能已 pop）
- [ ] 不在 `onAction` 内同步抛错；如需抛异常，外层 `try/catch` 转为 `appApiError` 展示
- [ ] 联动的几个控件（如 HVAC 开关 + 温度滑杆）共享同一个 `ClickGuard` 实例，避免不同控件独立互锁

### 安全
- [ ] 表单做客户端校验（长度、格式、必填），**后端独立再校验**（前端不可信）
- [ ] 密码只进入当次 HTTPS 请求 body、不得持久化；token record/待撤销 refresh 走 `flutter_secure_storage`，不进 shared_preferences
- [ ] 员工 token 使用单个权威 record；保存/清理 refresh 用完整快照 CAS，登录/改密 latest-intent，损坏记录写 tombstone 且不从旧双键复活
- [ ] 退出先激活 logout fence，再在跨标签 token 锁内把旧 refresh 持久交接到专用加密撤销队列，成功后才写 tombstone；交接失败保留 fence 与 token 供重试；该队列只重试幂等 logout，禁止复用为业务离线队列
- [ ] 仅精确 `status + code` 的 refresh 拒绝清会话；HTML/空体/未知 401、status/code 错配、503/429/超时均保留 token
- [ ] 自动连接只重试 GET/HEAD/OPTIONS 两次（400ms/1200ms）；POST/PUT/PATCH/DELETE 不因连接恢复重放；health 只认 200 + JSON `status=UP` 并带 jitter
- [ ] staff JWT 只含标准字段 + `sub/typ/av/ae`；服务端账号/权限解析故障为结构化 503；Tomcat/Nginx/设备审计头均有有限预算和边界测试
- [ ] 不硬编码密钥、任意 URL、Mock 账号；开发秘密用不入库 `.env`，生产走受控环境/Vault/KMS；原生公开端点只用构建期本地/云端 allowlist，Web 只同源，部署配置**不进系统设置页**
- [ ] 路由守卫：未登录访问受保护页跳登录页；菜单/按钮按权限点 `hide`（**不是 disable**）
- [ ] 错误信息不暴露后端细节（stack/code/SQL），统一 `ApiException` → `context.appError(...)`
- [ ] **对象级授权**：列表、详情、更新、删除、审核、红冲和批量入口使用同一数据范围守卫；必须用他人 UUID 做越权负向测试
- [ ] **计划需求与商业订货分离**：采购/委外申请由生产物料分析按用户所选缺口形成且业务端只读；通知不自动下商业订单，订货只从任务中心分解，一张单保持单供应商/委外商且单仓库
- [ ] **审核组审批授权**：共享任务、批准、驳回分别使用 `finance_order_approval:view/approve/reject`；动作还须通过财务部门资格或 active 个人点名加授的实时判定（`WorkflowReviewerEligibility`），超管不自动代审。业务详情只读，决定仅接受 `{caseId,expectedVersion}` 的财务任务批协议；旧 orderId+version 单笔入口 fail-closed（V328/ADR-027）
- [ ] **超量到货 fail closed**：财务决定前不写库存/AP/订单累计；批准量仍由仓库再审，未批准量只交原下单人退回
- [ ] **候选不是部署事实**：公司目标库既有只读证据仍为 V238；当前源码目录为 V392/354，开发原库为 V244/225，一次性生产物料克隆仍是 V250/231 历史证据。完成备份恢复、V239–V392 正式迁移、全表/金额/数量/状态/业务标识/PII 回填与派生一致性对账、V288/V289 借用端点/终态守卫/审计核对、真实权限负测、岗位/实物 UAT 和签字前必须标生产 NO-GO
- [ ] **本地/云端边界**：本地全部 `/api/**` 校验真实公司 CIDR；云端员工 login/refresh/逐请求复核默认关闭的 `remote_access`；visitor 仅按独立公网边界例外
- [ ] **端点不可外送凭据**：原生 Release 同时固化批准的本地/云端 HTTPS host，Web Release 只同源 `/api`；禁止手填任意 host，且“仅本地/仅云端”必须有未登录可信恢复入口
- [ ] **附件切流门禁**：共享私有 OSS 固定精确 `versionId`，目标库 `storage_version IS NULL` 为 0；PostPolicy 大小限制、staging/final、AV、删除 outbox、孤儿治理和真实恢复未完成前 NO-GO
- [ ] **生产先分析、后计划**：物料分析不得后台偷建正式计划；全树平铺必须保留 `analysis_item_id/path/parent/depth`，MAKE 子层归系统 `MAKE_COMPONENT` child item
- [ ] **阶段与包装计量**：BOM 只允许 `START/ASSEMBLY/FINISH/SHIP/REFERENCE` 和 `PER_UNIT/PER_PACKAGE/FIXED_BATCH`；SHIP/REFERENCE 只提示，整包/固定批次逐边向上取基本单位 4 位，不得用平均单耗近似
- [ ] **三类齐套不混用**：一次只选一个目标仓；ready_start 只作可开工参考，正式计划只认 ready_finish，ready_ship 只作预计可发参考；在途、待检、拒收和 MAKE 未合格入库不得计当前齐套或预留
- [ ] **备料通知有业务事实但不自动下单**：先写 `preplan_supply_actions/allocations`，再精确关联真实采购申请、委外申请或 `PREPLAN_MAKE_TASK`；消息/角标不能代替 action，采购/委外人员仍须从任务中心建立商业订单
- [ ] **生成与批准分权**：`production_material_analysis:generate` 只写草稿、ACTIVE planning draft、SUBMITTED plan link且零预留；`production_plan:approve` 才原子 READY、LINEAR/EXACT_SNAPSHOT 需求、完整预留/DRAW/销售分摊；有证据的 ZERO_MATERIAL 不造假需求/DRAW；`approveNow` 需两权并显式确认
- [ ] **BOM 策略已下线（ADR-057/V423）**：货品不再有生产 BOM 策略字段；无 BOM 自制件在物料分析中按「直接自制」处理（ZERO_MATERIAL/DIRECT_MAKE，证据=物料分析事实）；有 BOM 照旧按子层级硬门槛齐套
- [ ] **来源谱系与反向**：新链路不建 WAITING/AUTO_WAIT/DEFERRED；旧数据只读兼容。外部化 action/allocation 及申请/订货来源不可通用拆改；已有申请、订单、发料或正式计划必须走专用取消/释放/红冲，历史来源单不得复活
- [ ] **新增数据导出**：五道防线（独立 `*:export` 权限不复用 view / Bucket4j 频率 + 进程内并发闸门 / Agile AES-256 加密、密码入 body / 行数上限 / 审计）—— [§十.1](../05-架构/安全策略.md)
- [ ] **新增高危配置（系统设置）**：六道防线（`authorization:manage` + 超管限定 / Controller 与 Service 鉴权 / **二次密码确认** / 审计 / 类型校验 / 只允许策略阈值）—— [§十.2](../05-架构/安全策略.md)
- [ ] **用户可见 API 操作审计**：进入应用的 GET/POST/PUT/PATCH/DELETE/HEAD `/api/**` 必须有请求记录；写事务首个 DML 前绑定请求、用户与设备上下文；不得保存 query 值、正文、凭证或令牌
- [ ] **审计证据权限**：除超管固有全权限外，查看只允许个人 `audit_log:view`，导出再叠加个人 `audit_log:export`；禁止部门配置或继承
- [ ] **审计不可变与设备核对**：无人工修改/删除入口，只由留存任务自动归档清理；读取目标本机回执前必须联网完成服务端授权，拒绝或离线时零本机读取
- [ ] **报表行带元数据但不展示**：列 key 以 `__` 前缀（如 `__srcId`），`execute()` 自动过滤；前端 `row['__srcId']` 可读用于行点击跳源头

### 数据模型
- [ ] 涉及新实体 → 同步更新 [04-数据模型/实体字典.md](../04-数据模型/实体字典.md)
- [ ] 涉及新关联 → 同步更新 [04-数据模型/ER草图.md](../04-数据模型/ER草图.md)
- [ ] 涉及生产计划前分析 → 同步 [生产履约 V2 实体关系](../04-数据模型/生产履约V1实体关系.md)、[ADR-029](../99-决策记录-ADR/ADR-029-生产计划前需求与物料分析重构.md)、[V234–V250 迁移说明](../数据迁移/56-生产计划前需求与物料分析重构.md)及[迁移总索引](../数据迁移/README.md)
- [ ] 新增业务表后审计刷新必须扫描全部 `public` 业务表并强校验唯一启用 AFTER ROW I/U/D 触发器；不得把 V202 写成定向三表，也不得补造历史审计

### 后端编码
- [ ] **不可变集合禁止 null 键查询**：`Map.of/copyOf/toUnmodifiableMap/unmodifiableMap` 的 `get/getOrDefault/containsKey`、`Set.of` 的 `contains`，key 可能为 null（可空外键、可空请求字段、可空 DB 列）时必须先判空；空 `Map.of()` 同样会抛 NPE —— [12-后端编码规范.md](12-后端编码规范.md)

### 文档
- [ ] 新增页面 → 在 [03-页面/](../03-页面/) 下创建该页文档
- [ ] 新增组件 → 在 [02-组件库/](../02-组件库/) 下创建该组件文档
- [ ] 重大决策 → 在 [99-决策记录-ADR/](../99-决策记录-ADR/) 下记录
- [ ] 文档新增行已按 [01-命名规范 §3.3](01-命名规范.md#33-文案与标点) 扫描，无 U+FF08/U+FF09

### 提交与合并
- [ ] 单维护者日常只保留一个主要功能分支；只有并行写入时才增加独立 worktree
- [ ] 只暂存本任务文件，已检查 `git diff --cached --name-status`、`--stat` 和 `--check`
- [ ] 未包含真实 `.env`、密钥、业务数据、备份、构建产物、运行时媒体或临时 scratch 文件
- [ ] Commit/PR 标题符合 Conventional Commits，PR 只覆盖一个明确业务目标
- [ ] 日常上传前已运行定向测试；完整 Flutter/后端门禁在准备合并 `main` 时完成
- [ ] 使用 `publish_feature_branch.ps1` 或等价显式步骤推送功能分支，并核对本地/远端 SHA
- [ ] 单维护者如实自审，不虚构第二 reviewer；通过一个成型 PR 合并 `main`
- [ ] Quality Gate、CodeQL、Dependency Vulnerability Scan 本次运行全绿后才合并
- [ ] 未直接推送或 force push `main` 绕过失败门禁
- [ ] 源码上传、合并、签名制品、服务器部署和目标库验收已分别报告

---

## 📐 关键设计决策速查

| 问题 | 答案 |
|---|---|
| Widget 前缀 | `Uten`（如 `UtenButton`），不用 `Utn` |
| 状态管理 | Riverpod 2.x（`Provider`/`Notifier`/`AsyncNotifier`） |
| 路由 | go_router，路径用 kebab-case |
| 响应式断点 | compact <600 / medium 600–840 / expanded >840（dp） |
| 性能档 | lite / standard / rich，默认 standard，可手动覆盖 |
| 主色 | 深绿 `#0F3D2E`，强调色青绿 `#14B8A6` |
| 字号档 | 小 / 中（默认）/ 大 / 超大，通过全局缩放因子实现 |
| 国际化 | 中文 `zh` + 英文 `en`，arb 在 `lib/core/l10n/` |
| 后端 | Spring Boot 3.5.16 + PostgreSQL + Flyway；共享工作树最高 V392/354（354 个唯一版本、无重号），开发原库 `uten_imp` V244/225，一次性隔离克隆 V250/231；公司目标库保留 V238 既有只读证据。公司主库唯一可写，云端异步热备仅为目标架构。工程验证不是目标库升级、历史对账或岗位验收；各业务域、OSS、灾备和生产运维门禁仍须逐项关闭 |
| 网络 | Dio + Repository；原子 token record + CAS/latest-intent + logout fence；原生固定本地/云端双入口且公司内优先本地，Web 同源；local CIDR + cloud `remote_access` 服务端门禁；GET/HEAD/OPTIONS 有限重试，业务写和云端断链 503 不自动重放 |
| 权限模型 | **部门配置（含上级）+ 个人覆盖 + 按权限点脱敏**；V135 用户版本/全局授权 epoch 逐请求校验；审计查看/导出除超管外仅允许个人点名授权、禁止部门继承 |
| 审计中心 | API 请求、显式事件、公开业务表变化三层留痕；默认用户写操作、对象/Request ID 筛选、`snapshotId` 高水位边界、V184 触发器强校验、V185 软删除/脱敏加固、四页详情、设备证据、个人查看/导出双权限、加密 Excel 与两阶段留存见[审计中心](../03-页面/审计日志页.md) |
| 统一表格 | `MasterDataTableView`（全局唯一数据展示：6 报表族 + 6 主档 + 单据列表共用，[文档](../02-组件库/MasterDataTableView.md)） |
| Excel 导出 | `UtenExportButton` → POI SXSSF；密码为空时普通下载，非空 1–128 位时 Agile AES-256，密码只入 body，并保留限流与审计（[ADR-032](../99-决策记录-ADR/ADR-032-导出文件可选密码保护.md)） |
| 存储分级 | 偏好 shared_preferences / **token record 与待撤销 refresh 使用 flutter_secure_storage**；密码不持久化；两端附件共用私有 OSS 并固定精确 `versionId`，生产附件残余门禁见总控清单 |
| 文字选择 | `SelectionArea` 包登录后外壳（`app_router.dart` 的 `ShellRoute.builder`，须在 Navigator 内，放 `MaterialApp.builder` 会崩）——Text 默认不可选 |
| 主 Tab 转场 | compact 左右滑动 / medium+ 即时切换（不在大屏整页横移，见 [02 §3.1.1](02-响应式与多端适配.md)） |

---

## 🧭 如何使用本文件夹

1. **新加入项目的开发者**：从 01 到 11 顺序读完，再去看 [01-规划/](../01-规划/)
2. **开始一个新功能**：打开本文件，对照"开发前的对照清单"
3. **不确定某个细节**：去对应的 01–11 单项准则文档里查
4. **做一个新决策**：在 [99-决策记录-ADR/](../99-决策记录-ADR/) 记录为什么
5. **碰数据导出 / 高危配置 / 隐藏元数据**：必看 [../05-架构/安全策略.md §十](../05-架构/安全策略.md)（强制规范，2026-07-27 起）
6. **新增 API、设备声明或调查证据**：同时检查[审计中心](../03-页面/审计日志页.md)与[安全策略](../05-架构/安全策略.md)，补请求、显式事件、表触发器和权限负向测试
7. **准备提交或合并**：按 [11-Git提交与合并规范](11-Git提交与合并规范.md)核对范围、门禁、远端 SHA 和 PR 状态

### 本文件夹文件清单

| # | 文档 |
|---|---|
| 00 | 准则索引与开发清单（本档） |
| 01 | [命名、文案与标点规范](01-命名规范.md) |
| 02 | [响应式与多端适配](02-响应式与多端适配.md) |
| 03 | [自适应布局组件](03-自适应布局组件.md) |
| 04 | [字体与字号可调](04-字体与字号可调.md) |
| 05 | [国际化与多语言](05-国际化与多语言.md) |
| 06 | [动画规范](06-动画规范.md) |
| 07 | [性能自适应](07-性能自适应.md) |
| 08 | [主题与配色](08-主题与配色.md) |
| 09 | [组件库使用规范](09-组件库使用规范.md) |
| 10 | [安全准则](10-安全准则.md)（2026-08-09 更新，对齐远程访问、本地 CIDR、双入口、共享 OSS 与生产门禁） |
| 11 | [Git 提交与合并规范](11-Git提交与合并规范.md) |
| 12 | [后端编码规范](12-后端编码规范.md)（2026-08-16 建立，首条：不可变集合禁止 null 键查询） |
