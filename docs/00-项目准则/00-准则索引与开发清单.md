# 00 - 准则索引与开发清单

> ⚠️ **这是本项目最高优先级的文档。**
> 每次开发任何新功能、修改任何 UI、新增任何组件之前，**必须先看一遍下面的对照清单**。
> README 第一链接就指向这里。

> 📍 **项目现状（2026-07-27）**
> - 后端 Spring Boot 3 + PostgreSQL + Flyway **全量接入**——业务模块（销售/采购/委外/仓库/生产/钱流/基础资料/HR/鉴权/访客/通知/建议/报销/工资条）**已全部走真实后端，不再 Mock**。
> - 前端 Flutter + Riverpod + go_router + Dio + flutter_secure_storage。
> - **角色体系已下线**（[ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md)）：权限 = 全员基础 ∪ 部门配置（含上级部门）± 个人覆盖，按权限点脱敏。
> - 2026-07-27 新增能力：加密导出（独立 `:export` 权限 + Bucket4j 限流 + Agile AES-256 + 审计，[ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md)）、系统设置页（运行时策略阈值 + 二次密码确认，[ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)）、报表行跳源头（`__srcId` 隐藏列）、统一表格 `MasterDataTableView`。
> - 安全强制规范见 [../05-架构/安全策略.md §十](../05-架构/安全策略.md)。

---

## 🎯 项目七大核心准则

| # | 准则 | 一句话 | 详细文档 |
|---|---|---|---|
| 1 | 响应式多端适配 | 一套代码跑通手机/平板/桌面/Web，布局按断点自动切 | [02-响应式与多端适配.md](02-响应式与多端适配.md) |
| 2 | 自适应布局组件 | 用 LayoutBuilder + 断点，不用散落的 if/else | [03-自适应布局组件.md](03-自适应布局组件.md) |
| 3 | 字体可调 | 字号走 token，支持小/中/大/超大，不硬编码 sp | [04-字体与字号可调.md](04-字体与字号可调.md) |
| 4 | 国际化多语言 | 所有文案走 i18n，中文 + English | [05-国际化与多语言.md](05-国际化与多语言.md) |
| 5 | 动画规范 | 中等强度，走统一动画 token，不临时拼 | [06-动画规范.md](06-动画规范.md) |
| 6 | 性能自适应 | 三档（lite/standard/rich）自动降级，照顾低端机 | [07-性能自适应.md](07-性能自适应.md) |
| 7 | 主题与配色 | 深绿+青绿，深浅双模式，颜色全 token 化 | [08-主题与配色.md](08-主题与配色.md) |
| 8 | **顶部通知** | 所有用户反馈用顶部 `AppNotification`（自建），禁止底部 SnackBar | [02-组件库/AppNotification.md](../02-组件库/AppNotification.md) |
| 9 | **防连点 + 等待回执** | 按钮点击 → 进入置忙 → 视觉 loading → 等待回执 → 解锁。默认用 `UtenActionButton` | [02-组件库/ClickGuard与UtenActionButton.md](../02-组件库/ClickGuard与UtenActionButton.md) |

加上三条工程纪律：

| # | 准则 | 详细文档 |
|---|---|---|
| 10 | 命名规范 | [01-命名规范.md](01-命名规范.md) |
| 11 | 组件库使用规范（必须用 Uten 组件，不直接堆 Container） | [09-组件库使用规范.md](09-组件库使用规范.md) |
| 12 | 安全准则 | [10-安全准则.md](10-安全准则.md) |

> 📌 **架构层安全总览**：[../05-架构/安全策略.md](../05-架构/安全策略.md)（含 **§十 数据导出与高危配置强制规范**）。
> 决策记录：[ADR-006](../99-决策记录-ADR/ADR-006-后端栈选型SpringBoot+Postgres.md) / [ADR-009](../99-决策记录-ADR/ADR-009-后端安全加固与功能补全.md) / [ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md) / [ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md) / [ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)。

---

## ✅ 每次开发前的对照清单（Copy 到 PR 描述里）

> 实现一个新功能 / 修改一个页面时，对照下列每一项确认。

### 命名
- [ ] 所有类名带 `Uten` 前缀（仅 Uten 组件库），或符合 Feature 命名
- [ ] 文件名 snake_case，页面用 `<feature>_page.dart`
- [ ] 路由路径 kebab-case
- [ ] 不出现 `MyButton`、`Widget1`、`temp`、`aaa` 这类随意命名

### 响应式与自适应
- [ ] 在 **compact（<600dp）**、**medium（600–840dp）**、**expanded（>840dp）** 三档下都测过布局
- [ ] 用 `LayoutBuilder` 或 Uten 响应式组件，不写死宽度高度
- [ ] 导航按断点分化：compact 底部悬浮胶囊 / medium+ 左侧 Rail；页面内容在三档宽度下不溢出、不被胶囊/Rail 遮挡
- [ ] 主 Tab 转场：compact 左右滑动 / medium+ 即时切换（不在大屏整页横移）
- [ ] 登录后页面文字可选可复制（`app_router.dart` 的 `ShellRoute.builder` 已套 `SelectionArea`，须在 Navigator 内；别挪到 `MaterialApp.builder`——会崩）
- [ ] 列表在窄屏单列、宽屏多列自适应

### 字体
- [ ] 字号全部走 `Theme.of(context).textTheme` 或 Uten 字号 token
- [ ] **不硬编码** `fontSize: 14` 这类数字
- [ ] 在字号=超大模式下不被截断

### 国际化
- [ ] 所有用户可见文案走 `AppLocalizations.of(context).xxx`
- [ ] **不硬编码** 中文或英文字符串到 Widget 里
- [ ] arb 文件中英文都补全

### 动画
- [ ] 动画走 Uten 动画 token（duration / curve 统一）
- [ ] 不在性能档=lite 下还跑重动画
- [ ] 列表进场用统一 stagger，不各写各的

### 性能自适应
- [ ] 在 **lite 档**下能正常跑（无模糊、无重动画）
- [ ] 用了 BackdropFilter / 粒子 / 复杂着色器的，包一层性能档判断
- [ ] 大列表用 `ListView.builder`，不用 `Column + List<Widget>`

### 主题
- [ ] 颜色走 `Theme.of(context).colorScheme` 或 Uten 色 token
- [ ] **不硬编码** `Color(0xFFxxxxxx)`
- [ ] 浅色 / 深色模式下都可读（对比度够）

### 组件库
- [ ] UI 全部用 `UtenXxx` 组件，不直接堆 `Container/SizedBox/Padding`
- [ ] 缺组件时先补组件库再写业务（参考 [02-组件库/组件总览.md](../02-组件库/组件总览.md)）
- [ ] 组件复用而非复制粘贴

### 顶部通知
- [ ] 所有用户反馈走 `context.appSuccess/Error/Warning/Info`，**禁止** `ScaffoldMessenger.showSnackBar(...)`
- [ ] API 错误用 `context.appApiError(e)` 自动提取 `fieldErrors`
- [ ] 通知位置为顶部（status bar 下方），不允许底部
- [ ] 同一页面同时存在的通知最多 3 条，超出按 FIFO 出队（系统自动）

### 防连点 + 等待回执
- [ ] 任何触发网络请求的按钮必须用 `UtenActionButton`（自带 loading） 或 `UtenButton(isLoading: ...)`
- [ ] Switch / Slider / SegmentedButton 等联动控件：动作前后用 `ClickGuard.run()` + `isBusy` 守卫
- [ ] `onAction` 回调里用 `context` 前必有 `context.mounted` 守卫（异步回执后页面可能已 pop）
- [ ] 不在 `onAction` 内同步抛错；如需抛异常，外层 `try/catch` 转为 `appApiError` 展示
- [ ] 联动的几个控件（如 HVAC 开关 + 温度滑杆）共享同一个 `ClickGuard` 实例，避免不同控件独立互锁

### 安全
- [ ] 表单做客户端校验（长度、格式、必填），**后端独立再校验**（前端不可信）
- [ ] 敏感数据（密码、token）走 `flutter_secure_storage`，不进 shared_preferences
- [ ] 不硬编码密钥、URL、Mock 账号；密钥/部署类配置走 `.env`，**不进系统设置页**
- [ ] 路由守卫：未登录访问受保护页跳登录页；菜单/按钮按权限点 `hide`（**不是 disable**）
- [ ] 错误信息不暴露后端细节（stack/code/SQL），统一 `ApiException` → `context.appError(...)`
- [ ] **新增数据导出**：五道防线（独立 `*:export` 权限不复用 view / Bucket4j 限流 / Agile AES-256 加密、密码入 body / 行数上限 / 审计）—— [§十.1](../05-架构/安全策略.md)
- [ ] **新增高危配置（系统设置）**：五道防线（`/admin/` 路由守卫 / `@PreAuthorize('user:manage')` / **二次密码确认** / 审计 / 类型校验）—— [§十.2](../05-架构/安全策略.md)
- [ ] **报表行带元数据但不展示**：列 key 以 `__` 前缀（如 `__srcId`），`execute()` 自动过滤；前端 `row['__srcId']` 可读用于行点击跳源头

### 数据模型
- [ ] 涉及新实体 → 同步更新 [04-数据模型/实体字典.md](../04-数据模型/实体字典.md)
- [ ] 涉及新关联 → 同步更新 [04-数据模型/ER草图.md](../04-数据模型/ER草图.md)

### 文档
- [ ] 新增页面 → 在 [03-页面/](../03-页面/) 下创建该页文档
- [ ] 新增组件 → 在 [02-组件库/](../02-组件库/) 下创建该组件文档
- [ ] 重大决策 → 在 [99-决策记录-ADR/](../99-决策记录-ADR/) 下记录

---

## 📐 关键设计决策速查

| 问题 | 答案 |
|---|---|
| Widget 前缀 | `Uten`（如 `UtenButton`），不用 `Utn` |
| 状态管理 | Riverpod 2.x（`Provider`/`Notifier`/`AsyncNotifier`） |
| 路由 | go_router，路径用 kebab-case |
| 响应式断点 | compact <600 / medium 600–840 / expanded >840（dp） |
| 性能档 | lite / standard / rich，默认 standard，可手动覆盖 |
| 主色 | 深绿 `#0F3D2E`，强调色青绿 `#14B8A6` |
| 字号档 | 小 / 中（默认）/ 大 / 超大，通过全局缩放因子实现 |
| 国际化 | 中文 `zh` + 英文 `en`，arb 在 `lib/core/l10n/` |
| 后端 | Spring Boot 3 + PostgreSQL + Flyway，**全量接入**（业务模块不再 Mock） |
| 网络 | dio + Repository 接口；`AuthInterceptor` 自动加 token + 401 静默刷新；token 走 `flutter_secure_storage` |
| 权限模型 | **部门配置（含上级）+ 个人覆盖 + 按权限点脱敏**（角色体系 2026-07-24 下线，[ADR-011](../99-决策记录-ADR/ADR-011-工作台部门分区与动态权限配置.md)） |
| 统一表格 | `MasterDataTableView`（全局唯一数据展示：6 报表族 + 6 主档 + 单据列表共用，[文档](../02-组件库/MasterDataTableView.md)） |
| 加密导出 | `UtenExportButton` → POI SXSSF + Agile AES-256，密码入 body + 限流 + 审计（[ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md)） |
| 存储分级 | 偏好 shared_preferences / **token·密码 flutter_secure_storage**（禁止混用） |
| 文字选择 | `SelectionArea` 包登录后外壳（`app_router.dart` 的 `ShellRoute.builder`，须在 Navigator 内，放 `MaterialApp.builder` 会崩）——Text 默认不可选 |
| 主 Tab 转场 | compact 左右滑动 / medium+ 即时切换（不在大屏整页横移，见 [02 §3.1.1](02-响应式与多端适配.md)） |

---

## 🧭 如何使用本文件夹

1. **新加入项目的开发者**：从 01 到 10 顺序读完，再去看 [01-规划/](../01-规划/)
2. **开始一个新功能**：打开本文件，对照"开发前的对照清单"
3. **不确定某个细节**：去对应的 01–10 单项准则文档里查
4. **做一个新决策**：在 [99-决策记录-ADR/](../99-决策记录-ADR/) 记录为什么
5. **碰数据导出 / 高危配置 / 隐藏元数据**：必看 [../05-架构/安全策略.md §十](../05-架构/安全策略.md)（强制规范，2026-07-27 起）

### 本文件夹文件清单

| # | 文档 |
|---|---|
| 00 | 准则索引与开发清单（本档） |
| 01 | [命名规范](01-命名规范.md) |
| 02 | [响应式与多端适配](02-响应式与多端适配.md) |
| 03 | [自适应布局组件](03-自适应布局组件.md) |
| 04 | [字体与字号可调](04-字体与字号可调.md) |
| 05 | [国际化与多语言](05-国际化与多语言.md) |
| 06 | [动画规范](06-动画规范.md) |
| 07 | [性能自适应](07-性能自适应.md) |
| 08 | [主题与配色](08-主题与配色.md) |
| 09 | [组件库使用规范](09-组件库使用规范.md) |
| 10 | [安全准则](10-安全准则.md)（2026-07-27 重写，对齐全量后端 + 加密导出 + 系统设置） |
