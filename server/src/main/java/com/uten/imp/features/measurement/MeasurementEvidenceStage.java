package com.uten.imp.features.measurement;

/** Draft input is intentionally absent: only reviewed business facts may learn. */
public enum MeasurementEvidenceStage {
    APPROVED,
    POSTED,
    REVERSED
}
