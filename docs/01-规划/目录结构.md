# 目录结构

> 本档定义 Uten IMP 项目的完整目录结构、每一层每一文件夹的职责。
> 任何新增文件必须放到正确的位置，禁止随意创建顶层目录。

---

## 一、顶层结构

```
uten_imp/
├─ README.md                       项目总览，第一链指向 00-项目准则
├─ pubspec.yaml                    Flutter 项目配置 + 依赖
├─ analysis_options.yaml           lint 规则（very_good_analysis）
├─ .gitignore
├─ .metadata                       Flutter 自动生成
├─ docs/                           ★ 所有设计文档
├─ lib/                            ★ Flutter 前端源代码
├─ server/                         ★ Spring Boot 3 后端（Java 21，独立 Maven 工程，见 ADR-006；全量接入）
│  ├─ pom.xml · docker-compose.yml · .env.example
│  ├─ src/main/java/com/uten/imp/  config/ security/ common/ features/(auth|org|rbac|master|purchase|sales|subcontract|stock|warehouse|production|finance|profilechange|visitor|preference|admin) audit/
│  └─ src/main/resources/db/migration/  Flyway 迁移 V1–V72（建表 + 组织树/RBAC/业务模块/系统设置/导出权限种子）
├─ test/                           单元测试 + Widget 测试
├─ integration_test/               集成测试
└─ assets/                         字体、图标、图片、lottie
```

---

## 二、`docs/` 目录

```
docs/
├─ 00-项目准则/                 ★ 最高优先级，每次开发对照
│  ├─ 00-准则索引与开发清单.md
│  ├─ 01-命名规范.md
│  ├─ 02-响应式与多端适配.md
│  ├─ 03-自适应布局组件.md
│  ├─ 04-字体与字号可调.md
│  ├─ 05-国际化与多语言.md
│  ├─ 06-动画规范.md
│  ├─ 07-性能自适应.md
│  ├─ 08-主题与配色.md
│  ├─ 09-组件库使用规范.md
│  └─ 10-安全准则.md
├─ 01-规划/                     愿景范围 / 技术选型 / 目录结构（本档）
├─ 02-组件库/                   每个 Uten 组件一份文档 + 组件总览
├─ 03-页面/                     ★ 核心：一页一档 + 页面总览
├─ 04-数据模型/                 实体字典 + ER 草图（随页面生长）
├─ 05-架构/                     路由 / 状态管理 / 网络Mock / 安全 / 目录
├─ 06-老系统融合/               双写策略 / 老系统调研 / 迁移方案
└─ 99-决策记录-ADR/             每个重大决策记录
```

### 文档目录命名规则
- 编号 + 中文名：`00-项目准则/`、`03-页面/`
- 编号决定阅读顺序，从 00 开始
- `99-` 表示"元信息"（决策记录）

---

## 三、`lib/` 目录（feature-first + shared core）

```
lib/
├─ main.dart                       应用入口
├─ app.dart                        MaterialApp.router 配置
│
├─ core/                           ★ 框架级能力（业务无关）
│  ├─ theme/                       主题
│  │  ├─ uten_colors.dart          品牌色板常量
│  │  ├─ uten_text_styles.dart     字号 token
│  │  ├─ light_theme.dart          buildLightTheme()
│  │  └─ dark_theme.dart           buildDarkTheme()
│  │
│  ├─ l10n/                        国际化
│  │  ├─ app_zh.arb                中文文案
│  │  ├─ app_en.arb                英文文案
│  │  └─ (gen) app_localizations.dart  自动生成
│  │
│  ├─ responsive/                  响应式
│  │  ├─ breakpoint.dart           Breakpoint 枚举 + 扩展
│  │  └─ responsive_container.dart UtenResponsiveContainer
│  │
│  ├─ performance/                 性能分级
│  │  ├─ performance_tier.dart     lite/standard/rich 枚举
│  │  └─ device_scorer.dart        设备能力打分
│  │
│  ├─ router/                      路由
│  │  ├─ app_router.dart           go_router 配置
│  │  └─ route_names.dart          路由路径常量
│  │
│  ├─ network/                     网络层（已接真实后端）
│  │  ├─ api_client.dart           dio 实例 + 拦截器（AuthInterceptor 自动加 token + 401 静默刷新）
│  │  ├─ api_endpoints.dart        接口路径常量
│  │  ├─ api_error.dart / api_exception.dart  统一错误码 + ApiException
│  │  ├─ session_event_bus.dart    登录态事件总线
│  │  └─ interceptors/             鉴权/日志/错误/导出限流拦截器
│  │
│  ├─ security/                    安全工具
│  │  ├─ secure_storage.dart       flutter_secure_storage 封装
│  │  └─ input_validators.dart     通用校验器（长度/格式/防注入）
│  │
│  └─ utils/                       通用工具
│     ├─ date_utils.dart
│     ├─ string_utils.dart
│     └─ logger.dart
│
├─ shared/                         跨模块共享
│  ├─ providers/                   全局 Provider
│  │  ├─ theme_provider.dart       ThemeNotifier（深/浅切换）
│  │  ├─ locale_provider.dart      LocaleNotifier（中/英切换）
│  │  ├─ font_scale_provider.dart  FontScaleNotifier（字号档）
│  │  ├─ performance_provider.dart PerformanceNotifier（性能档）
│  │  └─ current_user_provider.dart 当前登录用户
│  ├─ extensions/                  Dart 扩展方法
│  │  ├─ context_extensions.dart   context.breakpoint / context.theme 等
│  │  └─ string_extensions.dart
│  └─ models/                      跨模块共享的 model
│     ├─ user.dart
│     ├─ role.dart
│     └─ permission.dart
│
├─ components/                     ★ Uten 组件库
│  ├─ buttons/
│  │  ├─ uten_button.dart
│  │  └─ uten_icon_button.dart
│  ├─ cards/
│  │  ├─ uten_card.dart
│  │  └─ uten_stat_card.dart
│  ├─ inputs/
│  │  └─ uten_input.dart
│  ├─ feedback/
│  │  ├─ uten_empty.dart
│  │  ├─ uten_skeleton.dart
│  │  └─ uten_toast.dart
│  ├─ layout/
│  │  ├─ uten_scaffold.dart
│  │  ├─ uten_app_bar.dart
│  │  ├─ uten_responsive_grid.dart
│  │  └─ uten_master_detail.dart
│  ├─ data_display/
│  │  ├─ uten_animated_number.dart
│  │  ├─ uten_data_table.dart
│  │  └─ uten_tag.dart
│  ├─ settings/                   设置类组件
│  │  ├─ uten_theme_switcher.dart
│  │  ├─ uten_locale_switcher.dart
│  │  └─ uten_font_scaler.dart
│  └─ index.dart                  统一导出（barrel file）
│
├─ features/                       ★ 业务模块（每个 feature 独立）
│  ├─ auth/                        登录认证（真实后端）
│  │  ├─ pages/
│  │  │  ├─ login_page.dart
│  │  │  └─ forgot_password_page.dart
│  │  ├─ providers/
│  │  │  └─ login_provider.dart
│  │  └─ repositories/
│  │     └─ auth_repository.dart   Dio 实现（Argon2id + JWT 轮换）
│  │
│  ├─ shell/                       App 外壳（导航 + 路由壳）
│  │  ├─ pages/
│  │  │  └─ main_shell_page.dart   包含 NavRail/BottomNav
│  │  └─ widgets/
│  │     ├─ uten_nav_rail.dart
│  │     └─ uten_bottom_nav.dart
│  │
│  ├─ dashboard/                   工作台首页
│  │  ├─ pages/
│  │  │  └─ dashboard_page.dart
│  │  └─ widgets/
│  │     └─ stat_card_grid.dart
│  │
│  ├─ settings/                    设置（主题/语言/字号/性能档）
│  │  └─ pages/
│  │     └─ settings_page.dart
│  │
│  ├─ employee/                    员工档案（真实后端）
│  ├─ department/                  部门（真实后端）
│  ├─ hr_profile/                  员工信息变更审核（HR 队列，真实后端）
│  ├─ payroll/                     工资条（员工端 / 人事生成 / 财务审核，真实后端）
│  ├─ expense/                     报销（员工端 / 财务审批，真实后端）
│  ├─ notice/                      通知（真实后端）
│  ├─ suggestion/                  建议箱（真实后端）
│  ├─ profile/                     个人资料 + 我的修改申请（真实后端）
│  ├─ entry/                       入口选择页（员工/访客分流）
│  ├─ visitor/ · visitor_approval/ 访客预约 + HR 审批（双主体 JWT，真实后端）
│  ├─ security/                    保安扫码核验（真实后端）
│  │
│  ├─ basic_data/                  ★ 基础资料（货品/模具/客户/供应商/颜色/单位/账户/币种/仓库/收付款类别）
│  │  ├─ pages/                    各主档页 + basic_data_hub_page
│  │  ├─ repositories/             Dio 实现
│  │  ├─ models/                   主档 node + master_facet
│  │  └─ widgets/                  ★ MasterDataTableView（全局唯一数据表格）+ showMasterEditDialog + showMasterDetailSheet
│  │
│  ├─ purchase/                    采购管理（申请/订货/收货/退货 + 9 报表，真实后端）
│  ├─ sales/                       销售管理（报价/订货/出货/退货/其它出货 + 8 报表，真实后端）
│  ├─ subcontract/                 委外管理（进仓/发料/退货/材料退/损耗 + 8 报表，真实后端）
│  ├─ warehouse/                   仓库管理（9 类出入库单据 + 14 报表，真实后端）
│  ├─ stock/                       库存查询（余额 + 出入库流水，真实后端）
│  ├─ production/                  生产管理（计划单/日报/BOM 成本 + 2 报表，真实后端）
│  ├─ finance/                     钱流管理（收付款/费用/收入/银行存取款 + 应收应付 + 22 报表 5 卡，真实后端）
│  │
│  ├─ report/                      ★ 报表共享层（跨模块复用）
│  │  └─ shared/                   report_column / report_data / report_date_range / report_sort / report_cell
│  │
│  ├─ admin/                       系统管理（超管）
│  │  ├─ pages/                    admin_permissions_page / admin_system_settings_page / admin_audit_log_page
│  │  ├─ repositories/             admin_repository / system_setting_repository / audit_log_repository
│  │  └─ widgets/                  部门权限视图 / 员工权限面板 / 权限目录分组
│  │
│  ├─ lab/                         实验室（前端已实现，后端待接入）
│  ├─ hvac/                        空调控制（前端已实现，后端待接入）
│  ├─ inventory/                   旧库存 UX（前端 demo；业务已迁至 stock 模块）
│  └─ placeholder/                 占位页（未落地模块的预览入口）
│
└─ assets/                         资源
   ├─ fonts/
   ├─ icons/
   ├─ images/
   │  └─ logo_primary.svg
   └─ lottie/
      ├─ loading_dots.json
      └─ success_check.json
```

---

## 四、Feature 内部结构（统一约定）

每个 `features/<name>/` 内部都遵循同样的子目录：

```
features/<name>/
├─ pages/                          页面（带 Scaffold 的顶层 Widget）
│  └─ <name>_page.dart
├─ widgets/                        该 feature 私有的子组件（不进 Uten 组件库）
├─ providers/                      该 feature 的 Riverpod Provider
├─ repositories/                   数据访问（前端 Mock / 后端 dio）
├─ models/                         该 feature 的数据模型
└─ <name>.dart                     barrel file（可选）
```

**何时建子目录**：只有该 feature 真的需要时才建，不预创建空目录。比如一个只有 1 个页面的 feature，就只建 `pages/` 即可。

---

## 五、命名细节

详见 [../00-项目准则/01-命名规范.md](../00-项目准则/01-命名规范.md)。这里补充几个易混淆点：

- 组件文件名：`uten_<name>.dart`（如 `uten_button.dart`）
- 页面文件名：`<feature>_page.dart`（如 `login_page.dart`）
- 私有子组件：`_<name>.dart`（如 `_stat_tile.dart`，仅 feature 内用）
- 测试文件：与源文件同路径 + `_test.dart` 后缀（如 `test/components/buttons/uten_button_test.dart`）

---

## 六、禁止事项

- ❌ 在 `core/` 里放业务逻辑
- ❌ 在 `components/` 里放业务相关组件（业务相关放 feature 的 widgets/）
- ❌ 跨 feature 直接 import 对方的内部文件（必须通过 shared/）
- ❌ 在 `lib/` 根目录散落 .dart 文件（除 main.dart / app.dart）
- ❌ 随意新建顶层目录（任何新目录必须先更新本档）
- ❌ 在 features 里直接堆 Material 组件（必须用 components/）

---

## 七、目录与文档的对应关系

| 代码目录 | 对应文档目录 |
|---|---|
| `lib/core/theme/` | `docs/00-项目准则/08-主题与配色.md` |
| `lib/core/responsive/` | `docs/00-项目准则/02-响应式与多端适配.md` + `03-自适应布局组件.md` |
| `lib/core/performance/` | `docs/00-项目准则/07-性能自适应.md` |
| `lib/core/l10n/` | `docs/00-项目准则/05-国际化与多语言.md` |
| `lib/core/network/` | `docs/05-架构/网络层与Mock.md`（已接真实后端，"Mock" 为历史名） |
| `lib/core/security/` | `docs/00-项目准则/10-安全准则.md` + `docs/05-架构/安全策略.md` |
| `lib/components/` | `docs/02-组件库/` |
| `lib/features/basic_data/widgets/MasterDataTableView` | `docs/02-组件库/MasterDataTableView.md` |
| `lib/features/<name>/pages/` | `docs/03-页面/<页面名>.md` |
| `lib/features/<name>/models/` | `docs/04-数据模型/实体字典.md` |
| `server/src/main/.../features/<module>/` | `docs/数据迁移/<n>-<模块>-新库与迁移.md` |
| `server/src/main/resources/db/migration/` | `docs/数据迁移/27-DDL一致性契约.md` |

---

**最后更新**：2026-07-27（对齐全量后端：server features 扩至业务八模块 + admin；lib/features 列全；core/network 标注已接真实后端）
