# 销售订单生产进度
> **2026-09-03 分类范式收口（ADR-066）**：看板顶部指标卡（待完成/财务驳回/待排产/生产中/可发货/已发货）退役，改为统一分段卡：财务驳回/待排产/生产中/可发货（计数徽章，stage-counts 全量口径）+ 搜索 + 末尾「历史记录」段（已发货入历史：时间门控 时间段/全部，未选时间不发请求）；默认不选显示引导占位，「待完成」聚合视图由默认占位语义取代。/sales/orders/progress 接口新增 keyword/dateFrom/dateTo 可选参数。

> **页面状态**：现有计划/执行分段深链已实现；V234/V237/V239 与 V247–V250 物料分析核心已有源码候选、自动化和隔离真实 HTTP 证据，但本页“订单行 → analysis item → SUBMITTED 草稿 → APPROVED 多批”的完整新投影仍是待实现/待验收合同。隔离链已验证批准分批后销售已排量守恒，不等于销售进度页面已返回全部 analysis/plan-link/三阶段字段；目标库和真实岗位 UAT 未完成，生产发布仍为 **NO-GO**
> **入口**：销售管理 hub「订单进度查询」看板（`/sales/progress`）+ 销售订货单详情底栏「进度追踪」按钮
> **目标路由**：`/sales/progress`（看板）；`/sales/progress/:orderId`（订单进度详情整页，2026-08-19 起替代排产进度底表弹窗，见[销售订单进度详情页](销售订单进度详情页.md)）；`/production/plans/:id?executionSegmentId=:segmentId`（单据深链跳转）
> **更新日期**：2026-08-19（快递式履约进度时间线：点卡/详情按钮进入整页，每环带责任人与时间、最新在最上；V300 财务闸门口径不变——财务确认前产品进度区隐藏，时间线仍展示审核轨迹）
>
> **2026-08-18 财务闸门呈现（V300）**：订单行带 `financeConfirmed`。财务确认通过前——看板卡片显示「等待财务审核」徽章、进度环归零、数量区替换为等待说明；点卡进入进度详情页后产品进度区按同一口径隐藏（原"点按提示"交互已随弹窗下线移除）。现货预留不受闸门影响，未确认订单的「去发货」保持可用（V294 既定：审核落点预留照旧，闸门只放行计划可见性）。

---

## 2026-08-11 现行覆盖：销售投影与生产工作台不是同一页面

当前已实现的生产工作台子计划展开文案是「子计划 N 张（点开展示进度）」，行内把「已报工 X」「已入库 X / 排产 X」与物料齐套文字分开，整行点击进入真实子计划。375px 宽度使用两行布局，触控高度至少 56dp，并以 1.3 倍系统字体无溢出为回归合同。销售进度页只能读取经对象范围裁剪后的相同权威事实；它不得复制生产写按钮，也不得因为能看深链就获得排产、派工或报工能力。

调度台待排区和物料分析候选区的移动分页能力不同：物料分析候选手机卡已有「上一页/下一页」和跨页保留选择；当前调度台 compact 待排卡仅展示首批 100 行，没有分页控件。销售进度文档不得借用任一生产页面的分页说明来宣称本页投影已经实现，本文〇节仍是待实现/待验收合同。

「备料汇总预览」是只读「生产备料计划汇总单」，不创建计划或采购/委外单；MAKE/root 只有在「填写生产计划单」向导中按车间汇总并点「确认并提交审批」后才形成草稿/提交关联。销售端应分别投影待分析、备料、草稿待审、正式下达、报工、成品入库和发货，不能把只读预览或子件入库当作父成品已完成。

## 〇、V234 订单行—计划前分析—分批生产口径

本节依据 [ADR-029](../99-决策记录-ADR/ADR-029-生产计划前需求与物料分析重构.md)，优先于后续只覆盖正式计划的旧投影说明。它是计划前分批销售进度的验收合同，不表示当前销售进度 API/页面已经返回 analysis item、plan link 或全部阶段数量。生产端当前操作见[生产物料分析页](生产物料分析页.md)。

销售订单生产进度按以下层级展示：

```text
销售订单
  └─ 销售订单行（产品、订货量、交期）
      ├─ 待分析/备料的 material-analysis item
      ├─ SUBMITTED plan link（计划草稿待批准，不计已下达）
      ├─ 正式批次 1（已完成）
      ├─ 正式批次 2（生产中）
      └─ 正式批次 3（READY、待发料）
```

一条销售订单行可以分批关联多张正式计划和多个执行批次；一张合并生产计划也可以精确分摊到多条销售行。界面不得按货品名称临时 JOIN，也不得把第一张计划的状态覆盖整条订单行。

每条订单行至少显示：

- 订货有效量、已取消量；
- 已进入计划前需求量、待分析/待料量；
- 已提交待批准量、已批准正式下达量、READY 待发料量、生产中量；
- 合格入库量、可发货量、已发货量和剩余量；
- 当前最早预计齐套日、订单风险和投影更新时间。

展开正式批次显示计划号、本批分摊数量、车间、状态、计划/实际日期和深链。计划前需求仅显示销售可解释的“待分析、备料中、预计齐套日、结构待确认”等状态，不暴露采购价格、供应商谈判、内部绩效或敏感缺陷责任。

MAKE `MAKE_COMPONENT` child analysis item、正式子件计划、报工和子件入库作为父批次的物料依赖单独下钻。只有子件合格入库并最终推动父产品生产/入库后，才增加销售订单行的父成品阶段量；子件自身数量不能直接记入销售完成。

阶段状态由数量投影派生，不回写一个人工百分比真相：

| 阶段 | 判定重点 |
|---|---|
| 待分析 | 有有效剩余需求，但尚无当前有效物料分析 |
| 备料中 | 有 supply action 或预计供给，尚无正数 READY 下达覆盖 |
| 已提交待批准 | 存在 `production_material_analysis_plan_links=SUBMITTED` 及计划草稿/ACTIVE planning draft，但尚未批准；不得计 READY 或已预留 |
| 部分已下达 | 已批准下达量大于零且小于有效需求量 |
| 生产中 | 有已发料/开工/审核报工但未全部合格入库的批次 |
| 已完成生产 | 父成品合格入库量覆盖有效需求量；不等于已发货 |
| 部分/全部已发货 | 继续以仓库审核发货事实判定 |

订单改量或取消时，未转下游计划前需求可撤回；已有正式计划、库存预留、发料、报工或入库时显示“处理中/需反向”，直到对应事实完成反向后再降低阶段数量。读模型不得提前把业务事实抹掉。

销售用户默认只能查看自己及授权范围的订单。生产计划和计划前需求深链是否可点击由服务端对象范围和 `allowedActions` 决定；无权时保留销售可见阶段量并显示锁定说明，不借深链扩大生产、采购或员工信息权限。

移动端按订单行折叠批次，首屏优先展示数量和风险；批次列表支持分页/按需加载。状态使用文本、图标和语义色共同表达；数字使用稳定对齐，动态字体下不得截断关键数量。加载失败显示数据截止时间和重试，不把空响应当“没有生产”。

## 一、页面职责

销售订单生产进度把订单行对应的生产计划和执行分段按销售可见口径展示，回答：

- 已排产、已生产、已入库各有多少；
- 当前由哪些生产计划和执行分段承接；
- 分段处于物料不齐套备料中、物料齐套备料中、备料完毕、生产中、已完成、已取消或已红冲的哪一阶段；历史 `DISPATCHED` 只显示为兼容工单，不再代表一个待人工办理步骤；
- 哪些分段已延期，以及销售可以对客户说明的原因。

该弹层是履约读模型，不是生产写入口，也不能用一个百分比替代各阶段数量。

## 二、点击行为

- 点击生产计划编号，先关闭销售弹层，再打开该生产计划详情；
- 点击某个执行分段，打开同一父计划详情并携带 `executionSegmentId`；
- 计划详情在分段加载完成后自动打开对应分段详情；
- 计划 ID 缺失、当前账号无查看权限或路由失败时必须给出明确反馈，不能静默无响应。

销售侧把执行分段称为“执行子计划”仅是展示文案；它仍不是 `subplan_links` 中的独立自制件子计划。

## 三、权限与敏感信息

- 只有具备生产计划查看权限或被允许的跨部门查看范围，计划和分段才表现为可跳转；
- 无权时保留销售可见进度，但显示锁定状态，点击提示“当前账号没有生产计划查看权限”；
- 对象范围与敏感字段必须由服务端裁剪，前端不得仅靠隐藏控件保护成本、报价、内部不良责任和员工绩效；
- 从销售页跳入生产页不会授予任何编辑、派工或报工权限。

## 四、数量与异常口径

- 分摊量、报工量、入库量分别展示，不能相加成单一完成率；
- 部分生产、部分入库、部分发货必须保留各自余量；
- 自制件子计划只展示自身计划/报工/入库事实；V194 的 MAKE peg 只推动父物料需求齐套，子计划计划量/报工量/入库量不得直接冒充父成品或销售订单完成量；
- 执行分段不存在或已被合法反向时，刷新服务端投影后显示真实状态；
- 读模型更新时间和主单据事实冲突时，以服务端权威单据为准并允许刷新；
- 没有生产计划的现货订单不应伪造生产进度。

## 五、验收证据与边界

`test/features/sales/widgets/sales_plan_progress_sheet_test.dart` 已覆盖：

- 有权限时点击执行分段；
- 销售弹层关闭；
- 路由进入正确父计划；
- 查询参数保留正确的 `executionSegmentId`。

该回归解决跨模块点击与定位问题，不证明 MAKE 回供父需求；尚不能替代真实账号权限、浏览器返回栈、历史订单、移动端尺寸和父需求 peg 的集成 UAT。

## 六、关联文档

- [生产执行分段与报工页](生产执行分段与报工页.md)
- [生产调度工作台](生产调度工作台.md)
- [销售订货到发货全链路 SOP](../07-业务链路/01-销售订货到发货全链路-SOP.md)
- [生产订单排产与执行全链路需求](../07-业务链路/04-生产订单排产与执行全链路需求.md)

---

## 七、订单进度查询看板 + 待关注角标（2026-08-27）

本节记录 2026-08-04 已有看板实现，供 V234 兼容和回归。其单一生产百分比只能保留为次要摘要，不能取代〇节的阶段数量和一行多批结构。

### 入口与路由
- 销售管理 hub「销售管理」分组入口卡 **订单进度查询**（`/sales/progress`，权限 `sales_order:view`），右上角带 **待关注角标**；工作台首页“销售管理”卡复用同一数据源。
- 看板页 `sales_order_progress_page.dart` 列出当前归属销售的全部已审订单，每单一卡。
- **布局**：与货品资料 / 任务工作台一致的「顶部折叠 + 列表内滚」——上滑先收起顶部阶段指标筛选卡腾出空间，之后订单卡列表（`ListView(primary: true)`）内部滚动；分页条常驻底部。阶段卡为 待完成(默认) / **财务驳回** / 待排产 / 生产中 / 可发货 / 已发货；再点已选卡回全量视图（内部 `ALL` 无对应卡片）。

### 历史进度口径
- 当前外层圆环使用 `Σ已产 / Σ订货`（clamp ≤1）。V234 新投影验收后它只作摘要，且不能单独靠绿/橙/红表达状态；卡片必须同时展示文字阶段、精确阶段数量、待分析/备料量和更新时间。
- 每卡显示 Σ已产 / Σ订货 / Σ已发 / Σ可发；阶段优先级为：财务驳回 REJECTED → 待排产 PENDING → 生产中 PRODUCING → 可发货 SHIPPABLE → 已发货 SHIPPED。REJECTED 卡使用红色文字+图标+原因/审核人/时间，不靠颜色单独表达，也不再误写成“等待财务审核”。
- 点卡片进入 `/sales/progress/:orderId` 整页，展示产品进度与快递式时间线；被驳回且当前用户可写时，卡片/详情提供“查看并修改订单”入口。
- 有可发货行（`reserved_qty>0`）时显「去发货」→ 复用 `showSalesBatchShipPanel` 批量发货面板（选可发行 + 改数量；审核后 `shipped_qty↑ / reserved_qty↓ / chain_status→8·9`，分批发货原生支持）。

### 后端
- `GET /api/sales/orders/progress?page=&size=&stage=`（权限 `sales_order:view`）：原生 SQL 聚合有效 `sales_order_items`（qty/produced_qty/shipped_qty/reserved_qty/planned_qty）by order，派生 `productionPct` 与 `stage`，并下发 `financeRejected/reason/byName/at`；归属范围同 `/stats`（`accessPolicy.nativeReadScope`）。
- `GET /api/sales/orders/progress/stage-counts` 同一聚合 SQL 返回 REJECTED 等阶段计数，作为销售返工角标的服务端真源，不另造通知计数口径。

### 待关注角标（业务状态 + 进度未读）
- 完工（成品入库审核 / 报工审核）经 ChainNoticeService→business_outbox→Notice(type=workflow, audience=订单归属销售) 投递；V209 给 `notices` 加 `source_event` 列，完工通知带 `PRODUCTION_FINISHED_INBOUND` / `PRODUCTION_REPORTED`。
- 角标 = 当前归属范围内未解决 `REJECTED` 订单数 + 该销售未读的完工进度通知数；前者来自订单状态，后者来自 `GET /api/notices/unread-count-by-source`。
- 打开看板只调 `POST /api/notices/read-by-source` 清完工进度未读；未解决驳回数继续保留。只有受控修订回草稿并重新销售审核后，REJECTED 返工数才消失并转回财务待确认角标。
- 财务驳回通知同时进入通知中心、顶部条和持久居中强提醒；员工点“关闭”只写 `popup_acknowledged_at`，不会清本角标。

### 迁移与上线
- 历史基础迁移：V208（shipment_policy 可空）/ V209（notices.source_event）/ V210（sales_returns.return_reason）；ADR-052 增量为 V406（每用户 popup ack）。**须重启后端并重建前端**。
- 原有 analyze/compile/定向测试不能替代 V406 空库/非空升级、受控修订、并发锁与真实岗位 UAT；未完成前生产 **NO-GO**。
