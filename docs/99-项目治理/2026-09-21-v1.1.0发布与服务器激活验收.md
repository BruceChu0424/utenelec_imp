# 2026-09-21 v1.1.0 发布与服务器激活验收

> 单维护者发布链(ADR-060)例行发版留证：多会话并行改动收口 → 本地隔离 worktree 验证 →
> 手动 Simple Release(用户明示不等 CI) → main 快进合并 → 服务器更新器暂存/激活 → V625 运行权限边界收紧。
> 本文只记录事实与证据位置，不重复业务设计(见各 ADR 与页面文档)。

## 一、发布候选与验证

- 分支 `feat/platform-v607-v627-20260920` → PR #21 → `main` 快进到 `6c6f2817`(2026-09-21 00:43Z 标记为已合并)。
- 本轮提交链(同一工作区三个并行会话的成果由部署会话按文件归属分批提交)：
  `0986e025` 基线 → `5d14a7e1` V628/ADR-095 车间路线随时可换+逐种物料事实 → `14e9e465` PR #21 后端全链九红收口
  → `e2a73a53` 清空业务数据受理/失败回执(ADR-067 §9) → `8d81e3ac` V629/ADR-096 零料任务三路线+路线记忆
  → `e1090899` V630 退役客户货款类别标签 → `d0599f4f` 服务器状态页/告警人话名称(ScheduledTaskCatalog)
  → `6c6f2817` ClientFacetVisibilityConsistencyTest 锚点 46→44(V630 少一列 facet，CI backend-fast 唯一红)。
- 迁移目录头：V630/586(V544、V576、V604 跳号)；五处同步点由各并行会话随迁移推进，契约测试在隔离 worktree 复验。
- 本地隔离 worktree(`D:/Projects/uten-imp-verify-wt`，与主树共享的 `server/target` 隔离，Testcontainers 真实 PostgreSQL)：

| 提交 | 验证 |
| --- | --- |
| `14e9e465` | 原 CI 九红对应类全绿：FullChain 154、RouteGate 46、WorkbenchQuery 10、SalesReturnAmountAuthority 4、ProcurementInspectionQuantityProjection 1、LegacyBootstrapCoordinator 1、LegacyBootstrapSchemaCompatibility 3、LegacyMigrationSafety 11、两 reset 契约 12、WorkshopContinuousSupply 5、PreplanFutureTransfer 1 |
| `e2a73a53` | BusinessDataResetServicePostgresTest 4、RuntimeMaintenancePrivilegePostgresTest 8、BusinessDataResetSparsePostgresTest 13 |
| `8d81e3ac` | RouteGate 47、WorkbenchQuery 11、ExecutionBatch 9、ContinuousSupply 5、SegmentService 25、四类契约；flutter analyze 0；flutter test production/basic_data/components 943 |
| `d0599f4f` | dart format 0 改动；ScheduledTaskCatalog/ServerStatusProbe/AlertScheduler/ClientCreditFloor/架构边界等 13 类 98 项；analyze 0；flutter test admin/basic_data/sales/finance/dashboard 710 |
| `6c6f2817` | CI 等价快道 `mvn test`(不设 UTEN_RUN_DB_TESTS)4917 项 0 失败(1153 跳过为 DB 门控) |

- GitHub：`6c6f2817` 的 CodeQL、Dependency Vulnerability Scan 与 Quality Gate 快道(secret/backend-fast/deployment/website)全绿；
  Flutter 与 backend-db 两个慢作业在发布时仍在运行(用户明示「不用放在 CI 了，等下次再一起检查」)，结果以 GitHub Actions 为准。
  `14e9e465`/`8d81e3ac` 的 Quality Gate 因后续推送被 cancel-in-progress 取消；`d0599f4f` backend-fast 唯一红已在 `6c6f2817` 修复。
- Simple Release：`workflow_dispatch`(version=v1.1.0，allow_running_checks=true)run 35547980790 success(约 12 分钟)：
  双 JAR + Flutter Web(APP_VERSION=v1.1.0，APP_BUILD_ID=6c6f2817)→ 两次门禁核对 → ED25519 签名 → OSS `releases/v1.1.0/** + LATEST.txt` → 旧版本清理成功。
  首次 dispatch(35547816276)被门禁按设计拒绝，原因即 `d0599f4f` 的 backend-fast 红。
- tag `v1.1.0` → `6c6f2817`：tag 触发的 Simple Release 不允许 allow_running，须等该提交 Quality Gate 全绿后再推 tag(见 §四)。

## 二、服务器更新(内网测试服务器，2026-09-21 08:40–08:50 CST)

激活前基线：active v1.0.0，Flyway 606/562，主档 员工 143 / 货品 138 / 供应商 386 / 客户 260 / 仓库 14 / 部门 40 / 账号 7，业务数据 0；
与本地开发库主档逐项一致，故走 RUNBOOK 第四节前向迁移，不重灌库。

1. 更新器：`/usr/local/sbin/uten-imp-updater` 由 e7de5bc9… 升到仓库 `deploy/simple/uten-imp-updater.sh`(5567dc21…，启动失败/回滚失败/首装无旧版三个分支修复)，候选 sha256 核验后 install，旧版另存备份。
2. Nginx：站点配置补两处精确 location(`/api/expense-claims/invoices/recognize` 9m/130s、`/api/system-test/business-data/reset` 600s，与 `deploy/nginx/uten-imp-http-lan.conf` 模板同源)，原文件备份 `uten-imp.bak-<时间戳>`，`nginx -t` 通过后 reload。
3. `uten-imp-updater check`：下载验签通过(ED25519 key SHA256:CKlYy…)，暂存 `/opt/uten-imp/releases/v1.1.0`，正确识别「含数据库迁移需人工激活」。
4. `server.env`(备份 `server.env.bak-<时间戳>`，0600)：`UTEN_SERVER_STATUS_APPLICATION_VERSION` 由陈旧的 v2026.09.16-1 改为 v1.1.0(管理台服务器状态页显示的版本号来自此变量，更新器不维护它)；
   新增 `UTEN_EXPENSE_OCR_PROVIDER=paddle`、`UTEN_EXPENSE_OCR_ENDPOINT=http://127.0.0.1:8501`(PaddleOCR 侧车此前已部署、engine_loaded，2026-09-20 样本验证见当前版本验证)。
5. `uten-imp-updater activate v1.1.0`(08:46:08–08:46:30)：pg_dump 备份 `/var/backups/uten-imp/uten_imp-v1.1.0-20260921-084608.aQSRJt.dump`(29.5MB，0600)
   → migrator 校验 562 → 应用 V607–V630 共 24 个迁移(8.9s，V630 一条「约束不存在跳过」为幂等提示)→ 原子切换 current → 健康检查通过。
6. V625 运行权限边界收紧(数据迁移 215 号文档要求的受控运维步骤，此前实机未做)：
   快照角色图/库与 schema ACL/默认 ACL/对象属主到 `/var/backups/uten-imp/role-graph-{report,revoke}-<时间戳>.txt`(0600)
   → 停应用并终止 `uten` 会话 → `REVOKE uten_migrator FROM uten` → 缺口报告：431 表/1 分区表/52 视图/6 物化视图/16 序列全部带显式 `uten` 授权(默认 ACL 表 `uten=arwd`、序列 `uten=rwU`)；
   仅 `legacy_finance_import_sources`、`legacy_procurement_receipt_import_sources`、`legacy_subcontract_order_import_sources` 按 V624/V626/V627 设计对 `uten` 只读(SELECT)；
   无 EXECUTE 的函数只有三个私有首导入口与 `fn_require_runtime_maintenance`(设计如此)；schema CREATE 为假。
   回滚命令：`GRANT uten_migrator TO uten` 后 `systemctl restart uten-imp`。

## 三、验收(全部通过)

| 检查 | 结果 |
| --- | --- |
| `uten-imp-updater status` | active/latest = **v1.1.0**，health UP，staged v1.0.0 保留为回退 |
| 服务 | uten-imp / nginx / uten-imp-updater.timer / uten-paddle-ocr 均 active，NRestarts=0(激活与撤权重启各一次均按计划) |
| 健康与版本 | `https://192.168.1.13/actuator/health/readiness` → UP；`/version.json` → v1.1.0 / 6c6f2817 |
| Flyway | **630** / 586 条成功记录，V607–V630 无失败行 |
| 新对象 | `fn_execution_segment_material_facts`/`_summary`、`fn_can_change_execution_route`、`fn_execution_start_material_ready` 存在；`clients` 无 `*payment*` 列(V630)；`expense_claim*` 5 张表(V608/V617)；七个 SECURITY DEFINER 维护入口对 `uten` 可执行(V625) |
| 只读冒烟(服务器本机以超管身份自签 5 分钟 token，经 nginx HTTPS) | `/api/auth/me`、`/api/admin/server-status`(applicationVersion v1.1.0)、`/api/production/workshop-tasks` 与 `/count`、`/api/expense-claims/mine`、`/counts`、`/summary`、`/api/master/warehouses`、`/api/system-test/business-data/last-result`、`/api/notices` 全部 200；撤权重启后复跑仍全部 200，数据库会话均为 `uten` |
| OCR 链路 | `POST /api/expense-claims/invoices/recognize`(8×8 空白 PNG)经 nginx 新 location → 后端 → 侧车 `/ocr/invoice` 200 → 后端返回 400「发票识别失败，请确认图片清晰后重试」业务错误：证明 provider=paddle 已接线(空白图无文字属预期；disabled 时文案为「未配置」) |
| 后端日志 | 激活与撤权后无 ERROR/WARN；唯一一条 ERROR 是冒烟脚本对 `/api/expense-claims` 误用 GET 触发的方法不支持记录 |
| 主档 | 143/138/386/260/14/40/7 与升级前一致 |

## 四、边界与后续

- `6c6f2817` 的 Quality Gate Flutter / backend-db 结果在发布后落地；若红，按用户指示下次一起检查并以 hotfix 补版；tag `v1.1.0` 在其全绿后推送。
- `GlobalExceptionHandler` 把 `HttpRequestMethodNotSupportedException` 记为 500「未处理异常」，应映射 405；不影响功能，待下版顺手修。
- 工作区新一轮在途改动(销售会话已认领 V630 之后的号从 V631 起)不在本版。
- 真实岗位 UAT 未做：本记录只证明部署、迁移、权限边界与只读接口事实。
- 服务器 `/opt/uten-imp/releases/` 下 15 个旧日期号目录与 v1.0.0 目录保留为回退证据；OSS 只保留 v1.1.0；三份权限快照与激活前 dump 路径见 §二。
