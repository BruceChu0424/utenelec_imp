# 本地 V553 探针撞号恢复

范围：2026-09-12，开发容器 `uten-imp-postgres`，`127.0.0.1:5433/uten_imp`。本次按用户续接请求执行本地备份、隔离演练、探针记录归档及正式迁移；不代表公司服务器安装或发布。

## 根因与处理边界

启动失败的最深原因是 Flyway V553 校验失败。原本地历史第 511 行为 `V553__gate_probe_delete_me.sql`，校验和 `-1570814608`；正式文件为 `V553__system_setting_effective_value_descriptions.sql`，校验和 `1616977702`。前 510 条正式迁移与当前源码完全一致。

本次将精确匹配的探针行完整归档，并在同一维护事务中写入数据库审计、移出冲突历史；随后由真实 Flyway 执行正式 V553–V557。没有改写正常迁移历史或 SQL 字节，没有执行 `flyway repair`，没有关闭校验或业务约束。

这是用户明确授权的单次本地非正式探针维护，不是正式目标库迁移规范中的通用异常处理方案。探针原始 SQL 未找到；本次保留当前对象与数据，不能据此声称撤销了未知探针的全部影响或证明其无副作用。

## 备份与隔离演练

本地备份（未提交，含本地业务数据）：

`D:/Projects/uten_imp/.codex-tmp/performance-20260912/local-probe-recovery/private/development-before-probe-recovery.dump`

- 大小：17,633,249 字节。
- SHA256：`0ec7962c62fd4c21cd58650a605173806af95f5aacbdbe5b346aa96ca8746563`。
- 已恢复至隔离容器 `uten-dev-probe-recovery-97a43f5b` 中的 `v553_backup_verified_843422ad`。
- 先前恢复到默认 PostGIS 数据库时遇到 `topology` schema 已存在；成功验证采用从 `template0` 创建的新数据库，没有覆盖失败副本。
- 本轮维护前重新比较原库与成功恢复副本：398 张表、511 条历史、完整设置行均一致。
- 隔离归档、真实执行五项正式迁移、Flyway 校验与数据对账全部通过后，才处理原本地库。

## 原本地库执行结果

| 版本 | 正式文件 | 数据库校验和 |
| --- | --- | --- |
| V553 | `V553__system_setting_effective_value_descriptions.sql` | `1616977702` |
| V554 | `V554__procurement_source_remaining_supply.sql` | `-703764070` |
| V555 | `V555__iqc_original_replacement_entitlement_continuation.sql` | `385506227` |
| V556 | `V556__audit_classification_shared_literal_matching.sql` | `477134062` |
| V557 | `V557__iqc_batch_decision_request_identity.sql` | `1850947557` |

2026-09-12 13:33:37（America/Denver），原本地库真实执行五项迁移；目录头为 V557，共 515 条，全部正式脚本名、校验和与 success 状态核验通过。

迁移后、应用启动前对账：

- 原 510 条正式历史完整 JSON 不变。
- 398 张表的原有行指纹保持一致；仅精确排除 V553 两项设置的 `description/updated_at`，随后逐项检查预期说明和所有其余字段。
- 所有配置 `value` 不变，其余设置完整行不变。
- 旧 IQC 事件除新增 `batch_request_hash` 列外原字段不变，新增列全部为 NULL。
- 原审计及归档行不变；新增恰好三条审计：一条探针历史归档、两条设置说明更新。
- 正式源码与冻结迁移目录 SHA256 再次核对，迁移期间未发生漂移。

证据保存在 `D:/Projects/uten_imp/.codex-tmp/v553-recovery-20260912/`：`clone-proof.json`、`source-proof.json`、`source-migrate.json`、`source-migrate.log`、`source-probe-archive.json`、`manifest.json`。该目录已被 Git 忽略；连接配置与数据快照不可提交。

## 启动验证

2026-09-12 13:35:08（America/Denver），`Started UtenImpApplication in 51.114 seconds`；JPA `EntityManagerFactory` 初始化成功，Tomcat 监听 8080。Flyway 在应用真实启动中再次确认 V557 已是最新，无需迁移。13:35:34 的 `GET /actuator/health/readiness` 返回 HTTP 200、`{"status":"UP"}`。

验证使用 `server` 目录的 `.env` / dev 配置，Maven 构建输出隔离到本轮证据目录，避免影响共享 `target`：

```powershell
mvn '-Duten.build.directory=D:/Projects/uten_imp/.codex-tmp/v553-recovery-20260912/build' '-Dmaven.test.skip=true' spring-boot:run
```

本轮执行当前全部主源码编译、数据库真实恢复与迁移对账、应用完整启动及 HTTP readiness；未运行完整测试套件，启动命令跳过测试源码编译。证据为 `startup.log`、`startup-proof.json`。验证实例随后通过终端 Ctrl+C 结束，用户可在 `D:/Projects/uten_imp/server` 重新执行通常的 `mvn spring-boot:run`。

## 恢复边界

备份与隔离证据保留。归档维护事务与各项 Flyway 迁移是分开提交的；若迁移中途失败，应保留证据、定位根因后继续正式迁移，或整体恢复已验证备份。正式 V553 已执行后不能仅重新插入旧探针行，不能用校验和 repair 掩盖状态。

本轮仅新增这份记录和被忽略的本地恢复证据；没有改动正式 SQL 或应用源码，没有提交、推送或部署。
