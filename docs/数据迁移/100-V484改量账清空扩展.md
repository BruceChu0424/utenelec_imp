# V484 改量事实账 · 清空孪生运行时扩展

- 版本：V484（`V484__sales_qty_change_reset_extension.sql`）
- 日期：2026-09-05
- 性质：运行时函数补丁；**零表/零列/零数据改写**。

## 背景

系统测试「清空业务数据」（ADR-067）有三处必须同步的分类事实：
`ops/reset_business_data.sql`（psql 停机版）、应用内孪生函数
`business_data_reset()`（V464 整函数 + V474/V478 式运行时扩展行）、以及
`BusinessDataResetSqlContractTest` 的登记表。V482 新增
`sales_order_qty_change_logs` 时 ops 脚本已补行（322→323、CLEAR 226→227），
但**孪生函数的运行时扩展与测试登记缺失**，导致三处契约红。

## 内容

沿用 V474/V478 的「读取已安装函数定义 + 失败关闭锚点替换」机制：读取
`pg_get_functiondef('business_data_reset()')`，锚点
`('preplan_root_output_events', 'CLEAR')` 缺失即 `RAISE EXCEPTION`；在锚点前
插入 `('sales_order_qty_change_logs', 'CLEAR')` 后重建函数。追加式事实账随
系统测试清空，与 V474/V478 同口径。

同步项：

- `ops/reset_business_data.sql` 版本目录元组补 `(483, 445)`、`(484, 446)`
  （V483/V484 均不新增表），CLEAR 数量门补 `>=482 → 226` 档；
- `BusinessDataResetSqlContractTest.RUNTIME_RESET_EXTENSIONS` 登记
  `sales_order_qty_change_logs → 484`，`extensionSql` 并读 V484；
- `ResetBusinessDataScriptContractTest` 期望 323/227；
- `BusinessDataResetServicePostgresTest` 期望 clearedTableCount 227。

## 验收

- `BusinessDataResetSqlContractTest` 5/5、`ResetBusinessDataScriptContractTest`
  5/5、`ResetBusinessDataV446PostgresTest` 1/1（容器真库全迁移到 V484 后跑
  ops 脚本）、`BusinessDataResetServicePostgresTest` 1/1（容器真库走应用内
  孪生）全绿。
