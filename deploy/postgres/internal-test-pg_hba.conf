# Internal-test PostgreSQL 16 authentication boundary. Exact file; no includes.
local   all         postgres                                peer
local   all         all                                     reject
host    uten_imp    uten,uten_migrator  127.0.0.1/32        scram-sha-256
host    uten_imp    uten,uten_migrator  ::1/128             scram-sha-256
host    all         all                 0.0.0.0/0            reject
host    all         all                 ::0/0                reject
