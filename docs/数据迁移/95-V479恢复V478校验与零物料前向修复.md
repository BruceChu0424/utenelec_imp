# 95 - V479 恢复V478校验与零物料前向修复

- 更新：2026-09-05。
- 固定文件：[V478__analysis_root_supply_fulfillment.sql](../../server/src/main/resources/db/migration/V478__analysis_root_supply_fulfillment.sql)。
- 前向修复：[V479__root_supply_zero_material_evidence.sql](../../server/src/main/resources/db/migration/V479__root_supply_zero_material_evidence.sql)。
- 当前目录：V479/441，441个文件、441个唯一版本，无重号。
- 状态：本轮定向回归40/40通过，exit0，失败/错误/跳过均0；现有库440条已应用校验和与恢复源码全部一致。没有修改现有库history、执行V479或启动现有服务。

## 一、用户启动证据与更正

用户提供的2026-09-05 10:29启动日志 (UTC-06:00)显示应用因Flyway校验失败退出：

```text
Migration checksum mismatch for migration version 478
Applied to database : 1310089889
Resolved locally    : 834637198
```

此证据确认用户连接的数据库已登记早版V478，不能继续使用“用户库未执行V478”的当前判断。日志没有给出V478原始成功执行时间；10:29是本次失败启动证据时刻，本说明不推断迁移何时执行。

上一轮代理没有向现有业务库执行迁移，但也没有实时核对该用户库的flyway_schema_history。后续零物料补丁仍留在V478文件中，导致本地字节与已登记校验值漂移。“未由代理写库”不等于“数据库没有应用未提交的迁移”。

修复方向是让V478回到已应用字节，把新增SQL放入新版本；不能把数据库校验记录改成后来本地候选的值来掩盖差异。

## 二、固定V478与新V479

已从本地V478移出最后追加的ZERO_MATERIAL执行分段证据补丁，并保留根供料/产出交接及既有计划链接修复。恢复文件匹配已登记Flyway校验值1310089889。当前核验值为：

| 文件/证据 | 字节数 | SHA-256 | Flyway校验值 |
|---|---:|---|---:|
| 固定V478恢复文件 | 20369 | 3A192ECB0B1F36FE917E2D0582F11A8204564AA8AB04EE41A9ECD0381FC9CE45 | 1310089889 |
| 新增V479前向补丁 | 920 | F4142E5A50C1D973905FEBC5D432151188A0FD1449C5B62A6F69E05AF4147B37 | -339431368 |
| 发生不匹配时的R3本地V478候选 | 21109 | 804CC4F472A5175148C2D84ABF05834D2C14C921C4314E028E87C26F7EBB9176 | 834637198 |

最后一行只保留为故障/R3测试时点证据，不能再作为部署V478的文件。V478现在已经有已应用证据，必须固定字节；今后的追加修复继续使用新的前向版本。

数据库的V478 history记录应保持原值。本地复原并不回滚或重做已执行的V478业务结构；在正常受控启动/升级流程中，应先通过既有V478校验，再仅应用待执行的V479。若另一目标的history值不同，须先核对其实际迁移来源，不把本次定点复原直接套用为所有环境一致。

### 2.1 现有目标的只读核验

本轮已对用户日志明确对应的目标做只读核验：容器 `uten-imp-postgres`，映射 `localhost:5433`，数据库 `uten_imp`。检查同时使用default_transaction_read_only=on和BEGIN READ ONLY，未执行迁移、repair或业务写入。

history中V477的checksum为860255851、V478为1310089889，均success；当前440条、最新478。导出的version/script/checksum位于 `build/migration-recovery-20260905/applied-history.csv`。逐项按Flyway算法计算并对照当前源码，440/440全部一致，唯一待执行版本为V479。

现有零物料函数仍未排除ROOT_SUPPLY，与固定早版V478一致，说明零物料修复确需通过新V479前进。这些只读结果证明当前校验对齐及待执行差异，不是已经应用V479或用户服务已成功恢复的证明。

## 三、V479的精确作用

根供料身份ROOT_SUPPLY不是制造BOM。旧 `fn_guard_execution_segment_requirement_shape()` 在ZERO_MATERIAL/DIRECT_MAKE证据查询中把它作为活动制造物料计入，导致真正无BOM自制任务被错误拒绝。

V479读取该函数定义，确认预期表名和active谓词存在后，只将制造物料存在性查询限定为 `node_role='BOM_COMPONENT'`。找不到预期结构则迁移失败关闭，不静默跳过。计划/来源/单位谱系、真实BOM、状态、零物料证据不可变与授权门禁保持。

此补丁属于V479，不再追加回V478。V478已有的计划链接BOM_COMPONENT计数与根完成投影继续保留。V479不新增表、业务行或权限码，不改历史来源/路线/库存/审计；重置脚本只同步目录版本门到V479/441，既有CLEAR/PRESERVE策略不因此扩大。

## 四、校验与实施边界

本轮不使用repair改写history、不关闭validate、不删除数据库，也不手工跳过版本。本次没有连接现有业务库执行写入或代用户重启应用；正常启动后的数据库/服务状态仍须有成功回读证据。

本轮最终验证：

| 验证项 | 当前记录 |
|---|---|
| 恢复V478及全部已应用版本校验 | 长度/SHA已核验；指定目标440/440与源码一致，唯一待执行V479 |
| 已应用旧V478的非空库，validate通过后仅前进V479 | 12项根迁移测试通过；已有6类业务行及全部旧history逐字段保持，validate通过，重复migrate执行数0 |
| 真实无BOM自制及原顶层供料服务链 | FullChain定向10/10；零料函数前后仅BOM_COMPONENT谓词变化，原有真实BOM/谱系/授权检查保留 |
| 固定V478校验及reset版本门/执行 | 不可变性7/7、reset SQL合同5/5、脚本合同5/5、隔离reset服务1/1；226个CLEAR和96个PRESERVE保持 |
| 用户现有服务成功启动及实际功能回读 | 尚无本轮完成证据 |

上轮R3的110/110和Flutter236/236等数字保留在[原验收记录](../99-项目治理/2026-09-05-顶层路线记忆与确认修复验收记录.md)，属于当时本地候选的测试范围，不自动证明“固定V478 + 新V479”的本轮升级已通过。

### 4.1 可复现命令及结果

在`server`目录执行；数据库均由测试自己的Testcontainers创建，不指向用户业务库：

```powershell
$env:UTEN_RUN_DB_TESTS = 'true'
mvn -o -q '-Dtest=FullChainEndToEndTest#rootSupply_*+materialAnalysis_directMakeLeafRunsFromAnalysisThroughFqcAndFinishedInbound,RootSupplyMigrationPostgresTest,FlywayMigrationImmutabilityTest,BusinessDataResetSqlContractTest,ResetBusinessDataScriptContractTest,BusinessDataResetServicePostgresTest' test *> target/v478-checksum-forward-v479-20260905.log
```

命令退出码0。逐类JUnit XML回读为40项、0失败、0错误、0跳过：FullChain10、根迁移12、迁移不可变性7、reset SQL合同5、reset脚本合同5、reset服务1。其中23项使用隔离PostgreSQL，已计入40，不另行相加。

已覆盖空库441条迁移及Spring启动、原无BOM生产/FQC/入库、9条根供料真实服务链、已有V478非空库到V479的单次前向升级、旧确认/销售预留/产出/完成量及完整history保持、重复校验/迁移、真实隔离清理服务。真实公司克隆演练未执行；MigrationRehearsalSupport只同步当前头479/441，不能据此声称目标克隆或恢复演练通过。

最终迁移目录441个文件/唯一版本，V478/V479哈希与上表一致，补丁检查及本次文档链接检查通过。本轮没有改前端或Java业务行为，不把上轮Flutter236或后端110与本轮40相加。

### 4.2 用户本地重新启动

源码校验恢复后，可在`D:\Projects\uten_imp\server`重新执行`mvn spring-boot:run`。正常Flyway流程会校验已应用V478，再应用唯一待执行的V479；无需清库、repair或关闭校验。本轮尚未代用户执行此启动命令，不能宣称现有服务已经运行。
