# 仓库到货登记页

本页维护单张和批量登记的现行设计及实现入口，替代此前叠加的页面演进说明。验证与部署状态统一见[本地审计与整改验收](../99-项目治理/2026-09-07-全平台本地审计与整改验收.md)。

## 入口和职责

预计到货任务中心 `/warehouse/inbound/expectations` 中，单张任务进入 `/warehouse/inbound/receipts/new`；多选任务进入批量登记页。来源由 `ProcurementReceiptPrefill` 带入，不能无来源编造订货行。

仓库填写实际到货量、单位、每行实际仓库、收货人和库位等信息。页面不录入价格、金额、币种、汇率和结算方式；服务端按已获财务批准的原订货单读取这些信息。创建的就是采购收货单或委外进仓单，无需其它部门重复录入。

## 业务步骤和数量

1. 采购订货财务批准后形成预计到货。新流委外还须已有真实审核的目标件出仓；前置自制完成、准备出仓和出仓草稿都不能冒充已出仓。
2. 仓库按本行单位填写本次实到数量。少到、多到都据实登记，不把不同单位的数量直接相加。数量乘原换算率才是库存基本量；当前页面不另设实际重量输入，重量型单位的数量本身就是重量。API保留兼容的可选实际重量，不根据货品单重自动猜测。
3. 点击“登记并送检”，服务端在一次事务中创建、审核收货并进入品质待检。超批准量先隔离，财务决定接受量或退回；隔离部分不能直接增加库存或应付。
4. 品质 PASS 只形成合格待入库量。仓库核对本次实际入库量和库位后，才增加库存、推进物料齐套。支持分批确认，不能把已登记或已检验量当成已入库量。
5. FAIL 走实际退货和补回处理，沿原订货行保留数量及来源。撤回只反向已经实际发生的记录；存在不可撤回的下游使用时拒绝整次动作，不能留下半笔撤回。

委外可回厂量同时受实际目标件出仓、已审核回厂、合法实物退回补货额度和订货净未收量约束。免费补回不再次消耗原自有材料；补货分配不能在“已审核回厂”之外又重复扣除一次。

## 批量登记表的表头快速筛选（2026-09-11）

批量到货登记（`warehouse_arrival_batch_receipt_page.dart`，`UtenEditableGrid`）一次可拉进
多张订货单的上百行。「来源订货单 / 类型 / 货品 / 颜色 / 单位 / 入库仓库」六列列头带下拉
小箭头，点开按去重值 + 计数收敛当前视图；「本次实收 / 库位号 / 物料系列」等录入列不做
筛选。筛选是纯视图级的：不动行数据、已填数量与勾选，清除即全部恢复；空值不建桶（计入
「未填」，不会出现空串桶或一堆「—」桶）；某列全空时列头退回纯标签。

## 到货来源

单张、批量登记的每行均提供“到货来源”，使用同一个控件：

| 选择 | 处理方式 |
|---|---|
| 自动识别 | 只有一种可收来源时由服务端识别；正常到货和已退未补同时存在时，要求员工明确选择 |
| 正常到货 | 使用正常未收额度，不占用已退未补额度 |
| 先补退货 | 先补回已退数量，超出部分按正常到货处理；是否免费或重新计款以原退货处理结果为准 |

框内图标支持鼠标悬停、点击和键盘阅读说明。这个选择属于本批实物事实，不学习为该货品以后所有到货的默认值。`replacementIntent` 使用 `NORMAL` / `RETURN_REPLACEMENT`；自动模式不发送该字段，保留旧调用的单一来源兼容。

## 仓库、去向和记忆提示

每行必选真实入库仓库，主/子仓级联选择，支持统一设置全部行。优先带出货品/颜色在本主仓范围内最近一次成功审核收货的有效子仓，没有记录才回退分析建议仓或原来源仓。临时下拉选择、失败、草稿、隔离和已撤回收货不作为成功仓库记忆。

原物料 UUID、来源切片和实际叶仓共同决定预定归属。按 ADR-075，有完整来源证明的合格专属物料跟随实际叶仓，不因换到另一主仓就自动变成公共库存；其它分析的专属物料和未检库存仍隔离。页面显示每条原预定的预计去向、基本量、产品、分析、计划/工单和负责人，变更实际仓须核对。登记阶段不产生合格库存；最终归属以合格后的仓库实际入库事务及 `actualAllocations` 为准。

自动带出的库位、系列、编码和仓库显示黄框、淡黄底色、框内来源图标。手动修改后清除黄标；只聚焦、移动光标或输入法组合状态变化不算修改，表格滚出再滚入也不恢复已确认的黄标。文本状态由 `UtenAutofillTextController` 持有。

登记成功后异步调用 `goods-profile-hints`，不阻塞返回任务中心。空值跳过，编码只在主档为空且全局不重复时回填；系列、库位实际改变才更新，并记录负责人。货品UUID始终是引用身份，编码和名称只是业务标识；主档建议库位不能覆盖历史入库实际库位。

## 提交、重试和任务跟进

单张页按实际仓库分组，批量页按订货单和实际仓库分组，每组一张收货单顺序提交。每次新打开的登记页持有独立登记批次标识，参与请求身份计算，避免两次真实到货数量相同却回放上一张单。来源、数量或实际仓改变会改变该组请求身份；原页相同内容丢响应重试保持相同 key。已成功组在原内容重试时回放原结果，不重复登记；失败组保留内容供处理。

服务端按当前制单人和 `idempotencyKey` 锁定命令，并比较规范请求哈希：同 key 同请求返回原结果；同 key 改内容返回冲突，不能覆盖原事实。到货来源进入哈希。制单人、命令终态及结果不可变。

成功后返回任务中心就地刷新列表和徽章，不自动跳进采购/委外金额详情。任务步骤明确显示：待登记、草稿待继续送检、超量待财务、待品质、合格待仓库入库。旧草稿可通过 `arrivals/{receiptId}/complete` 续办，由服务端修复缺失的来源表头再走同一审核链。已登记草稿量计入 `registeredQty`，避免未送检期间重复登记。

预计到货搜索匹配订货单号、供应商、货品编码和名称，分页与计数使用一致的任务可见条件，输入防抖且仅最新请求回写。新流委外仅实际出仓后按出仓单 UUID 幂等通知仓库预计回厂，通知不构成到货事实。

2026-09-12：采购和委外仓库审核网关共用各自的 `approveReceipt` 事务内核，仅执行权威校验与送检，不再额外加载仓库不消费的商业详情。普通采购/委外审核接口仍返回原详情合同。批量继续送检仍使用整批来源预锁和原审核链，不把超量隔离或品质待检改成可用库存。

## 权限和代码入口

页面与服务端同时要求 `warehouse_inbound:view` 和 `warehouse_inbound:stock_in`；提交时重新校验权限、来源、对象范围和状态。采购/委外编辑权限不能替代仓库动作权限。预计到货接口不向仓库下发单价，普通收货详情及异常中的价格依专门价格权限脱敏。

| 层次 | 文件和关键入口 |
|---|---|
| 单张登记 | `lib/features/warehouse/pages/warehouse_arrival_receipt_page.dart`：`_save`、`_learnGoodsProfiles`、`_arrivalLineColumns` |
| 批量登记 | `lib/features/warehouse/pages/warehouse_arrival_batch_receipt_page.dart`：`_save`、`_lineColumns` |
| 来源控件 | `lib/features/warehouse/widgets/warehouse_arrival_source_field.dart`：`WarehouseArrivalSourceField`、`WarehouseArrivalSource` |
| API与模型 | `lib/features/warehouse/repositories/procurement_inbound_repository.dart`：`registerArrival`、`completeArrival`；`lib/shared/models/procurement_inbound.dart`：`toReceiptPrefill` |
| 服务端入口 | `server/src/main/java/com/uten/imp/features/warehouse/inbound/WarehouseInboundController.java`：`registerArrival` |
| 登记事务 | 同目录 `WarehouseArrivalRegistrationService.java`：`register`、`complete`、`requestHash`；`ProcurementArrivalControlService.java`：到货容量与异常处理 |
| 检验与实收入库 | 同目录 `ProcurementInspectionService.java`：品质处置、实际库存入账及原来源反向 |
| 来源数据库 | V419幂等命令、V436委外实际出仓门、V516来源字段、V518计款/补回事实、V527未使用采购入库原切片反向；规则见[迁移索引](../数据迁移/README.md) |
| 页面回归 | `test/features/warehouse/arrival_receipt_review_navigation_test.dart`、`warehouse_arrival_batch_receipt_page_test.dart` |
| 真实业务回归 | `server/src/test/java/com/uten/imp/businesschain/FullChainEndToEndTest.java`：`receiving_iqcQuarantineThenWarehouseConfirmationAccumulatesStock`、`partiallyRejectedReturnKeepsOriginalOrderAsSingleReplacementChannel`、`rootSupply_buyReceiptTransfersToSalesAndReceiptReverseRestoresRootDemand` |

到货来源页面专项10项通过，含原页丢响应重试及新批相同实物数量的不同请求身份；5个相关文件定向分析无问题，记录位于本地 `operational-closure-20260907/arrival-source-tests-r4.log` 和 `arrival-source-analyze-r3.log`。这不代表最终全项目回归或服务器已经更新。

相关页面：[品质任务中心](品质任务中心.md)、[待检处置](待检处置页.md)、[合格待入库任务](采购委外IQC合格待入库任务页.md)、
[产成品待点收任务页](产成品待点收任务页.md)（2026-09-10 起「登记成品」对照本页：行级实际成品仓、统一填写库位、备注、按仓拆批并合并品质检查单、撤回登记）。

> 2026-09-10：单笔/批量到货登记的「本次实收」通用说明改放列头 ⓘ（`headerInfo`），格内不再塞 44px 图标挤占「大于 0」占位；入库仓库格去掉自带 6 圆角/(4,8) 内边距/双行小字，改吃明细表行级单元规格（圆角 10、正文字号、单行省略），必填空仍红框、改离建议仓仍橙框、学习预填仍黄标。
