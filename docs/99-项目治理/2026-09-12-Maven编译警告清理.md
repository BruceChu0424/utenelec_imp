# Maven 编译警告清理

用户所贴 `mvn spring-boot:run` 日志包含业务源码 4 条编译警告，以及测试源码的前 100 条警告。该目标先执行 `test-compile`，因此测试泛型和弃用 API 警告也出现在启动日志中。

本次修复原生查询结果的类型边界、Mockito 的 Specification / RowMapper / Criteria / Captor 泛型、泛型 varargs，以及 Jackson、Spring、AssertJ 和权限兼容别名的弃用调用。原 `any(Class)` 的非空语义通过显式泛型 `notNull()` 保留。未新增告警压制或关闭编译检查。

## 验证

- 业务源码 1,658 文件，严格编译成功、0 warning。
- 测试源码 955 文件，严格编译成功、0 warning；同时清除了被原日志数量上限遮住的弃用调用。
- 23 个相关测试类：共 206 项，205 项通过、0 失败、0 错误；1 项 PostgreSQL 用例因未设置 `UTEN_RUN_DB_TESTS` 未启用。此次未运行重型数据库规模测试。
- `git diff --check` 通过。

严格编译使用 `-Dmaven.compiler.failOnWarning=true`。通过读取本机 compiler-plugin 3.14.1 的插件描述确认此参数同时作用于 compile / testCompile。现有 POM 的 `showWarnings`、`showDeprecation` 和 `-Xlint:unchecked` 保持启用。

验证输出使用独立 `server/target-warning-cleanup-20260912`，没有清理运行服务的默认 `target`，未启动或重启业务服务。本次没有新增数据库迁移。

日志：

- `server/target-warning-cleanup-main.log`
- `server/target-warning-cleanup-testcompile-final.log`
- `server/target-warning-cleanup-tests.log`

可用以下命令再次验证业务和测试源码：

```powershell
mvn.cmd "-Duten.build.directory=target-warning-cleanup-20260912" "-Dmaven.compiler.failOnWarning=true" -DskipTests test-compile
```
