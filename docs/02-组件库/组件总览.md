# 组件库总览

> 本文只登记当前源码中真实存在的公共 UI。以 `lib/components/` 为基础组件层，`lib/shared/widgets/` 为跨模块业务组合层；模块私有组件仍放在各自 `features/<module>/` 下。
>
> 使用约束见[组件库使用规范](../00-项目准则/09-组件库使用规范.md)。最后核对：2026-09-11。

---

## 一、分层原则

1. 基础组件不得依赖具体业务模块、接口模型或权限模型。
2. 两个及以上模块稳定复用、且包含业务语义的组合组件放在 `lib/shared/widgets/`；仅一个模块使用的组件留在该模块内。
3. 页面优先组合已有组件；只有重复交互已经稳定时才继续抽象，避免为“看起来统一”制造过度封装。
4. 颜色、字号、圆角、阴影和动效取自 `lib/core/theme/`；业务页面不得新增裸 `Color(0x...)`。
5. 公共交互必须覆盖 loading、disabled、empty、error、键盘、焦点、窄屏和权限拒绝状态。
6. 项目当前没有 `lib/components/index.dart` barrel；调用方直接导入所需文件，避免无关依赖扩散。

---

## 二、基础组件清单

### 2.1 品牌与按钮

| 源码 | 对外能力 | 适用范围 |
|---|---|---|
| [`uten_brand_mascot.dart`](../../lib/components/brand/uten_brand_mascot.dart) | `UtenBrandMascot` | 品牌吉祥物 |
| [`uten_wordmark_logo.dart`](../../lib/components/brand/uten_wordmark_logo.dart) | `UtenWordmarkLogo` | 品牌文字标识 |
| [`celebration_particles.dart`](../../lib/components/effects/celebration_particles.dart) | `CelebrationParticleField` | 庆典彩屑粒子场（CustomPainter+AnimationController，`animated` 由调用方按性能档传入，控制器 dispose） |
| [`uten_button.dart`](../../lib/components/buttons/uten_button.dart) | `UtenButton`，primary/secondary/tonal/ghost/danger，三种尺寸，loading/disabled，可选 `onDisabledTap`（禁用态点击出提示） | 常规动作 |
| [`click_guard.dart`](../../lib/components/buttons/click_guard.dart) | `ClickGuard`、`UtenActionButton` | 异步防连点和动作状态 |
| [`uten_back_button.dart`](../../lib/components/buttons/uten_back_button.dart) | `UtenBackButton` | 统一返回；默认行为委托 `nav_helpers.backTo`（全站唯一返回契约：能 pop 则 pop → 栈空读 `?returnTo` → 再无则 `defaultPath`/工作台，2026-09-10 收敛，见 [路由设计 §十一](../05-架构/路由设计.md)） |
| [`uten_export_button.dart`](../../lib/components/buttons/uten_export_button.dart) | `UtenExportButton` | 表格导出（可选密码保护） |
| [`uten_import_button.dart`](../../lib/components/buttons/uten_import_button.dart) | `UtenImportButton` | 文件导入入口 |
| [`uten_drafts_button.dart`](../../lib/components/buttons/uten_drafts_button.dart) | `UtenDraftsButton`：新建单据页 AppBar 的「草稿(N)」入口，计数来自 `draftCountsProvider`，n=0 时只显「草稿」，无该单据 `*:view` 权限时隐藏；点击 `goFrom(列表路径?status=draft)`，见 [UtenDraftsButton.md](UtenDraftsButton.md)（2026-09-11） | 管理卡 `skipListOnCreate` 直达新建页后，用户回到自己草稿的入口 |

### 2.2 卡片与数据展示

| 源码 | 对外能力 | 适用范围 |
|---|---|---|
| [`uten_card.dart`](../../lib/components/cards/uten_card.dart) | `UtenCard`，solid/glass/outlined | 通用容器 |
| [`uten_hub_card.dart`](../../lib/components/cards/uten_hub_card.dart) | `UtenHubCard`，图标+标题+副标题，两个计数槽位：`badge`（右上角红色待办徽章浮层）与 `labelSuffix`（标题右侧中性括号数字，走 `WidgetSpan` 内联不影响换行），支持 enabled/未启用 | 二级模块页入口卡片；两槽位的取舍见 [徽章与计数口径](../00-项目准则/14-徽章与计数口径.md) |
| [`uten_person_card.dart`](../../lib/components/cards/uten_person_card.dart) | `UtenPersonCard` | 人员或实体摘要 |
| [`uten_info_row.dart`](../../lib/components/data_display/uten_info_row.dart) | `UtenInfoRow` | 标签—值信息行 |
| [`uten_status_badge.dart`](../../lib/components/data_display/uten_status_badge.dart) | `UtenStatusBadge` | 语义化状态 |
| [`doc_status_badge.dart`](../../lib/components/data_display/doc_status_badge.dart) | `docStatusBadgeType(int? code)`：单据 0/1/-1 状态码 → `UtenStatusBadgeType`（草稿中性 / 已审绿 / 红冲红），列表页状态列统一 `UtenStatusBadge(size: small)` 且保留 `value` 纯文本；见 [DocStatusBadge](DocStatusBadge.md)（2026-09-10） | 生产计划 / 生产日报 / 财务 / 销售 / 委外 / 采购单据列表状态列（主档「使用/停用」不适用） |
| [`uten_rack_grid.dart`](../../lib/components/data_display/uten_rack_grid.dart) | `UtenRackGrid` + `UtenRackGridRack`（库行布局）/ `UtenRackGridItem`（格内货品）：库行 × 层 × 位 货架图，一格聚合同库位多货品并显「+n」，`onCellTap` 回传库位号、`selectedPlace` 受控高亮并自动 `ensureVisible`（表格反查），空位弱化不可点，「未分层」残值平铺成一段；lite 档零动画；见 [UtenRackGrid](UtenRackGrid.md)（2026-09-10） | 货架目视化清单页（库位可视化定位/反查） |
| [`uten_totals_summary_bar.dart`](../../lib/components/data_display/uten_totals_summary_bar.dart) | `UtenTotalsSummaryBar` + `UtenTotalEntry` / `utenQuantityTotalEntry`（合计数量按 unitId 分组，**不同单位绝不相加**）/ `utenAmountTotalLabel`（币种取表头，不硬编码 ¥）：明细表下方合计条，与表体同宽、顶部分隔线（`showDivider` 可关）、右对齐「标签: 值」序列，关键金额 `danger` 标红；空值/「—」项整体隐藏；见 [UtenTotalsSummaryBar](UtenTotalsSummaryBar.md)（2026-09-11 全量推广） | 采购/委外/销售/钱流单据详情与编辑页、订货审批与销售订单财务审核详情 |
| [`uten_user_avatar.dart`](../../lib/components/data_display/uten_user_avatar.dart) | `UtenUserAvatar` | 图片/姓名回退头像 |
| [`uten_gauge_ring.dart`](../../lib/components/data_display/uten_gauge_ring.dart) | `UtenGaugeRing` + `utenGaugeStatusColor`（`UtenGaugeRingPainter` 仅供测试）：270° 圆环仪表，进度色=语义状态色，unknown 走虚线环+`—`（读不到不画 0），环外画黄/红阈值刻度，尺寸按字号自适应且可被父约束收窄；lite/减少动画/TickerMode 关闭时无过渡；见 [UtenGaugeRing](UtenGaugeRing.md)（2026-09-10） | 服务器状态页（占用率/连接数/积压/备份时效等「值+阈值」监控指标） |
| [`uten_animated_number.dart`](../../lib/components/data_display/uten_animated_number.dart) | `UtenAnimatedNumber`：值变更时一次性滚动（**仅 rich 档**，其余直显终值），null 显占位符不填 0，`tabularFigures` 宽度不抖；见 [UtenAnimatedNumber](UtenAnimatedNumber.md)（2026-09-10 重新引入，有真实调用方与测试） | 周期刷新的监控数字（环心数字、响应耗时等），列表/表格数字不适用 |

统计 KPI 当前由页面业务卡组合实现（原 [`DocKpiBar`] 已于 2026-09-04 随死码清理删除——ADR-066 分段卡上线后全站零引用）。项目中没有 `UtenStatCard`、`UtenDataTable` 或 `UtenChartPlaceholder`；时间线场景使用已实现的 `UtenProgressTimeline`（2026-08-19 新增，见 2.4），不得再假设存在 `UtenTimeline`。

### 2.3 输入与表单

| 源码 | 对外能力 | 适用范围 |
|---|---|---|
| [`uten_input.dart`](../../lib/components/inputs/uten_input.dart) | `UtenInput` | 文本、数字、密码等输入；见 [UtenInput](UtenInput.md) |
| [`uten_search_bar.dart`](../../lib/components/inputs/uten_search_bar.dart) | `UtenSearchBar` | 清空 + 300ms 防抖；`onInputChanged` 可在按键当下使旧请求失效，uncontrolled `initialValue` 支持响应式双位置文本同步 |
| [`uten_dropdown_field.dart`](../../lib/components/inputs/uten_dropdown_field.dart) | `UtenDropdownField`、`UtenDropdownItem` | 标准下拉选择；`UtenDropdownItem` 支持 `enabled=false`（分组标题，如仓库层级下拉的父仓，2026-09-04 V476）与 `indent` 缩进 |
| [`uten_filter_picker_field.dart`](../../lib/components/inputs/uten_filter_picker_field.dart) | `UtenFilterPickerField`：标签 + 当前值 + chevron 的紧凑单行筛选入口字段，点开侧滑面板选；圆角固定 `UtenRadius.control`、与 `UtenSearchBar` 等高，已生效时边框/值走 primary；见 [UtenFilterPickerField](UtenFilterPickerField.md)（2026-09-11） | 页面工具栏的**层级型筛选**（货品分类树 / 仓库主子层级）；单据表单里的一格仍用 `UtenDropdownField` |
| [`uten_date_field.dart`](../../lib/components/inputs/uten_date_field.dart) | `UtenDateField` | 日期选择 |
| [`uten_employee_picker.dart`](../../lib/components/inputs/uten_employee_picker.dart) | `UtenEmployeePicker` | 异步员工选择；主行/已选值统一 `姓名(工号)`，部门在副行；见 [UtenEmployeePicker](UtenEmployeePicker.md) |
| [`uten_field_message.dart`](../../lib/components/inputs/uten_field_message.dart) | `UtenFieldMessage`、`UtenOverflowMessage`、nullable 适配器与统一 `TextFormField.errorBuilder` | 实时状态消息（校验错误/预填提醒）及可点击通知长文案；仅真实溢出显示问号，hover/tap/focus 查看全文，错误 live-region；见 [UtenFieldMessage](UtenFieldMessage.md) |
| [`maker_audit_fields.dart`](../../lib/components/forms/maker_audit_fields.dart) | `utenMakerAuditCells` | 制单/审计只读字段(2026-08-18 起 label 统一带「(系统自动生成)」后缀，明示不可填) |
| [`link_quantity_validator.dart`](../../lib/components/forms/link_quantity_validator.dart) | `validateLinkQuantity`、`LatestLinkRequestGuard` | 关联数量校验与竞态防护 |

不存在独立的 `UtenSelect`、`UtenDropdown`、`UtenDatePicker` 或 `UtenSwitch`。对应场景分别使用 `UtenDropdownField`、`UtenDateField`；开关使用 Material `Switch` 并遵循主题。2026-09-05 起，静态说明、校验错误和预填提醒统一由 `UtenInputDecoration` 收进框内 `UtenFieldHintIcon`，悬停、点击或键盘聚焦显示完整内容；错误红框、预填黄框和原生表单校验继续生效。`fieldLabel(info:)` 提供说明元数据，字段下方不显示消息。`lib/` 页面禁止裸 `helperText/errorText`；直接 `TextFormField` 必须使用适配器及 `utenTextFieldErrorBuilder`。AST 源码契约逐个检查，唯一精确例外是适配器内部原参数转发。详见 [UtenFieldMessage](UtenFieldMessage.md)。

### 2.4 反馈与通知

| 源码 | 对外能力 | 适用范围 |
|---|---|---|
| [`uten_empty.dart`](../../lib/components/feedback/uten_empty.dart) | `UtenEmpty` | 空状态 |
| [`uten_skeleton.dart`](../../lib/components/feedback/uten_skeleton.dart) | `UtenSkeleton`、`UtenSkeletonList` | 加载骨架 |
| [`uten_dialog.dart`](../../lib/components/feedback/uten_dialog.dart) | `UtenDialog` | 主题化确认对话框；2026-09-03 起 AlertDialog 整体包 `SelectionArea`（弹窗文字可框选复制，准则 §3.4）；**2026-09-11 比例护栏**：content 统一限宽 460 + 限高 60% 屏高 + 超出自滚——AlertDialog 的 content 既不限宽也不滚动，一段长说明就能把弹窗顶到满屏高（用户反馈「登记并送检的弹窗巨长」）；正文按「一句结论 + 每行一个要点」写，限高只是兜底 |
| [`uten_center_alert.dart`](../../lib/components/feedback/uten_center_alert.dart) | `UtenAlertLevel` 与居中告警实现 | 重要/紧急提醒；统一通过 `UtenNotify.alert` 调用 |
| [`uten_notification_badge.dart`](../../lib/components/feedback/uten_notification_badge.dart) | `UtenNotificationBadge`：红色数字徽章；count<=0 不渲染、>99 显 99+ | **只给「需要我处理、不处理会出事」的待办数**（待审/待确认/待收货/待出库/待检/被驳回/超期/异常），并被上层容器逐级累加；浏览型计数改用 `UtenCountSuffix`，见 [徽章与计数口径](../00-项目准则/14-徽章与计数口径.md) |
| [`uten_module_todo_chip.dart`](../../lib/components/feedback/uten_module_todo_chip.dart) | `UtenModuleTodoChip`：hub 顶栏右上角「待办 N」红底白字药丸（+ 悬停说明） | 本模块待办累计的**自解释形态**。2026-09-11 前这里是一颗光秃秃的红色数字徽章，用户问「右上角为什么会有个消息数量徽章啊」——它没有任何文字说明自己是什么。数字仍由 `todoModuleCount(TodoModule.x, ref.watch)` 求和得出，页面里不手写加法；0 时整枚不渲染 |
| [`uten_count_suffix.dart`](../../lib/components/feedback/uten_count_suffix.dart) | `UtenCountSuffix(count, hideWhenZero, semanticsLabel, color)`：中性括号数字 `(N)`（**浏览型计数**专用形态，与红色待办徽章泾渭分明）；`null` 不渲染、卡片上 0 不渲染、分段标签传 `hideWhenZero:false` 保留 `(0)` 队形；`color` 给背景会变色的宿主（分段选中态）覆盖用，默认 `onSurfaceVariant`；见 [徽章与计数口径](../00-项目准则/14-徽章与计数口径.md)（2026-09-11） | 「有多少条、供我掂量」的入口：草稿、我的某某、历史/记录、分段标签 |
| [`uten_segment_badge_label.dart`](../../lib/components/feedback/uten_segment_badge_label.dart) | `UtenSegmentBadgeLabel(label, count, countForm)` + `UtenSegmentCountForm{browsing, actionable}`：分段按钮标签的计数槽，**默认中性括号 `(N)`**（0 显 `(0)` 保队形），显式挑 `actionable` 才是红色待办徽章（0/null 不渲染）；颜色跟随分段前景色，选中态也可读（2026-09-11 加形态参数，此前一律红徽章） | `SegmentedButton`/Tab 的 label 槽位；由 [UtenFilterToolbar](UtenFilterToolbar.md) 统一透传 |
| [`uten_draft_count.dart`](../../lib/components/feedback/uten_draft_count.dart) | `UtenDraftCountSuffix(kind)`：草稿数括号数字，数据源 `draftCountsProvider`；count<=0 或无 `*:view` 权限不渲染，见 [UtenDraftsButton.md](UtenDraftsButton.md) §三（2026-09-11 由原 `UtenDraftBadge` 红徽章改为中性括号数字） | 各模块 hub 单据卡标题后「我还有 N 张草稿」 |
| [`uten_toast.dart`](../../lib/components/feedback/uten_toast.dart) | `UtenToast` | 兼容入口；内部转发统一通知队列 |
| [`uten_context_menu.dart`](../../lib/components/feedback/uten_context_menu.dart) | `UtenContextMenuRegion`、`UtenMenuItem`、`showUtenContextMenu` | 行级右键/长按操作菜单（三端一致），见 [UtenContextMenu.md](UtenContextMenu.md) |
| [`uten_progress_timeline.dart`](../../lib/components/feedback/uten_progress_timeline.dart) | `UtenProgressTimeline`（+ `ProgressTimelineEvent` 模型） | 快递式全链路进度时间线：阶段+责任人+时间，最新在最上高亮、PENDING 垫底，见 [UtenProgressTimeline.md](UtenProgressTimeline.md) |
| [`uten_reviewer_responsibility_notice.dart`](../../lib/components/feedback/uten_reviewer_responsibility_notice.dart) | `UtenReviewerResponsibilityNotice`、`showUtenReviewerConfirmDialog` | 正式审核/通过/驳回前显示当前审核员姓名工号与责任语义，见 [UtenReviewerResponsibilityNotice.md](UtenReviewerResponsibilityNotice.md) |
| [`uten_batch_reject_dialog.dart`](../../lib/components/feedback/uten_batch_reject_dialog.dart) | `UtenBatchRejectDialog`、`showUtenBatchRejectDialog` | 审批队列多选后的统一「批量驳回原因」对话框：内置审核责任提示、原因必填（空提交不关闭），返回原因交由调用页逐单应用，见 [UtenBatchRejectDialog.md](UtenBatchRejectDialog.md)（2026-09-10） |
| [`uten_live_pulse_dot.dart`](../../lib/components/feedback/uten_live_pulse_dot.dart) | `UtenLivePulseDot`：8px 圆点，由外部计数器驱动、每次采样成功扩散一圈光晕（非循环），过期变灰静止；lite/减少动画/TickerMode 关闭时恒静态；见 [UtenLivePulseDot](UtenLivePulseDot.md)（2026-09-10） | 轮询型只读页面的「数据还在更新」指示（当前：服务器状态页总览卡），不替代加载指示器 |

新代码统一使用 [`UtenNotify`](../../lib/core/ui/uten_notify.dart)；全局排队、去重和展示由 [`app_notification.dart`](../../lib/core/ui/app_notification.dart) 负责。顶部弹条与连接恢复横幅的视觉外壳（居中 / 最大宽 720 / 圆角 14 / elevation 4 / 柔和容器色）统一由 [`uten_top_banner_card.dart`](../../lib/core/ui/uten_top_banner_card.dart) 的 `UtenTopBannerCard` 提供，两者同款。通用错误反馈规则见 [`action_feedback.dart`](../../lib/core/ui/action_feedback.dart)。

### 2.5 布局

| 源码 | 对外能力 | 适用范围 |
|---|---|---|
| [`uten_app_bar.dart`](../../lib/components/layout/uten_app_bar.dart) | `UtenAppBar` | 页面顶栏 |
| [`uten_filter_toolbar.dart`](../../lib/components/layout/uten_filter_toolbar.dart) | `UtenFilterToolbar`、`UtenFilterSegment`（含 `countForm`）、`UtenFilterPlaceholder` | 全平台「分类分段 + 搜索」统一范式（默认不选不发请求、分类行无「全部」段、末尾历史段时间门控；**分段计数默认中性括号 `(N)`，红徽章显式挑**），见 [UtenFilterToolbar.md](UtenFilterToolbar.md) |
| [`uten_history_time_filter.dart`](../../lib/components/layout/uten_history_time_filter.dart) | `UtenHistoryTimeFilter`、`UtenHistoryTimeValue`、`UtenHistoryTimePlaceholder` | 「历史记录/历史单据」段的时间门控行（时间段/全部，默认不选不加载），见 [UtenHistoryTimeFilter.md](UtenHistoryTimeFilter.md) |
| [`uten_content_container.dart`](../../lib/components/layout/uten_content_container.dart) | `UtenContentContainer` | 内容最大宽度与边距 |
| [`uten_adaptive_panel.dart`](../../lib/components/layout/uten_adaptive_panel.dart) | `showUtenAdaptivePanel<T>` | compact 底部抽屉 / medium+ 右侧面板展示壳 |
| [`uten_bottom_action_bar.dart`](../../lib/components/layout/uten_bottom_action_bar.dart) | `UtenBottomActionBar` | 底部固定动作区 |
| [`uten_picker_confirm_bar.dart`](../../lib/components/layout/uten_picker_confirm_bar.dart) | `UtenPickerConfirmBar` | 滑窗选择器统一「取消/确定」确认栏（二次操作契约），见 [UtenPickerConfirmBar.md](UtenPickerConfirmBar.md) |
| [`uten_section_header.dart`](../../lib/components/layout/uten_section_header.dart) | `UtenSectionHeader` | 区块标题 |
| [`uten_collapsible_section.dart`](../../lib/components/layout/uten_collapsible_section.dart) | `UtenCollapsibleSection` | 可折叠区块 |
| [`uten_collapsing_header_scroll_view.dart`](../../lib/components/layout/uten_collapsing_header_scroll_view.dart) | `UtenCollapsingHeaderScrollView` | 顶部可折叠 + 表格吸顶内滚联动（大屏列表页顶部卡上滑收起、表格内滚），见 [UtenCollapsingHeaderScrollView.md](UtenCollapsingHeaderScrollView.md) |
| [`uten_form_grid.dart`](../../lib/components/layout/uten_form_grid.dart) | `UtenFormGrid` | 响应式表单网格 |
| [`uten_responsive_grid.dart`](../../lib/components/layout/uten_responsive_grid.dart) | `UtenResponsiveGrid`、`UtenResponsiveColumns` | 自适应卡片网格 |
| [`uten_paged_grid.dart`](../../lib/components/layout/uten_paged_grid.dart) | `UtenPagedGrid`、`UtenGridPager` | 服务端分页卡片网格 |
| [`uten_list_two_pane.dart`](../../lib/components/layout/uten_list_two_pane.dart) | `UtenListTwoPane` | 列表页「筛选在上 / 表格在下」布局（2026-09-10 起不再左右分栏；筛选整宽封顶 45% 内部可滚，表格占满剩余高度） |
| [`uten_split_view.dart`](../../lib/components/layout/uten_split_view.dart) | `UtenSplitView` | 左树/列表 + 右详情可拖分栏（分割线拖动调宽、双击复位、宽度本地记忆），见 [UtenSplitView.md](UtenSplitView.md) |
| [`uten_segmented_filter.dart`](../../lib/components/layout/uten_segmented_filter.dart) | `UtenSegmentedFilter` | 分段筛选 |
| [`uten_editable_grid.dart`](../../lib/components/layout/uten_editable_grid.dart) | `UtenEditableGrid` 及控制器/列定义 | 可编辑明细表；列宽可拖拽调整(手动拖过即锁定)，列定义提供 `textOf` 后随内容自动加宽(只增不减、封顶 480)；另支持「只选不编」任务办理模式（`selectable` 勾选列与增删行操作条解耦 + `canSelectRow` 同时门控复选和任务右键 + `selectionEnabled` 在提交期间统一冻结选择/批量/菜单 + `selectedOf`/`onRowSelect` 外部受控选中 + `rowColor` 行语义底色)；2026-09-03 起可编辑模式行级右键/长按操作菜单（复用 [UtenContextMenu](UtenContextMenu.md)：复制选中/粘贴/批量粘贴/在上方插入空行/删除选中，右键未勾选行先自动单选、已勾选保留多选，粘贴统一追加表尾；页面须提供 `cloneRow` 才显复制粘贴组）；2026-09-04 起可选表头设置（`showColumnSettings`：必填列 `required` 锁定不可隐藏；列定义 `headerInfo` 渲染表头 ⓘ 悬停说明）；2026-09-05 起表头体系与主数据表统一走 [UtenTableColumnKit](UtenTableColumnKit.md)（按钮处锚定浮层勾选列表 + root Overlay 跟手浮层拖出隐藏 + 表头长按拎起横拖直接排序），编辑模式操作条精简为「表头设置+宿主批量动作」（全选走表头复选框、复制/粘贴/删除走行菜单，任务/只选模式不变）；**纯勾选模式（selectable 且非编辑）点行身非输入区=切换选中**（与主数据表「单击行选中」契约统一，输入格自消费不误触）；2026-09-05 起①列定义 `filterValueOf` 表头筛选（自研锚定下拉，视图级过滤不动数据与勾选，多列 AND、含计数与搜索）②`showSelectAllToggle`（默认 true）可隐藏操作条「全选/取消全选」钮（表头复选框已覆盖当页选择）；选择摘要「已选 N 项+✕」升位公共组件 [UtenSelectionSummaryPill](UtenSelectionSummaryPill.md)（原 MasterDataTableView 私有实现）；**2026-09-10 单元规格统一**：`UtenEditableGridCellSpec`（圆角 10 / 内边距 14·12 / 单行）由行级 Theme 注入，列 cellBuilder 不得自带 border/contentPadding/小字/双行；列头 ⓘ 改走 [UtenTableColumnKit](UtenTableColumnKit.md) 的 `UtenColumnHeaderInfo`（与主数据表、输入框内 ⓘ 同一份 UtenFieldHintIcon 实现）；`chromeWidth` 须计入预填黄标图标 44（`hintIconWidth`）与下拉箭头 20（`dropdownChevronWidth`）；**2026-09-11 表头快速筛选补齐**：①`filterValueOf` 返回值口径——空/未知一律返回 `null`（不建桶、计入「未填」），**绝不返回空串，也不得拿行唯一 id（单号/行 id）兜底**（每行各成一桶等于没筛）；②某列在当前行集上无任何可选值时列头退回纯标签，不画「点开只有『所有』」的死箭头（与 MasterDataTableView `_FilterCell` 的 interactive 口径统一）；③行集变化（批量动作后整批换行/重载）自动剪掉已失效的筛选值、列集合变化（切 docType）直接清空筛选，杜绝「表头看不见筛选、表体却是空表」；已接入下达车间计划表（类型/货品/车间/负责人/状态）、品质合并明细（判定/货品）、到货批量登记（订货单/类型/货品/颜色/单位/仓库）、产成品汇总登记（报工单/货品/颜色/单位/成品仓）、从上游引入明细（货品/颜色/单位）、应收应付选单（来源类型/币别）；**2026-09-11 四项**：①**表尾并作一行**——「添加行/添加多行」与 `footer`（合计条）同排，左按钮右合计，可用宽 < 560 才退回上下两行（硬挤会把金额压成省略号）；②**表头最小可读宽**——量宽不再只看单元内容，所有列都按「标签（含必填 *）+ 左右内边距 + 筛选箭头 22 + 列头 ⓘ 28」抬一次地板，声明宽偏窄的列（下达采购「缺口」）不再一进页面就只剩一个图标；③**`rowMenuExtraBuilder`**——给行右键/长按菜单追加作用于**当前选择集**的条目，非空时即使 `showAddRow=false` 且无 `onRemoveRows`（纯勾选办理表）也会挂菜单（下达车间/采购/委外的「批量清空可填内容」「填满剩余数量」「恢复默认下达数量」）；④**行首勾选列横滚冻结**（见 [UtenTableColumnKit](UtenTableColumnKit.md) §四之二）；**2026-09-11 追加两个工具条开关**：`toolbarActions`（「表头设置」右侧的**常驻**入口，不随选择态禁用——「从上游引入」由此并入工具条同排同高，不再单独占一行）、`showRemoveRowsAction=false`（只撤工具条上的「移出/批量删除」按钮，`onRemoveRows` 的能力仍在行右键菜单里）。两者配合 `showSelectAllToggle=false` 可把表头上方清空；全清空时操作条整条不渲染（不留 8px 空白）。到货登记两页据此改成「勾选多行 → 改其中任意一行的仓库/库位 = 整批落值」，撤掉全部常驻批量按钮 |
| [`uten_table_column_kit.dart`](../../lib/components/layout/uten_table_column_kit.dart) | `UtenColumnChooserButton`、`UtenColumnHeaderDragHost`、`UtenColumnDragGhostCell` | **表格表头列管理唯一实现**（2026-09-05 收敛）：表头设置=深绿实心按钮+按钮处锚定浮层勾选列表（全选行/勾选显隐/可选拖拽排序与恢复默认/必填列锁定）；表头拖出隐藏=root Overlay 最顶层跟手浮层（原格变淡、过阈值红底红×、单指契约）；表头长按拎起横拖=直接排序（跟手浮层+插入位指示线+松手落位，与弹窗排序同一状态）；主数据表与编辑明细表共用，见 [UtenTableColumnKit.md](UtenTableColumnKit.md) |

应用外壳位于 `lib/features/shell/`，没有独立 `UtenScaffold`。公共布局 API：`UtenListTwoPane`（列表页筛选在上、表格在下）与 `UtenSplitView`（分类树主档页的树 + 详情左右分栏，分割线可拖）。

### 2.6 设置与打印

| 源码 | 对外能力 |
|---|---|
| [`uten_theme_switcher.dart`](../../lib/components/settings/uten_theme_switcher.dart) | `UtenThemeSwitcher` |
| [`uten_locale_switcher.dart`](../../lib/components/settings/uten_locale_switcher.dart) | `UtenLocaleSwitcher` |
| [`uten_font_scaler.dart`](../../lib/components/settings/uten_font_scaler.dart) | `UtenFontScaler` |
| [`uten_performance_switcher.dart`](../../lib/components/settings/uten_performance_switcher.dart) | `UtenPerformanceTierSwitcher` |
| [`uten_print_preview.dart`](../../lib/components/print/uten_print_preview.dart) | `UtenPrintPreviewButton`、`showUtenPrintPreview` |

---

## 三、跨模块组合与模块组件

| 层级 | 源码 | 能力 |
|---|---|---|
| 跨模块组合 | [`master_detail_card.dart`](../../lib/shared/widgets/master_detail_card.dart) | `MasterDetailCard`，统一主数据详情卡 |
| 跨模块组合 | [`uten_location_field.dart`](../../lib/shared/widgets/uten_location_field.dart) | `UtenLocationField`、`showUtenPickerSheet` |
| 跨模块组合 | [`warehouse_picker_panel.dart`](../../lib/shared/widgets/warehouse_picker_panel.dart) | `showUtenWarehousePickerPanel` 仓库侧滑选择面板，两种口径同一面板：运营（默认，主仓钻层 → 只落叶子仓，单据登记用）与查询（`allowParent` + `includeAll`：整棵层级缩进一次铺开、任意层级一点即选 = 子树聚合，顶部「全部」返回 `WarehousePickerResult.all`，带本地搜索）；另导出 `warehouseFullLabel` / `warehousesShareMain`。见 [UtenFilterPickerField](UtenFilterPickerField.md) §3.2（2026-09-11 加查询口径） |
| 跨模块组合 | [`procurement_supplier_cell.dart`](../../lib/shared/widgets/procurement_supplier_cell.dart) | 采购/委外订货明细「供应商」行内选择格（2026-09-03 收录） |
| 跨模块组合 | [`procurement_commercial_grid.dart`](../../lib/shared/widgets/procurement_commercial_grid.dart) | `CommercialTermsRowMixin`/`RemarkRowMixin`（行级条款与备注状态）、`ProcurementTermDropdownCell`（grid 紧凑下拉格）、`procurementCommercialColumns`（币种/汇率/税率/结账方式四列）、`procurementRemarkColumn`（末列备注）；见 [ProcurementCommercialGrid](ProcurementCommercialGrid.md)（2026-09-03，ADR-068） |
| 跨模块组合 | [`commercial_terms_batch_sheet.dart`](../../lib/shared/widgets/commercial_terms_batch_sheet.dart) | `showCommercialTermsBatchSheet`「统一设置条款」批量面板（供应商+结账方式+币种+汇率+税率一次写，留空保持原值）；见 [CommercialTermsBatchSheet](CommercialTermsBatchSheet.md)（2026-09-03，ADR-068） |
| 跨模块组合 | [`uten_tree_table_cell.dart`](../../lib/shared/widgets/uten_tree_table_cell.dart) | `UtenTreeTableCell` 表格树形层级单元格：缩进+连续参考线+级联序号+层级标签四重冗余标识，独立 48dp 展开命中区（2026-09-10 起 28px 层级色实心圆底箭头 + 可选 `childCount`「N」徽章，一眼可见有子层），`sequenceInline` 紧凑模式（P几+名字同行、编号下行）；见 [UtenTreeTableCell](UtenTreeTableCell.md)（2026-09-04，货品组件信息/物料分析统一物料表共用） |
| 跨模块组合 | [`business_attachment_section.dart`](../../lib/shared/attachments/business_attachment_section.dart) | `BusinessAttachmentSection`（已保存单据：读取/上传/预览/删除，叠加 `attachment:*` 权限并由后端对象策略终审）；`BusinessAttachmentSection.draft`（新建单据保存前暂存：只选文件/移除，配 `PendingAttachmentController` + `flushPendingAttachments` 在拿到 UUID 后逐个确认上传，拆单时同一文件挂到每张单据；2026-09-10，ADR-074）。2026-09-11 分类改为「传完之后在文件旁边可选设置」：上传/加入暂存都不问分类，也没有决定归类的芯片；每行一个 `AttachmentCategoryControl`（未设 =「＋分类」虚位，已设 = 分类胶囊，菜单含本页词表 + 清除，两态同几何不跳行高），已保存单据走 `PUT /api/attachments/{id}/category`（`attachment:upload` + 与删除同一条对象授权），暂存文件存本地随 flush 带走；文件 ≥4 个且用了 ≥2 种分类才另出一行「只看」筛选 |
| 跨模块组合 | [`attachment_preview_dialog.dart`](../../lib/shared/attachments/attachment_preview_dialog.dart) | `AttachmentPreviewDialog` 内嵌预览，六条通道由 [`attachment_file_rules.dart`](../../lib/shared/attachments/attachment_file_rules.dart) 的**唯一**能力矩阵决定（列表图标/提示/弹窗同源，不承诺会失败的预览）：图片（缩放）、PDF（pdfrx + 上下页 + ←/→）、文本（UTF-8/UTF-16/GB18030 自动识别，老 GBK 导出不再乱码）、CSV（对齐表格，表头固定）、zip（条目清单，不解压）直接看；doc/docx/rtf/odt、xls/xlsx/ods、ppt/pptx/odp、svg 共 11 种经服务端 LibreOffice 转 PDF（`AttachmentService.previewBytes`），失败提示并回落下载；tiff/heic/7z/rar 只收不预览。见 [AttachmentPreviewDialog](AttachmentPreviewDialog.md)（2026-09-10 起，2026-09-11 扩到办公常见类型） |
| 基础资料模块 | [`master_data_table_view.dart`](../../lib/features/basic_data/widgets/master_data_table_view.dart) | `MasterDataTableView`，排序、筛选、分页、列配置 |
| 基础资料模块 | [`category_tree_search.dart`](../../lib/features/basic_data/widgets/category_tree_search.dart) | 分类/部门与具体内容的关联搜索解析、完整分页位置汇总、祖先展开、树外 ID 失败关闭和分支判定；见 [UtenHierarchySearch](UtenHierarchySearch.md) |
| 基础资料模块 | [`uten_category_tree_view.dart`](../../lib/features/basic_data/widgets/uten_category_tree_view.dart) | 分类树；支持内部名称/编号搜索及 `visibleFilterIds`、外部查询/loading/error 受控状态 |
| 基础资料模块 | [`product_category_picker_panel.dart`](../../lib/features/basic_data/widgets/product_category_picker_panel.dart) | `showUtenProductCategoryPickerPanel` 货品分类侧滑选择面板（面板内就是货品资料那棵 `UtenCategoryTreeView`：展开折叠 + 搜索 + 选中高亮 + 子树货品数）+ 「全部」清空行 + `pruneCategoriesWithoutGoods`（零货品分类整支剪掉）+ `findCategoryName`；点行即选即关。2026-09-11 取代 `ProductCategoryDropdown`（已删），见 [UtenFilterPickerField](UtenFilterPickerField.md) §3.1 |
| 基础资料模块 | [`uten_goods_picker.dart`](../../lib/features/basic_data/widgets/uten_goods_picker.dart) | `showUtenGoodsPicker`；见 [UtenGoodsPicker](UtenGoodsPicker.md) |
| 基础资料模块 | [`uten_client_picker.dart`](../../lib/features/basic_data/widgets/uten_client_picker.dart) | `showUtenClientPicker` / `UtenClientPickerField`；见 [UtenClientPicker](UtenClientPicker.md) |
| 基础资料模块 | [`master_edit_dialog.dart`](../../lib/features/basic_data/widgets/master_edit_dialog.dart) | 主数据编辑弹窗 |
| 基础资料模块 | [`master_detail_sheet.dart`](../../lib/features/basic_data/widgets/master_detail_sheet.dart) | 主数据详情抽屉/面板 |
| 部门模块 | [`uten_department_tree_view.dart`](../../lib/features/department/widgets/uten_department_tree_view.dart) | 组织树；支持名称/编号和页面级外部关联搜索状态 |
| 员工模块 | [`department_employee_picker.dart`](../../lib/features/employee/widgets/department_employee_picker.dart) | 最小权限部门树 + 全分页员工选择；同样使用 `姓名(工号)`；见 [UtenDepartmentEmployeePicker](UtenDepartmentEmployeePicker.md) |
| 通知模块 | [`celebration_mascot.dart`](../../lib/features/notice/widgets/celebration_mascot.dart) | `CelebrationMascot`，按 `NoticeType` 取「小优」庆典美术，缺失回落 `UtenBrandMascot`（ADR-026） |
| 通知模块 | [`celebration_subjects.dart`](../../lib/features/notice/widgets/celebration_subjects.dart) | `CelebrationSubjectsPanel`（详情页主角面板：吉祥物+全员徽章）、`CelebrationSubjectChips`（主角姓名徽章行，`maxChips` 超员折叠 +N，周年逐人 `eventLabel`）；数据来自 `Notice.subjects`（V454 聚合祝福卡） |

`MasterDataTableView`、分类搜索辅助和主档选择器虽然被多个业务调用，仍依赖基础资料/组织模型，因此暂留各自模块；不得下沉到纯基础组件层。业务 API 由页面/选择器注入，树组件本身不读取货品、客户或员工仓库。

---

## 四、新增或调整组件的流程

1. 先用 `rg` 确认是否已有同类能力和真实调用方。
2. 明确放置层级：纯 UI → `components`；跨模块业务组合 → `shared/widgets`；单模块 → `features/<module>/widgets`。
3. 使用主题 token，覆盖 loading/disabled/error/empty、响应式、键盘与可访问语义。
4. 为有状态、权限相关或容易回归的交互增加 widget 测试。
5. 同步本文及对应独立文档；没有独立文档时直接链接源码，不创建空壳文档。
6. 运行 `flutter analyze`、相关测试，并检查是否出现新的业务裸色或重复组件。

---

## 五、已清理的旧名称

2026-07-30 静态可达性核对后，删除了无调用的 `UtenListItem`、`UtenStatCard`、`UtenAnimatedNumber`、`UtenSelect`、旧主题包装文件、未使用的 theme barrel、`AppLogger` 和下拉辅助文件。以下规则长期适用：

> 例外：`UtenAnimatedNumber` 已于 2026-09-10 随服务器状态页圆环仪表**重新实现**（见 2.2 与 [UtenAnimatedNumber](UtenAnimatedNumber.md)）——它满足「先有真实调用方 + 降级路径 + widget 测试」三条门槛，不是恢复旧死码。其余已删名称仍然不存在。

- 不得重新引用已删除的类名或文件路径。
- 规划文档若讨论尚未实现的组件，必须明确标注“目标设计”，不能列入当前组件清单。
- 删除公共组件前先跑全仓调用检索；删除后同步本文、使用规范和页面文档。

---

## 六、依赖方向

```mermaid
graph TD
    Theme["core/theme"] --> Components["components"]
    Components --> Shared["shared/widgets"]
    Components --> FeatureWidgets["features/*/widgets"]
    Shared --> FeatureWidgets
    FeatureWidgets --> Pages["features/*/pages"]
```

依赖只能沿图向下；`components` 和 `shared/widgets` 不得反向导入页面。
