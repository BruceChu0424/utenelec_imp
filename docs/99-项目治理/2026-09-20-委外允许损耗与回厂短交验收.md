# 2026-09-20 委外允许损耗与回厂短交判定(V636 / ADR-098)验收记录

> 范围：[ADR-098](../99-决策记录-ADR/ADR-098-委外允许损耗与回厂短交判定.md) 全链路——委外下单「允许损耗%」列 + 货品主档记忆预填、仓库到货登记短交确认弹窗(409 逐行大白话)、短交案件与紧急通知、委外任务中心三段合并「进行中」+ 颜色状态列 + 表头筛选 + 红徽章口径、全新「委外回厂短交判定页」(分批到货 / 接受损耗结案)、供应商损耗率汇总(供应商列表列 + 供应商详情「委外损耗」区)。
> 隔离：worktree `.claude/worktrees/sc-loss`(分支 `feat/subcontract-loss-tolerance-20260920`, 基于 27ce4404 + 主树在途快照)；克隆库 `uten_imp_scloss`(pg_dump 自 uten_imp, Flyway 头 632 → 本轮前向到 636)；冒烟服务端口 8082(jar 独立启动, 与 8080/8081 互不影响)。

## 1. 结论

- 后端真库全链路(Testcontainers, 全量迁移到 V636)两条用例绿：严重短交 → 仓库确认 → 紧急通知 → 分批判定 → 二批仍短 → 接受损耗结案(自动损耗单 + 受控改量 + 记损耗率) → IQC 合格入库后订货单关闭 → 供应商汇总；以及「未设允许损耗只开中性案件、到齐自然完成」。
- 运行时冒烟(8082 + 克隆库, 自签 staff token, 两个真实账号切换：制单人 13026669123 / 财务审核 17665410007)全部通过，且把 7 个 Flutter `fromJson` 键集合与真实 JSON 逐一对过(无缺键)。
- Flutter：新增 3 套 widget 测试 + 任务中心状态列测试绿；受影响的邻居用例(成本页字段数 18→19)已同步。
- 未做：浏览器可视走查(本会话浏览器工具被安全分类器临时拦截, 见 §5)；ARB 多语言未接(页面按仓库现状直接中文)。

## 2. 设计落点(与用户口径逐条对应)

| 用户要求 | 落点 | 证据 |
| --- | --- | --- |
| 委外下单每个货品加「允许损耗范围」列 + 记忆下次自动填 | `subcontract_order_items.allowed_loss_pct`(保存冻结) + `goods.subcontract_allowed_loss_pct`(保存订货单回写主档, `/last-terms` 预填并打黄色「自动带出」标) | 冒烟：保存前 last-terms 为空 → 保存后 `allowedLossPct=5 / GOODS_MASTER`, 货品详情 `subcontractAllowedLossPct=5` |
| 仓库收货少于允许范围就弹窗、分批到货自己判断 | 登记前只读评估, 低于下限未确认 → 409 `SUBCONTRACT_SHORT_DELIVERY_UNACKNOWLEDGED` 逐行文案；分批已判定时文案追加「委外已判定分批到货，预计 D 到齐」；确认后同事务开案/刷新 | 冒烟 409 正文：「D118开关进线铜柱 D1181022」订 20 个，允许损耗 5%(最少应到 19 个)，此前已到 0 个，本次 12 个，累计 12 个，少 8 个(40%)，属严重短交 |
| 通知委外人员 | Outbox `SUBCONTRACT_SHORT_DELIVERY_DETECTED`(紧急卡给制单人, 普通任务卡给采购员/有判定权的关注人)、分批逾期 `_WAIT_OVERDUE`(每日 08:53 扫描)、结案 `_RESOLVED` 撤卡 | 冒烟 notices：1 张 urgent + 2 张 task；E2E 断言分批判定后原卡办结、二批到货不再打扰 |
| 任务中心：等待财务审核/财务已通过/财务驳回并成「进行中」+ 状态列 + 颜色拉开 + 表头筛选 + 红徽章只给重要 | `status=IN_PROGRESS` 映射三档；`display_stage` 8 档(等财务 蓝 / 财务退回 红 / 待发料出仓 紫 / 委外加工中 青 / 部分回厂 橙 / 已回厂待入库 绿 / 分批等待中 品红 / 容差内待结案 灰 / 回厂短交待判定 红+紧急+整行红底)；红徽章 = 待处理 + 财务退回 + 有待判定短交的订货单 | 冒烟 `statusCounts={FINANCE_APPROVED:2, ORDER_PENDING_APPROVAL:1, IN_PROGRESS:3}`, `exceptionCounts={SHORT_DELIVERY:1}`, 行 `displayStage` 依次 SHORT_DELIVERY → WAITING_MORE_BATCH → RECEIVED_PENDING_STOCK |
| 出问题的单标红/紧急 | 状态列红底「紧急」标签 + 行底红, 点状态直达判定页只看这张单 | widget 测试 `subcontract_decomposition_progress_status_test` |
| 全新页面：数量严重低的重点显示, 委外自己判分批还是允许入库 | `/subcontract/short-deliveries` 四段：待判定(红) / 容差内待结案(中性) / 分批等待中 / 历史记录；两种判定弹窗(分批：预计到齐日必填；接受损耗：低于下限说明必填, 弹窗写明自动做三件事) | widget 测试 3 条 + 冒烟 decide 两次 |
| 记录每单损耗比, 按供应商汇总 | 案件 `loss_qty/loss_pct`；视图 `v_subcontract_supplier_goods_loss_summary` / `v_subcontract_supplier_loss_summary`；供应商列表「损耗率」列真值；供应商详情「委外损耗」区(总览 / 按货品 / 最近案件 + 深链) | 冒烟 supplier-summary 与列表 `lossRate` 同为 0.29%(3 件 / 1020 件, 含历史结清行) |
| 其它业务要不要 | 采购到货不受影响(登记忽略确认位)；生产完工入库另有齐套/报工口径, 不套用；ADR-098 §6 记录了判断 | 采购登记路径回归绿(见 §4) |

## 3. 真库 E2E 证据(`SubcontractShortDeliveryEndToEndTest`, UTEN_RUN_DB_TESTS=true)

`UTEN_RUN_DB_TESTS=true mvn -q test -Dtest=SubcontractShortDeliveryEndToEndTest` → Tests run 2, Failures 0, Errors 0(第 5 轮, 约 51 s)。

用例一(订 20 @5%)的关键断言：
1. 保存即冻结 `allowed_loss_pct=5`, 主档 `subcontract_allowed_loss_pct=5`, `masterDefaultTermsPerGoods` 回 `GOODS_MASTER`。
2. 财务批准 → 目标件出仓草稿审核 → 供应商处 20。
3. 第一批 12 未确认 → 409(文案含「订 20 / 最少应到 19 / 累计 12 / 少 8 / 属严重短交」), 409 前不留案件不建收货单；确认后 `SUBMITTED_FOR_INSPECTION`, 案件 PENDING_OWNER/SEVERE, 下限 19、短交率 40%、制单人收 1 张 urgent 卡；任务中心 `displayStage=SHORT_DELIVERY`、`exceptionCode=SHORT_DELIVERY`、`countPending>=1`、判定页 pending=1。
4. 判定分批(预计 +7 天) → WAITING_MORE, 原卡办结, `displayStage=WAITING_MORE_BATCH`, waiting=1。
5. 第二批 5：仍 409 且文案含「委外已判定分批到货」；确认后案件刷新(累计 17 / 少 3 / 15%, arrival_count=2, 事件 DETECTED→WAIT_MORE_DECIDED→REDETECTED)，**不再发通知**。
6. 接受损耗 → ACCEPTED_LOSS：损耗单已审(3 件, 允许量 1 件)、发料明细 `wasted_qty=3`、订货量 20→17、`inbound_expectations=CLOSED`、改量日志 old 20 new 17 且回填 `qty_change_log_id`、自动开财务复核案、超耗 2 件转 `subcontract_loss_cases=OPEN`、案件保留原订货量 20；判定页 pending=waiting=0；任务中心 `displayStage=RECEIVED_PENDING_STOCK`(结案不等于入库)。
7. 两张收货单 IQC 合格 + 仓库确认入库 17 → 订货单 `is_closed=true`(既有口径：仓库实收净量 ≥ 订货量), 案件不变, 订货单离开「进行中」。
8. 供应商汇总视图：结清 1 行、接受损耗 1 次、订 20 损 3 → 15%；`supplierSummary` byGoods/recentCases 各 1；历史段 1、待判定 0。

用例二(未设允许损耗)：登记不弹窗, 案件 UNSET_TOLERANCE 中性、不发紧急通知、任务中心 `TOLERANT_SHORT` 且 `exceptionCode=null`、红徽章不计、判定页容差内段 1；到齐后案件 COMPLETED、汇总损耗 0 但算结清行。

## 4. 运行时冒烟与回归

- 冒烟脚本 `C:\Users\bruce\AppData\Local\Temp\uten_wf\scloss_smoke_api.py`(8082 jar + 克隆库；期初其他入库 20 → 订货 EO20260921000006 → 财务认领+批准 → 发料审核 → 12/5 两批 → 两次判定)：全部 PASS；结案后 `count` 回到基线 `{pending:0,tolerant:0,waiting:0}`；供应商列表 `lossRate` 与 `supplier-summary.lossPct` 同源同值。
- Dart 键契约：`SubcontractShortDeliveryCase`(40 键) / `Counts` / `Event` / `SupplierLossSummary`(10 键) / `GoodsLossRow` / `OperationsWorkbenchTask.displayStage` / `ProcurementCommercialTerms.allowedLossPct*` 全部能在真实 JSON 里找到。
- Java 定向回归(worktree, 见本记录末尾「回归结果」)：`*Subcontract*Test, *Workbench*Test, *Arrival*Test, *MasterDefaults*Test, Goods*Test, Supplier*Test, *Notice*Test, ScheduledTaskCatalog*, ArchitectureBoundary*, *BusinessDataReset*, *ResetBusinessData*, AuditTrigger*, LegacyMigration*, *ProcurementInspection*, *IqcStockIn*, FullChainEndToEndTest`。
- Flutter：`flutter analyze` 全仓 0 issue(3 条 info 已清)；新增 `subcontract_short_delivery_page_test`(3) / `subcontract_decomposition_progress_status_test`(1) / `subcontract_short_delivery_confirm_dialog_test`(3) + `subcontract_decomposition_page_test`(6) + `goods_cost_tab_test` 绿。邻居全量 618 通过；唯一残留红 `go_router_state_usage_gate_test` 来自并发会话在主树未提交的 `page_resume_provider.dart`(闸门登记 2 次、实际 3 次), 不属本批。

## 5. 偏差与留待

- 浏览器可视走查未做：本会话 `preview_start/navigate` 被安全分类器(模型临时不可用)连续拒绝；UI 层以 widget 测试 + 真实 JSON 键契约替代。合并后建议按 [委外回厂短交判定页](../03-页面/委外回厂短交判定页.md) 手工走一遍：下单列黄标预填 → 到货登记弹窗 → 任务中心红行 → 判定页两种弹窗 → 供应商详情损耗区。
- 「已回厂待入库」是本轮 E2E 暴露后补的第 8 档状态(回厂净量到齐只差质检入库)；此前用「部分回厂」表达 17/17 已回厂的单是错的。
- Flutter 架构边界测试(`architecture_boundaries_test`)曾报新边 `basic_data->subcontract`(供应商详情页读委外损耗汇总)：已把短交读模型移到 `lib/shared/models/subcontract_short_delivery.dart`、汇总请求放 `lib/shared/repositories/subcontract_loss_summary_loader.dart`，委外仓库层委托同一函数；边界测试复绿。
- 迁移头同步五处已做(README 头行 V636/589、reset_business_data.sql 策略行 + 版本对 + 异常文案、BusinessDataResetSqlContractTest、ResetBusinessDataScriptContractTest、AuditTriggerCoverageMigrationContractTest)；迁移正文按 V618 模式给 `business_data_reset()` 打运行时补丁。

## 6. 回归结果

Java 定向回归第一轮(worktree, `UTEN_RUN_DB_TESTS=true`, 975 用例)暴露 7 类红，逐项归因与处置：

| 用例 | 现象 | 归因 | 处置 |
| --- | --- | --- | --- |
| `FullChainEndToEndTest.directCustomerShipment_unrepresentableLocalMoneyDoesNotShipOrCreateAr` | 期望 ApiException 未抛 | 并发会话在途快照(V632 出货放行汇率)自带；在纯快照基线 `tmp/sc-loss-peers-base` 上单跑同样红 | 不属本批, 留给该会话 |
| `SubcontractPreparationPaginationPostgresTest`(7) | `relation subcontract_short_delivery_cases does not exist` | 该测试手工建最小 schema, 进行中 display_stage 的 LATERAL 子查询新读 4 张表 | 测试 schema 补 4 张最小列表(案件/订货明细/发料头/发料明细) |
| `SubcontractOrderMaterialAuthorityTest`(2) | 手工 new 服务时 `shortDeliveryHooks` 为 null → NPE | 可选协作方回调未做空安全；红冲回调位置在守卫之前 | `shortDeliveryHook()` 空安全包装；红冲回调移到守卫全部通过后、置 -1 之前 |
| `WarehouseArrivalRegistrationContractTest.requestContractKeepsWarehouseSurfaceMinimal` | 记录组件多了 `shortDeliveryAcknowledged` | 仓库侧字段面契约锁定 | 契约登记该布尔确认位(非金额) |
| `BusinessDataResetServicePostgresTest` | clearedTableCount 296≠298 | 新增 2 张 CLEAR 表 | 期望 296→298 |
| `ResetBusinessDataV446PostgresTest` | 脚本 fail-closed 恒等式拒跑(CLEAR 298) | `reset_business_data.sql` 的 `current_operational_table_count` 清单未登记新表(第六处同步点) | 清单 +2 张 |
| `GoodsOwningWarehouseSyncPostgresTest` | `NoClassDefFoundError NativeSavepointJpaDialect` | 回归进行中我在同一 target/ 并发起了另一次 `mvn test` 重编译 | 单独复跑确认 |

第二轮(上表处置后)复跑 10 个类：`ResetBusinessDataV446PostgresTest` 1 / `ResetBusinessDataScriptContractTest` 5 / `BusinessDataResetSqlContractTest` 7 / `BusinessDataResetServicePostgresTest` 4 / `WarehouseArrivalRegistrationIdempotencyTest` 4 / `WarehouseArrivalRegistrationContractTest` 12 / `SubcontractOrderMaterialAuthorityTest` 4 / `GoodsOwningWarehouseSyncPostgresTest` 9 / `SubcontractPreparationPaginationPostgresTest` 7 / `SubcontractShortDeliveryEndToEndTest` 2 —— 55 用例全绿(2026-09-21 00:50)。Flutter 全量第二轮(最终源码)：仅剩 `go_router_state_usage_gate_test`(并发会话主树在途改动)一红, 其余全绿；`flutter analyze` 全仓 0 issue。
