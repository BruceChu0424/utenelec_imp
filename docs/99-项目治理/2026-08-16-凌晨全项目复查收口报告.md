# 2026-08-16 凌晨全项目复查收口报告（02:00 定时任务）

> **性质**：凌晨 2 点排定的全项目审查第二遍（安全/敏感数据/UI 规范/响应式/重复代码/
> 迁移链/远程更新/文档对齐），在前一轮「全链路溯源联动与计算准确性审计修复」（同日更早，
> 改动仍在工作区未提交）基础上复核并补漏。
> **Owner**：全链复查 | **时间**：2026-08-16 02:00–04:00 | **分支**：`uimp/chore/full-integration-20260815`

## 一、工作区状态与复核结论

开工时工作区已有前一轮会话留下的**约 55 个修改文件 + 8 个新文件**（对应
《2026-08-16-全链路溯源联动与计算准确性审计修复报告.md》，含委外回厂消费重写、
催交口径修复、V290、对账三件套、IQC 待检页、SourceDocLink 等），全部未提交。
本轮**不覆盖、不回退**这些改动，在其上复核：

| 复核项 | 结论 |
|---|---|
| `mvn compile` | ✅ 绿（当前工作区全量） |
| `flutter analyze` | ✅ 0 issues |
| `dart format --set-exit-if-changed lib test` | ✅ 738 文件 0 改动（2026-08-09 记录的 101 文件格式债已在此前清零） |
| `flutter test --no-pub` | 见文末验证记录 |
| V290 迁移 | ✅ 纯增量可空列 + COMMENT，无回填无触发器依赖，注释准确 |
| 对账三件套 | ✅ 纯 SELECT + `@Transactional(readOnly)` + `stock:balance:adjust` 权限 + Profile("!cloud") + Asia/Shanghai 时区钉死 + 异常只 warn 不影响业务 |
| SourceDocLink | ✅ 弹性行 + ellipsis + token 化，降级路径（无 id→纯文本、无单号→「—」）完整 |

## 二、敏感数据与安全复核（全部通过）

1. **.env 体系**：真实值只在 `server/.env`、`website/.env`（gitignore 三层覆盖：根 +
   server/ + website/）；入库的 `server/.env.example`、`website/.env.example` 及 deploy 下
   7 个 `*.env.example` 逐一核对，**全部为占位符/空值**，无真实密钥。
2. **git 历史**：`git log --all -- '**/.env'` 为空——.env 从未入库；gitleaks 已在 CI 固定
   版本直跑（全历史扫描），此前 388 项命中均为构建产物/本地文件假阳性（08-15 已复核）。
3. **硬编码扫描**：server Java 无 `secret/password/apiKey = "..."` 字面量赋值；Flutter 无
   dart-define 密钥注入。三大密钥（JWT/PGP_MASTER/HMAC）任何 profile 无默认值、缺失 fail-fast，
   prod/cloud 有强度门禁（≥32 字节非占位）。
4. **已知低危（维持记录不改，用户已决策）**：docker-compose 开发库弱口令 `uten`（dev-only）、
   超管登录手机号 3 处硬编码默认值。
5. 加密体系（Argon2id 密码、Ed25519 发布验签、pgcrypto 字段级、AES-256 导出）在 08-07/08-09
   审计已确认标准实现，本轮无新改动，结论延续。

## 三、本轮修复（全部已落地并过门禁）

| # | 问题 | 修复 |
|---|---|---|
| 1 | **SnackBar 违例 5 处**（规范：所有反馈走顶部通知，禁 SnackBar）：付款方式页禁用提示、应收应付选择器、计划列表批量完成、计划向导应用/未填完整 | 4 文件改 `context.appSuccess/appWarning`（`payment_style_page` / `ar_ap_picker_dialog` / `production_plan_list_page` / `production_plan_wizard_page`） |
| 2 | **业务文件裸色 8 处**（规范：裸 `Color(0x..)` 唯一合法位置 uten_colors.dart）：下拉「添加」行用了 Material Green（E8F5E9/A5D6A7）与全站 emerald 语义成功色并存，同屏两套绿；表头悬浮/拖拽徽章阴影 2 处裸色；节庆粒子色板 6 处裸色 | 下拉改 `UtenColors.successBg/success`；新增 `floatingCellShadow`/`dragBadgeShadow`/`festiveConfetti` token 并替换。**业务/组件文件裸色清零**（余 7 处均在 core/theme 定义层，合法） |
| 3 | **进度归一启发式 4 处重复**（前轮报告遗留#3）：`>1 ? /100` 散落在看板页/物料分析模型/仓储层/销售进度表 | 收敛到 `lib/shared/models/progress_ratio.dart::normalizeProgressRatio` 唯一入口，4 处改调用 |
| 4 | **契约测试过期导致 729 套件 1 红**：`subcontract_material_issue_safety_contract_test` 仍断言"发料审核必须关闭+阻断原因含 BOM 快照/子件台账/409"，而前轮已按后端 V221 事实解除前端门禁（前轮改了代码漏改测试） | 测试更新为新契约：`approvalEnabled` 为 true、`approvalBlockedReason` 为 null、`approveEffect` 含冻结 BOM 单耗语义、注释写明安全兜底在后端守恒 + CAS + 409 |

修复后 `flutter analyze` 0 issues、`dart format` 738 文件 0 改动。

## 四、机械扫描结论（非违例，记录依据）

- **Platform.is 9 处**：全部在 core/audit（设备审计标识）、core/io（文件保存）、
  core/performance（设备评分）——平台能力检测，非 UI 切换，规范允许。
- **MediaQuery.of(context).size 仅 2 处**：祝福墙底部弹层按屏高 80%、销售报表打印预览
  弹窗宽度封顶——均为合理用途，主布局断点未散落 MediaQuery。
- **Flyway 链**：V1→V290 共 271 文件，唯一跳号 V100→V120（Flyway 允许，非错误）。
- **远程更新/发布**：release.yml 与 quality.yml 的 action 引用钉版一致（checkout v6 /
  setup-java v5 / flutter-action v2 / upload-artifact v4.6.2 / aliyun-credentials v1，全部
  SHA 固定）；`deploy/current-test-server-status.zh-CN.md` 为 2026-08-15 版权威口径——
  仅内部 ERP 测试环境，官网/外部服务器全部不在本轮范围，与"内部先行"边界一致。

## 五、新发现债（入档，不在本轮修）

1. **fontSize 硬编码 218 处 / 68 文件**（规范：字号五档走全局 textScaler + token）。
   分层盘点：68 处在 core/theme 两个主题文件（定义层，合法）；约 39 处在打印/纸面复刻
   视图（`production_plan_summary_sheet_page` 23、`uten_print_preview` 7、
   `production_execution_card_print_preview` 9——A4 纸面输出需固定磅值，建议规范明确豁免）；
   **真实债约 110 处 / 约 55 文件**（production/sales/notice/basic_data 页面为主）。
   建议按 2026-08-09 报告的口径在冻结候选之上分批收敛，每批过全量 729 测试 + analyze。
2. **对话框/侧板固定宽 720–840px 约 15 处**（goods/ar_ap/doc_link 选择器侧板 840、
   物料结算单 840、通知详情 760 等）：依赖父级约束钳制，小屏自动收缩不会硬溢出，但未走
   三档断点显式适配（小屏应收窄/全屏）。与「适老化 + 多端」目标对齐时一并处理。
3. `website/` 子项目在 08-09 整治轮明确排除、本轮维持无结论（与治理决策一致）。

## 六、验证记录

- 后端 `mvn compile` 绿；Flutter `flutter analyze` 0 issues；`dart format` 门禁绿（739 文件 0 改动）。
- **`flutter test --no-pub` 729/729 全绿**（修复过期契约测试后复跑全量确认；该红为前轮
  未提交改动遗留，与凌晨复查轮改动无关，已按新业务契约修正测试）。
- 未运行 `UTEN_RUN_DB_TESTS=true mvn verify`（本轮零后端代码改动，前一轮会话已在同
  工作区跑过 FullChainEndToEndTest 54/54 全绿）。

## 七、遗留清单增量（合并前两轮）

前轮报告「遗留 5 项」中第 3 项（进度归一收敛）**已完成**；其余 4 项不变（委外 order_no
历史列不补数、销售退货/报价来源引用后端未下发、采购退货友好容量校验、完整 WMS 未落地）。
全局遗留以 2026-08-15 收口报告 §4 + `deploy/current-test-server-status.zh-CN.md` §4 为
权威清单，本轮新增仅第五节 3 项 UI 债。
