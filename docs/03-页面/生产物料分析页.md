# 生产物料分析页、节点任务与逐级计划

> 2026-09-06 数量与分仓口径修正见 [ADR-073](../99-决策记录-ADR/ADR-073-本批物料履约与同主仓分仓领料.md)：下达后保留本批需求及实物缺口，合格入库后才减少；同主仓的子仓参与本批物料履约，实际领料按子仓分单。“现货分配/已到货”列统一改为“已备数量”，按服务端已分配量显示，包含本批已领用量；精确到货已在分配中，不再相加。本批已备数量与仓库即时余额分开。本文较早的单叶仓及“全量下达即完成”描述以该决策为准。

- 更新日期：2026-09-05。
- 状态：本地源码候选；本轮未上传 GitHub、未执行远端 CI、未部署或迁移现有业务数据库。目标库升级、真实岗位与实物流转 UAT、恢复演练未完成，生产发布仍为 **NO-GO**。
- 入口：`/production/material-analysis`；历史记录：`/production/material-analyses`；生产计划：`/production/plans`。
- 决策：[ADR-070](../99-决策记录-ADR/ADR-070-物料分析分桶详情页与三处学习记忆及超量下单.md)、[ADR-071](../99-决策记录-ADR/ADR-071-下达车间原子化与计划侧齐套判断下线.md)；本轮验证：[顶层路线记忆与确认修复验收记录](../99-项目治理/2026-09-05-顶层路线记忆与确认修复验收记录.md)。上轮 V476/209 项证据保留在[准备与生产链记录](../99-项目治理/2026-09-05-物料分析准备与生产链本地验收记录.md)，不能代替本轮最终验收。
- 本轮新增前向迁移 [V477 原因选填](../数据迁移/93-V477物料路线原因选填.md)与 [V478 顶层供料/产出交接](../数据迁移/94-V478顶层供料路线与产出交接.md)；目录头以[总索引](../数据迁移/README.md)为准，不修改已应用迁移字节。V478 最终隔离 PostgreSQL 报告仍待回填，现有业务库未升级。
- **2026-09-06**：行级流程阶段（`MaterialView.flowStage`，服务端 [V487](../数据迁移/103-V487工作台根进度与零料标志.md) 配套批量推导）+ 全站统一流程词表（§3.7）落地；零料直制改「无需领料 · 可开工」、未下达按路线显示第一步。

## 一、员工看到的主流程

```text
已审核销售订单行 / 合法手工需求
  → 选择产品与本次需求数量
  → 选择主仓库范围，核对实际领料子仓
  → 联合分析，查看产品层级或按物料汇总
  → 勾选根产品或物料，核对上次路线/本次草稿，点击“确认路线”
  → 分别进入“下达采购 / 下达委外 / 下达车间”
     ├─ 采购：确认需求与独立公共补库/备货，形成采购申请
     ├─ 无子层委外：形成委外申请
     └─ 自制（顶层产品/候选/有子层委外/已建子件一视同仁）：
        填「本批数量 + 生产车间 + 负责人」→「创建生产计划」单次原子提交
        （服务端一个事务：候选先建子件任务 → 逐行生成计划 → 有审核权同事务审核下达；
         任一行失败整体回滚，失败不再残留已建子件行）
  → 计划不再做齐套判断：缺料批次以 WAITING 下达（零预留、无 DRAW）
     → 物料齐套（含上游车间完工入库）后执行段原子提升 READY 并生成 DRAW
  → 仓库实际发完全部正式需求后才允许开工
  → 报工 → FQC → 仓库逐行点收，实际入库量才成为下一层合格供给
```

确认路线只保存所选各自的实际路线与选填原因，不自动下达采购、委外、自制任务或生产计划。进入页面、切换视图、刷新与轮询均不得隐式创建子件任务。

自制/有子层委外采用计划锚点：子件行只承载计划链接与执行进度，不接管子树、不迁移 exact 权益、不再展开第二份 BOM。物料需求和已确认路线始终保持在原树，计划员按原节点下达采购、委外或自制。合格入库后，正式齐套按同分析、同原树直属节点及物料维度核对，足量才把 WAITING 提升为 READY 并生成实际分仓 DRAW。根委外同样保留原树直属子件，见[V495](../数据迁移/111-V495根委外前置自制物料归属.md)。历史遗留委托通过授权刷新归还，`lowerLevelPending` 只解释下层缺料。

## 二、来源、仓库范围与历史兼容

### 2.1 来源选择

销售来源只接受服务端可分析的已审核销售订单行，以销售订单行 UUID 为真源。合法手工需求保留来源类型、稳定来源编号、货品/颜色/单位 UUID、数量和原因。单次联合分析最多 500 个来源，较大任务分批分析。

`MAKE_COMPONENT / SUBCONTRACT_MAKE` 只由服务端响应显式命令生成，员工不能伪造；刷新历史分析时从持久化的用户来源重建，排除这些系统子件，避免重复追加来源。计划产品编号留空时由服务端按真实计划单号分配，不用货号、名称或 `MA-UUID` 反查关系。

### 2.2 计划只选主仓库

计划人员只选择主仓库、查看主仓汇总。不再展示参与子仓多选、领料仓设置、子仓存放明细或“先调入再齐套”的提示；子仓由仓库在真实领料环节处理。

- 新分析的 `warehouseId` 传所选真实主仓 UUID，`warehouseIds` 仅包含同一主仓 UUID。所属库存范围由服务端解析，客户端不枚举子仓、不挑选默认叶仓。
- 旧分析可能保存叶仓 UUID。打开页面时通过主档真实 `parentId` 映射显示主仓；不为显示名称修改旧 `warehouseId`、`warehouseIds`、原单或预留。刷新、追加来源继续保留该旧身份，由服务端作同主仓兼容。
- 缺失父关系或循环关系不能在客户端被伪装为新主仓；仓库资料未能解析时明确显示待更新。
- 同主仓物料按服务端合计齐套，并按实际库存仓分别预留和生成领料单。有原单及品质、实收入库证明的本批专属来源，继续跟随其实际仓；公共库存不能因货品相同就挪用。

同主仓与来源履约规则见 [ADR-073](../99-决策记录-ADR/ADR-073-本批物料履约与同主仓分仓领料.md) 和 [ADR-075](../99-决策记录-ADR/ADR-075-合格来源跟随实际入库仓与车间通知.md)。计划页、物料分析历史列表和计划汇总单均通过 `MasterDictionaryService.mainWarehouseOf` 读取真实主仓名，原业务 UUID 保留。

### 2.3 主仓公共库存与领料分工

物料主表、节点详情和提交确认统一读取 `MaterialView` 的 `mainWarehousePublicAvailableQty`、`mainWarehouseOpenSafetySupplyQty`、`mainWarehouseSafetyReplenishmentGapQty`。这三个数是服务端按主仓汇总的公共库存、公共补库在途和剩余补库量；同货品在多条 BOM 路径重复出现时不能重复相加。安全阈值仍以货品规则为准，按主仓核算一次，不按每个子仓重复扣除。

`MaterialAnalysisCommandService.groupSafetySnapshot` 校验主仓字段完整、非负、同组一致，并核对剩余补库量。旧响应或旧构造缺失主仓预算时，不能拿任意叶仓补出授权预算；命令要求刷新后重试。叶仓 `warehouseBreakdown` 保留原数据含义，但不再作为计划页面安全补库的判断依据。自动刷新比较包含三个主仓字段，预算变化会更新页面。

计划页不再单独显示“物料存放位置”，也不据其它仓数量在客户端推演调拨后缺口。实际领料沿已有领料单办理：`lib/features/warehouse/pages/stock_doc_detail_page.dart` 顶部显示真实领料仓及对应物料数量。仓库侧明细和追溯保持可用。

## 三、分析详情的交互

### 3.1 三个生产准备入口

主页面固定为“下达采购 / 下达委外 / 下达车间”，每个详情内分“未下达 / 已下达 / 需处理”。入口计数和详情使用同一分析快照，不把展示筛选当作新业务状态。

| 入口 | 未下达 | 已下达与进度 |
|---|---|---|
| 下达采购 | 已确认 BUY 且有可提交余量的根产品/组件 | 申请、订货、财务审批、收货、IQC、实际入库的真实来源和进度 |
| 下达委外 | 已确认SUBCONTRACT的根产品/组件，按有无生产子层办理申请或前置准备 | 前置自制、已产/已通知/可通知量、委外申请与回厂入库 |
| 下达车间 | 已确认MAKE根产品、真实自制子件和普通MAKE组件候选；不含根BUY/委外 | 已生成计划、待审核/待料、领料、生产、FQC 与实收入库 |

“需处理”从服务端排产资格、结构、路线和数量约束解释当前阻断。单纯物料不足不应变成禁止先下达车间的理由；已下达行的下一步仍须读取实时下游事实。产品有未排余量且已有计划时，可同时属于未下达和已下达，不能把两个计数相加当作产品总数。

### 3.2 八列主表与多选

共用 `MasterDataTableView`，默认保留八个业务列：

| 列 | 展示重点 |
|---|---|
| 产品 / BOM 层级，或物料 / 来源 | 名称、货号、颜色/单位、层级连线与来源数量 |
| 供料路线 | 下拉草稿或已确认路线 |
| 本批需求 | 当前节点的服务端有效需求 |
| 现货分配 | 本节点已分配的合格现货，不能重复汇总共享池 |
| 齐套缺口 | 当前实际缺料，醒目标红 |
| 待补数量 | 扣除已覆盖需求后的补料提示，提交时仍服务端重算 |
| 预计供给 | 在途与未来覆盖，不能当现货 |
| 进度 / 待办 | 真实阶段与下一步；完整细节双击查看 |

移除最后的“处理”列和独立“建议路线”列。表头设置、全屏、搜索和视图切换在同一工具条。宽屏使用联动折叠滚动，窄屏回退有界表格；不再在准备入口与主表间预留悬浮区高度。

名称、层级标志和供料路线在浅色未选中时为黑色，选中后为白色并配深绿底。缺口仍保留数字和文字，不能只靠颜色。全屏与普通视图共用选择、草稿和列配置，切换产品/物料视图不销毁正在使用的表格状态。

首列复选框使用稳定任务身份，跨分页和搜索保留选择。V478 的产品 P1 行直接承载 `Product.rootMaterialLineId` 指向的真实 `ROOT_SUPPLY`，其复选框只选择根自身，不递归选择子 BOM，也不再渲染第二条根物料行。旧响应缺少根 UUID 时，根路线区域仅作只读上下文；不能在客户端伪造 UUID。分页补充祖先只读，不增加业务数量或重复确认。物料聚合选择明确映射到各条精确路径；“全选筛选任务”及全局清除始终显示真实选择总数。主表每页 100 行。

### 3.3 按产品与按物料汇总

按产品保留父子层级，搜索具体名称或货号时保留命中路径的祖先。按物料按货品、颜色、单位 UUID 汇总，各任务 UUID 与数量分摊不合并；缺货品 UUID 的历史行按物料行隔离，不凭相同名称猜成同一物料。

已创建的自制/委外前置 child 是**计划锚点行**：不展开自己的物料树（其物料需求保持在原父树原位，单一份数据不重复计量），只承载计划链接与执行进度；详情面板显示「已下达自制 / 计划量 / 已完工入库」。主表进度列在锚点存在时内联子件真实执行状态。

汇总只累加服务端逐节点分配事实，不累加每行重复展示的整仓余额。深层诊断不得再消耗已分配给直接层完整套料的同一公共库存；exact 权益继续只属于精确受益节点。

### 3.4 上次路线与显式确认

1. 已确认路线与已下达事实优先，不被学习默认覆盖；手动修改的本地草稿优先于记忆。未确认节点按货品、颜色、单位 UUID 读取上次确认路线，再回退主档规则；均无可用值时默认委外。每份分析冻结已读取默认，确认一组不能把未选组改成统一委外。
2. `GET .../last-routes` 按 100 个货品分块、限并发读取。会话主体/模拟身份、权限、分析 ID/版本/指纹与加载代际共同防止迟到 GET 覆盖手动草稿或另一份分析。读取期间禁止抢先确认默认值；失败明确提示核对当前路线，不声称已恢复上次选择。
3. 下拉只改草稿并选择对应真实任务。“确认路线(N)”只提交所选未确认或已修改组的各自实际值；根产品与子 BOM 使用同一 `materialLineId/actionGroupKey` 确认通道，不各建一套默认值。
4. **「路线说明(选填)」与「全选筛选结果」已于 2026-09-05 下线**（用户口径：准备页只保留逐行选择 + 确认路线）：路线确认不再采集原因（服务端 API 仍接受可选 reason，V477 的原因可空约束继续生效，历史 `route_reason` 原样保留）；批量选择走表头复选框（作用于当页，跨页靠翻页累积勾选，选择集全局保留）。表头工具条只留视图切换（缺料/全部 BOM/按产品/按物料）；查找框常驻顶部卡片「更新时间」右侧，表格全屏时经 `onFullscreenChanged` 回到工具条（两处共享同一控制器）。
5. 已有计划、有效供料行动或产出交接时根路线冻结；普通节点仍受既有下游状态守卫。409/刷新只保留仍属于原任务、且未被他人改变确认路线的草稿，不能静默覆盖服务端事实。
6. 打开页面、读取上次路线、切视图和轮询均不自动保存或下达。`last-routes` 的返回值只是预填依据，不是新授权；默认车间继续由 V192 学习。

批量确认先按 BOM 深度降序，ROOT_SUPPLY 统一最后，同深度按稳定身份排序，再每 500 项分块；后块使用前块新版本。这样顶层采购刷新停用原 BOM 时，已选组件的路线已先确认，不会让下一批失去目标；根委外则保留原树供前置自制。中途失败显示已完成量，未完成草稿保留，同键重试不重复计量。确认路线不等于下达任务。

### 3.5 顶层供料节点与产出交接

V478 为销售与合法手工来源提供真实 `ROOT_SUPPLY` 节点：`nodeRole=ROOT_SUPPLY`、`level=0`，没有父节点或 BOM 行。产品头以 `rootMaterialLineId` 引用它。普通节点为 `BOM_COMPONENT`；新旧节点、来源分析行和后续单据均以系统 UUID 关联。

- 根 `MAKE` 使用原产品分析行直接下达车间，不通过 notify 再造一个自制 child。**根路线必须显式确认为 MAKE 才能进车间桶与选入车间计划（2026-09-05 起与服务端 canSchedule 同口径双向闸）**：未确认的根与未确认的子层候选一样只留在主表（红色「路线待确认」、无勾选框），车间桶入口计数不含它；确认采购/委外的根只进各自路线桶。服务端对未确认根返回「根产品供料路线未确认，请先确认为自制再下达车间」并拒绝 issue-plans；无根节点的旧分析沿用旧合同（剩余量>0 即可排产）。`readyNowQty=0` 不等于禁止先下达待料计划。
- 根 `BUY/SUBCONTRACT` 进入对应供料任务，不留在未下达车间计划。供料仍使用既有 notify 权限、来源数量、仓库及采购/委外后续审批链；有子层委外继续遵守前置备料合同。
- 根库存/供给使用货品基本单位，计划输入保留原需求单位。例如需求 10 箱、1 箱 20 件，根表和供给为 200 件，计划输入仍为 10 箱；不能把 10 与 200 当同一种数量相加。
- 外部根供给的合格实收入库通过追加式 `preplan_root_output_events` 交接。销售来源将精确入库权益释放并转为销售预留；依法使用已分配存量的销售根也需显式命令。手工来源不能仅凭公共现货写出一笔无来处的完成事实。
- `root_fulfilled_qty` 从 FULFILL/REVERSE 事件按冻结换算率投影，不能直接改数。收货反向必须匹配原切片；已进入拣货/发货的销售预留不允许直接红冲入库。

V478 不在迁移时批量改写旧分析路线。旧记录的根指针初始为空；新建或授权刷新才补真实根节点。旧响应的根区域只读兼容，既有子 BOM、历史单据及旧排产入口仍按原合同处理。详细约束和待验收边界见[V478说明](../数据迁移/94-V478顶层供料路线与产出交接.md)。

### 3.6 详情与批量任务

双击物料打开只读详情，展示完整路径、计量规则、安全库存、精确权益、其它仓参考、下游来源与 child 的“已转需求 / 计划量 / 已实收入库量”。打开或关闭详情不改变路线、版本或业务状态。

采购/委外/自制的批量编排仍由宿主页统一执行，弹层叠在任务详情上，完成后留在详情更新状态。创建生产计划属于终态动作，仍先返回宿主页显示提交遮罩与结果反馈（遮罩挂宿主页 Stack，分桶页为不透明路由会盖住）；不再转入旧“填写生产计划单”向导。采购/委外任务页每页 200 行；车间计划输入首屏 100 行，继续按需装载。

**2026-09-05 下达改版（用户口径：数量统一在表格里改，不再弹逐行数量对话框）**：

- 采购/委外分桶表的「未下达」段提供行内「下达数量」输入框（默认=本批缺口−已在途，可改小分批；有超量权限可超过缺口，超出部分服务端记公共超量）；数量类列收敛为「需求量 / 单位 / 已到货 / 缺口（红）/ 下达数量」，原「已下达（单号列表）/未下达」两列删除——单号列只在「已下达」段出现（该段核心信息），未下达量并入可编辑的「下达数量」。
- 点「提交采购需求(N)/下达委外(N)」只弹**总结确认弹窗**（品种数 + 合计 + 明细清单 + 根现货交接提示），确认即按既有分批/幂等/CAS 链提交；取消整批放弃。公共安全补库仍按 goods/color/unit 去重并入合计（仅采购路线）；委外遇安全缺口 fail-closed。
- 顶层产品进度未确认路线时与物料行同款红色「路线待确认」徽章（不再使用「请先确认顶层供料路线」长文案）。
- 「已选 N 项」胶囊随批量动作进右下悬浮组（MasterDataTableView 全站统一口径）；批量选择走表头复选框（当页），跨页翻页累积勾选，选择集全局保留。
- **顶层显示与流程同构（2026-09-05 二次收口）**：顶层产品与子层自制件走同一套流程——先「路线待确认」（红），确认自制后进「下达车间」桶，已下达看执行段状态。不再有顶层专用状态分支：桶准入（`_belongsInWorkshop` 要求根已确认 MAKE）、勾选门（`_canSelectProduct` 不再接受「有历史计划即视同已确认」的旁路——历史计划只影响草稿预填 MAKE，不跳过确认门）。
- **下达车间做减法（2026-09-05 三次收口，用户口径「计划只管下发，齐不齐车间自己判断」；ADR-071 四次收口=原子化）**：可安排桶一张表、一种称呼、一个状态词、一个按钮——
  - 类型列：顶层产品行也显示「**自制候选**」（不再有「产品」形态；真实子件仍标 自制子件/委外子件）；
  - 状态列合一（原「物料状态 + 执行状态/下层」两列合并）：路线待确认（红）/ 阻断原因 / 「**未下达**」/ 已下达看执行段；齐套与否不在这里区分，缺口数量仍在主表「齐套缺口」列如实施示；**「待安排生产」「物料准备中」等按齐套分叉的文案废弃，未下达自制任务一律「未下达」+同款颜色/图标（2026-09-05 状态统一）**；
  - **候选行与产品行同一张表单**：所有自制行（顶层/子件/候选）都填「本批数量 + 生产车间 + 负责人」——**数量默认=剩余需求**（齐套口径的「建议首批/可排产上限」随 ADR-71 下线；齐套拆批由执行段 WAITING/READY 自动完成），学习默认车间/车间负责人同样带出；数量上限=剩余需求（业务上限，非库存上限）；
  - 右下角**单按钮「创建生产计划(N)」**：单次原子调用 `issue-plans`——候选行建子件任务（自制/有子层委外分流）与逐行生成计划、可选审核在服务端**同一事务**完成；混合勾选一次完成（N=全部勾选行数；校验逐行点名第一处错误；未选不占位）。原「创建子件任务(N)/生成生产计划(N)」双按钮退役；**旧 notify→plan-preview→generate 三段式整体退役**——失败整体回滚不残留子件行，重试幂等不重复建行；「订单/上级」「当前齐套」「建议首批」「可排产上限」四列下线；
  - 有子层级委外件在「下达委外」桶点下达 = 与上述自制逻辑同一套（自动进车间桶出计划）；委外管理侧进度文案完成前统一「**等待车间生产完成**」（通知中/等料/生产中的细分由车间任务页承载），完成后「已完工入库·待通知委外→已通知委外」。
- **已下达单据列只显示单号**（2026-09-05）：不再拼「单号 · 状态」，状态与全链路进度走行点击弹窗。
- **委外复用自制链**：有子层委外下达后，只增加委外目标件的计划锚点，原子件及已确认路线保持原 UUID。根层与中层都不复制子树、不重复下达子件；旧“复制子树后继承路线”实现已退役。前置自制领用的原材料在目标件等待委外回厂时继续覆盖本批，不能再次生成同一份缺口。
- **委外通知撤回**：物料详情可撤回已解除下游的通知，已完成标记的委外行动仍可进入此操作。必须先删除关联订货草稿或完成订货/出仓/回厂反向，服务端再追加批次冲销，不删除原申请或通知事实；之后才允许仓库反向前置成品入库。部分通知按原任务未通知责任加真实委外申请责任计一次，见[V496](../数据迁移/112-V496委外通知冲销与前置产出反向.md)。

### 3.7 统一流程词表（2026-09-06，全站唯一口径）

「进度 / 待办」的状态文案只允许来自**一份词表**：`lib/features/production/models/production_flow_stage.dart`
（展示组件 `production_flow_stage_cell.dart`）。物料分析主表、三个分桶详情、我的车间任务、
计划详情执行段卡、生产调度与进度全部引用它，任何页面不得自造状态词。阶段事实的来源分两路：

- **物料行（BUY/委外/自制子件锚点）**：服务端 `MaterialAnalysisFlowStageService` 按真实单据链
  批量推导 `MaterialView.flowStage` 键（下单/审批/收货/IQC/入库沿 `preplan_supply_actions →
  订货 → 审批 case → 收货 → 检验`)。**入库齐套权威口径**：V490 后本批需求不随下达归零，
  缺口只扣合格库存、精确权益及本批正式领用覆盖。整批下达只改变计划容量与在途责任，
  不能宣称已入库；历史归零行仍沿单据链逐步判定(未下单=
  等待采购下单 → … → IQC 全部合格且仓库实际入库才算已入库；全部不合格=原订单补货通道
  等待补货到货），修复了「采购未下单却显示已入库 / 进度弹窗跳步直达已完成」；弹窗内的逐步
  时间线（supply-progress）与阶段服务读同一批单据事实、同一口径。
- **2026-09-07 部分到货/验收/入库收口**：同一申请明细分批下多个订单时保留全部订单，不以最后
  一张覆盖此前财审或待检事实；收货明细使用真实receipt_item.id关联IQC，并保留order_item_id
  谱系。任一已收货明细缺少检验事实或仍待检，继续等待IQC；合格量大于实际入库量时显示等待入库。
  合格部分虽已全入库但本批仍有shortage，采购等待到货、委外等待回厂，不能标整批已入库。
  历史required=0行须逐订单明细证明收货检验完成、合格部分全部入库且覆盖订单基本量；某订单超收
  不能覆盖另一订单未到货。需求仍在本批且shortage归零的真实库存/权益覆盖口径保持不变。
  全部事实仍按分析批量查询，没有新增逐行HTTP/SQL，没有新增前端状态键，也不把未来到货当现货。
- **执行段 / 顶层产品**：`ProductionFlowStage.forSegment/forProduct` 就地推导（执行段状态 +
  零料标志 + 报工/计划数量，报工量缺失时回退入库完成比，绝不发明 0%）。

| 路线 | 步骤链（词表顺序） |
|---|---|
| 自制 | 等待下达车间 → 计划待审核 → 车间已收到 · 等待物料 → **物料齐套 · 可开工**（零料直制=**无需领料 · 可开工**；2026-09-06 由「等待车间领料」改名）→ **生产中 · 可报工** N%（报工/计划比，进度条）→ 已完工 |
| 采购 | 等待下发采购 → 等待采购下单 → 已下单 · 待财务审批 → 财务已审批 · 等待仓库收货 → 仓库已收货 · 等待品质验货 → 品质已通过 · 等待入库 → 已入库 |
| 委外 | 等待下发委外 →（有子层先走「前置自制 · +自制链」）→ 等待委外下单 → 已下单 · 待财务审批 → 财务已审批 · 等待目标件出仓 → 已出仓 · 等待回厂 → 已回厂 · 等待品质验货 → 品质已通过 · 等待入库 → 已入库 |

**三轮用户口径（2026-09-06）**：
- **仓库够货不跳过采购/委外流程（2026-09-06 三轮）**：仓库现货已覆盖需求且该路线尚无
  未撤销行动的行（需求仍在行内、缺口归零），仍进对应桶的「未下达」段，不因齐套被隐藏——
  新增**「仓库余量」列**（仓库可用现货池，够需求=绿、不够=红）；下达数量默认 0，计划员
  可填**富余量**下单：富余部分走公共超量备货通道（`production_material_analysis:over_supply`
  权限，不绑定本需求），仓库余量维持与需求的现货绑定，剩余新采购的进富余池。已转出
  （required=0）或已有在案行动（含已完成）的行不回流未下达段。
- **下达委外与下达采购同款总结弹窗+二次确认（2026-09-06 三轮）**：委外混合批次（有子层
  全量剩余 + 无子层行内数量）合并为一张 `MaterialSupplySubmitConfirmDialog` 确认，取消整批
  放弃（此前有子层路径不弹任何确认直接下达）。
- 采购/委外桶「已下达」段的「下达数量」列显示**该行已实际下达给下游的量**（按路线
  匹配的下游分摊合计），未下达段仍为可编辑的本次下达量（默认=缺口−已在途）；选中行
  深绿底下自绘的数量输入框/只读文字走白字口径（2026-09-06 修复「选中后看不见」）。
- BOM 表五个视图切换按钮（全部 BOM/只看缺料/待确认路线/按产品看/按物料汇总）改挂
  `MasterDataTableView.toolbarLeadingActions`：渲染在「表头设置/全屏」左簇内紧挨全屏按钮
  （原先贴右与全屏相距过远且多按钮零间距）。
- 车间桶「已下达」表列重设计：**产品 / 需求量 / 下达数量(计划量) / 完成数量(完工入库+
  百分比进度条) / 状态 / 生产车间 / 负责人 / 阻断摘要(按阶段给下一步指引)**；
  「订单/上级/剩余」列下线（来源与上级路径在详情面板看）。生产车间/负责人来自最新
  计划执行段（服务端 `planExecutionWorkshopName/planExecutionResponsibleName`）。
- **车间+负责人双记忆（V488，2026-09-06）**：`production_goods_workshop_preferences` 补
  `responsible_employee_id`（存量按「同车间最近一次确认段负责人」回填）；正式排产确认与
  车间改派时一并学习最近一次人工选择的负责人（多段不同人不更新、离职员工读侧过滤）。
  下达车间预填优先级：显式路线种子 > **学习记忆的车间+负责人** > 组织树车间主管；
  手工换车间时若恰为记忆车间也带出记忆负责人（黄标提醒核对）。
- 「已创建备料子任务」横幅不再放「下达车间」跳转按钮——备料子件的计划下达统一在
  「下达车间」入口办理，横幅只作提示（此前采购/委外桶会突兀出现该按钮）。
- **锚点转产行（required=0）也显示具体阶段**：进度/待办列优先取词表阶段（服务端
  阶段推导不再跳过零需求行），不再退化为「本批需求已转入生产计划」通用文案；仅
  参考/未激活等真正无需求的节点保留需求状态文案。

**零料直制语义修正**：主档无 BOM 子件的叶子件按 ADR-057 直接自制——它不需要领料，
`material_ready=TRUE` 是空真。2026-09-06 起这类段（V487 视图 `zero_material` 列）显示
「无需领料 · 可开工」，**不再冒充「备料完毕」**（修「计划详情明明没备料却显示备料完毕·可报工」
的误读）。计划详情执行段卡同步改为：等待物料 → 等待车间领料 → 料已发 · 可报工（零料=无需
领料 · 可开工）→ 生产中 → 已完工，并在卡头增加快递式流程步骤条（取最落后活动段的位置）。

「未下达」的旧词随词表下线：分桶「未下达」段按路线显示第一步（等待下达车间 / 等待下发采购 /
等待下发委外）；齐套与否不进状态列（缺口数量列如实现示，计划只管下发——ADR-071 口径不变）。
计划详情页新增「**本批次关联单据**」卡片（V487 起消费 related-documents 端点，读权限放宽到
`production_plan:view`）：展示本分析生成的采购/委外申请与订货单（单号+状态，可点击进入对应
单据详情查看审批/收货/IQC/入库进度；无对应模块权限时只显示单号）。

## 四、计划、齐套与执行门禁

### 4.1 三类数量与先下达车间

`canSchedule / maxSchedulableQty / scheduleBlockedReason` 是排产资格；`readyNowQty` 是当前可完工入库的完整齐套量。两者必须分开。

| 指标 | 含义 |
|---|---|
| ready_start | START 阶段的分析参考，不证明已领料或可以实际开工 |
| ready_finish / readyNowQty | START + ASSEMBLY + FINISH 硬门槛完整齐套量 |
| ready_ship | 增加 SHIP 参考物料后的预计可发提示，不作为实际发货授权 |

`SHIP/REFERENCE` 不写正式生产需求、reservation 或 DRAW。确实在生产消耗的包装材料应放到 FINISH，并按 `PER_UNIT / PER_PACKAGE / FIXED_BATCH` 逐边计算、向上取基本单位四位小数，不能平均单耗近似。

例：需求 10、现货只齐 6 套，默认本批数量=剩余需求 10；审核后执行段自动拆 READY 6 + WAITING 4（齐套拆分不再由计划侧预填）。WAITING 不锁零散库存，不生成 DRAW。后续物料实际到仓后，仅当对应待料执行段的完整需求可满足时原子提升；本轮不宣称已实现任意待料段再次细分。

### 4.2 审核、领料与通知

`generate` 只允许创建草稿、ACTIVE planning draft 与 SUBMITTED 来源关联；`approveNow` 还需独立 `production_plan:approve`。审核事务按真实齐套拆分 READY/WAITING，保留精确销售已排量和独立来源。

WAITING 可在车间任务中持续跟踪。完整齐套后，服务端同事务执行精确权益转正式预留、完整库存分配、生成真实 DRAW、提升 READY，并写 Outbox。仓库收到待发料任务，精确车间收到领料单号和实际发料仓提醒。

READY 只证明齐套和预留。DRAW 审核不代表实物交接；正式需求全部 FULFILLED 后才能 START，部分发料禁止开工。重复通知不代替业务事实，车间待办从实时投影读取；重复命令使用幂等键，旧版本/旧指纹失败关闭。

### 4.3 完工、委外与反向

报工只引用 IN_PROGRESS 执行段。日报审核记录完工申报，FQC PASS 后仍须仓库逐行点收，只有实收量增加库存和 `iqty`；完成率按有效 `iqty / qty`，不是报工量、齐套比例或工资。

有子层委外先内部完成目标件，合格实收进入前置任务专属预留；满批或显式分批后再通知委外。该入库不能提前满足原生产需求；委外回厂经 IQC 和仓库实际入库后才成为原需求合格供给。参见[ADR-062](../99-决策记录-ADR/ADR-062-委外先自制后通知与分批通知.md)与[ADR-046](../99-决策记录-ADR/ADR-046-生产领料仓库点收报工归属与未来计件边界.md)。

取消/红冲走专用命令，不能通用改写已外部化 action、申请、订单、领料、报工或来源分摊。未执行的取消按权益切片恢复；已有真实执行则要求先完成精确反向。历史单据、origin 与物理库存流水不被本轮 UI 或刷新改写。

### 4.4 默认车间、打印与合单边界

默认车间唯一来源为 V192 `production_goods_workshop_preferences`；正式确认/合法改派才学习，打开页面、改草稿或取消不学习。默认负责人来自该车间组织树负责人，预填需核对，不能替代服务端组织资格校验。无 BOM 自制件沿已有 ZERO_MATERIAL/DIRECT_MAKE 证据，不造空需求或空 DRAW。

当前仍是一分析行对应独立计划单；同车间批量输入不等于物理合单。共享计划头需另行迁移精确 plan_item 来源桥及取消、报工、入库守恒，本轮未实施。只读备料计划汇总单 (`/production/material-analyses/{analysisId}/summary`)不调用 generate/notify；正式打印使用真实 planId/packageId/segmentId，不把 WAITING 的无 DRAW 描述为无物料自制。

## 五、数量、权限与接口

需求 exact、公共安全补库和主动公共超量分账。BUY 可显式确认公共安全补库；MAKE/有子层委外没有公共超量子件链，仍失败关闭。A 需求 500、批准采购 2000 时，A 只占 500，公共 1500；B 显式采用 1000 后公共剩 500，不重复占用 A。参见[ADR-056](../99-决策记录-ADR/ADR-056-计划前生产需求与公共安全库存补库分账.md)、[ADR-065](../99-决策记录-ADR/ADR-065-备料下达同批合并采购委外申请.md)和[V474](../数据迁移/91-V474运行时公共在途与入库去向.md)。

| 动作 | 接口尾部 | 独立权限/业务事实 |
|---|---|---|
| 查看历史、详情与进度 | GET 只读接口 | view + 对象范围；来源采购/委外单据仍单独过滤 |
| 联合分析/刷新 | POST preview | create 或 refresh；持久化节点快照，不下达任务 |
| 创建所选路线 | PUT routes | route；保存路线和原因，重算分析 |
| 提交采购/委外或创建 child | POST notify | notify；写 action/allocation 和真实来源 |
| 主动公共备货 | notify 的独立公共量 | 另需 over_supply，不扩大需求 exact |
| 采用公共在途 | POST claim-shared-future | claim_shared_future + 容量守恒 |
| 下达车间（创建生产计划） | POST issue-plans | generate；立即审核再叠加 production_plan:approve；单事务原子完成建子件任务+逐行出计划+可选审核 |
| 撤回、让料及取消 | 各专用命令 | 独立权限、状态、双端版本与来源反向约束 |

实际路由、工作台入口、页面内授权和权限管理员目录必须一致，详见[权限体系总设计](../05-架构/权限体系总设计.md)。所有写按钮取客户端有效权限与服务端 `allowedActions` 交集；服务端仍独立校验对象范围、库存锁、数量、版本、指纹和幂等。任务详情没有另一套独立授权面，也不能递归挂载宿主页权限入口。

## 六、大批量与验证边界

- 界面按分析快照缓存、复合节点索引、分页和增量装载，避免一帧构建全部产品/物料 Widget。
- 深层分配使用稳定优先队列；同一 nodeKey 在不同 analysisItemId 下隔离。已有完整套料优先保护，深层诊断只消费剩余公共量，exact 不重复扣减。
- 5000 产品/10000 节点、2000 份共享库存的两次纯计算样本约 0.390 / 0.503 秒；该样本超过前端单分析 500 来源上限，用于算法压力验证，不能当成数据库、HTTP、网络和渲染的端到端耗时。
- 同批委外 BOM 分流改为一次批量查询，验证 500 产品重复物料不产生逐行重复往返。
- 旧活动分析通过正常授权刷新重算新投影；历史来源、单据和已应用迁移不改写。旧接口保留兼容不意味着恢复退役 UI 或绕过新门禁。

本轮自动化按不同测试范围分别记录，不能叠加重复测试得出“全链通过”。上轮 V476 的209项Flutter回归与后端分组证据保留在[历史验收记录](../99-项目治理/2026-09-05-物料分析准备与生产链本地验收记录.md)。本轮路线记忆、顶层供料、跨批次确认、V477/V478及最终前后端组合结果见[本轮记录](../99-项目治理/2026-09-05-顶层路线记忆与确认修复验收记录.md)，范围有重叠，不相加冒充全链验证。旧六桶、强制原因、退役即时保存、child硬阻断和旧向导均不作为现行验收标准。


## 2026-09-07 下达数量提示

采购/委外桶的下达数量与自制批次量均提供框内图标，说明本次安排量不是实收量、在途不算库存，以及可下达不等于车间已可开工。指标保持“还缺实物”与“还需安排”各自原有口径；选中深色行的数字、单位与输入底色继续可读。
