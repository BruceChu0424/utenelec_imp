# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
/dist/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# === Spring Boot 后端 (server/) ===
server/target/
server/dependency-reduced-pom.xml
server/logs/
server/.env
server/.env.*
!server/.env.example

# === 官网前端 (website/) ===
website/.env
website/.env.*
!website/.env.example
server/*.iml
server/.mvn/
server/mvnw
server/mvnw.cmd
*.p12
*.jks

# === AI / 本地工具配置（不进仓库） ===
.claude/
.zcode/

# === 本地数据/临时文件（不进仓库） ===
/YTDQ_2023-202607
/.tmp-shot.png
__pycache__/
*.pyc
server/backups/
# 老库迁移数据导出（真实业务数据/PII：客户/供应商/货品/模具主档 + 分类，本地导出，绝不入库；
# 需要时用 server/legacy_migration/export_legacy.ps1 重新生成）
server/legacy_migration/data/
server/src/main/resources/legacy-migration/
# HR 正式名录源表（真实 PII：141 人身份证/手机/住址，由 build_hr_roster.py 消费，绝不入库）
/中山市优腾电器职工信息表.xls
# 新 ERP 产品列表导出（真实业务数据/PII：货品主档 80 列 + 导入比对报告，
# 由 server/legacy_migration/import_product_lists.py 消费，绝不入库）
/product lists/

# 调试/调查草稿区（一次性脚本与临时文件，完成后清理，不进仓库）
# 覆盖根 _scratch/ 与 server/legacy_migration/_scratch/
_scratch/

# 临时补丁/会话草稿（不入库）
/.tmp-*.patch
/.tmp_*.patch
/scratchpad_problems_dump.txt
/YTDQ.txt

# 临时账号排查脚本与导出（真实登录账号 PII：ID/账号/姓名/状态，不入库）
server/legacy_migration/inspect_users*.ps1
server/legacy_migration/users_names.txt

# 临时会话/调试目录（含 DB dump 等，不入库）
.codex-tmp/
# Flutter 构建产物（误生成于源码目录的 .last_build_id，可随时重建）
**/.last_build_id

# 真实服务器资产交接（含内网拓扑、账号与云资源标识，仅保留在受控本机）
/服务器部署交接.md
/server-handoff.*.private.md
# One-time evidence-bound scripts for the audited physical test host. They can
# contain immutable host identity and receipt hashes; never publish them.
/deploy/setup/phase1-existing-test-host-*
/deploy/setup/test_phase1_existing_test_host_*
/deploy/setup/phase1-smart-long-test-assess.py
/deploy/setup/test_phase1_smart_long_test_assess.py

# PostgreSQL repo2 commissioning inputs/receipts may contain provider identities,
# private evidence references or secrets. Only reviewed *.example.json files belong in Git.
/deploy/postgres/backup/repo2-policy.json
/deploy/postgres/backup/repo2-secrets.json
/deploy/postgres/backup/worm-evidence.json
/deploy/postgres/backup/pitr-acceptance.json
/deploy/postgres/backup/repo2-enabled.approved
/deploy/postgres/backup/*.candidate
/deploy/postgres/backup/*-receipt.json
/deploy/postgres/backup/private/
/**/backup-acceptance-receipts/
