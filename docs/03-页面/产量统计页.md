# 产量统计页（未实施提案）

> 计划路由 `/production/output` 当前没有注册，计划文件
> `production_output_stats_page.dart` 不存在。当前真实生产报表位于 `/production/report/*`，
> 使用服务端报表接口。

## 一、候选目标

按日期、产线、产品和班次汇总合格量、不良量、不良率与达标率。立项前先评估现有生产报表能否
增加相同维度；如果可以，应扩展现有报表而不是创建重复页面。

## 二、数据口径

- 所有 KPI 必须由服务端统一聚合，明确定义分母为零、返工、冲销、补录和跨日班次。
- 查询必须服务端过滤、稳定排序和分页；长期数据不能拉到 Flutter 汇总。
- 导出与页面使用相同筛选、数据范围和统计 SQL，并进入导出审计。
- 若使用物化视图，页面必须展示数据新鲜度，并定义刷新失败降级。

## 三、目标 UI

目标组件为 `UtenAppBar`、`UtenDateField`、`UtenDropdownField`、业务 KPI 卡/
`DocKpiBar`、`MasterDataTableView`、`UtenExportButton` 和 `UtenEmpty`。正式图表库尚未选定，
不得实现或引用 `UtenChartPlaceholder`、`UtenStatCard`、`UtenDataTable`、
`UtenViewContextSelector` 等不存在组件。

## 四、权限与验收

- 新功能应登记明确的查看/导出权限；数据范围由后端权威实施。
- 页面无权时隐藏入口，直接调用接口仍返回拒绝。
- KPI、明细、导出在同一筛选下对账一致。
- 30k+ 明细和多年聚合查询须有 PostgreSQL 执行计划与 P95/P99 证据。
- 日期边界、时区、空数据、零分母和重复源记录测试通过。

**状态**：未排期产品提案；当前应优先完善真实 `/production/report/*`。
