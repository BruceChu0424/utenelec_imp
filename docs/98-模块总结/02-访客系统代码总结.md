# 02 - 访客系统代码总结

> 访客系统：外部人员手机验证码登录 → 来访预约 → HR 审批 → 被访人确认 → 保安扫码核验。
> 前后端打通，双主体 JWT（staff / visitor），签名二维码凭证。

---

## 一、技术架构

| 层 | 实现 |
|---|---|
| 前端 | Flutter：入口选择 / 访客登录（手机+验证码）/ 预约表单 / 我的预约 / 二维码 / 审批列表 / 保安扫码（mobile_scanner）|
| 后端 | Spring Boot：`features/visitor/` 全套（Auth/Sms/Application/Approval/Directory/Security 6 个 Service + Controller）|
| 数据库 | V12 迁移：`visitor_accounts` / `visitor_applications` / `visitor_approval_steps` / `visitor_sms_codes` / `visitor_refresh_tokens` 5 表 |
| 鉴权 | 双主体 JWT（`typ=staff` / `typ=visitor`），`JwtAuthFilter` 按 typ 分支复查 DB 状态 |
| 加密 | 访客手机/身份证 pgcrypto 加密；验证码 SHA-256 哈希；二维码 HMAC 签名 |
| 短信 | `SmsGateway` 接口：`LogSmsGateway`（开发）/ `AliyunSmsGateway`（生产 stub） |

## 二、安全要点（已审查修复）

| 项 | 状态 |
|---|---|
| IDOR 越权（访客读任意申请 PII） | ✅ 修复（C1：requireStaff 拦截 isVisitor） |
| 权限拆分（visitor:approve vs host-confirm） | ✅ 修复（C2：V13 迁移拆分） |
| 员工检测（phone_hash） | ✅ 修复（H1：onboard 写入 phone_hash） |
| SMS 限流 + 手机校验 | ✅ 修复（H3：LoginRateLimiter + 正则） |
| 拉黑接口 | ✅ 修复（H4：POST /security/blacklist/{id}） |
| 二维码 HMAC 签名 + 过期 + 一次性 | ✅ 原设计正确 |
| 访客 token blocked 复查 | ✅ 原设计正确 |
| 刷新轮换 + 重用检测 + 悲观锁 | ✅ 原设计正确 |
| hostConfirm 状态守卫 | ✅ 修复（M1） |
| checkIn 并发锁 | ✅ 修复（M2：PESSIMISTIC_WRITE） |
| 发送间隔修复 | ✅ 修复（M3） |
| 目录权限 | ✅ 修复（M4：isAuthenticated） |
| 注册竞态 | ✅ 修复（M5：catch DataIntegrityViolation） |
| 中文消息 | ✅ 修复（M6） |
| 常量时间比较 | ✅ 修复（M7：MessageDigest.isEqual） |

## 三、前端规范（已审查修复）

| 项 | 状态 |
|---|---|
| i18n（zh+en，99 key 齐全） | ✅ en 真英文翻译 |
| 硬编码 '访客$tail' | ✅ 修复（I-1：→ visitorNo） |
| CJK 标点漏英文 | ✅ 修复（I-2/I-3：→ ASCII） |
| hint 当错误 | ✅ 修复（I-4：专用 key） |
| 日期走 intl | ✅ 修复（I-5：DateFormat.yMd(locale)） |
| 桌面/Web 扫码兼容 | ✅ 修复（R-1：kIsWeb 守卫） |
| 硬编码颜色/字号 | ✅ 修复（U-2：→ UtenColors / textTheme） |
| 图标 size 缩放 | ✅ 修复（context.scaled() 机制 + 应用） |

## 四、待完善

| 项 | 说明 |
|---|---|
| 车牌加密 | `plate_no` 目前明文（与员工 name 明文一致）；如需加密加 `plate_no_enc` 列 |
| UtenSelect / UtenDialog | 访客表单用了原生 `DropdownButtonFormField` / `AlertDialog`；待建 Uten 组件替换 |
| maxWidth 限制 | 部分页面 expanded 无 `ConstrainedBox(maxWidth:1600)` |
| 访客会话恢复 | 冷启动只本地解 JWT（建议加 /me 验证） |
| 图标缩放推广 | 更多页面的 `Icon(size:)` 待替换 `context.scaled()` |

---

**最后更新**：2026-07-23 · **审查状态**：CRITICAL/HIGH/MEDIUM 全部修复，BUILD SUCCESS + flutter analyze 0 错误
