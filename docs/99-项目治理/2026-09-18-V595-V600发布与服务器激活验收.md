# 2026-09-18 V595-V600 发布与服务器激活验收

> 单维护者发布链（ADR-060）例行发版留证：同一提交本地全量检查 → GitHub 三工作流全绿 →
> tag 触发 Simple Release → 服务器验签暂存 → 维护窗口激活。本文只记录事实与证据位置，
> 不重复业务设计（见各 ADR 与页面文档）。

## 一、发布候选

- 提交链：`90caa4a3`（feat(platform): V595-V600 车间开工三路线与到货链大批次 + 生产路线直显 UI + 测试收口）
  → `742cafde`（style(dart): CI format 门对齐 26 文件）→ `bc59a7f2`（test(ci): CI 首轮后端红收口）。
- tag：`v2026.09.18-1`（指向 `bc59a7f2`）。
- 本地检查（发布前）：
  - `flutter analyze --no-pub` 0 issues；`flutter test` 2698 全绿；
  - `UTEN_RUN_DB_TESTS=true mvn verify` 首轮 5 类红（二次确认/版本号/V598 口径/迁移计数），
    修复后 165 用例归零；CI 首轮后端 7 类红（fixture 缺 V584/V595/V599 列与函数桩、
    到货通知单测路由、dispatch 二次加锁），修复后本地全部归零。
- CI（`bc59a7f2`）：Quality Gate（Flutter 14m27s ✓ / Backend ✓）、CodeQL ✓、
  Dependency Vulnerability Scan ✓ 三工作流 completed/success。
- Simple Release（run 35348148965，10m34s success）：制品签名上传
  `releases/v2026.09.18-1/** + LATEST.txt`（阿里云 OSS，发布密钥 ED25519）。

## 二、服务器激活（内网测试环境）

- 激活前基线：active `v2026.09.16-1`，健康 UP，数据库 Flyway **594**，服务 active。
- `uten-imp-updater check`：下载验签通过（ED25519 key SHA256:CKlYy…），暂存
  `/opt/uten-imp/releases/v2026.09.18-1`；提示含数据库迁移需人工激活。
- `uten-imp-updater activate v2026.09.18-1`（2026-09-18 21:20 服务器本地时间）：
  - pg_dump 全量备份先行（updater 自动，0700/0600 权限）；
  - migrator：validate 557 迁移通过 → V595/V596/V597/V598/V599/V600 六个迁移
    成功应用（execution time 00:00.355s），schema public 594 → **v600**；
  - 原子切换 current → v2026.09.18-1，健康检查通过，激活成功。

## 三、激活后验收

| 检查 | 结果 |
| --- | --- |
| `uten-imp-updater status` | active/latest = v2026.09.18-1，health UP |
| `/actuator/health` | UP |
| Flyway 版本 | 600 |
| 存量段路线回填（V599） | 活动 WAITING/READY/DISPATCHED 段中 `start_route IS NULL` = **0**（升级零摩擦达标） |
| V595/V599 判定函数 | `fn_execution_route_allows_auto_promote` / `fn_can_change_execution_route` / `fn_can_start_continuous_supply` 均存在 |
| nginx | active，`/api` 反代 301 → HTTPS（口径符合内网 HTTPS 要求） |

## 四、本轮发布内容速览

- V595/ADR-089 车间直送 v2：持续生产、线边仓服务端自动配置、直送需求不参与齐套；
- V596/V597/ADR-090 到货与产成品「先入库后质检」两条入库链；
- V598 货品来源单一事实源（确认路线回写主档 + 存量回填，last-routes 退役）；
- V599/ADR-091 开工路线确认门控（齐套/分批/持续三选一）+ 到货进展聚合卡；
- V600 庆典祝福默认不自动发送；
- UI：车间任务等待物料「生产路线」列直显选项（点芯片预选→一次确认；单选项直确认），
  生产中/历史任务去掉该列，表头 ⓘ 说明；检查单队列「储放位置」带库位号；
  全站列头筛选批次、详情页操作遮罩、`UtenInlineNotice` 组件；
- 工程收口：`_NextStepDropdown`/last-routes 等旧件退役删除，`dispatch` 复用已锁段少一次锁查询。

## 五、边界与后续

- 真实车间/仓库/品质岗位 UAT 未做：新链路（路线确认、持续生产、先入库后检）需现场按
  ADR-091 §三情况表逐条演练；本记录只证明部署与迁移事实。
- ADR-091 §七已知边界（直送撤回补偿引导、线边仓余料处置、红冲不撤旧进展卡）维持后续排期。
- OSS 只保留最新一版（旧版本由发布流水线自动清理）；数据库备份在服务器
  `/var/backups/uten-imp/`（激活时 updater 另存迁移前 dump，路径见激活输出）。
