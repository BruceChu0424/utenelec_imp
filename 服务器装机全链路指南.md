# 公司服务器装机全链路指南（零基础版）

> 机器：联想 ThinkSystem ST258 V3（i7-12700 / 16GB / 512GB SSD + 2×2TB HDD）
> 目标：Ubuntu Server 24.04.4 LTS + mdadm RAID1 + Cockpit 图形管理面板
> 用途：Uten IMP ERP 公司本地服务器（唯一写主库）
> 全程约 1~1.5 小时

---

## 你需要先准备的东西

| 物品 | 说明 |
|---|---|
| 已下载好的 ISO | `ubuntu-24.04.4-live-server-amd64.iso`（约 3.2GB，在你的办公电脑上） |
| 空 U 盘 | 8GB 以上，**里面数据会被清空** |
| 显示器 + 键盘 | 临时接到服务器上，装完就可以拔掉 |
| 网线 | 服务器插到公司路由器/交换机，能上网 |
| 你的办公电脑 | Windows，用来制作 U 盘和以后远程管理服务器 |

---

## 第一步：用 Rufus 制作启动 U 盘（在办公电脑上操作）

1. 浏览器打开 https://rufus.ie ，页面往下拉，下载 **Rufus 4.x**（便携版，双击直接用，不用安装）
2. 把 U 盘插到办公电脑
3. 双击打开 Rufus，从上到下这样选：
   - **设备**：选你的 U 盘（看准容量，别选错）
   - **引导类型选择**：点「选择」按钮，找到下载好的 `ubuntu-24.04.4-live-server-amd64.iso`
   - **分区类型**：GPT
   - **目标系统类型**：UEFI（非 CSM）
   - 其他全部保持默认
4. 点「开始」→ 如果弹出「检测到 ISOHybrid 镜像」，选「以 DD 镜像模式写入」或「以 ISO 镜像模式写入（推荐）」都可以，选推荐的
5. 弹出警告「U 盘上的所有数据将被清除」→ 点「确定」
6. 等 3~5 分钟，进度条变绿显示「准备就绪」→ 关闭 Rufus，安全弹出 U 盘

---

## 第二步：服务器进 BIOS 改设置（关键！）

1. 把显示器、键盘、网线接到服务器上，U 盘插到服务器 **USB 2.0 口**（黑色口，兼容性最好）
2. 开机，看到联想 Logo 时**反复按 F1**，进入 BIOS Setup
3. 找存储模式设置（菜单名可能是以下之一，用方向键翻找）：
   - `System Settings` → `Storage`
   - 或 `Devices and I/O Ports` → `SATA Configuration`
   - 关键词：**SATA Mode / RAID / RST / VMD**
4. 把模式从 **RAID（或 RST）改成 AHCI**
5. 顺便确认启动模式是 **UEFI**（Boot Mode: UEFI Only）
6. 按 **F10** 保存并退出，机器会重启

> ⚠️ 这一步必须做。板载 RAID 是"假 RAID"，Ubuntu 装不上或以后没法维护。改 AHCI 后我们用 Linux 自己的软件 RAID，更可靠。

---

## 第三步：从 U 盘启动安装器

1. 重启后看到 Logo 时**反复按 F12**，出现启动菜单
2. 用方向键选带 **UEFI: U盘品牌名** 的那一行，回车
3. 出现黑色 GNU GRUB 菜单，选 **Try or Install Ubuntu Server**，回车
4. 等 1~2 分钟，进入安装向导（全键盘操作：方向键移动、回车确认、空格勾选、Tab 切到按钮）

---

## 第四步：安装向导逐屏操作

| 屏幕 | 你做什么 |
|---|---|
| **语言** | 选 **English**（服务器系统不要装中文，报错日志才规范） |
| **Installer update** | 选 **Continue without updating** |
| **键盘布局** | 默认 English (US)，直接 **Done** |
| **安装类型** | 默认 **Ubuntu Server**，直接 **Done** |
| **网络** | 默认自动获取（DHCP）就行，看到拿到 192.168.x.x 的 IP 即可，**Done**（固定 IP 装完再配，更简单） |
| **代理 Proxy** | 留空，**Done** |
| **软件源 Mirror** | 建议改成 `https://mirrors.aliyun.com/ubuntu`（国内速度快很多），改完 **Done** |
| **磁盘（最重要）** | 选 **Custom storage layout** 或向导式都可以；**只动 512GB 那块固态硬盘**，选「Use entire disk / 使用整块磁盘」；**两块 2TB 机械盘一定不要勾选、不要格式化**。最后摘要页确认只有 512G 盘会被分区，**Done** → 弹出警告选 **Continue** |
| **用户信息** | Your name：你的姓名；Server name：`uten-imp-prod`；Username：你的名字拼音（全小写，如 `bruce`）；密码：**设一个强密码并立刻记到手机备忘录里**，以后天天要用 |
| **Ubuntu Pro** | 选 **Skip for now** → **Continue** |
| **SSH** | **按空格勾选 Install OpenSSH Server**（必勾！）→ **Done** |
| **推荐软件** | 什么都不勾，直接 **Done** |
| 安装中 | 进度条跑 10 分钟左右，去喝杯水 |
| 完成 | 显示 **Reboot Now**，回车；提示拔 U 盘时拔掉 U 盘再回车 |

---

## 第五步：首次登录和系统更新

重启后出现黑底白字登录界面：

1. `uten-imp-prod login:` 后面输入你的用户名，回车
2. `Password:` 后面输入密码——**注意：输密码时屏幕完全不显示，没有星号，这是正常的**，输完回车
3. 登录成功后执行（逐行复制，输完回车）：

```bash
sudo apt update && sudo apt upgrade -y
```

（`sudo` 会再问一次密码，同样不显示）

4. 查看服务器 IP：

```bash
ip addr
```

找 `192.168.` 开头的那段（比如 192.168.1.105），记下来。

**从这里开始，服务器可以拔掉显示器和键盘了**，后面全在你办公电脑上远程操作：
按 `Win + X` 打开 PowerShell，输入 `ssh 你的用户名@192.168.x.x`，第一次会问 yes/no，输 yes，然后输密码。

---

## 第六步：固定 IP（路由器绑定法，最省事）

ERP 服务器的 IP 必须永远不变，否则员工电脑会连不上。

1. 在你办公电脑浏览器打开路由器管理页（一般是 `192.168.1.1`，看路由器背面贴纸）
2. 找「设备管理 / DHCP 设置 / IP-MAC 绑定 / 地址预留」之类的功能
3. 找到服务器（主机名 uten-imp-prod），把它的 MAC 地址和一个固定 IP 绑定，比如 `192.168.x.10`
4. 保存。以后服务器重启、断电，IP 都固定是这个

> 服务器的 MAC 地址在上面 `ip addr` 命令输出里，`link/ether` 后面那串就是。

---

## 第七步：两块 2TB 机械盘做 RAID1 镜像（SSH 里操作）

1. 先确认盘符：

```bash
lsblk
```

应该看到：`sda`（512G，系统盘，不要碰）、`sdb` 和 `sdc`（各 2T，空的）。如果盘符不一样，以下命令里的 sdb/sdc 换成实际看到的。

2. 逐行执行：

```bash
sudo apt install -y mdadm
sudo mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb /dev/sdc
```

（会问一次确认，输 y。随后两块盘开始同步，后台进行，不影响继续操作）

```bash
sudo mkfs.ext4 /dev/md0
sudo mkdir -p /data
echo '/dev/md0 /data ext4 defaults,nofail 0 2' | sudo tee -a /etc/fstab
sudo mdadm --detail --scan | sudo tee -a /etc/mdadm/mdadm.conf
sudo update-initramfs -u
sudo mount -a
```

3. 验证：

```bash
df -h /data
cat /proc/mdstat
```

`df` 显示 /data 约 1.8T 即成功；`mdstat` 里显示 `[UU]` 表示两块盘都健康（`[U_]` 表示一块掉了，要告警）。

---

## 第八步：装 Cockpit 图形管理面板

这就是你要的"GUI"——不用接显示器，浏览器里管理服务器：

```bash
sudo apt install -y cockpit cockpit-storaged
```

装完在你办公电脑浏览器打开：

```
https://192.168.x.10:9090
```

（第一次会提示证书不受信任，点「高级 → 继续前往」，自签名证书，正常）

用服务器用户名密码登录，界面是中文的，可以看 CPU/内存/磁盘、开关服务、看日志、管理 RAID（Storage 页签里能看到 md0 阵列状态）、还有网页版终端。

---

## 第九步：基础防火墙

```bash
sudo ufw allow from 192.168.1.0/24 to any port 22     # SSH（网段换成你公司实际的）
sudo ufw allow from 192.168.1.0/24 to any port 9090   # Cockpit
sudo ufw allow from 192.168.1.0/24 to any port 80     # ERP 网页入口（后续 Nginx）
sudo ufw allow from 192.168.1.0/24 to any port 443
sudo ufw enable
```

（`192.168.1.0/24` 换成你公司内网实际网段，就是服务器 IP 前三段加 `.0/24`）

---

## 到这里，系统层全部完成 ✅

接下来是 ERP 应用层部署，全部照项目里的权威文档执行：

- `deploy/cloud/README-cloud.md` — PostgreSQL 16（数据目录放 SSD，PITR 备份链放 /data）
- `deploy/README.md` — 不可变制品发布、systemd 服务、Nginx、watchdog
- `deploy/systemd/` 里的模板直接可用

---

## 常见问题（装机时卡住先看这里）

| 现象 | 原因和解决 |
|---|---|
| 输密码屏幕没反应 | 正常！Linux 输密码不显示任何字符，输完直接回车 |
| 按 F12 没有 U 盘选项 | U 盘没做好（重做 Rufus）；换 USB 2.0 口；确认 BIOS 是 UEFI 模式 |
| 安装器里看不到硬盘 | BIOS 里 SATA 模式没改成 AHCI，回第二步 |
| 安装到一半卡住不动 | 软件源太慢，回第四步把 Mirror 换成阿里云源重装 |
| ssh 连不上 | 安装时没勾 OpenSSH；在服务器上执行 `sudo apt install -y openssh-server && sudo systemctl enable --now ssh` |
| Cockpit 打不开 | 确认网址是 `https://` 开头且带 `:9090`；检查防火墙第九步 |
| mdstat 显示 `[U_]` | 一块盘掉线，执行 `sudo mdadm --detail /dev/md0` 查看详情 |
