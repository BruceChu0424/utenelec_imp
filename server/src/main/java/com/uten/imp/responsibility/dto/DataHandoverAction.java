package com.uten.imp.responsibility.dto;

/** How one preview group will be handled by an employee data handover. */
public enum DataHandoverAction {
    TRANSFER,
    HISTORY_ACCESS,
    RELEASE,
    BLOCKING
}
