# 2026-09-21 v1.2.0 发布与服务器激活验收

> 紧接 [v1.1.0 记录](2026-09-21-v1.1.0发布与服务器激活验收.md)的第二次发版：用户指示「修改了就直接发布，重跑后有问题再修改」，
> 故本版由部署会话收口并行会话的 V631 批次后立即手动 Simple Release、快进合并 main、激活公司服务器；CI 慢作业结果事后核对。

## 一、发布候选与验证

- 分支 `feat/platform-v631-sales-outbound-20260921`(自 `34af66b7` 起)→ PR #22 → `main` 快进到 `3b319cb6`(2026-09-21 01:43Z 标记为已合并)。
- 提交：`ba5bd396` feat(sales) V631 出货明细按行记实际发出仓 + 财审汇率闸门 + 订单进度出货在途四量 + 出货列表状态分段(62 文件，含并行会话三批 A/B/C 与已结束会话的徽章改动；部署会话补 4 个仓库文件 dart format)
  → `3b319cb6` style(warehouse) 一处 if 补花括号(dart format 折行后触发 `curly_braces_in_flow_control_structures`，CI analyze 唯一红；教训：格式化之后要再跑一次 analyze)。
- 迁移目录头：V631/587(V544、V576、V604 跳号)；五处同步点由并行会话推进，契约测试复验。
- 「其他支路」核对：Codex worktree `uimp/material-platform-audit-20260920`(699 个未提交文件)是 2026-09-20 早上批次的旧快照，与 `5fb81387` 相比只多 63 行全部为被后续提交取代的旧内容(35k 行落后)，不需要合并；`origin` 其余未合并分支全是 Dependabot 依赖升级 PR(#1–#16，含 Spring Boot 4.x 大版本)，未纳入本版。
- 本地隔离 worktree 验证(`ba5bd396`，Testcontainers 真实 PostgreSQL)：CI 等价快道 `mvn test` 4918 项 0 失败；真库 SalesShipmentWarehouseAssignmentEndToEndTest 6、PreplanFutureTransfer 1、SalesOrderProgressStage 10、WarehouseSalesOutboundContract 2、SalesShipmentFinanceGatePolicy 2、LegacyMigrationSafety 11、两 reset 契约 12 共 44 项 0 失败；`flutter test` sales/warehouse/finance 617 通过；主树 dart format 0 改动、flutter analyze 0。
  并行会话自报：FullChainEndToEndTest 154、flutter 全量 2881、8081 克隆库 V631 迁移成功。
- GitHub(`3b319cb6`)：Dependency Vulnerability Scan 绿(push 与 pull_request)；CodeQL 与 Quality Gate 在发布时仍在运行。`ba5bd396` 的 Flutter 作业因上述 analyze 提示红，其余快道绿。
- Simple Release：`workflow_dispatch`(version=v1.2.0，allow_running_checks=true)run 35551166706 success(约 12 分钟)：双 JAR + Flutter Web(APP_VERSION=v1.2.0，APP_BUILD_ID=3b319cb6)→ 两次门禁 → ED25519 签名 → OSS `releases/v1.2.0/** + LATEST.txt` → 旧版本清理成功。
  `ba5bd396` 上的首次 dispatch(35550948487)被门禁按设计拒绝(Flutter 作业已红)。

## 二、服务器更新(内网测试服务器，2026-09-21 09:44–09:46 CST)

激活前基线：active v1.1.0，Flyway 630/586，业务数据 0。

1. `uten-imp-updater check`：下载验签通过(ED25519 key SHA256:CKlYy…)，暂存 `/opt/uten-imp/releases/v1.2.0`(version.json v1.2.0 / 3b319cb6)，识别「含数据库迁移需人工激活」。
2. `server.env`(备份 `server.env.bak-<时间戳>`)：`UTEN_SERVER_STATUS_APPLICATION_VERSION` v1.1.0 → v1.2.0；OCR 两项沿用 v1.1.0 配置。
3. `uten-imp-updater activate v1.2.0`：pg_dump 备份 `/var/backups/uten-imp/uten_imp-v1.2.0-20260921-094454.FvanXS.dump` → migrator 校验通过 → 应用 V631(0.116s)→ 原子切换 current → 健康检查通过。
4. 运行权限边界沿用 v1.1.0 的撤权状态(`uten` 不再继承 `uten_migrator`)，V631 新列由迁移角色建在既有表上，默认 ACL 与显式授权覆盖，冒烟未见权限问题。

## 三、验收(全部通过)

| 检查 | 结果 |
| --- | --- |
| `uten-imp-updater status` | active/latest = **v1.2.0**，health UP，staged v1.0.0/v1.1.0 保留为回退 |
| 服务 | uten-imp / nginx / uten-imp-updater.timer / uten-paddle-ocr 均 active，NRestarts=0 |
| 健康与版本 | readiness UP；`/version.json` → v1.2.0 / 3b319cb6；`/api/admin/server-status` applicationVersion v1.2.0 |
| Flyway | **631** / 587 |
| V631 对象 | `sales_shipment_items.warehouse_id`、`sales_shipment_warehouse_events.line_warehouses` 存在 |
| 只读冒烟(超管自签短时 token，经 nginx HTTPS) | `/api/auth/me`、`/api/admin/server-status`、`/api/production/workshop-tasks`(+count)、`/api/expense-claims/mine`、`/counts`、`/api/sales/shipments?stage=SHIPPED`(新阶段筛选)、`/api/warehouse/sales-outbound`、`/api/master/warehouses`、`/api/system-test/business-data/last-result`、`/api/notices` 全部 200；OCR 识别探针经侧车返回业务错误(接线正常) |
| 后端日志 | 激活后无 ERROR/WARN |
| 数据库会话 | 均为 `uten` |

## 四、边界与后续

- tag `v1.1.0`(→ `6c6f2817`)与 `v1.2.0`(→ `3b319cb6`)尚未推送：tag 触发的 Simple Release 不允许 allow_running，而两个提交在 main 上的 Quality Gate 运行都被后续 main 推送按并发组取消。
  推 tag 前先 `gh run rerun <该 SHA 在 main 上的 Quality Gate run id>` 等其全绿(约 2 小时)，再 `git tag v1.2.0 3b319cb6 && git push origin v1.2.0`；tag 触发的重发布会用同一提交重建并覆盖 OSS 同版本，服务器 `check` 视 latest=active 为无操作。
- `ba5bd396`/`3b319cb6` 的 Flutter 与 backend-db 慢作业结果以 GitHub Actions 为准；若红按用户指示下版一起修。
- Dependabot 15 个依赖升级 PR 未合并(含 Spring Boot 3.5→4.1.1、jjwt 0.13、file_picker 8→11 等大版本)，需单独评估。
- 真实岗位 UAT 未做；本记录只证明部署、迁移与只读接口事实。
