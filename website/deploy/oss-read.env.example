# Non-secret coordinates only. Authentication must be an ECS website-specific
# read-only RAM role; never copy CI OIDC credentials or long-lived keys here.
WEBSITE_OSS_BUCKET=__WEBSITE_RELEASE_BUCKET__
WEBSITE_OSS_ENDPOINT=https://oss-cn-__REGION__.aliyuncs.com
