# 生产执行分段与报工页

## 2026-08-30 V427 详情卡片与多人报工合同

1. 已下达生产计划详情不再重复展示六列“明细”表。产品、计划量、有效报工、待补、品质、入库、物料、车间班组、负责人和日期统一进入执行子计划卡；草稿尚未形成执行段时只显示薄的计划产品摘要。底层 `production_plan_items`、`sourcePlanItemId` 和 `planItemId` 关系继续保留，不能因页面精简而删除或改用名称关联。
2. 执行子计划使用响应式卡片。合法的调整、解除暂缓、派工、确认开工和分批报工直接显示在卡面，详情仍供只读核对和深链自动打开。只有持开工权限、状态为 `DISPATCHED` 且仓库已全部发料的卡片可勾选；`POST /api/production/plans/{planId}/execution-segments/batch-start` 按 UUID 稳定锁序、先全量预检再写入，任一项失败时整批事务回滚。
3. 从执行卡点“分批报工”必须携带精确 `executionSegmentId`。读侧只返回一个权威销售分摊且仍可报时，日报直接应用来源并预填当前可报量；同一执行段有多个销售分摊时仍须人工确认，禁止默认第一条造成串单。
4. V427 `production_daily_report_workers` 保存一张生产日报的有序整单参与人员，首位同步到历史 `production_daily_reports.worker_id` 作为责任人兼容。候选只允许“制造与研发管理中心 / 生产部”组织树内未删除且在职或试用的员工；前端可多人确认并回显，服务端重复校验。
5. 整单参与人员只证明本次日报有哪些人共同参与，不分配每条明细或每个人的产量，不是计件工资、质量责任或绩效证据。未来计件仍须行级人员贡献、工序版本、合格实收分摊和独立薪资 ADR。
6. FQC 协议码仍为 `PASS/PARTIAL/FAIL`，执行子计划和日报用户界面把 `PASS` 展示为“通过”；不得修改持久化枚举或用中文展示值替代协议码。

当前源码目录为 V427/389。上述源码和聚焦测试不等于公司目标非空库迁移、真实车间多人报工、品质/仓库联动 UAT、恢复演练或部署回读；完成这些门禁前生产仍为 **NO-GO**。

## 2026-08-28 V409–V415 现行合同

1. `READY` 表示计划已按完整套料形成正式预留与 DRAW，不等于已经发料；DEMANDED 段须等仓库实际 `issue` 使全部需求 `FULFILLED` 后，才能从 `DISPATCHED` 开工到 `IN_PROGRESS`。
2. 生产日报填写的是“完工申报量”。审核只增加当前有效 `fqty` 并逐行登记 FQC，不直接生成库存或 `iqty`；新日报必须引用真实 `IN_PROGRESS` execution segment，V414 后不能再用无段日报绕过质量。
3. FQC 页面 `/quality/production-inspections` 按 inspection UUID 登记 PASS/PARTIAL/FAIL。PASS 数量才生成 FINISHED_IN 待点收草稿；FAIL 数量不入库，并回退当前有效贡献。
4. REWORK recovery lot 可在原在制返工后补报并再次 FQC。SCRAP/REJECT 会进入生产看板补产物料待办：计划员冻结 BOM、建立 exact demand/reservation 和单一 DRAW；仓库真实发料且需求全部履约后才开放补产报工。
5. 仓库从 `/warehouse/production-finished-in/tasks` 进入权威待点收队列，逐行登记实收。只有实收增加库存和 `iqty`；`完成率 = iqty / 计划量`。V418 下整单零实收保留 REJECTED 原单并生成全量同源重新交付任务，执行卡显示“仓库拒收·待重新交付”，不开放重复报工。
6. 执行卡必须分别显示普通剩余、FQC 待检/PASS/FAIL、返工或补产待报、待仓库点收和已入库。普通计划量尚未报满时优先保留普通报工入口，不能让较早 BLOCKED recovery lot 遮住可继续生产的数量。
7. 日报、FQC、补产确认和仓库点收均使用稳定幂等键、请求哈希、对象范围与状态门禁；相同键不同载荷冲突，通知与角标只触发刷新，不是业务事实。

本节为当前最高优先级；下方 2026-08-22/V191–V195 内容保留为历史演进说明。目标非空库、车间—品质—仓库实物 UAT、恢复和部署回读完成前生产仍为 **NO-GO**。

## 2026-08-22 历史快照：发料、报工与点收后置合同

- 执行卡把“已预留”与“已发料 X/Y”分开。READY 可派工，但 DEMANDED 段只有仓库把每个正式需求全部实际发料后才允许“开工”；ZERO_MATERIAL 沿冻结原因例外。
- 真实 DRAW 生成后通过 Outbox 投递仓库，仓库入口显示有权履约待办角标；全发和反出库分别通知计划/生产。通知与角标只帮助发现任务，不能代替 DRAW、`issue_status` 和不可变 posting。
- 报工按钮只对 `IN_PROGRESS` 段显示，服务端也只接受该状态；`DISPATCHED` 不再靠首次报工隐式开工。日报审核只增加 `fqty` 并生成 FINISHED_IN 待点收草稿。
- 生产来源 FINISHED_IN 必须由仓库逐行输入实收量。短收必填原因；正数行按实收入账，零收/短收余量自动形成同来源新草稿，整单零收才拒收；只有实收量增加库存和 `iqty`。
- 已点收 FINISHED_IN 禁止通用红冲；专用红冲反向库存/`iqty` 后，按原 accepted slice 重建待点收草稿并追加不可变反向映射。仓库动作还须满足动作权限与 `SUB_WH` 仓储组织范围，不能凭全局权限猜 UUID 跨部门处理。
- 日报只记录数量事实，单价/金额已从页面移除，服务端拒绝非空 `price/total/stotal`；计划/日报 worker、班组和负责人不是计件工资归属。未来计件见 [ADR-046](../99-决策记录-ADR/ADR-046-生产领料仓库点收报工归属与未来计件边界.md)。

本节描述 V337/V338 关联源码候选，不代表目标库、真实仓库/车间 UAT 或部署已完成；生产与计件仍为 **NO-GO**。

## 2026-08-11 当前工作台可见合同

生产调度「进行中」卡必须把计划/排产数量、完工申报/当前有效 `fqty`、仓库实收 `iqty`、FQC/待点收和正式 V1 计划包物料状态分开显示。展开入口当前文案为「子计划 N 张（点开展示进度）」；每条真实子计划至少显示「已报工 X」「已入库 X / 排产 X」和物料/质量下一步。采购/委外 IQC PASS 只能推动合格库存和物料齐套，不能直接增加这里的报工或成品入库。

子计划整行是进入该子计划详情的点击域，最小高度 56dp；展开/收起只由「子计划 N 张」区域处理，不能吞掉行点击。375px 等窄屏改成两行布局，第二行允许「已报工 / 已入库 / 排产 / 物料齐套」换行；在系统字体放大 1.3 倍时仍须无溢出且整行可点击。只读用户也必须得到详情或明确无权反馈。

计划向导里的车间只是本批安排建议。默认车间唯一读取 V192 `production_goods_workshop_preferences`；只有执行段正式确认或后续合法 `assign` 改派成功后才学习，打开、拖动、修改、取消向导或生成草稿都不学习且不回写历史。V253 是官网询盘迁移；当前全局 V419/381（生产核心 V409–V415、拒收修复 V418）仍没有引入第二份默认车间权威。

本节优先于下方 V191–V195 历史实现说明；源码/Widget 回归仍不替代目标库、真实登录账号和岗位 UAT。

<!-- PRODUCTION-PLANNING-V195-CURRENT -->
## 2026-08-02 当前执行与学习口径（V191–V195）

执行段的车间、班组、负责人和日期是持久化安排。V192 仅在正式下达或 `assign` 成功且同货品本次车间无歧义时学习未来默认车间；空值、多车间、失败事务和幂等回放不学习，用户始终可以覆盖建议，历史执行段不回写。

候选新数据的处置明确分为 `READY`（整套占用、可进入派工）、`AUTO_WAIT`（系统缺料、到料后允许自动齐套）和 `DEFERRED`（用户主动延期、禁止自动提升）。`release-defer` 只允许将已确认的 `DEFERRED` 单向人工放行：同事务解除延期并立即重做齐套，齐套转 `READY`，仍缺料转 `AUTO_WAIT`，不能用该动作把已放行段重新延期。

安排必须通过服务端组织校验：车间是 `DEPT_PROD` 直属子部门，班组直属所选车间，负责人为在职/试用且位于所选班组或车间路径内，计划开工不晚于计划完工。前端联动过滤和清空失效选择只改善体验，不能替代服务端及 V194 数据库守卫。

V194 将直接层 MAKE 子计划明细作为父需求的明确供给来源。已审核 `FINISHED_IN` 必须带真实上游子计划明细，按货品/颜色/单位/计划包/供给 peg 精确回供；系统缺料 WAITING 可在全套齐料后自动转 READY，用户主动延期 `USER_DEFER` 即使齐套也保持延期。入库红冲在父段未派工/领料时释放回供并回到 WAITING；已进入执行时 fail-closed，必须先走业务反向。

生产执行工卡从确认计划包和执行段只读生成，不在本页手工创建另一套数量或状态事实。V195 只刷新 V194 新增业务表的未来写入审计覆盖，不回填历史业务行或历史审计；目标环境仍为 V190，候选未部署且未完成真实岗位 UAT，因此生产发布仍为 **NO-GO**。

> **页面状态**：执行分段查看与状态动作已实现；生产发布仍为 **NO-GO**
> **主路由**：`/production/plans/:id`
> **执行分段深链**：`/production/plans/:id?executionSegmentId=:segmentId`
> **更新日期**：2026-08-02

---

## 一、页面职责

生产计划详情统一展示父计划事实、BOM/MRP 预览、执行分段和真实自制件子计划。页面不得把不同层级揉成一个列表：

- `production_plans` 通常是可独立审核和下钻的计划；但 `EXECUTION_V1` 派生子计划禁止通用删除/红冲，必须从父计划包走受控反向，不能拆断谱系；
- `subplan_links` 表达父计划与自制件子计划的谱系；
- `production_execution_segments` 表达同一计划明细按齐套状态、车间和日期拆出的执行部分；
- 执行分段的报工、领料、成品入库和结案都必须回到同一父计划及其数量权威。

## 二、加载与失败隔离

计划详情、MRP 物料需求、执行分段和子计划列表分别加载。MRP 预览失败不能吞掉子计划列表；子计划列表失败显示独立错误卡和重试；执行分段加载失败显示明确错误和重试。

当路由携带 `executionSegmentId` 时：

1. 先打开对应父计划；
2. 执行分段数据加载完成后定位目标分段；
3. 自动打开一次分段详情；
4. 目标不存在时停止重复尝试，不叠加多个对话框。

## 三、执行分段交互

点击任一执行分段必须打开自适应详情：

- 紧凑屏使用底部弹层；
- 中等和展开屏使用对话框；
- 查看者可看到分段编号、状态、计划/已报工/剩余数量、物料齐套、车间、班组、负责人和计划日期；
- `WAITING` 明确显示待料，不能伪装为可派工；
- `COMPLETED` 明确显示已完成，不能继续展示非法动作。

动作按“权限 + 当前状态”共同决定：

| 状态 | 允许动作示例 | 约束 |
|---|---|---|
| `READY` | 调整、派工 | 派工前需物料齐套及必要分配信息 |
| `WAITING`（`AUTO_WAIT`） | 调整 | 不允许派工或开工；到料后可自动重做齐套 |
| `WAITING`（`DEFERRED`） | 调整、人工放行 | 不允许派工或开工；只有 `release-defer` 可单向解除延期 |
| `DISPATCHED` | 开工 | 仍由服务端校验版本和状态 |
| `IN_PROGRESS` | 报工 | 跳转日报新建并携带 `executionSegmentId` |
| `COMPLETED/CANCELLED/REVERSED` | 查看 | 反向须走专用状态机，不在详情里直接改值 |

只读账号点击行也必须得到详情或明确无权提示，不能出现可点击样式却没有反馈。

## 四、真实子计划交互

详情底部“子任务 · 子计划”来自 `subplan_links`，每行显示子计划单号、交货日、入库/计划数量和状态。点击后打开子计划自己的详情页；空 ID、自引用或路由失败显示强提示。这里展示的是子计划自身事实，不证明其入库已按 supply peg 回供父需求；未闭环前不得把子计划入库直接计为父计划或销售履约完成。

执行分段的“详情”与真实子计划的“跳转”是两套行为。任何后续改版都必须用独立组件和测试守住该边界。

## 五、权限与数据安全

- 查看权限不能推导出编辑、派工或报工权限；
- 前端隐藏按钮只改善体验，服务端仍须校验权限、对象范围、锁版本和状态；
- 车间、班组、负责人和日期的组织层级约束由服务端强制执行，禁止仅靠下拉框过滤；
- 销售深链只能定位被授权查看的计划和分段，不得把成本、供应商报价、员工绩效或内部质量责任带回销售端；
- 日期字段兼容后端返回的日期对象或文本格式，但兼容转换不改变业务日期语义。

## 六、自动化证据与剩余验收

`test/features/production/widgets/production_execution_segments_card_test.dart` 已覆盖：

- 只读用户点击分段能打开详情；
- 按状态与权限只显示合法动作；
- 深链自动打开指定分段；
- 连续点击不会叠加多个详情弹层。

`test/features/sales/widgets/sales_plan_progress_sheet_test.dart` 覆盖销售侧携带分段 ID 的跨模块深链。相关回归已通过，但尚未使用真实登录账号完成浏览器人工 UAT；生产发布结论仍为 **NO-GO**。

## 七、关联文档

- [生产调度工作台](生产调度工作台.md)
- [销售订单生产进度](销售订单生产进度.md)
- [生产履约任务工作台](生产履约任务工作台.md)
- [生产订单排产与执行全链路需求](../07-业务链路/04-生产订单排产与执行全链路需求.md)
- [生产履约 V1 实体关系](../04-数据模型/生产履约V1实体关系.md)
- [2026-08-01 全链路安全复核报告](../99-项目治理/2026-08-01-销售仓库生产采购委外全链路安全复核报告.md)
