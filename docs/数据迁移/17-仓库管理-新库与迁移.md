# 17 - 仓库管理模块 · 新库设计与迁移方案

> **2026-08-31 V446 IQC 两阶段后置覆盖**：下文“PASS 自动/立即 `DIR_IN`、刷新分析或推进正式供给”
> 只代表 **V445 及以前历史实现**，不得作为现行操作、测试或发布依据。现行流程由品质 PASS 只生成
> `/warehouse/iqc-stock-ins` 待入库任务；仓库核对实际量与实际库位并可分批确认，确认后才写库存、
> 分析归属、reservation、DRAW 与 READY。品质终态和仓库入库完成分别对账；红冲只反向已实际入库切片。见
> [采购 / 委外 IQC 合格待入库任务页](../03-页面/采购委外IQC合格待入库任务页.md)。
>
> **2026-08-28 V419 到货登记创建幂等后置口径**：`POST /api/warehouse/inbound/arrivals` 必须携带页面动作生命周期内稳定的 `idempotencyKey`。服务端按当前登录制单人 + key 串行化，计算不含 key/服务端单号的规范请求 SHA-256；同 key 同 hash 精确回放原收货单/异常结果，同 key 异 hash 409。`warehouse_arrival_registration_commands` 新表先写 PENDING，只允许同事务一次转 COMPLETED/QUARANTINED；actor/key/hash、采购或委外 receipt UUID、原响应与终态不可改删，表有显式 `fn_audit` 触发器。迁移不回填或推断既有收货单；目标非空库、断网重试和真实仓库 UAT 前仍 **NO-GO**。

> **2026-08-28 IQC 正式供给后置口径**：每次采购/委外 PASS 都在处置事务按“累计 `passed_base_qty` − 既有 `EFFECTIVE` 收货分配”推进正式供给、reservation、DRAW 与 READY 判定；同一 receipt 其它待检行不阻塞，幂等重放不重复。整单终态只追加一次结案事件并做幂等最终对账。PostgreSQL 场景 4/4 已覆盖 PARTIAL PASS、另一行 PENDING、重放、后续结案与完整红冲。

> **2026-08-14 V279 编号后置口径**：调拨/其它入/其它出/领料/退料/成品出/成品入/盘点/损耗固定前缀为 `CB/QR/QC/SL/ST/CC/CR/PQ/QW`，新单号由数据库按上海业务日生成 `PREFIXYYYYMMDDNNNNNN`。客户端不传或拼接号码；制单人、仓库、车间、来源命令与业务单据关系只认 UUID。旧号原样保留并登记占用/冲突，历史 `source_doc_no` 只作展示和对账。

> 设计依据：[16-仓库管理-老库溯源.md](16-仓库管理-老库溯源.md)
> 设计原则（用户钦定并再次强调）：**比老系统更优秀、更高效、海量数据处理更快** · **适配老数据** · **底层数据库重新设计**（不照搬）。
> 模块归属：**PMC 运营部 → 仓库管理**（9 单据）+ **仓库管理报表**（9×2）。
> 迁移代码带幂等/注释，但当前 Shell 入口会清理并重建模块，只能用于首次引导或演练；
> 切流后的新数据不得“直接重跑”，后续增量必须使用独立 checkpoint/reject/对账流程（见 §八和
> [迁移总 README](README.md)）。
>
> **2026-07-30 状态校准**：以下“实测”是历史开发库/抽样证据，不是生产同构验收。仓库模块仍须
> 完成全新库 Flyway、正式快照对账、并发库存不变量、权限/E2E、容量和备份恢复门禁。
>
> **2026-08-09 历史迁移快照**：当日公司目标库已应用到 V238，V191–V195、V196–V202 与 V234 均已迁入；当时源码到 V246、隔离克隆验证到 V244。当前状态以紧随其后的 2026-08-10 后置口径为准；迁移成功不等于生产上线。
> **采购/委外入库覆盖（V196–V202 已迁入）**：财务批准订货后才生成仓库预计到货；仓库负责实物收货录入与最终审核，不负责决定商业超量。发现超量时收货保持草稿并返回 409；共享任务需 `finance_order_approval:view`，全批/自定义额外批准需 `:approve`，不批需 `:reject`，且动作叠加实时审核资格。接受量必须回到仓库再审后才写库存/AP/订单累计。仓库的预计到货和异常任务只读，不能通过页面或直接 API 越过财务。
> **2026-08-11 IQC 历史口径（已由上方 2026-08-28 段取代）**：该时点曾要求整张 receipt 终态后才推进正式供给；只用于解释旧测试和迁移快照，不得作为现行操作或验收规则。
> **2026-08-14 UUID/历史后置口径**：V262 候选为统一仓库明细冻结货品编号/名称，历史报表读快照；V274 候选确认 `B_Storage.WorkID` 命中 `Sys_Operator.ID` 而不是车间，故旧 `workshop_legacy_id` 只保留操作员溯源，当前所属车间只写 `workshop_department_id -> departments.id`，历史车间只接受按仓库 legacy ID 人工复核的 crosswalk；V275 候选把授权余额调整的幂等/权限关系移到 `stock_balance_adjustment_requests.stock_document_id`，`source_doc_no` 降为显示快照。不得按名称、旧 WorkID 或单号前缀恢复运行时关系。
> **2026-08-14 仓库明细单位收口**：新建或编辑仓库单时，未显式选择单位只允许从 `goods.unit_id` 补入基本单位 UUID；货品只有 `unit_legacy_id`、当前单位悬空或已删除时直接拒绝保存，不再运行时按 `units.legacy_id` 反查。历史报表为展示旧记录保留的 UUID-first fallback 不因此改写历史关系；正式切流前须在副本完成 legacy-only 货品清单、受控回填和对账。

---

## 一、关键洞察（决定全局设计）

1. **库存流水骨架已就绪**：V45 `stock_movements.movement_type` 当初就为仓库管理预留了 5–12 号位（5生产领料 6生产退料 7调拨入 8调拨出 9盘盈入 10盘亏出 11其它入 12其它出）。采购已用 1/2。**9 类仓库单据的"库存效果"全部能由流水表达**，无需新账本。
2. **9 类单据高度同构**：主表（BillNo/BillDate/StockID/MakeID/ApproverID/Status/Cancel）+ 明细（GoodsID/ColorID/UnitID/URate/QTY/STotal/Weight）几乎一致，差异只在少数列（调拨双仓、盘点盘盈亏、领料挂BOM/计划）。
3. **老库的臃肿**：9 套同构表（18 张）+ 18 张报表视图 + 按月分列台账（StockGoods 88 列）—— 重复、僵化、慢。

**结论**：新库**不照搬 9 套表**，采用「**统一单据表 + `doc_type` 判别 + 流水驱动库存**」的重新设计。9 张表 → 2 张表，18 张报表视图 → 2 个参数化查询。

---

## 二、设计决策总览

| # | 决策点 | 选型 | 理由（更优秀/更快） |
|---|---|---|---|
| 1 | 单据存储 | **统一 `stock_documents` + `stock_document_items` + `doc_type`** | 9 类同构，合并省 16 张表；单一索引/计划/审核路径；维护与扩展（未来加单据类型=加枚举值）零成本 |
| 2 | 库存驱动 | **审核同事务写 `stock_movements` + upsert `stock_balances`**（V45 已建） | 流水+余额模型，取代老库按月分列台账；任意时间范围/跨年/多类型，余额实时 O(1) |
| 3 | 调拨特殊 | **一笔调拨 = 两条流水**（调出仓 direction=-1 type8 / 调入仓 direction=+1 type7） | 调拨是"双仓双动"，流水天然表达；余额两仓一减一增 |
| 4 | 盘点特殊 | **按行算 `surplus = NowQTY − 账面`**：>0 写盘盈入(+1 type9)，<0 写盘亏出(-1 type10)；授权余额直接调整也复用该模型，并以结构化来源区分 | 盘点是差额调整，非全量出入；不允许裸改余额 |
| 5 | 报表 | **单据报表查统一头/明细；库存跨单据汇总查流水与月度物化视图** | 保留单据语义和人员字段，同时避免重复建设 18 套视图 |
| 6 | 物理性能 | **当前两表均为普通表，依靠日期/类型/仓库/货品索引与月度物化视图** | 现有数据量索引可承载；达到容量阈值后再用后续迁移分区，不把规划写成现状 |
| 7 | 主键/审计 | UUID 主键 + 非唯一 `legacy_id` 溯源 + 审计软删 | 各老表 IDENTITY 会跨表重复，须结合 `doc_type` 才能定位来源 |
| 8 | 人员字段 | **worker 走 B_Worker→employees UUID；maker/approver 走 Sys_Operator 姓名快照** | 两套 legacy 整数命名空间会撞号，不能统一匹配 `employees.legacy_id`；详见 §六/§七 |
| 9 | 库位 | 首版 `place TEXT`（仓内文本），不建独立库位表 | `B_StoragePlace` 仅 7 行，独立表过重；后置按需 |
| 10 | 金额双口径 | `*_original`/`*_local`（仓库单据多为本币，双口径留位与采购一致） | 统一范式 |

> 与采购的关系：采购用 4 套独立表（4 类单据差异大：申请无供应商、收货有交货人…），仓库用统一表（9 类同构）。**两者共享同一 `stock_movements` 骨架**——这是全链路打通的枢纽。

---

## 三、物理结构摘要（以 Flyway 为准）

> 本节只记录稳定的结构决策，不再复制一份容易漂移的完整 DDL。当前物理结构以
> [`V45__stock.sql`](../../server/src/main/resources/db/migration/V45__stock.sql)、
> [`V48__stock_documents.sql`](../../server/src/main/resources/db/migration/V48__stock_documents.sql)
> 及后续增量迁移为唯一权威；已执行迁移不得回改。

### 3.1 统一单据头 `stock_documents`

- `id UUID` 单主键；`legacy_id INT` 只用于老库溯源，**不唯一**，因为不同 `O_*` 表的 ID 会重复；
- `UNIQUE (doc_type, bill_no)` 保证同类单号唯一；`doc_type` 区分 9 类仓库单据；
- 仓库、调入仓、经手人、制单人、审核人、业务日期、状态、备注和金额字段统一放在单头；
- V67 增加 `worker_legacy_id` / `maker_legacy_id` / `approver_legacy_id` 和 DRAW 专用 `ass_team`；
  V182 在不回改 V67 的前提下纠正字段语义，并增加 `maker_name_snapshot` /
  `approver_name_snapshot`；
- `source_doc_no` 承载软关联。V167 规定服务端生成的
  `AUTHORIZED_BALANCE_ADJUSTMENT:<idempotencyKey>` 为授权余额调整来源，并以部分唯一索引去重；
- 常用索引覆盖 `doc_type + bill_date`、仓库、调入仓、状态和 `legacy_id`。

### 3.2 统一明细 `stock_document_items`

- 当前是**普通表，不是按年分区表**；`id UUID PRIMARY KEY`，`doc_id` 外键关联单头；
- `bill_type` / `bill_no` / `bill_date` 冗余用于报表与未来分区，不代表当前已经分区；
- 通用字段包括货品、颜色、单位、换算率、数量、重量、金额、库位、上游明细和来源单号；
- `CHECK` 使用 `qty` 保存账面快照、`count_qty` 保存实盘/目标数量、`surplus_qty` 保存差额；
- 索引覆盖单据、货品、类型+日期、老库 ID 和上游明细。

### 3.3 流水 `stock_movements` 与余额 `stock_balances`

V45 建立两张普通表：

- `stock_movements`：UUID 主键，按交易日期、货品、仓库、来源和类型建索引；
- `stock_balances`：按“仓库 + 货品 + 颜色”使用 `UNIQUE NULLS NOT DISTINCT` 保证唯一余额行；
- V80 为余额增加重量，但 `CHECK` 盘点和授权余额调整仍只修正数量；
- 仓库单据流水的 `source_doc_type=STOCK_DOC`，`source_doc_id` 指向单头，`source_item_id` 指向明细；
- 类型 9 为盘盈、10 为盘亏；授权余额调整复用 9/10，不新增账本类型。

当前没有把 `stock_document_items` 或 `stock_movements` 改成 RANGE 分区。62 万级历史量先由索引和
`stock_monthly_mv` 承载；只有容量验证显示必要时，才通过**新的** Flyway 迁移设计分区或 pg_partman。

### 3.4 月度物化视图 `stock_monthly_mv`

V49 按月份、流水类型、货品、仓库和颜色汇总数量与金额，用于跨单据库存汇总。单据明细/汇总报表
仍查 `stock_documents` / `stock_document_items`，因为人员、装配班组和订单号等业务字段不在流水表中。

---
## 四、状态机与库存联动（Service 层，去触发器）

```
录入 status=0(草稿) / 1(已审，贴老库"保存即生效")
审核 0→1：同事务内，按 doc_type 生成 stock_movements（direction/type 见 §二决策3/4）+ upsert stock_balances
红冲 1→-1：同事务追加反向流水并回退余额；原单和原流水不删除。跨期日期规则见 [盘点修正文档](50-仓库盘点修正与历史单据处理.md#45-红冲)
```

**doc_type → 流水规则（单一参数化 Service 路径）：**

| doc_type | movement_type | direction | 说明 |
|---|---|---|---|
| OTHER_IN | 11 | +1 | 其它入 |
| OTHER_OUT | 12 | -1 | 其它出 |
| DRAW（领料）| 5 | -1 | 生产领料出 |
| WDRAW（退料）| 6 | +1 | 生产退料入 |
| FINISHED_IN | 13 | +1 | 产成品进仓 |
| FINISHED_OUT | 14 | -1 | 产成品出仓 |
| TRANSFER（调拨）| 8 调出仓 -1 **&** 7 调入仓 +1 | 双流水 | 双仓双动 |
| CHECK（盘点）| 每行 surplus>0→9(+1) / <0→10(-1) | 差额 | 盘盈/盘亏 |
| WASTE（损耗）| 12 | -1 | 表空，结构留位 |

> 一个 `applyStockEffect(doc)` 方法覆盖全部 9 类——这正是统一表 vs 9 套表的效率差。

---

## 五、报表设计（7 单据类型 × 明细/汇总 = 14 张，已实现）

老库 18 张 `View_O_*` 视图 → 新库**直接查统一表 `stock_documents`/`stock_document_items`**（镜像采购/销售 `ReportTableResponse` 范式，非查流水——因明细=逐行货品需单据头字段如人员/装配班组/订单号，流水没有）。

- **明细报表**：`FROM stock_document_items i JOIN stock_documents o`，`WHERE i.bill_type=:docType`，一行=单里一样货品（同单号重复），JOIN goods/colors/units/warehouses/clients；worker 按 UUID 优先、UUID 为空才回退 B_Worker legacy；maker/approver 只按当前 UUID JOIN employees，历史 UUID 为空则显示 Sys_Operator 姓名快照。
- **汇总报表**：`FROM stock_documents o WHERE o.doc_type=:docType`，一行=一整张单（单号唯一）。
- **库存查询/余额**：直接查 `stock_balances`；流水查 `stock_movements`。余额详情与授权调整规则见 [库存余额页](../03-页面/库存列表页.md) 和 [盘点修正文档](50-仓库盘点修正与历史单据处理.md)。
- **月度上卷**：`stock_monthly_mv`（V49）保留供跨单据汇总视角，迁移末尾 `REFRESH`（防汇总空）。

**14 张报表**（跳过 `OTHER_OUT` 其它出库——用户指定删、数据仍入库存；`WASTE` 生产损耗——老库 0 行）：

| doc_type | 明细列（节选） | 汇总列 |
|---|---|---|
| TRANSFER 调拨 | 单号 日期 调出仓 调入仓 经办人 是否审核 编号 型号 客户型号 货品名称 规格 颜色 单位 重量 数量 | 单号 日期 调出仓 调入仓 经办人 制单员 审核员 |
| OTHER_IN 其它入库 | 单号 日期 仓库 经办人 是否审核 系列 编号 型号 客户型号 货品名称 | 单号 日期 仓库 经办人 审核员 制单员 备注 |
| DRAW 生产领料 | 单号 日期 客户名称 装配班组 订单号 领料人 制单员 审核员 是否审核 编号 型号 客户型号 货品名称 颜色 重量 领料数量 实发数量 | 单号 日期 仓库 装配班组 客户名称 订单号 领料人 |
| WDRAW 生产退料 | 单号 日期 仓库 退料人 是否审核 系列 编号 型号 客户型号 货品名称 规格 清退数量 | 单号 日期 仓库 退料人 制单员 审核员 备注 |
| FINISHED_IN 产成品进仓 | 单号 日期 仓库 跟单员 是否审核 客户名称 系列 编号 型号 客户型号 货品名称 规格 颜色 材质 净重 | 单号 日期 仓库 跟单员 制单员 审核员 备注 |
| FINISHED_OUT 产成品出仓 | 单号 日期 仓库 跟单员 是否审核 系列 编号 型号 货品名称 规格 颜色 数量 | 单号 日期 仓库 跟单员 制单员 审核员 备注 |
| CHECK 盘点 | 单号 日期 仓库 跟单员 是否审核 编号 型号 客户型号 货品名称 规格 颜色 帐面数量 帐面重量 实际数量 实际重量 | 单号 日期 仓库 跟单员 制单员 审核员 备注 |

> **入口（销售风格）**：仓库管理 hub 内两张卡「仓库明细报表」「仓库汇总报表」，点进页面用 ChoiceChip 切 7 单据类型；左筛选（日期+搜索+查询）右 Excel 表（表头每列 autofilter + 横滚 + 翻页）。工作台 PMC 运营部**不再**单列「仓库报表」入口（收归仓库管理 hub 内，避免重复）。

---

## 六、老库 → 新库 字段映射（迁移口径）

### 主表（以 `O_OtherOut` → `stock_documents(doc_type='OTHER_OUT')` 为例）

| 老库 | 新库 | 映射 |
|---|---|---|
| ID | legacy_id | 直填 |
| BillNo | bill_no | 直填 |
| BillDate | bill_date | cast date |
| StockID | warehouse_id | `warehouses.legacy_id` 映射 |
| OStockID/IStockID (Transfer) | warehouse_id / to_warehouse_id | 双仓 |
| WorkerID/GetID/ReturnID/CheckID | `worker_legacy_id` + `worker_id` | B_Worker.ID；按 `employees.legacy_id` 精确写员工 UUID |
| MakeID | `maker_legacy_id` + `maker_name_snapshot` | Sys_Operator.ID；按同表 ID 冻结 `fname`，不回填 employees UUID |
| ApproverID | `approver_legacy_id` + `approver_name_snapshot` | Sys_Operator.ID；按同表 ID 冻结 `fname`，不回填 employees UUID |
| O_PDraw.GetID / O_WDraw.ReturnID / O_Check.CheckID | worker_legacy_id | DRAW 领料人/WDRAW 退料人/CHECK 盘点人（这三类老库无 WorkerID，按类型取对应人字段） |
| O_PDraw.AssTeam | ass_team | 装配班组（文本，仅 DRAW） |
| Status | status | 1→1, -1→-1 |
| Total | total_original/local | 直填 |

### 6.1 人员命名空间实测纠偏

对 8 个仓库主表 CSV 共 55,470 行逐项核对后：

- maker 非零引用 48,853/48,853 命中 `legacy_operators_ref`，0 命中 B_Worker；
- approver 非零引用 55,414/55,414 命中 `legacy_operators_ref`，0 命中 B_Worker；
- worker 非零引用 42/42 命中 B_Worker。

因此 V67 中“maker/approver→B_Worker”的旧注释属于已应用迁移里的历史错误，不能回改该文件或
Flyway checksum。V182 只通过后续增量列、列注释、迁移脚本和报表读法纠偏：历史 operator 保留
legacy ID + 姓名快照；新系统创建的 maker/approver 若有当前员工 UUID，报表优先显示当前员工名。

### 明细（→ `stock_document_items`）

| 老库 | 新库 |
|---|---|
| GoodsID/ColorID/UnitID | 经主档 legacy_id 映射 |
| URate | unit_rate |
| QTY | qty；base_qty = qty×unit_rate |
| STotal/Total | amount_original/local |
| Weight | weight |
| StorgePlace | place（文本，存库位 legacy id 或名） |
| NowQTY/SurplusQTY（盘点）| count_qty / surplus_qty |
| PDrawID（退料→领料）| upstream_item_id |
| SOrderNo/BomItemID/...（多值）| source_doc_no（合并占位文本） |

---

## 七、迁移执行（复用采购迁移范式 · 仅干净目标库可重跑）

> “可重跑”仅指同一离线快照在可清空的新库/演练库重复执行能得到相同结果。当前脚本会清理
> 库存流水、单据和余额，不能在切流库上导入未来新数据/新账套。上线后的多轮追平必须另做
> 增量 loader、checkpoint、reject、对账和回滚。

**新增文件（仿 `migrate_purchase.sql` / `export_legacy.ps1`）：**

1. **导出** `export_legacy.ps1` 增 9 段：8 类 `O_*` 主+明（`O_Waste` 空，结构留位）+ `StockGoods`（台账，迁余额用）。输出 `data/*.csv`（含真实数据，**不入 git**，已 .gitignore）。
2. **DDL** 新 Flyway：`V48__stock_documents.sql`（统一单据+明细+分区+`stock_*:view|edit` 权限 seed）+ `V49__stock_report_mv.sql`（`stock_monthly_mv` + 刷新函数）。
3. **灌数** `migrate_stock_docs.sql`（幂等）：
   - 开头清三处：`stock_movements`(STOCK_DOC 源) + `stock_document_items` + `stock_documents` + `stock_balances`（余额从 StockGoods 全量重建）；只对可清空目标库重跑安全。
   - 统一 staging（主/明细各一张 TEMP + doc_type 列）→ 8 类 `\copy` accumulate → 各一条 INSERT（JOIN 用 (legacy_id, doc_type) 消歧，因各 O_ 表 IDENTITY 跨表重复）。
   - **缺失基础资料自动补录**（goods/units/colors/warehouses，同采购 §3.3）。其中 goods stub 只承载
     历史仓库明细外键，必须 `auto_created=true`；货品选择、活动 BOM/MRP 一律排除，见 [31](31-货品组装BOM与成本预算.md)。
   - **B_Worker 员工关系**：从 `legacy_workers.csv` 建 `worker_stage`，缺失人员建立隔离 stub；单据同时写 `worker_legacy_id` 与精确映射的 `worker_id`。
   - **Sys_Operator 快照**：从 `legacy_operators_ref.csv` 建 `operator_ref_stage`，按 MakeID/ApproverID 精确写 `maker_name_snapshot` / `approver_name_snapshot`；保留原 `*_legacy_id`，不建员工 stub。
   - **台账余额迁移**：从 `StockGoods` 按 `StockID+GoodsID+ColorID` 聚合 `QTY` → 灌 `stock_balances`（期初余额）。采购迁移未做，本模块补上——全链路打通。
   - **流水回填**：已审单据按 doc_type→movement_type 写 `stock_movements`（调拨双流水、盘点按盘盈亏）。
   - 末尾 `REFRESH MATERIALIZED VIEW stock_monthly_mv`（防月度汇总报表空，同 sales/subcontract 修复）。
   - 结尾校验：主/明细数、worker UUID 覆盖、maker/approver 快照覆盖、装配班组数、孤儿检查。
4. **入口**：当前有效目标为 `--stock-docs`；执行时必须追加 `--confirm-destructive`。
   - 必须先用同一次 `export_legacy.ps1 WarehouseDocs` 生成 8 类单据、StockGoods、B_Worker 和 `legacy_operators_ref.csv`；
   - `export_manifest.json`、`export_manifest.sha256` 缺失、不是同次导出，或 operator CSV 未进入清单时，preflight / `copy_csv` 必须拒绝执行；不得跳过 manifest。

**执行边界**：`migrate.sh` 一次只允许一个目标，不能传 `--purchase --stock-docs` 多目标命令。
采购与仓库依赖需分两次执行；该脚本会重建目标模块，只用于首次导入/演练，不能作为切流后的增量同步：

```bash
bash server/legacy_migration/migrate.sh --purchase --confirm-destructive
bash server/legacy_migration/migrate.sh --stock-docs --confirm-destructive
```

---

## 八、复用性设计（未来直接迁新数据）

- **引导迁移幂等边界**：`legacy_id` 只做溯源且跨 `doc_type` 可重复；当前 bootstrap 依赖显式确认后的同链表清理重建和补录去重。切流后不得重跑破坏性脚本，增量必须使用 checkpoint、复合来源键、reject 和对账。
- **统一表的红利**：新增单据类型（如未来"组装单"）= 加 `doc_type` 枚举值 + 一段导出/灌数 + Service 的 `doc_type→流水规则` 映射加一行，**不改表结构、不改报表**。
- **注释规范**：每段 SQL 标注「老库源表 → 新库目标 + 字段映射 + 重跑注意」，未来维护者/我自己直接照搬。
- **校验段**固定输出：各类单据主/明细数、链路挂接数、孤儿数——迁完一眼验完整性。

---

## 九、风险与待确认

| 风险 | 影响 | 对策 |
|---|---|---|
| **统一表 vs 9 套表**（本设计选统一） | 影响整个模块范式 | ✅ 已决策：统一（更优秀/更快/更易扩展）；与采购"独立表"不冲突，共库存骨架 |
| 人员 legacy 整数命名空间碰撞 | 错把 Sys_Operator 制单/审核账号映射成同号 B_Worker 员工，历史人名串人 | V182 只回填 worker UUID；maker/approver 保留 operator ID + 姓名快照，报表禁止 operator→employee legacy JOIN |
| `stock_document_items` / `stock_movements` 当前均未分区 | 数据继续增长后范围查询可能变慢 | 先做容量与执行计划验收；达到阈值后用新迁移改 RANGE 分区或 pg_partman |
| 调拨双仓 | 流水/余额两仓联动 | 一笔调拨双流水（§二决策3），Service 保证两仓一减一增同事务 |
| 盘点金额 | 盘盈盘亏无可靠单价 | 当前只调整数量，不写 CHECK 差额金额、不改标准成本或余额金额字段；V421 后即时库存展示 `stock_balances.amount_local` 台账事实，不再用标准成本×数量推导。数量与金额差异进入价值核对，详见文档 50 |
| `O_Waste` 空 | 损耗单无数据 | 跳过灌数，结构/枚举留位 |
| 库位 `StorgePlace` | 新库无库位主档 | `place TEXT` 存名/id，后置按需建表 |

---

## 十、实施步骤（Task 分解）

1. ✅ 老库调研（[16]）
2. ✅ 新库设计（本文档，统一表方案）
3. ✅ DDL：`V48__stock_documents.sql`（统一单据+明细+`stock_doc:view/edit` 权限）+ `V49__stock_report_mv.sql`（stock_monthly_mv + 刷新函数）
4. ✅ 导出脚本：`export_legacy.ps1 WarehouseDocs`（8 单据主/明 + StockGoods + B_Worker/Sys_Operator 参考；全部进入同次 manifest）。
5. 🟡 迁移 SQL：既有主/明细、余额、流水实测计数保留；V182 新增 worker UUID 双写和 maker/approver Sys_Operator 姓名快照，尚待隔离库全量重导并重新对账。
6. ✅ 后端：`features/stock/` StockDocument/Item + repos + StockDocService（统一 CRUD + 审核状态机 + `applyStockEffect(doc_type→流水)` + assTeam 端到端）+ StockDocController `/api/stock/docs`。**smoke 通过**（approve 余额-5+1流水，reverse 归 0）。
7. ✅ 前端：`lib/features/warehouse/`（hub 8 tile + list/detail/edit 配置驱动 + 装配班组录入[DRAW]，复用采购主从表模式 + goods picker + 名称解析），挂 PMC 工作台「仓库管理」。
8. ✅ 库存查询与修正：`StockQueryController` 提供余额/流水查询；V167 增加独立 `stock:balance:adjust`、幂等索引和 `/api/stock/balances/adjust`，前端余额详情自动生成 `CHECK` 并可从流水回查。规则见 [文档 50](50-仓库盘点修正与历史单据处理.md)。
9. 🟡 **仓库报表（V67 + V182 + 14 张）**：worker 保留 UUID 优先/B_Worker legacy fallback；maker/approver 已改为当前 UUID 优先、历史 Sys_Operator 快照 fallback，待重导后做 14 张报表抽样。
10. 🟡 V196/V201 已迁入公司目标库，把采购/委外财务已批订单投影到仓库预计到货，并在收货审核时强制来源与超量门禁；V222/current service 已实现 PASS/FAIL 待检隔离、合格量入库、反向和分析唤醒的自动化契约。真实仓库操作、特采、供应商退回实物 UAT 与历史对账尚未完成。

---

## 十一、预计到货与超量到货（V196–V202 已迁入，生产待验收）

- `GET /api/warehouse/inbound/expectations`、`GET /api/warehouse/inbound/expectations/count` 只投影财务已批采购/委外订单；`GET /api/warehouse/inbound/arrival-exceptions`、`GET /api/warehouse/inbound/arrival-exceptions/count` 只展示待处理异常，均要求 `warehouse_inbound:view`，都不是库存、IQC 或应付事实。
- 仓库仍在采购收货/委外进仓单中录入实物数量并执行最终审核。审核重新锁订单行、校验来源和批准余量；超量时持久化 `PENDING_FINANCE` 异常/Outbox 后稳定返回 `ARRIVAL_EXCEPTION_PENDING` 409，收货保持草稿，库存/AP/订单累计不变。
- 商业超量决定只在财务端执行；仓库异常列表没有决定按钮。接受量大于 0 时服务端收窄草稿，仓库必须再审；接受量为 0 时该草稿行被移除，无需再审。未批准量由精确退货任务 owner 负责退供应商。
- 标准 10→100 场景：财务已批 10 吨、草稿申报 100 吨，超量 90。全批接受/退回=`100/0`；自定义额外批准 5 吨=`15/85`；不批超量=`10/90`。财务决定不立应付、不动库存。
- V201 的追加额度绑定具体异常、收货单和订单行，另一张收货单不能借用。V202 再扫描全部 `public` 业务表并 fail-closed 校验审计触发器，只保护应用后的未来写入，不补历史。
- 收货审核不再直接等于生产可用：每个收货行先形成 V222 inspection 冻结事实，质检只能按剩余基本量执行 PASS/FAIL。PASS 数量写 `DIR_IN`，FAIL 不写库存；部分合格按合格比例转换生产供给，整批 FAIL 关闭原在途并释放计划投影，允许按真实短缺重新通知替代需求。
- 同一收货单的处置须按 receipt 级稳定锁序串行。每个 PASS 事件在库存入账后立即按累计 `passed_base_qty` 扣除既有 `EFFECTIVE` 收货分配的差额推进正式供给、reservation、DRAW 与 READY 判定；同单其它行待检不阻塞，幂等重放不得重复分配。整单 IQC 终态只触发一次结案事件和一次幂等最终对账，不能再承担首笔正式推进。收货红冲仍须先全部质检结案，并只反向已 PASS 的库存及可安全回退的生产供给，保留检查与事件历史。

---

## 十二、货架库位（目视化清单，2026-08-17 源码候选）

- **口径**：现场挂牌的「库行-层-位」库位号（如 A31-3-1、A39-1-5）只存在于打印挂牌，
  老库 `B_Goods.StockPlace` 仅 151 行且为 `'18'/'20'` 等历史残值，**两者无关，不从老库导出**。
  新库权威字段 = `goods.stock_place`（V32 已有列，货品资料详情可维护）。
- **迁移入口**：仓库部门按挂牌整理 `server/legacy_migration/data/shelf_labels.csv`
  （`|` 分隔 UTF-8：`place|goods_code|goods_legacy_id`，样例见 `shelf_labels.example.csv`），
  执行 `migrate.sh --shelf-labels --confirm-destructive` → `migrate_shelf_labels.sql` 按
  legacy_id 优先、物料编码其次回填 `goods.stock_place`；人工 CSV 不在老库导出 manifest，
  sha256 现算直接登记 `legacy_migration_run_files`；未匹配/重复行不静默（SELECT 对账输出 +
  DO 块拒绝空锚/重复货品）。幂等可重跑；空 place 行 = 显式清除该货品库位。
  依赖 `--goods-data` 先迁；不进 `--bootstrap-all`（属挂牌数据收集后的增补，而非老库快照回放）。
- **种子数据（2026-08-17）**：`data/shelf_labels.csv` 已按现场挂牌照片逐格核对收录 28 行
  （A31 库行 23 行 + A39 库行 5 行）。本地库（未跑全量货品迁移）导入 0/28 匹配属预期；
  目标库 `--goods-data` 完成后重跑本目标即可落位。
- **老库残值治理（2026-09-10 脚本落地）**：货品迁移会把老库 `StockPlace` 残值
  （`'18'/'19'/'20'/'Y12'` 等）原样带入 `goods.stock_place`。
  **展示侧已收口**：清单页按 `ShelfPlaceParser` 三段格式判定，残值一律 `parsed=false` 归
  「未分层」桶，不再冒充库行进分段/下拉（见 [货架目视化清单页](../03-页面/货架目视化清单页.md) §六）。
  **数据侧治理脚本**：`server/legacy_migration/clean_shelf_place_residue.sql`
  ——**人工确认脚本，不是 Flyway 迁移，也不进 `migrate.sh --bootstrap-all`**。
  两步用法：① 直接 `psql -f` 演练（建临时表 + 输出「残值明细 / 残值汇总 / 治理前计数」三段对账，
  末尾自动 `ROLLBACK`，不改数）；② 仓库逐条确认清单里没有真实非标库位（如 `Y12` 是否实物存在）后，
  加 `-v confirm=1` 重跑才执行 `UPDATE goods SET stock_place = NULL` 并输出治理后计数。
  执行前先 `pg_dump goods` 留档；`audit_log` 触发器会记录每行 `stock_place` 变更
  （`AuditEventInterpreter` 已把该列译为「库位号」），**可追溯但不可自动回滚**。
  某个残值确为真实库位时，先在货品资料改成三段格式（如 `Y12` → `A31-3-1`）再跑脚本。
  正式库尚未执行（待仓库对账）。
- **验证记录（2026-08-17，本地库）**：V296 经 Docker 内 JDK21 构建 + Spring Boot 启动由
  Flyway 实际应用（`now at version v296`）；SUB_WH 有效权限 14 项关键码提交态核对全绿；
  新端点已在 api-docs 注册、无 token 返回 401。本地库无仓储部账号，交互级 UAT 未做。
- **展示/打印/导出**：仓库管理 hub「库存查询」分区的「货架目视化清单」页
  （`/warehouse/shelf-labels`，`stock:view`）。**2026-09-10 信息架构重做**：
  `GET /api/stock/shelf-labels` 下发 `level/slot/parsed/qty/unitName/disabled`
  （库位号按「库行-层-位」三段解析，残值 `parsed=false`），新增
  `GET /api/stock/shelf-labels/layout` 给货架图网格维度，`/racks` 只回已分层库行；
  页面 = 货架图（`UtenRackGrid`，点格定位）+ 统一表格（`MasterDataTableView`，点行反查）
  + `UtenFilterToolbar`（库行分段 / 搜索 / 仓库真过滤 / 显示已禁用货品 / 未分层 chip）。
  预览打印仍复刻挂牌版式（UTEN 头 + 库行徽章 + 斑马纹 + 末尾留白行，每库行独立 A4 横版页）；
  Excel 走 `POST /api/stock/reports/export` report=`shelf-labels`（`stock_report:export`，
  已加「层/位/即时库存/单位」列）。详见 [货架目视化清单页](../03-页面/货架目视化清单页.md)。
- **报表增量**：7 类明细报表统一加「库位号」列（`g.stock_place`），拣货/上架对位可见。
- **单据联动增量（2026-08-17 第 2 批）**：货品列表 API `GoodsListItem` 增返 `stockPlace`，
  货品选择器副标题前置「库位 xxx」；采购收货/退货、委外进仓/发料/退货/材料退
  （配置 `itemHasStockPlace`）、销售出货/其它出货/退货（`hasWarehouse` 三类）的编辑明细与
  详情明细统一加只读「库位号」列（选择器带回或 `goodsLookup` 字典补全，主档权威不落单据行）。
  到此全部实物出入库单据（仓库 9 类 + 采购收货/退货 + 委外 5 类 + 销售 3 类）都能按挂牌对位。
- **老库复核结论**：`StockLabel`/`StockSLabel` 为数量快照（非库位），不迁；详见 [16] 新增复核段。
- **验证边界**：Flutter analyze 0 issue（全仓）；后端 `mvn compile` 通过（2026-08-17，
  含 `GoodsListItem.stockPlace` 与报表库位列）。目标库应用与打印实物验收未完成，
  生产维持既有 NO-GO 口径。

## 十三、仓库实物单据历史专页与商业字段隔离（V445 源码候选）

- 仓库 Hub 的采购收货、委外进仓、委外出仓、委外成品退货、委外材料退回、委外损耗六个历史入口，
  已从采购/委外业务页面切到 `/warehouse/history/{type}` 和专用详情。
- 服务端 `/api/warehouse/document-history/**` 使用固定枚举白名单选择六组主从表，DTO/SQL 不定义
  单价、金额、币种、汇率、税率、结算、应付或索赔字段；即使调用账号同时持有商业查看权，
  仓库响应也保持 quantity-only。
- V445 纯追加注册六个历史权限/Surface 和一个 IQC 仓库查看权限/Surface，并只授 `SUB_WH`
  叶子部门查看；IQC 实物登记仍需显式 `procurement_iqc_rejection:record_return`。同时给权限目录增加
  `bulk_assignable/sensitivity`，商业敏感权限不参与批量授权。V445 不删除任何现有授权。
- 八类原生 `stock_documents` 页面删除成本列/成本排序，库存单据 JSON 不再序列化成本字段；
  内部过账金额仍保留，财务价值查询走专用报表。
- 销售出库与 IQC 实物退回任务中心也已切到 `/api/warehouse/**` 安全投影：销售只保留仓库状态
  目标与实物交接，IQC 只保留拒收量和退回凭证，均丢弃原业务/财务宽 DTO。
- 空库 PostgreSQL 16 已回放 407 个迁移到 V445，六类历史与 IQC 查询逐一解析且动态 Surface
  注册表测试通过。
  这不是公司目标库迁移或真实岗位 UAT；V302 父部门历史价格授权回收属于影响现有账号的权限切换，
  须另行只读盘点、明确确认和前向迁移。
- 页面与权限矩阵详见[仓库实物单据历史专页](../03-页面/仓库实物单据历史专页.md)。

---

**历史更新**：2026-08-11 · **当时状态**：🟡 公司目标库已到 V238 的事实不变；当时源码目录 V253/234（V253 官网询盘，非生产默认车间），既有空库回放证据 V252/233，开发原库保持 V244/225，一次性 clone 仍是 V250/231 历史证据。预计到货、超量门禁和 IQC PASS/FAIL 的源码/自动化已具备，但本轮隔离 HTTP 只到预计到货。当前目录事实见本页顶部 2026-08-14 后置口径与[迁移总索引](README.md)。
**未完成**：公司目标库 V239–V252 生产增量及 V253 官网询盘增量的可恢复副本演练和非空历史对账、真实仓库—财务—采购/委外多账号收货/IQC UAT、特采、供应商退回实物和发布签字，生产 **NO-GO**。
**踩坑（已修）**：① 统一表 legacy_id 不可 UNIQUE（各 O_ 表 IDENTITY 独立、跨表 ID 重复）；② 补录段须 staging 载入后跑；③ 盘点引用已删货品只能建立 `auto_created=true` 历史锚，按标志位校验且不得进入选择器/BOM/MRP；④ 明细表缺 created_by/updated_by（同 V47）；⑤ 后端必须从 server/ 起才读对 .env（JWT secret）；⑥ Javadoc 里 `FINISHED_*/CHECK` 的 `*/` 会提前闭合注释。详见 memory `warehouse-report-completion`。
