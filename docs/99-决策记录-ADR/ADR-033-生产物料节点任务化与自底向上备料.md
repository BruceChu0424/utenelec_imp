# ADR-033：生产物料节点任务化与自底向上备料

- 状态：业务设计已决策；当前共享工作树迁移目录最高 V289/270，270 个唯一版本且无重号。逐路径节点键、路线驱动下层需求、MAKE 下层门禁、child ownership 委派投影和同页任务交互均为源码候选。下述 Flutter 52/52、后端 15/15、通知 21/21 与 PostgreSQL 16/Flyway V1→V252 是本 ADR 在 2026-08-11 的冻结证据；该时点源码迁移头为 V253/234，V251 是货品导入、V252 是审计 sweep、V253 是网站询盘，三者都不是显式 `delegated_qty` 实现。V279–V287 的编号、附件/人员、局部 PII、审批快照与客户结算增量同样不实现 `delegated_qty`；V288/V289 已形成独立现货借用在线链，但仍没有显式 `delegated_qty`，两者不能混称。分批委派量、完整反向及真实 HTTP/浏览器/岗位实物流转仍未闭环；未部署目标环境，生产写链路仍为 **NO-GO**。现行迁移边界见[迁移总索引](../数据迁移/README.md)
- 日期：2026-08-11
- 相关：ADR-019、ADR-024、ADR-028、ADR-029、V150–V253、生产履约 V1
- 优先级：本 ADR 部分取代 ADR-029 §4.2、§6.1、原 2026-08-10“深层缺料可直接下达”订正和相关页面口径；ADR-029 关于分析与正式计划分层、库存/质检/审批权威、追加式反向及历史兼容的规则继续有效

> **2026-08-22 entitlement 交接补充**：本 ADR 的 child ownership 还要求已归属父树后代节点的合格 exact entitlement 随所有权交接。V337 保留不可变 origin 和物理 `stock_reservation`，以成对 `MAKE_DELEGATE_OUT/MAKE_DELEGATE_IN` 把 current beneficiary 精确移到 `MAKE_COMPONENT` child 的同 BOM、同维度 depth=1 节点；事务失败不得留下 child 已创建但权益仍锁在父树的中间态。该迁移只补 exact 权益交接，不宣称任意分批 `delegated_qty` 已实现。child 后续仍须完成计划批准、DRAW 全部发料、开工、报工与 V338 仓库点收；未来计件不属于本节点任务合同。详见 [ADR-046](ADR-046-生产领料仓库点收报工归属与未来计件边界.md)。全部仍为源码候选，生产 **NO-GO**。

## 一、背景

ADR-029 已把“先造正式计划再看缺料”改为“先分析、齐套后生成计划”，也保存了完整 BOM 路径。但是后续页面仍把大部分深层节点解释为“随上级件”，只有直接层或碰巧计算出缺口的深层行可以操作。现场看到第二、三、四层货品，却看不到每条路径自己的现货、有效需求、任务状态和下一步，无法按真实备料顺序工作。

此前增补提出“`depth=1 或 shortage>0` 即可操作，并按 `goods+color+unit` 的开口供给去重”。该规则不成立：

- `shortage` 本身取决于需求是否已经沿父子路径激活，不能拿计算结果反过来定义任务身份；
- 同一货品出现在两条 BOM 路径时，责任上是两个节点任务，可能有不同父件、需求日、路线、批次和反向范围；
- 物理库存可以在仓货色维度共享分配，但业务任务不能仅凭货号合并或相互抵消；
- 按货品去重会让一条路径的 action 错误吞掉另一条路径的需求，失去来源、数量和反向证据。

因此需要把“树上的展示行”提升为有稳定身份的物料节点任务，并明确何时激活后代、何时把任务所有权委派给 MAKE 子分析。

## 二、术语与任务身份

### 2.1 每条 BOM 路径都是独立节点任务

完整 BOM 树中的每个 `analysis_item_id + path/node_key` 是一条独立的物料节点任务。至少保存或稳定投影：

- 所属分析需求、父节点、路径、深度和 BOM 快照；
- 本批激活需求量、已由现货覆盖量、真实短缺量和被委派量；
- 建议路线、人工确认路线、路线原因和版本；
- 目标仓在手、有效预留、冻结、安全库存、合格可用量及其他仓参考；
- 下游 action、采购/委外申请、MAKE 子分析、正式计划和执行状态的来源链接。

同一货品出现在不同路径时任务身份不得合并。服务端可把兼容的 BUY 或 SUBCONTRACT 需求合并到一张下游申请，但必须通过逐节点 allocation 保留每条路径的数量、父件和反向范围；不得用 `goods+color+unit` 的开口数量作为节点任务去重依据。

### 2.2 异常优先展示，完整树始终可达

2026-08-11 现场易用性复核后，本节取代旧的“全部产品完整树首次进入即全部铺开”：一次联合分析可能包含数百个产品和深层 BOM，把所有正常节点同时铺开会降低查找效率并造成前端一次性构建压力。

调度台只保留“新建物料分析 / 联合分析所选 N 项”一个主入口；它只进入或恢复分析，不得用“进入分析并生成计划”暗示已经产生生产、采购或委外单据。单次联合分析的销售来源与手工来源合计最多 500。

进入分析详情后默认选择“只看缺料”，顶部必须分别显示“直接命中 X / 保留上级后 Y / 全部 Z”，并同时提供“待确认路线”“全部 BOM”和产品/物料搜索；过滤命中子节点时保留其祖先路径作为上下文，不得把祖先误算成缺料或漏出显示计数。员工切到“全部 BOM”后，当前产品和分支默认展开，且可分别折叠；为避免 500 产品深树一次铺满，BOM 首次只构建筛选后前 30 个产品组，再按固定批次增量构建，加载更多不改变业务节点与选择。只有路线已确认且当前可执行的节点显示真实复选框，未确认、未保存、MAKE 下层未齐和只读状态使用明确文字控件。刷新、生成计划或返回本页不得丢失业务节点、选择状态或把仍在当前过滤范围内的未处理分支静默隐藏。

产品结论先按“可生产 / 待备料”汇总，可生产项稳定置前；前端首次只构建有限数量的产品卡并提供“继续显示下一批”，但表头全选仍针对全部可排产产品，不得只处理已渲染卡片。

每一行无论本批需求是否激活，都显示目标仓现货与合格可用量。推荐的主列为：

| 物料与路径 | 路线 | 本批需求 | 合格可用 | 缺口 | 任务状态 | 操作 |
|---|---|---:|---:|---:|---|---|

`required_qty=0` 只表示当前批次不需要为该路径补货，文案统一为“本批无需补货”；不得显示“齐套”“有货”或“随上级件”来暗示已经形成独立需求。现货仍照常展示。

## 三、需求沿路径激活

### 3.1 只按父件实际未覆盖数量展开

根产品本批数量先激活其直接组件。任一父节点只把自身未被合格现货覆盖的实际短缺数量向下展开，不能按父件全部需求重复展开后代。

父节点路线决定后代是否激活：

| 父节点路线 | 父节点短缺的处理 | 后代任务 |
|---|---|---|
| BUY | 形成 BUY 物料任务与采购申请 | 不激活；各行仍显示仓库现货并标“本批无需补货” |
| MAKE | 先在当前树按父件实际短缺量展开其 BOM | 激活，用于判断该 MAKE 节点可生产批量 |
| SUBCONTRACT | 形成委外物料任务；若该委外件有有效供料 BOM，则按父件实际短缺量展开我方供料 | 激活我方供料节点；无供料 BOM 时不虚构后代需求 |
| 未确认/结构异常 | 保留路径和现货展示 | 不生成下游事实，显示待确认或结构阻断 |

SUBCONTRACT 在这里表示外部加工成品由委外供给；其已配置 BOM 的下层材料是我方需备齐并依法发给委外商的供料，不代表由供应商自行采购。委外发料、供应商处库存、耗用、退料、损耗和回厂 IQC 仍遵守既有权威链路；该链未验收前继续 fail-closed。

### 3.2 库存分配与任务身份分离

目标仓合格自由库存仍在仓货色维度进入一个服务端共享池，按稳定优先级分配，不能被多个路径重复宣称。分配结果必须回写到各自路径任务。

“共享库存池”不等于“共享任务身份”。同货不同路径即使最终被合并采购，仍须分别保存：

- 各路径激活需求、现货覆盖和缺口；
- 各路径 action allocation；
- 各路径被取消、改道、委派、收货兑现或反向的数量。

## 四、路线动作、MAKE 委派与计划生成

### 4.1 BUY 和 SUBCONTRACT 是物料任务

BUY 行可执行“提交采购需求”，形成幂等 supply action、逐路径 allocation 和真实采购申请。

SUBCONTRACT 行可执行“提交委外需求”，形成幂等 supply action、逐路径 allocation 和真实委外申请；若有我方供料 BOM，其后代物料任务继续按自身路线备料。采购/委外任务不是 `production_plans`，不得显示成生产子计划。

通知、消息和角标都不是完成事实。状态必须来自 action、申请、订货审批、预计到货/回厂、IQC 和合格入库。

路线确认或任务下达超过单请求 500 条时，客户端必须先按稳定路径任务身份排序，再以最多 500 条分块。每个请求块在服务端自身原子；后一块使用前一块返回的最新 analysis version/指纹，已成功的前块不因后块失败而回滚。幂等键只绑定请求前 CAS 事实和该块排序后的精确任务集合，不得包含重试时可能重新编号的 batch index；服务端须先合并、映射并去重 `actionGroupKeys` 与 `materialLineIds`，再对最终任务集合强制 1–500，不能让两个字段各 500 绕成一次 1000 项。

### 4.2 MAKE 先齐下层，再创建子分析

MAKE 节点在下层实际齐套前只是当前父树中的自制物料任务，不立即创建 `MAKE_COMPONENT`，更不能直接生成正式子计划。

当服务端按目标仓、当前版本和 BOM 指纹重算，确认某个正数量批次的直接下层物料全部合格齐套后，员工才可在该 MAKE 行点击“安排生产”。该命令必须在一个事务中：

1. 锁定父节点任务、相关后代路径、共享库存软分配和幂等命令；
2. 计算本次可生产的正数量批次，不超过该 MAKE 节点真实短缺；
3. 幂等创建系统 `MAKE_COMPONENT` child analysis item，并精确回指父路径任务；
4. 把本批对应的后代路径和数量标记为 `DELEGATED`，由 child analysis 接管；父树保留可见和下钻，但不得再次确认路线、通知或占用同一数量；
5. 若仍有未安排数量，仅剩余数量继续在父树保持活动，不能把整条路径一次性委派而吞掉余量。

`DELEGATED` 是任务所有权转移，不是现货预留、领料、开工或完工。child analysis 仍须按 ADR-029 的齐套门禁生成正式计划草稿。

当前源码候选以“父路径已存在系统 `MAKE_COMPONENT` child item”推导 child ownership，并让父树后代不再形成可操作需求；截至 V253 仍无显式 `delegated_qty` 记录。因此当前证据只能证明节点级所有权投影，不能宣称“同一路径任意部分数量委派、剩余数量继续活动及其反向”已经持久化闭环。若一个节点允许分批委派，必须在上线前用现有事实证明数量可唯一恢复，或按 §7 追加 V254+。

### 4.3 只有 MAKE 与根产品生成生产计划

- BUY、SUBCONTRACT 只形成物料供应任务及其权威下游，不生成生产计划。
- MAKE child item 的本批直接材料齐套后可以生成真正的子生产计划。
- 根产品本批直接组件齐套后可以生成最终组装生产计划。
- 任一正式计划仍先生成草稿、`ACTIVE` planning draft 和 `SUBMITTED` plan link；批准后才原子形成 READY、完整物理预留、DRAW 和销售分摊。

生成计划后页面保持在当前物料分析，不强制跳转。对应 MAKE 或根产品行刷新为“草稿待审/已批准待领料/生产中/已完工入库”等服务端投影，并提供“查看计划”深链。员工可继续处理其他节点。

## 五、状态和操作必须来自权威事实

节点任务至少区分以下可解释状态，名称可由 UI 本地化，但不得由前端直接改写：

- 本批无需补货、路线待确认、缺料待处理；
- 采购需求已提交、采购处理中、等待合格入库；
- 委外需求已提交、委外处理中、等待供料/回厂/IQC；
- 自制下层备料中、下层已齐套、已委派子分析；
- 计划草稿待审、READY 待领料、已部分领料、生产中、已完工入库；
- 结构阻断、已取消或待反向。

“已领料”只来自仓库实际 `issue`；“生产中”只来自正式执行段/报工；“已完工入库”只来自审核合格的成品入库。页面按钮不能直接制造这些状态。

## 六、继续有效的权威边界

本 ADR 不改变以下 ADR-029 不变量：

1. 分析软分配不写 `stock_reservations`，也不扣在手。
2. 在途、待检、冻结和不合格数量不能计入当前齐套。
3. generate 不预留；approve 才原子创建 READY、完整预留、DRAW 和销售分摊。
4. DRAW 不是实物发料，仓库实际出库才扣库存。
5. MAKE 子件完工不能冒充父成品或销售订单完成。
6. 已接受的申请、订单、预留、发料、报工和入库只能追加式取消、释放、红冲或反向。
7. 所有写命令使用对象权限、版本、BOM 指纹、幂等键、稳定锁序和服务端数量重算。
8. 不恢复 ADR-028 的递归自动创建/自动批准整棵正式计划树。
9. 计划向导的默认车间只读取 V192 `production_goods_workshop_preferences`；只有正式确认且同货品车间无歧义，或后续合法改派成功时才学习。草稿、打开/取消向导、拖动和幂等回放不学习；不得向 `goods` 增加 `default_workshop_department_id` 或把 V253 当作生产车间迁移。
10. 采购/委外收货先形成待检事实；每次 IQC `PASS` 只按本次合格量进入可用库存并立即刷新相关物料分析，`FAIL` 不入库存。每次 PASS 均按累计合格量减既有有效分配的差额执行正式供给兑现、WAITING→READY、预留/DRAW；同单其它待检行不阻塞，整单终态只最终对账；收货红冲精确反向已 PASS 数量并刷新分析。

## 七、数据与迁移要求

当前候选使用稳定 path action key、现有 action/allocation、`parent_analysis_material_id` 与 child item 存在性表达路径身份和 child ownership，截至 V253 没有委派数量表。V251/V252/V253 已分别被货品导入、审计 sweep 和网站询盘占用；若这些既有事实不能无歧义恢复“激活量、委派对象、委派数量和反向余量”，只能通过 V254+ 新增字段、表或只读投影实现。不得修改任何已应用 Flyway 文件，也不得借 `required_qty=0`、删除节点或按货品合并掩盖所有权。

建议的逻辑唯一维度为“分析需求 + 路径节点 + 代次”，委派还须记录来源节点、目标 child item、数量、时间、操作者和幂等命令。一个下游 action 可以承接多个兼容路径，但 allocation 总量必须等于 action 数量，每条路径累计 action/委派/现货覆盖均不得超过其激活需求。

## 八、最低验收

至少验证：

1. 进入页面默认“只看缺料”并明确显示 X/总数；搜索/过滤保留祖先上下文，切“全部 BOM”可查看所有层级和目标仓现货；`required=0` 只显示“本批无需补货”。
2. 同一货品位于两条路径时形成两个节点任务；共享库存不超分，下游即使合单也保留两条 allocation。
3. 父件部分有货时，MAKE/SUBCONTRACT 只按实际短缺展开后代。
4. BUY 父件不激活后代；改为 MAKE 或有供料 BOM 的 SUBCONTRACT 后按版本重算。
5. MAKE 下层未实际齐套时不能创建 `MAKE_COMPONENT` 或生产计划。
6. 创建 MAKE child item 后，已委派数量在父树只读，不能重复通知、重复软分配或重复生成计划；剩余数量仍可后续分批处理。
7. BUY/委外动作不生成 `production_plans`；只有 MAKE 与根产品能进入计划生成。
8. 生成后留在当前页面，刷新能看到真实计划、领料、生产、入库状态；无权或失败必须明确反馈。
9. 并发刷新、通知、委派、生成、取消和反向保持数量守恒、幂等与来源完整。
10. 单次来源选择合并销售与手工来源后不超过 500；路线/下达的 500/501 边界、500+1 分块、新版 CAS 续传、服务端成功但响应丢失后的原块重放，以及中途失败后的未完成选择恢复均有契约测试。
11. V192 默认车间预填、无偏好提示、正式确认后学习、合法改派后更新，以及草稿/取消/歧义样本/幂等回放不学习均有服务端与页面证据。
12. 每次 IQC `PASS` 立即提高合格库存/可完工量，并按累计合格差额推进正式供给；只有全套需求被合格库存和正式预留覆盖时计划才 READY，同单其它待检行不阻塞，整单终态只最终对账，`FAIL`、红冲和幂等重放保持库存与来源守恒。

## 九、发布结论

本 ADR 是业务和验收合同，不是生产实现证明。当前并发工作树的源码候选和定向自动化已覆盖其中一部分，但既有 V250 隔离克隆与页面截图早于本 ADR；显式分批委派量、完整取消/反向、真实 PostgreSQL/HTTP/浏览器和多岗位实物流转仍未覆盖。

在 V254+（如需要）迁移、源码、数据库约束、自动化、目标库校验、历史对账、真实计划/采购/委外/仓库/质量/车间 UAT、并发与恢复演练及业务签字全部完成前，相关生产写能力继续 **NO-GO**。V253 已用于网站询盘，不得复用为生产委派量或 `goods` 默认车间迁移。
