# 07 - 物料分类 · 新库设计与老库迁移

> 本档记录新系统 `material_categories` 表的设计决策、老库迁移代码的用法、增量重跑说明。
> 老库溯源见 [02-货品分类-老库溯源.md](02-货品分类-老库溯源.md)。
> **建立时间**:2026-07-24 · **状态**:表 + API + 迁移代码已实现(编译通过)

---

## 一、新库表设计(`material_categories`)

**范本**:`departments`(V02)——邻接表(parent_id)+ 物化路径(path,触发器维护)+ 软删除 + 审计。Flyway `V31__material_category.sql`。

### 与 departments 的差异(针对老库数据特点优化)

| 项 | departments | material_categories | 原因 |
|---|---|---|---|
| `code` | TEXT **UNIQUE** | TEXT(**无 UNIQUE**) | 老库编码大量重复("白色"×31),定位改用 `id`/`legacy_id` |
| `level` | TEXT 语义枚举 | **INT 真实深度**(根=0) | 老库 `SystemItem.Level` 不可靠,迁移按 parent 链重算 |
| `manager_id`/`headcount` | 有 | **去掉** | 物料分类无负责人/人数 |
| `legacy_id` | 无 | **INT UNIQUE** | 老库 `SystemItem.ItemID`,迁移溯源 + 重跑/增量幂等 |
| path 触发器 | `fn_dept_path` | `fn_matcat_path`(逻辑同) | path 不要求 UNIQUE(排序用 `path, sort_order`) |

### 字段

```
id(UUID PK) · legacy_id(INT UNIQUE) · code · name · parent_id(自引用 FK)
level(INT 深度) · sort_order · path(触发器维护) · 审计4列 · is_deleted · deleted_at
```

### path 触发器逻辑

`path = 父path || code || '/'`（根节点 `/code/`），`BEFORE INSERT OR UPDATE OF parent_id, code` 触发器只重算当前节点。2026-08-02 起，真实换父由 Service 在事务级锁内防环，再以递归 CTE 按 parent 链原子重算完整子树的 `level/path`（含软删后代）；同父级请求不重算。

---

## 二、权限点(V31 已 seed)

| code | name | category | 说明 |
|---|---|---|---|
| `material_category:view` | 查看物料分类 | 主数据 | 预配给**所有部门**(基础资料全员可查) |
| `material_category:edit` | 维护物料分类 | 主数据 | 预配给 PMC 运营部(仓储/物料归属);超管恒有 |

前端路由 `/basicinfo` 查看不设守卫(登录即可进入);编辑类按钮按 `material_category:edit` 显隐。

---

## 三、后端 API(`/api/master/material-categories`)

完全仿照部门树。包 `com.uten.imp.features.master.materialcategory`:
`GET /tree`、`GET /{id}/subtree`、`GET /{id}`、`POST`、`PUT /{id}`（事务锁 + 防成环 + 子树 level/path 重算）、`DELETE /{id}`（校验无子）。

---

## 四、老库迁移代码(专门文件夹 `com.uten.imp.legacy`)

**专门文件夹** = `server/src/main/java/com/uten/imp/legacy/`,数据校对文件留 `server/legacy_migration/data/`。

```
legacy/
├─ config/LegacyProperties.java        (@ConfigurationProperties("app.legacy"))
├─ reader/
│  ├─ LegacyCategoryRow.java           (record)
│  └─ LegacySystemItemReader.java      (按需连 MSSQL,读 SystemItem WHERE ItemclassID=?)
├─ migration/MaterialCategoryMigrator.java  (核心:拓扑序 upsert + level 重算 + 孤儿挂虚拟根)
└─ web/LegacyMigrationController.java  (POST /api/admin/legacy-migration/material-category)
```

### 设计要点

- **按需连接**:迁移时手动 `new HikariDataSource` 读 `LegacyProperties`,读完即关——**不注册常驻 DataSource bean**,避免触发 Spring Boot 主数据源自动配置退让。
- **幂等 + 增量**:按 `legacy_id` upsert(`findByLegacyId` → 存在则更新/不存在则新建),可反复重跑;老库新增的分类下次自动纳入。
- **拓扑序写入**:Kahn 拓扑排序保证父先于子入库,每条 upsert 后 `em.flush()` 让触发器即时算 path、子挂父时父行已存在。
- **level 重算**:不信老库 `Level`,按 parent 链 `parent.level + 1`。
- **孤儿处理**:15 个父被删的节点 + 成环节点 → 挂虚拟根 `legacy_id=-1`(code=`LEGACY_ORPHAN`,name=`未分类(历史孤儿)`),报告计入 `orphans`,人工归位。
- **GBK**:老库 `Chinese_PRC_CI_AS`,JDBC 读 varchar 正常得中文。

### 配置(`application*.yml`)

```yaml
app:
  legacy:
    enabled: ${UTEN_LEGACY_ENABLED:false}     # 默认关,迁移时显式开
    datasource-url: ${UTEN_LEGACY_DB_URL:jdbc:sqlserver://(localdb)\MSSQLLocalDB;databaseName=YTDQ_2023;encrypt=false}
    datasource-username: ${UTEN_LEGACY_DB_USER:}
    datasource-password: ${UTEN_LEGACY_DB_PASSWORD:}
```
- **dev**:指向本机 LocalDB(集成认证,用户名/密码留空)。
- **prod**:换成生产 SQL Server 实例的账号密码(profile 隔离)。

---

## 五、如何触发迁移(平台上线后直接调用)

### 方式 A:HTTP(超管手动触发,推荐)

```bash
# 1. 启动 server 时开启老库迁移开关
UTEN_LEGACY_ENABLED=true mvn spring-boot:run

# 2. 用超管 token 触发(幂等,可重跑)
curl -X POST http://localhost:8080/api/admin/legacy-migration/material-category \
  -H "Authorization: Bearer <超管token>"
# → {"total":896,"inserted":881,"updated":0,"orphans":15}
```

### 方式 B:代码直接调用(将来颜色/模具复用)

```java
// 货品分类(ItemclassID=1)
migrator.migrateGoods();
// 模具分类(ItemclassID=18,后续)——同一套逻辑,换 itemclassId
migrator.migrate(18);
```

### 增量同步(老库新增了类别)

直接**再调一次**同一个端点即可——`legacy_id` 已存在的更新,新增的插入。无需清表、无需特殊处理。

---

## 六、迁移校验

```sql
-- 总数对账(应 = 881 连通 + 15 孤儿 + 1 虚拟根 = 897,或老库后续有新增)
SELECT count(*), count(*) FILTER (WHERE legacy_id IS NOT NULL) FROM material_categories;

-- 抽验路径与深度
SELECT legacy_id, code, name, level, path FROM material_categories
WHERE legacy_id IN (2113,2283,2290) ORDER BY path;
-- 期望:原材料(A,level 0)→ 包装材料(BC,level 1)→ 标贴(BT,level 2)

-- 孤儿
SELECT * FROM material_categories WHERE parent_id = (SELECT id FROM material_categories WHERE legacy_id = -1);
```

---

## 七、后续扩展(颜色 / 模具 / 部门)

同一套 `MaterialCategoryMigrator` + `LegacySystemItemReader` 按 `itemclassId` 参数化:

| 老库来源 | itemclassId | 说明 |
|---|---|---|
| 货品分类 | 1 | ✅ 本次 |
| 模具系列 | 18 | 待做(模具资料) |
| 部门 | 5 | 新系统已有 departments,可对照补 legacy_id |

颜色(`B_Color`)是独立自引用树,需另写一个 Migrator(结构类似,读 `B_Color` 的 `ParentID` 树)。

---

## 八、货品主档（`goods`，V32）

货品主档表见 `server/.../db/migration/V32__goods.sql`（老库 `B_Goods` 全 78 字段迁移；图片列建 `bytea` 但本次不灌二进制）。API `/api/master/goods`：
`GET ?categoryId=&page=&size=`（分类下分页）、`GET /{id}`（详情）、`POST`（新建，`goods:edit`）、`PUT /{id}`（编辑）、`DELETE /{id}`（软删）。请求体 `GoodsSaveRequest`——只开放「标识/物理/价格」核心字段（name/code/shortName/model/spec/price/material/thickness/mWeight/pack/pieces/status）；78 字段里的成本项/图片/关联 legacy_id 暂不进表单。`goods:edit` 仅超管恒有（V32 未授部门）。

> **mWeight Jackson 坑（已修）**：实体字段 `mWeight`（getter `getMWeight()`）被 Jackson 默认序列化成 `MWeight`（连续大写不折半），前端 `fromJson` 取 `mWeight` 取不到 → 详情弹窗「单重」一直空。已在 `GoodsDetail` / `GoodsSaveRequest` 加 `@JsonProperty("mWeight")` 显式锁定键名（见 MEMORY: Jackson 连续大写字段坑）。

前端 `/basicinfo/goods`（`ProductCategoryPage`）与客户/供应商/模具页一致：详情弹框带「编辑/删除」、分类面板「添加货品」、返回键回 hub、通用 `MasterEditDialog` 表单。

> **`auto_created` 历史引用锚（V177/V181，2026-08-01）**：迁移脚本为「老库单据明细引用了 `B_Goods` 不存在的货品号」建 stub（`migrate_subcontract`/`migrate_production`/`migrate_stock_docs`/`migrate_sales`），新库共 31 条（`category_id`/`status` 均空）。V177 加 `goods.auto_created BOOLEAN NOT NULL DEFAULT FALSE` 并回填；4 个迁移脚本的 stub INSERT 同步写 `auto_created=TRUE`。这些行只为历史单据/成本快照维持外键和原 legacy 身份，**不是待补全后继续使用的普通货品**：货品选择器隐藏，活动 BOM/MRP 拒绝。V181 软删 81 条误接入 BOM 的迁移行并加数据库守卫；历史引用锚本身不得强删（见 [31](31-货品组装BOM与成本预算.md)）。

---

**最后更新**：2026-08-01 · **状态**：表（V31/V32）+ master CRUD API + 迁移代码已实现；V177 标记历史 stub，V181 已应用并将活动 stub BOM/MRP 污染降为 0，31 个历史引用锚继续保留。前端页面与其余生产门禁状态见迁移总览。
