# 采购/委外到货与物料分析全链路审计整改（2026-08-20）

> 本文是源码候选与验收边界记录，不是目标数据库已迁移、服务器已安装或多岗位 UAT 已通过的证明。已应用的 Flyway 迁移必须保持字节不变，所有整改仅能通过新的前向迁移落地。

## 一、业务结论

仓库“收货审核”与品质“合格放行”是两个不同事实：

1. 仓库收货审核只建立待检事实，不得提前增加合格库存。
2. IQC `PASS` 才写入 `DIR_IN` 库存变动，并在同一事务内追溯到来源物料分析。
3. “本物料合格到货量”、“本分析可覆盖量”与“整套物料最多可生产量”必须分开：一项物料已合格到货，其他物料未齐时，`readyNowQty = 0` 是正确结果，但该物料的合格到货量不得仍显示为 0。
4. 只有实到仓库、货品、颜色、基本单位与分析维度一致，且扣除安全库存后，合格到货才能转化为当前批次的可生产覆盖。到其他仓的物料必须先走受控调拨。

## 二、权威主链

### 2.1 采购

`material analysis row UUID`
→ `preplan_supply_action_allocation UUID`
→ `purchase_request_item UUID`
→ `purchase_order_item.request_item_id`
→ 财务审批
→ `purchase_receipt_item.order_item_id`
→ `procurement_inspection_item.receipt_item_id`
→ IQC `PASS`
→ `stock_movement(DIR_IN)`
→ `PREPLAN_ANALYSIS` 库存归属
→ 刷新来源物料分析。

### 2.2 委外

委外与采购共用前半段的计划和财务门禁，但在委外商回货前增加公司自有材料出仓：

`material analysis row UUID`
→ `preplan_supply_action_allocation UUID`
→ `subcontract_application_item UUID`
→ `subcontract_order_item.application_item_id`
→ 财务审批
→ 冻结 BOM 发料计划/仓库出仓
→ 委外商回货
→ `subcontract_receipt_item.order_item_id`
→ IQC `PASS`
→ 库存与来源分析归属。

委外订单的手工行可以没有 `application_item_id`，但这种行没有物料分析来源，IQC 合格后只能进入公共合格库存，不得按货号猜测并绑定到某份分析。

## 三、本次确认的缺陷与整改

| 问题 | 影响 | 整改 |
| --- | --- | --- |
| 物料供给进度按整张订单/收货单汇总 | 同一合单内兄弟物料的收货或 PASS 会把当前物料误标为完成 | 改为 `allocation -> exact order_item_id -> exact receipt_item_id -> IQC` 逐行投影；委外发料也按精确订单行计算 |
| 任一分析写入口遇到旧 version/fingerprint | 服务端返回冲突，Flutter 仍显示旧快照或把过期草稿重新标 dirty | 所有 409 均只 GET 最新详情且绝不自动重试；服务端确认优先，只保留相对最新建议仍合法的草稿 |
| Flutter 丢弃服务端归属量，且把整套齐套量与单项到货混用 | 用户看不到“已合格且归属本节点”，容易把整套未齐误认为未到货 | 模型解析节点级 `exactPeggedQty` 与分析仓级 `ownPeggedQty`；物料卡显示“本节点合格入库绑定”，整套区独立显示“整套物料未齐”；“已入库”改为“已齐套（库存已覆盖）” |
| V298 只保留 `analysis_id`，丢失 `analysis_material_id` | 同一联合分析中，针对 A 产品到货的物料可被优先级更高的 B 产品重新分配 | 使用新的前向迁移建立 exact-peg 子账；收货按 action allocation 容量精确绑定原物料行，重算时先保障该行再分配公共池 |
| exact 引用的 material 行可被 BOM 刷新改维度或永久失活 | 不可变 origin 身份会漂移，合格库存可静默隐藏 | 刷新前按稳定 item+node 与 reservation/IQC 物理维度 fail closed；数据库冻结身份并延迟校验有效端点最终 active |
| 分仓展示和主重算的安全库存公式不一致 | 展示层可把本分析归属量加在安全库存之外，高估可用 | 统一为 `max(publicAvailable + ownPegged - safetyStock, 0)` |
| 实际 IQC 仓与分析目标仓不同却无解释 | 目标仓备料显示 0 在业务上正确，但计划员会误认为到货未回写 | 收货登记改离建议仓时明示不会计入原目标仓；分析页按物料维度去重列出“非目标仓的合格绑定量、实际仓、目标仓”，并明确普通调拨不会迁移该分析归属，必须走受控更正/红冲后在目标仓重登验收 |
| 生成/取消/计划审核/计划包与 IQC 的锁顺序相反 | 并发时可能 deadlock 或在预锁后换维度 | 统一 inventory→analysis→action/plan/package；计划包采用维度双读，analysis 锁后集合漂移即 409，绝不反序补锁 |
| 已转正式需求后仍可红冲、取消或回退计划包 | marker 可被覆盖或库存泄回公共池，留下无实物支撑的 READY/领料链 | 无可逆桥时收货/MAKE 红冲、action/analysis 取消、package lifecycle 全部先 409；只转移 READY demand，WAITING 保留原归属 |

## 四、借用与下一批优先归还

V288/V289 已实现的是“同一份物料分析内，两个产品的相同直接物料之间做软分配调整”。它不写物理库存变动，并且只有 `ACTIVE/REVOKED`；它不是跨分析借料账。

用户要求的跨分析业务至少需要：

- 出借与借入的 analysis/material UUID，不以可变货号或名称关联；
- 仓库+货品+颜色+单位的完全一致校验；
- `loan_qty/outstanding_qty/repaid_qty` 与追加式 `LEND/REPAY/REVERSE` 事件流水；
- 只允许借出已由 `PREPLAN_ANALYSIS` reservation 隔离的合格现货，不得把未到货或公共池投影伪装成可借库存；
- 借入方下一批精确关联的 BUY/SUBCONTRACT/MAKE 合格供给，必须先按到期时间、创建时间和 loan UUID 稳定归还 outstanding，剩余量才归新供给的原路径；
- IQC PASS、借用、计划正式确认、领料和收货红冲之间的统一锁顺序与反向规则。

在这些事实账、并发守卫和正式计划转移谱系完成前，不得宣称“跨物料分析借料+下一批自动优先归还”已上线。

## 五、必须保持的不变量

- 仓库收货审核不增加合格库存；每次部分 PASS 立即增加对应数量并刷新分析。
- 任何分配或借用前后，“公共可用+全部有效归属/预留”的总量守恒，不能变出库存。
- 超过上游 action allocation 的财务特批超收部分进入公共池，不伪造分析路径归属。
- 手工采购/委外行、历史无 UUID 谱系行与已取消 action 必须 fail closed，不按货号猜测归属。
- 已转为正式生产 demand 预留、已领料或已开工的库存，不得被收货红冲或软借用静默抽走。

## 六、验收与发布边界

源码候选至少需要以下动态证据：

- 采购和委外合单：兄弟行的收货/PASS 不会污染当前行进度；
- 采购和委外真链：计划任务、订货、财务、仓库、IQC、库存、来源分析全部使用真实服务和 PostgreSQL；
- 同一分析 A/B 同料：A 的精确 action 到货只覆盖 A，不受 B 的优先级影响；
- 部分/多次 PASS、幂等重放、超收、安全库存、错仓与调拨；
- 收货红冲、action 取消、分析取消和计划确认后的对称反向；
- Flutter 旧 CAS 冲突回读、草稿保留、合格到货和整套齐套的独立展示。

目标 PostgreSQL 迁移历史/校验和、最终 JAR/前端产物回读、五岗位真实 UAT、红冲/恢复演练中任何一项未完成时，发布结论仍是 **NO-GO**。

## 七、本次源码候选验证结果

截至 2026-08-21，隔离分支 `uimp/preplan-exact-stock-pegs` 已取得以下本地证据：

- V304 已按本机最早成功应用历史恢复为 canonical 字节，Flyway checksum `74666331` 由不可变测试冻结；目标库 V304 checksum 仍 unknown；
- Java/V304/V307/V308 聚焦回归 `96/96` 通过，覆盖 exact 分配、material 端点、UUID 谱系、安全库存、合单隔离、READY-only 转移、反向门禁、锁序、草稿状态与幂等 replay；
- 启用 PostgreSQL 门禁的全量 Maven `1,797` 项为 0 failure / 0 error、2 skipped，Failsafe 双 JAR 门禁 `2/2` 通过；
- PostgreSQL 16.15 空库从零成功应用 289 个唯一迁移至 V308；历史 V298 不做猜测式回填；
- PostgreSQL FullChain 采购与委外 `2/2` 通过。委外真实经过计划任务、申请/订单、财务审批、V304 发料计划、仓库发料、委外回货、IQC PASS、库存归属和分析刷新；
- 同一分析两个产品使用同一物料时，目标节点 exact/allocated=`10/10`，兄弟节点 exact/allocated=`0/0`、shortage=`10`；另一份分析保持 `0`；
- Flutter 物料分析页面 `53/53`、全量 `777/777` 通过，`flutter analyze --no-pub` 为 0 issue，精确 Dart 路径格式门禁及 Web release 构建通过；
- V308 在 V307 业务表之后重新执行完整 fail-closed audit sweep；latest sweep 合同与迁移 rehearsal 固定为 V308/289；
- 未满足 ADR-043 的 V309/V310、entitlement 与“跨计划让料”运行时/测试均未进入该隔离候选；
- `git diff --check` 通过；生成的 desktop plugin registrants 未纳入候选。

上述证据证明的是**本地源码候选**，不等于目标库已执行 V307/V308，也不等于已部署或多岗位验收。

## 八、仍未完成且必须显式阻断的能力

1. 跨分析借料、部分借出、欠量账和下一批 BUY/SUBCONTRACT/MAKE 自动优先归还尚未实现；现有页面只允许同一分析内软调配，并已明确提示该边界。
2. exact/分析级归属转正式 demand/reservation 后，尚无一对一可逆转移子账；采购、委外与 MAKE 红冲目前只能安全返回 409，不能自动回退。
3. MAKE 到货尚未进入 V307 节点级 exact 谱系；在补齐统一权益账、正式转移桥和反向规则前，不得用于跨分析自动还料。
4. 尚缺真实 PostgreSQL 双事务的 IQC PASS 与生成、审核、取消并发无死锁回归，以及目标非空库迁移、目标环境恢复演练和五岗位 UAT。

跨分析方案与本轮不启用的理由见
[ADR-043 合格到货精确归属与跨分析借料边界](../99-决策记录-ADR/ADR-043-合格到货精确归属与跨分析借料边界.md)。
