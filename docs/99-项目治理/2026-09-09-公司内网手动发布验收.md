# 公司内网手动发布验收

本次按用户明确指示，在最终CI继续运行时手动发布；不将待完成的检查标成通过。版本包仍由原阿里云OSS发布链构建、Ed25519签名、上传及下载，业务数据库、附件和备份位于公司服务器。

## 版本与数据

- 安装版本 `v2026.09.09-1`，应用提交 `e4edcfebbe5a2cd6a78b6ddd4c360d0ba1fe64c1`，Simple Release运行 `34305364449` 成功；服务器已验证签名及SHA256清单。
- 公司数据库从V476/438前向执行61个迁移至V540/499，独立migrator再次验证通过，未repair历史迁移。
- 停写后确认原业务表只有5条通知/庆典记录，其它现有业务表为空，再按用户授权执行正式清理脚本。保留主档、人事、账号、权限和审计；货品138、员工143、客户260、供应商386、账号6。
- 新生产配置启用前，原27字节HMAC与284条派生查重值成对维护；保留PGP版本2、旧版本1密钥环、全部原密文和UUID，284条真实维护审计记录完整。新密钥及旧配置均只存服务器私密维护目录，不进入仓库。
- 停机事前备份SHA256为 `80fb79db7f25e4fc10f24c18f1ede03bfc537b83e5437fcde6c3bf32f2c7c3a6`，15,958,424字节；更新器另有激活前备份。回退必须配对恢复数据库、文件与密钥配置，不能仅切换旧JAR。

## 现场配置修正

- 新增200GiB NVMe逻辑卷作为业务附件专用文件系统，`staging/final/scratch`同卷，保留10GiB余量；现有PostgreSQL和pgBackRest未搬迁。
- ClamAV仅监听本机3310，真实干净文件通过、EICAR被拒。Ubuntu以systemd套接字启动clamd，需要同时安装[回环套接字配置](../../deploy/systemd/clamav-uten-imp-loopback.socket.conf.example)，只写clamd.conf不会产生TCP监听。
- 更新器原HTTP健康地址会被生产环境跳转，导致一次错误回退。改用通过内部CA验证的Nginx HTTPS readiness地址后重新激活成功，未关闭HTTPS或修改OSS凭据。
- 实际办公网卡为 `/23`，旧允许范围误设为 `/24`，漏掉同公司网内的另一段地址。按实际网卡修正后，真实办公IP通过网络门禁、外部测试地址仍返回403。拒绝原因与ToDesk无关。
- 成套备份服务限定在原有三项capability内增加AmbientCapabilities，确保NoNewPrivileges下可进行PostgreSQL本机peer认证；没有修改数据库权限或给Python全局提权。

## 验证边界与证据

生产Java与页面打包完成；最新应用基础提交的前端、部署、密钥、CodeQL及OSV检查已通过，完整后端CI继续独立运行。前一轮唯一失败为自动领料单的旧人员断言，已修正并通过10项本地回归。既有材料齐套真实用户副本、主仓原仓领料、系统补偿及禁用仓选择验证见[V540说明](../数据迁移/138-V540停用仓既有库存与自动齐套恢复.md)。

本地详细执行记录在忽略目录 `.codex-tmp/ci-last-fix-20260909/`，服务器私密维护记录在 `/var/lib/uten-imp-maintenance/v2026.09.09-1/`。

## 最终现场验收

- 内网HTTPS的 `version.json` 返回 `v2026.09.09-1` / `e4edcfeb`，readiness为UP；应用、Nginx、PostgreSQL与ClamAV均active。
- 成套备份 `20260909T032852Z-798f277a4df9` 已校验并真正恢复到独立临时数据库：V540/499、货品138、员工143、账号6、销售订单/库存余额/附件0均正确。临时库已删除。此时原件0个，不冒充已用非零生产原件完成恢复；非零文件原件往返/冷恢复保留既有专项证据。
- 该备份dump SHA256为 `ff08ec4cd8195babc63dd7d28aa54daab42b7dbc0db9f8e02d8c0bc0b8c04dc6`。成套备份定时器已启用，旧非原子rsync备份定时器停用；原pgBackRest定时器保留。
- 备份与服务器状态采样已启用，真实状态源返回SUCCESS / PG_AND_PAIRED_VERIFIED。应用仅读取独立最小状态文件，磁盘探测使用备份所在文件系统；私密备份目录继续root-only。
- 局域网边界复验：真实公司网段请求已进入参数校验，外部测试网段仍403；后端HTTPS健康正常。恢复库与本机已结束的测试容器均关闭，仅保留正常业务数据库。

完整GitHub后端CI按用户要求继续后台运行，不影响本次已明确授权的手动发布。最终安装包对应的应用提交与之后补记的运维模板/验收文档提交分别记录，不混称为同一制品。
