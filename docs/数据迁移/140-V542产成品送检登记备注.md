# V542 生产成品送检登记备注

当前目录为 **V543 / 502 条迁移**；本篇记录 V542（2026-09-09 已应用于 dev 库）。纯前向加列，不回填、不重写任何登记批次。

## 用户口径

登记成品与库位对照采购入库补齐——单张/批量登记页头部卡带备注输入框，随登记批次留痕；品质侧与登记详情可回看。

## 结构变化

- `production_finished_arrival_registrations` 新增 `remark TEXT`（`ADD COLUMN IF NOT EXISTS`，可重放）。
- 约束 `production_finished_arrival_remark_chk`：`remark IS NULL OR length(remark) <= 500`，与采购收货备注上限一致；以 `NOT VALID` 追加，不扫描历史行（历史行本就全 NULL）。
- 空串归一 NULL 由应用层完成（服务端保存前 trim，空即 NULL）。

## 边界

- 备注只随登记批次留痕，不进入 FQC 判定、库存事实或成本；品质侧只读回看。
- 不新增权限码：写备注沿用登记本身的权限（`production_quality_inspection:*` 登记动作），读沿用登记详情/品质结果查看。
- 已应用文件不再修改；日后需要放宽长度或加结构化字段一律新迁移。

正式 SQL：`server/src/main/resources/db/migration/V542__production_arrival_registration_remark.sql`。
