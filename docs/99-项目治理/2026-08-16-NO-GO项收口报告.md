# 2026-08-16 NO-GO 项收口报告(代码侧)

> **性质**:对「生产 NO-GO」清单的逐项处置——**代码侧可修的全部修复**,不可自主修复的
> (目标环境/真实数据/人工 UAT/业务签字)逐项列明原因与责任人类型。
> **基线**:本分支(含同日《全链路溯源联动与计算准确性审计修复报告》全部改动)。
> **结论**:修复后生产仍为 **NO-GO**——剩余阻断全部是需要真实资源、真实数据或人工签字的项,
> 不存在可继续由代码修复消除的已知阻断。

## 一、本轮修复的代码侧 NO-GO 项

### 1. 🔴 销售退货处置纠错(SOP 06 §二,落地映射唯一 🔴)—— ✅ 已修复

- **V291 迁移**:扩展 `sales_return_quality_events.action` 枚举
  (`GOOD_RELEASE_REVOKED/SCRAP_REVOKED/REWORK_REVOKED`),不回填、不改既有行。
- **受控纠错命令** `POST /api/sales/returns/{id}/quality/{returnItemId}/correct`:
  按桶撤回已登记处置量。撤回 GOOD_RELEASE 反向出库,并要求该仓
  「余额 − 生效预留 ≥ 撤回量」(已被订单承诺的释放量禁止凭纠错抽走,须先处理预留或走
  盘点/人工);撤回 SCRAP/REWORK 恢复待处置。金额按累计舍入口径精确互抵;
  全量撤回后明细回 PENDING,「未处置才可整单红冲」语义自然恢复;独立幂等键空间;
  权限沿用 `sales_return_quality:view+handle`。
- **前端**:质检卡按桶显示「撤回 ××」按钮,对话框切换纠错口径(上限=已登记量)。
- **证据**:`SalesReturnQualityIdempotencyPostgresTest` 8/8(真实 PG:精确反向/幂等/
  超量拒绝/已被预留 fail-closed)。

### 2. 采购退货应用层容量校验(全链审计遗留 #4)—— ✅ 已修复

`requireReceiptReturnCapacity`:退量 ≤ 来源收货行「已收 − 已退」,超退给业务 409
(此前仅前端限制,DB 触发器兜底时报 500)。行锁由既有完整性校验持有到事务结束,
并发两张退货单串行校验。

### 3. 附件安全 ATT-01~05(部署清单 §4.4)—— ✅ 核实:源码已由后续工作实现,清单过时

核实 `features/attachment/` 现状:presign **配额预留**(ATT-01/05)、**staging→final**
暂存提升(ATT-02)、**恶意软件扫描** + 隔离状态(ATT-03)、**对象 outbox** 处理器(ATT-04)、
**对账服务**(ATT-05)均已存在。剩余为**真实 OSS/AV 资源与演练验收**(OSS-00~04、ATT-03
真实 AV 引擎)——人工/运维项,不得据源码存在倒推完成。已在部署清单加注。

## 二、评估后归入「不可自主修复」的 NO-GO 项(附原因)

| 项 | 为什么不能由代码夜班修复 |
|---|---|
| **计划链 TRANSFER 调拨路线**(SOP「TRANSFER 仍未闭环」) | 全代码库无此路线实现,属**未设计的新能力**(分析页 TRANSFER 路线→调拨建议单→跨仓 supply peg 挂接)。涉及仓库优先级/调拨规则等 §8.2 明确要业务负责人签字的配置,须先设计文档评审,不得无设计落地 |
| **完整 WMS 批次/库位/实拣/物流件级** | §8.2-4 明确为上线前业务签字配置(哪些货品启用批号/序列号/库位模型),模型设计依赖业务决策 |
| **完整 QMS(特采/批次责任/索赔/质量成本)** | §8.2-6 岗位责任与最小系统状态须业务签字;IQC 最小闭环(V222)已覆盖 P0 |
| **委外损耗/赔偿版本化合同配置** | §8.2-7 明确禁止全公司硬编码一套比例,须按版本化合同配置 |
| **BOM 源端 20,798 条 reject 治理 / V90 历史订单激活 / 历史数据对账** | 需真实目标库数据逐条业务核对签字(BUS-01/02/DATA-01),禁止猜测回填 |
| **DEC/NET/DB/OSS-00/BAK/DR/AUTH/REL/OPS/UAT/COM/GO** | 全部是目标环境资源创建、真实演练、受保护发布链、真实岗位 UAT 与签字——本质是执行与验收,不是编码 |

## 三、验证

- 后端:`UTEN_RUN_DB_TESTS=true mvn test` 全量(含 V291 迁移、FullChain 54/54、
  HTTP 契约 714 操作 smoke、迁移计数守卫 291/272)。
- Flutter:analyze 0 issue;sales 模块测试 44/44;dart format 合规。
- 迁移头:V291/272(`MigrationRehearsalSupport` 已同步)。

## 四、给负责人的行动清单(按 2026-08-09 部署清单顺序)

1. 冻结本分支 → 受审签名提交/tag(REL/AUTH-01)。
2. DB-01/02:目标库 V238 可恢复副本演练 V239→V291 迁移+全表对账。
3. OSS-00:最终 JAR 以 `cloud,prod` 连真实 OSS 启动验收。
4. BUS/FIN/UAT/GO:按清单第四章逐项由对应负责人签字。
