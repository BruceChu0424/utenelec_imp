# 执行纠偏与串行收口

用户指出耗时和额度消耗明显，整体交付进展不清楚。本页覆盖此前检查点中的“继续并行扩展”安排；不改变“全部整改、本地验收后再提交/CI/部署”的授权顺序。

## 本次确认的原因

- 多路修改同一工作树及公共类，特别是端到端测试大文件；在main源码扫描后添加类或改构造，造成测试编译失败及重复构建。
- 不断追查并展开新分支，没有及时固定一个自包含、可正常启动的源码与数据库组合。
- 已实现实体需要V523等新字段，但正式Flyway目录仍止于V516，其余在临时候选目录。隔离测试能启动不能证明用户普通启动可用。
- 2026-09-07本机22:21检查时没有本轮Java/Maven测试进程挂死。两个较早启动的Flutter调试进程未擅自关闭。
- 查询时账户周额度已用44%，属于全账户指标，不能将全部消耗归于本对话。没有执行购买或额度重置。

## 已采取的控制

停止此前三路长期扩展后，改为根任务统一维护候选与验收；可独立验证的小任务限定文件归属，禁止同时改公共文件或构建目录。两个“销售改单待财审期间原到货继续入库”场景见`analysis-finance-full-r3.log`。

该两个真实Spring/PostgreSQL场景现已2/2通过，49.65秒：改单等待财审时已下达的采购照常到货和合格入库；直接采购的产品保留真实入库归属、显示实物缺口0，复审后沿同一批来源交接给销售，幂等重放不重复采购或预留。未通过忽略财审、取消来源校验或改测试金额来放行。

## 用户最新优先级

用户随后明确“优先先完成所有业务链路检查、修复、更正、上传，财务放在最后，效率高点”。据此先串行完成运营业务链：销售→必要财审关卡→计划/物料分析→采购/委外/车间→品质→仓库→发货和退换货，同时覆盖对应权限、通知、入口、数量与反向。深入财务计价、总账/报表、新资金模块和精度扩展安排在这些业务链之后，不继续并行展开。

业务链通过对应本地验收后按用户要求准备上传。已改源码所依赖的数据库迁移和启动组件必须作为完整可运行组合一并整理；不为了拆开财务工作而遗漏依赖、提交无法启动的代码。仍不得把业务专项通过解释为全部财务已验收，也不提前更新公司服务器。

不再扩大功能范围。先整理当前改动、固定一个源码与迁移一致的可运行候选，再逐工作包收口。公共代码同一时间只由一人修改；测试构建的源码与迁移必须固定，不用不同轮次的通过数字代替最终组合。

## 财务后置范围的阶段证据

- 财务实际银行收付款/预收分摊：87项后端专项(包括完整Spring并发测试)、33项Flutter通过；详见[V519/V520/V523财务交接](2026-09-07-V519-V520-V523财务实际金额与银行链验收交接.md)。
- 成本真实三链：600材料成本→销售2000后COGS600；分次撤回实耗恢复WIP；实际产量变更、good/bad分开计量。最新3项真实Spring/PostgreSQL通过，不能等同于总账闭环。
- 委外自有材料、部分FAIL免费补回及混批：4项真实Spring/PostgreSQL通过。正常损耗、实际索赔成本和后补费用仍有未完工作。
- IQC贷项界面29项通过；销售退货成本服务已经接入，但完整正反向矩阵尚未完成。

## 启动与发布要求

普通启动必须使用完整源码/迁移组合，验证真实服务启动、健康检查和登录。已有V527真实HTTP启动烟测通过；当前源码已统一编译至V530，后续还须验证最终候选的实际启动。不修改已应用V515/V516，不repair，不以空实现、忽略缺表或关闭守卫来换启动成功。

其余工作继续保留在[全平台计划](../01-规划/2026-09-07-全平台业务链审计与整改计划.md)及专项检查点，不在启动收口期间继续向外展开。本地原开发库和公司服务器尚未由本轮统一更新；没有commit、push、CI、发布或清空服务器业务数据。

## 当前进展入口

当前已进入V531最终全量验证；各组修正、完整FullChain、实际服务器备份副本演练和原开发库启动自动审批拦截，统一记录在[验收记录](2026-09-07-全平台本地审计与整改验收.md)。本页不再复制容易过期的版本、测试数字和“仍在执行”状态。

文档重写与合并已保留原件及映射，本轮不新增重复正式交接页。发布仍按同一提交本地完整验证、GitHub门禁、打包上传、手动服务器更新的顺序执行。
