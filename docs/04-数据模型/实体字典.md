# 实体字典

> **当前版本边界（2026-08-14）**：共享工作树 Flyway 目录最高 V289，共 270 个迁移文件、270 个唯一版本且无重复；V272 建立客户/模具/供应商真实“未分类”系统根并收紧主档分类完整性，V277/V278 收紧账户科目和系统过账角色 UUID，V279 建立全局业务标识注册，V280/V281 是人员删除与附件授权/元数据治理，V282 才是员工 7 项扩展 PII 的局部加密候选，V283 修正生产关联采购/委外订单在财务审批时的一次性货品快照锁定守卫，V284 保护个人信息变更敏感快照并清理历史审计副本，V285 收紧客户默认结算方式 UUID，V286 允许 bootstrap/legacy 员工保存不含证件号/主手机号的扩展-only 敏感行，V287 禁止无主身份密文却残留相应 HMAC/last4 派生值。V288/V289 已形成生产物料分析现货借用结构、终态守卫和审计覆盖，源码候选已接通 create/revoke 持久化、双趟生效计算和 Flutter UI；目标库/UAT/签名发布仍为 NO-GO。生产未来默认车间仍只认 V192 `production_goods_workshop_preferences`。开发原库 `uten_imp`
> 实测为 V244/installed_rank 225，本轮一次性隔离克隆已真实迁移到 V250/installed_rank 231；公司
> 目标库仍保留 V238 既有只读证据。下方
> V190/V195/V202/V234 等“候选”段落保留为当时设计快照，不覆盖当前版本
> 事实。生产迁移、远程访问、附件与剩余门禁见
> [2026-08-09 本地云端部署与生产就绪清单](../99-项目治理/2026-08-09-本地云端部署与生产就绪清单.md)。

## 2026-08-18 当前增量：客户收货地址簿与财务驳回（V300）

| 实体/关系 | 权威用途 | 不变量与边界 |
|---|---|---|
| `ClientShipAddress` / `client_ship_addresses` | 客户收货地址簿：出货/其它出货保存同事务学习本次地址+电话，开单按最近使用优先带出；多地址弹窗查看/新增/删除 | 同客户规范化地址唯一（`md5(lower(btrim(address)))`，软删不占位）；学习异常静默跳过不影响单据落库；插入 `ON CONFLICT DO NOTHING` 并发安全；**不回写客户主档**（主档维护是 `client:edit` 职责）；删除软删且须 `client_address:delete`（默认 DEPT_SALES+GM，超管恒有） |
| `sales_orders.finance_rejected` (+`_reason`/`_by`/`_at`) | 财务驳回事实：已审未确认订单被财务驳回待销售修正 | 驳回不改订单状态与库存预留；确认自动清除驳回标记（驳回→修正→确认闭环）；驳回原因必填并通知归属销售；徽标/待办口径只含未驳回件 |

- 进度看板 `OrderProgressRow.financeConfirmed`：销售端呈现层闸门（未确认显示「等待财务审核」），与 V294 计划侧准入谓词同口径但不改变其落点。
- 发运策略必选：`sales_orders.shipment_policy` 新单 create/update/approve 三落点拒绝空值与 `LEGACY_UNSPECIFIED`；`CUSTOMER_CONFIRM` 仅历史保留、不允许改回。

## 2026-08-16 当前增量：销售订单财务确认闸门（V294）

| 实体/关系 | 权威用途 | 不变量与边界 |
|---|---|---|
| `sales_orders.finance_confirmed` (+`_at`/`_by`/`finance_confirm_remark`) | 销售订货单财务确认事实：已审订单须财务确认后才对计划部可见/可排产 | 确认人资格=财务部门树在职+账号启用+持 `sales_order_finance:confirm`（`SalesOrderFinanceConfirmerEligibility` 实时判定，ADR-027 同型）；确认只放行计划可见性，不动库存不立账；确认单向不回头（红冲/取消由既有规则约束）；存量非草稿订单迁移回填为已确认 |

- 计划侧准入谓词（`finance_confirmed = TRUE`）落点：待排产列表/计数、缺料计数、计划选单带入、BOM 缺口转发研发（`ProductionScheduleService`）、物料分析候选与来源校验（`MaterialAnalysisService`）、MRP 订单展开与 BOM 环校验（`MrpService`）、排产-订单行关联写入校验（`ProductionPlanService`）。
- 通知链：审核 → `SALES_ORDER_PENDING_FINANCE_CONFIRM`（→财务确认人池，直达 `/finance/sales-order-confirmations`）；确认 → `SALES_ORDER_FINANCE_CONFIRMED`（→计划员接手物料分析）。均走 business_outbox。

## 2026-08-14 当前增量：UUID 关系、历史身份快照与终身业务标识（V257–V287）

| 实体/关系 | 权威用途 | 不变量与边界 |
|---|---|---|
| `goods_bom_items.audited_at/audited_by`（V256） | BOM 行「已核对无误」审计标记；非空即已审，组装信息页签据此绿色高亮 + 「已审」列 ✓ | 编辑行内容时服务端清空（需重新核对）；打标不算 BOM 变更（不重算 `sourceE`、不发 `GOODS_BOM_UPDATED`）；写入需 `goods:bom:audit`，已审状态对所有人只读可见 |
| `SettlementMethod` / `settlement_methods` | 老库 `B_PStyle` 的稳定 UUID 主档，供销售、采购、委外和 AR/AP 共用 | `legacy_code` 只快照；不能接到 `payment_styles`；正常新写只传 UUID，旧整数由服务端反派生。2026-08-16 起支持在线内联新增：`POST /api/master/reference-methods/settlement`（`payment_style:edit`，名称查重，编号走 `JS` 前缀 `master_code_sequences` 流水，状态默认「使用」，`sort_order=0`；销售单据编辑页「结帐方式」下拉的「添加」入口） |
| `FinancePaymentMethod` / `finance_payment_methods` | 独立 `RecStyle` 的稳定 UUID 主档，供收款、付款、其它收入/费用使用 | `legacy_name_confirmed=false` 的占位行不进入正常选择器/新写；不能按 M_Style 名称或整数猜映射 |
| `LegacyWarehouseWorkshopLink` / `legacy_warehouse_workshop_links` | 人工复核的仓库 legacy ID → 生产车间部门 UUID crosswalk | 旧 `B_Storage.WorkID` 属于 `Sys_Operator.ID`，不是车间；名称匹配禁止 |
| `Warehouse.workshopDepartmentId` | 当前仓库所属车间 UUID | 只能引用未删除且为 `DEPT_PROD` 直接子级的部门；旧 WorkID 只保留为 `legacyOperatorId` 影子 |
| `SystemMasterCategoryRegistry` / `system_master_category_registry` | 客户、模具、供应商系统“未分类”根的固定 UUID 注册表 | singleton 且不可更新/删除；运行时不再用 `legacy_id=-1` 或内部 code 判断系统身份 |
| `StockBalanceAdjustmentRequest` / `stock_balance_adjustment_requests` | 授权库存余额调整的命令 UUID 与生成 CHECK 单据 UUID 关系 | `request_key` 只做重试元数据；`source_doc_no` 只作历史显示，不能决定权限、幂等或删除 |
| `MasterCodeReservation` / `master_code_reservations` | 每个业务主档域内规范化编号的终身占用 | `upper(btrim(code))`；删除、软删、停用或改号不释放；只追加并审计 |
| `MasterCodeReservationMember` / `master_code_reservation_members` | 记录当前/历史上拥有过编号的 UUID 与 legacy 身份 | 历史重复登记为多个成员而不静默改码；任何新身份不得加入已有占用 |
| `BusinessIdentifierNamespace` / `business_identifier_namespaces` | 文档、主档和系统号的不可变命名空间与固定前缀权威 | V279 登记 37 个文档命名空间及 2 个系统命名空间：`VISITOR_ACCOUNT=V`、`PRODUCTION_EXECUTION_SEGMENT=ZX`；销售订单为 `XD`，`XS` 终身属于财务收款 |
| `BusinessDocumentSequence` / `business_document_sequences` | 按命名空间和上海业务日原子分配 6 位日流水 | 新单号格式 `PREFIXYYYYMMDDNNNNNN`；回滚/冲突可跳号，不追求无缝连号 |
| `BusinessPrefixReservation` / `business_prefix_reservations` | 固定前缀与分类显式 `code_prefix` 的全系统 token 保留 | 忽略大小写、去首尾空白后精确 token 互斥且终身保留；`V` 与 `V6` 可并存，`V` 与 `v` 冲突 |
| `BusinessIdentifierReservation` / `business_identifier_reservations` | 主档编号、文档号和系统号的全系统终身保留 | V276 主档成员和旧单号原样登记；新身份不能复用任何已占用完整标识 |
| `production_plan_items.product_no` | 生产计划行的用户可见业务编号 | V279 以 `plan_id` 为稳定逻辑所有者终身占号；显式非空值保留，空值由数据库按 `<计划单号>-NNN` 原子生成；草稿行重建可由同一计划恢复原号，不同计划不可复用，关系仍使用行 UUID |
| `production_execution_segments.segment_code` | 执行分段打印/扫码标识 | 系统命名空间 `ZX` + 8 位永久流水，由 `MasterCodeService` 原子分配；不可自传、不可改号，关系仍使用分段 UUID |
| `BusinessIdentifierConflict` / `business_identifier_conflicts` | 只追加的历史重复、前缀冲突与格式异常证据 | 保留历史原号和多成员，不静默改码；证据表不可改删 |
| `goods_*_snapshot`（销售/采购/仓库/委外明细） | 保存/审核时货品编号与名称事实 | 草稿可刷新、审核冻结；报表读快照；`BACKFILL_*` 不冒充原始历史 |
| V283 生产订单审批快照锁 | 允许生产/预排来源守卫保护中的采购、委外订货行在财务审批时完成一次性货品显示快照冻结 | `BEFORE UPDATE` 必须证明单头仍为草稿且只发生规定的单向快照锁定；延迟来源守卫仅放行这一精确转换，其余生产关联更新/删除继续失败关闭；当前仅为源码候选 |
| V284 个人信息变更敏感快照保护 | 防止审批 old/new 快照及其审计副本继续保存手机、户籍地址或紧急联系人明文 | `value_encoding` 是权威状态；在线敏感写只接受带领域标记的 `PGCRYPTO_V1`，且 INSERT/UPDATE 必须绑定精确事务级 codec capability，旧实例/旧 JAR 因而不能改变敏感行；历史 `LEGACY_UNKNOWN` 由 JVM 逐行验证/加密，带版本密文按 keyring 选 key，无版本旧 pgcrypto 只试当前 key、不猜旧 key，疑似密文不可解时失败关闭；历史审计只保留业务元数据，不保留 old/new/审批自由文本；发布必须停写排空旧实例并单版本切换，禁止旧 JAR 回滚；当前仅为源码候选 |
| V286 员工扩展-only 敏感行 | 让 V282 runner 为尚未登记证件号/主手机号但存在旧扩展 PII 的 bootstrap/legacy 员工创建真实目标行 | 只移除 `id_card_enc/phone_enc` 的数据库 `NOT NULL`，不填造身份数据、不新增明文或密文列；证件号/主手机号存在时仍走既有加密/HMAC，正常入职及补开账号仍由服务层要求两者齐备；无任何敏感值的员工可以没有 `employee_sensitive` 行；当前仅为源码候选 |
| V287 可选主身份派生一致性 | 防止扩展-only 行出现“无证件号/手机号密文却仍有 HMAC/last4”的不可解释状态 | `id_card_enc IS NULL` 时 `id_card_last4/id_card_hash` 必须都为空，`phone_enc IS NULL` 时 `phone_hash` 必须为空；不要求主身份存在、不新增加密字段；当前仅为源码候选 |
| `source_*_id`、`source_doc_id`、`plan_item_id` 等 | 报价/订单/发运/退货/生产/总账来源身份 | 单号仅搜索和展示；不得用于 JOIN、授权、幂等、红冲范围或反写 |
| `Account.styleId` / `accounts.style_id` | 账户当前会计科目 UUID | 活动账户必须指向启用的 ACCOUNT 末级；`style_legacy_id` 只由 UUID 派生，运行时不按路径、账户类型或旧整数推断 |
| `SystemPostingStyleRole` / `system_posting_style_roles` | 7 个固定总账过账角色到 `payment_styles.id` 的 UUID 映射 | 角色键/要求大类不可改删；映射目标必须启用、未删除且大类匹配，同一科目 UUID 不能承担两个角色。迁移期路径/名称只做一次性唯一定位，运行时只按角色键读 UUID |
| `GoodsListItem.colorId/unitId` | 选货品后直接带入订单/采购/仓库/委外/生产明细的颜色与单位 UUID | 不经 `legacyId→UUID` 二次桥接；UUID 缺失保持未选择，不能按名称或“第一条”补关系 |
| `GoodsImportReport.planId` | Excel detect→commit 的短期一次性能力 | 绑定操作者、文件 SHA-256、主档指纹和逐行 UUID/new-token 计划；五分钟过期、单次消费，服务重启或数据漂移后重新检测，不是可长期持久化的业务身份 |

## 2026-08-10 当前增量：计划前分析终态（V234/V237/V239 与 V247–V250）

| 实体/字段 | 权威用途 | 不变量与边界 |
|---|---|---|
| V237 的公开业务表审计 sweep | 为 V234 新增计划前分析表刷新且校验唯一、启用的 AFTER ROW INSERT/UPDATE/DELETE 审计触发器 | 只保护 V237 之后的未来写入，不补造历史审计事实 |
| `production_material_analysis_materials.required_qty`（V239） | 表示尚未被计划 link claim 的剩余需求；全量 claim 后允许归零 | `required_qty >= 0`；V239 修复整批生成计划到终态时被 V234 旧 `> 0` 约束拒绝的问题，不放宽其他数量守恒 |
| `goods_bom_items.control_stage/hard_gate` | 将组件归入 `START/ASSEMBLY/FINISH/SHIP/REFERENCE`；只有前三类可成为生产硬门槛 | `SHIP/REFERENCE` 只提示，不得阻止正式计划；历史行保持 `START + hard_gate=true` |
| `goods_bom_items.consumption_basis/basis_output_qty/allow_partial_package` | 表达 `PER_UNIT/PER_PACKAGE/FIXED_BATCH` 及包装基数、尾包策略 | 所有边按基本单位向上取 4 位；整包和固定批次不能用平均单耗近似 |
| `production_material_analysis_materials` 的 V247 快照列 | 冻结本次分析使用的阶段、计量边、父件累计量及 `allocated_start/finish/ship_qty` | 新分析用 `EDGE_RULE`；历史分析保留 `LEGACY_CUMULATIVE_PER_UNIT`，禁止从今天可变 BOM 反推旧边 |
| `production_material_analysis_items.ready_start/finish/ship_qty` | 分别表示可开工参考、可完工入库上限和预计可发参考 | `ready_now_qty = ready_finish_qty`；当前服务实现的 `ready_ship_qty` 等于 finish 参考投影，SHIP/REFERENCE 不参与硬门槛；正式计划只采用 `ready_finish_qty` |
| `production_material_analysis_items.parent_analysis_material_id` | `MAKE_COMPONENT` 子需求行回链到父产品 BOM 里那个 depth-1 自制物料节点（`production_material_analysis_materials.id`） | 仅 MAKE_COMPONENT 非空；用于"用于组装 {父件}"可见性与自底向上追溯；非新列，只读透传到 `ProductView.parentAnalysisLineId/parentGoodsName` |
| `production_material_demands.requirement_mode` | `LINEAR` 保存线性逐件需求；`EXACT_SNAPSHOT` 冻结非线性段的产品量、精确需求和 SHA-256 规则指纹 | 精确快照身份与数量不可原地改写；执行段数量必须与快照产品量一致 |
| `production_execution_segments.material_requirement_mode` | `DEMANDED` 必须有正式需求；`ZERO_MATERIAL` 是有证据的显式例外 | 缺需求行不能自动推断零物料；ZERO_MATERIAL 只允许 `DIRECT_MAKE`、`PLAN_BOM_OVERRIDE` 或 `NO_PRODUCTION_HARD_GATE`，不得有 DRAW/WAITING；前两类冻结分析/越权证据，第三类在插入时校验 BOM 存在且无生产硬门槛 |
| V250 的 preplan 来源守卫 | 冻结 action、allocation、采购申请/委外申请及后续订货行的生产来源谱系 | 直接来源一经外部化即不可拆除；订货草稿在 action 尚未推进时可编辑，批准、反向或 action 推进后由数据库拒绝通用改删 |

现行生产迁移序列为 V234/V237/V239 与 V247–V250：V237 补未来写入审计，V239 关闭全量 claim 的
终态约束缺口。V247 会使旧组合预览失效；V248/V249 把执行段“需要什么料/确实零料”固化为数据库事实；V250 只加固
来源链，不代表采购或委外自动形成商业订货。生产通知生成的是采购申请、委外申请或 MAKE child 任务，
采购/委外人员仍须从任务中心选择来源、供应商/委外商和仓库，保存并提交正式订货财务审批。

<!-- PRODUCTION-MATERIAL-ANALYSIS-V234-SOURCE-CANDIDATE -->
## 历史快照：2026-08-08 生产计划前物料分析实体补充

| 实体/字段 | 权威用途 | 状态与历史策略 |
|---|---|---|
| `goods.production_bom_policy` | 显式 `BOM_REQUIRED/DIRECT_MAKE/NOT_PRODUCED`，禁止从无 BOM 猜 DIRECT_MAKE | V234 已迁移；采购/委外旧来源保守为 NOT_PRODUCED，其余 BOM_REQUIRED，仍需业务复核 |
| `production_material_analyses` | 分析聚合、单目标仓、版本、输入/预览指纹 | `ACTIVE/PARTIALLY_PLANNED/COMPLETED/CANCELLED`；不是审批状态 |
| `production_material_analysis_items` | SALES_ORDER_ITEM、五种手工来源或系统 MAKE_COMPONENT 的 requested/submitted/approved 数量 | `submitted_qty + approved_qty <= requested_qty`；历史计划零回填 |
| `production_material_analysis_materials` | 全树平铺节点、item/父子/path/depth、建议与人工路线、立即/预计快照 | 读快照不是库存真相；依赖和路径不能丢 |
| `preplan_supply_actions` / `preplan_supply_action_allocations` | BUY/MAKE/SUBCONTRACT 备料动作、逐节点分摊和真实下游引用 | `OPEN/CREATED/IN_PROGRESS/DONE/CANCELLED`；通知不是权威，分摊总量等于 action 数量 |
| `production_material_analysis_commands` | PREVIEW/ROUTE/NOTIFY/GENERATE_PLAN 幂等命令账 | 同分析、操作、幂等键唯一；不代替业务主表 |
| `production_material_analysis_plan_links` | item 到计划草稿的分批数量归属 | `SUBMITTED/APPROVED/RELEASED/REVERSED`；身份/数量不可改删 |
| `production_plans.material_analysis_id/material_analysis_item_id` | 正式计划精确回指分析来源；保存无 BOM 越权原因/人员 | 两个来源字段同空或同属一个有效分析；普通 generate 仍不预留 |

本段记录 V234 刚引入时的源码候选证据。普通 generate 只创建计划草稿、`ACTIVE production_planning_drafts` 和 SUBMITTED plan link；`production_plan:approve` 才能按一个目标仓原子形成 READY、完整预留、DRAW 和销售分摊。历史 WAITING/AUTO_WAIT/DEFERRED 不回填、不参与新共享调配。当前版本状态以上方 2026-08-10 说明为准；目标库、历史对账、真实岗位/实物 UAT 和发布签字完成前生产保持 **NO-GO**。

<!-- PRODUCTION-PLANNING-V195-CURRENT -->
## 历史快照：2026-08-02 生产预排实体补充

| 实体/投影 | 权威用途 | 历史策略 |
|---|---|---|
| `production_planning_drafts` | 审核前不可变预排载荷与终结证据 | V191 零回填 |
| `production_goods_workshop_preferences` | 货品未来默认车间建议 | V192 零回填，不学习旧单 |
| `production_material_supply_pegs.supply_type=PRODUCTION_PLAN_ITEM` | MAKE 父需求到直接层子计划明细的精确供给挂接 | V194 不补旧 peg |
| `production_material_make_receipt_allocations` | FINISHED_IN 回供需求、预约和 DRAW 的不可变来源链 | V194 初始 0 行 |
| `production_execution_segments.auto_promote_when_ready` | 区分 `READY`、系统缺料 `AUTO_WAIT` 与人工延期 `DEFERRED`；`release-defer` 仅允许 DEFERRED 单向放行并立即重做齐套 | 只约束新计划包，不猜旧状态 |
| `production_execution_segment_events(event_type=RELEASE_DEFER)` | 记录人工放行的前后状态、版本和操作者 | V194 不补历史事件 |
| V195 fail-closed 审计触发器 sweep | 覆盖 V194 后新增业务表的未来 INSERT/UPDATE/DELETE | 不回填历史业务行或 `audit_log` |
| `ProductionWorkCardView` | 确认包的只读 A4 打印投影 | 非表、非事实源、零回填 |

本段的“目标库 V190、V191–V234 候选”是 2026-08-02 的历史版本状态；当前应读取本页顶部。
迁移成功仍不等于目标库运行验收或生产放行完成；真实岗位 UAT 和发布签字前为 **NO-GO**。

> ⏳ **随源码和数据库演进的活文档**。本档同时记录概念模型与落地状态；真实表结构以
> `server/src/main/resources/db/migration/` 的 Flyway 迁移为准。
>
> 状态必须严格区分“已落地”和“目标设计”。页面存在不等于后端实体/API 已存在。

---

## 一、使用说明

### 1.1 何时更新
- 设计一个新页面 → 把它需要的实体加进来
- 修改一个页面 → 同步修改涉及的实体
- 发现新关联 → 更新 [ER草图.md](ER草图.md)

### 1.2 实体描述模板

```markdown
### <实体名>（中文 / English）

**用途**：一句话说明这个实体是什么
**所属模块**：employee / payroll / expense / ...
**双写标记**：是否老系统已存在（→ 见 06-老系统融合）

**关键字段**：
| 字段 | 类型 | 说明 |
|---|---|---|

**关联**：
- 关联到 XX（多对一/一对多/多对多）

**业务规则**：
- 关键约束、状态机、计算规则

**待定问题**：
- 还没想清楚的地方
```

本档“**双写标记**”仅指新平台向老系统的受控单向影子兼容，不是公司本地库与阿里云库双写。
本地/云端数据库严格遵守 ADR-031 的单主 + 异步物理热备，分区期间不双边写。

---

## 二、实体清单（按模块分组）

### 2.1 认证与账号（auth）

| 实体 | 中文 | 状态 |
|---|---|---|
| User | 用户账号 | ⏳ 待补 |
| Role | 角色 | ⏳ 待补 |
| Permission | 权限点 | ⏳ 待补 |
| LoginSession | 登录会话 | ⏳ 待补 |

### 2.2 组织架构（organization）

| 实体 | 中文 | 状态 |
|---|---|---|
| Department | 部门/组织节点 | ✅ 已落地 |
| Employee | 员工 | ✅ 已落地 |
| Position | 部门岗位目录 | ✅ 已落地 |
| EmploymentHistory | 任职记录（入职/调岗/离职/复职） | ✅ 已落地 |

### 2.3 工资（payroll）

| 实体 | 中文 | 状态 |
|---|---|---|
| PayrollBatch | 月度工资批次；部门/全公司范围，DRAFT→SUBMITTED→APPROVED/REJECTED→PUBLISHED | 🟡 V133 表和真实 API 源码已落地；生产验收未完成 |
| PayrollSlip | 员工月度工资快照；记录发布/查看/下载时间 | 🟡 V133 表和个人/全量读取、查看、PDF 下载 API 源码已落地 |
| PayrollItem | 工资项快照（收入/扣除，来源为薪酬快照或期间变量） | 🟡 V133 表和草稿批次生成源码已落地；变量输入入口未完成 |
| PayrollVariableInput | 员工期间加班、奖金、社保、公积金、税及其它精确输入 | 🟡 V133 表结构已落地；政策相关金额不由系统猜算 |
| PayrollApproval | 独立工资审批轨迹 | ⏳ 未落地；V133 先在 `payroll_batches` 保存提交/批准/驳回/发布主体与时间 |

### 2.4 报销（expense）

| 实体 | 中文 | 状态 |
|---|---|---|
| ExpenseClaim | 员工报销申请；DRAFT/SUBMITTED/REVIEWING/APPROVED/REJECTED/PAID | 🟡 V133 表和真实 API 源码已落地；当前为单阶段审批，生产验收未完成 |
| ExpenseClaimItem | 员工报销明细 | 🟡 V133 表结构已落地；金额、日期、行号与类别有数据库约束 |
| ExpenseCategory | 员工报销类别 | ✅ V133 以受约束枚举落地（交通/差旅/餐饮/办公/通讯/招待/培训/其它），不是独立主档表 |
| ExpenseApproval | 独立员工报销审批轨迹 | ⏳ 未落地；V133 先在 `expense_claims` 保存提交/批准/驳回/支付主体与时间 |

> **不得混用**：后端 `finance_expenses` 是钱流模块的一般费用单，审核会影响账户、对账和总账；
> 它不是员工提交的 `ExpenseClaim`。V133 已使用独立的 `expense_claims` /
> `expense_claim_items`；进入 `PAID` 时要求同事务创建并唯一关联一张已批准的
> `finance_expenses`，该关联用于幂等与会计交接。是否真正满足这一约束仍须通过服务端和数据库测试证明。

### 2.5 通知与建议（communication）

| 实体 | 中文 | 状态 |
|---|---|---|
| Notice | 通知公告（含工作通知：任务/审批/流程；字段见下） | ✅ 已落地（前端模型 + 后端表 `notices`，V92） |
| NoticeUserState | 通知每用户状态（已读 read_at / 删除 deleted_at；后端表 `notice_user_states`，V92） | ✅ 已落地（替代原拟 NoticeReadRecord——删除语义并入同表） |
| NoticeAcknowledgment | 通知回执「点击收到」（每用户幂等；后端表 `notice_acknowledgments`，V224） | ✅ 已落地（ADR-025；acknowledge 模式） |
| NoticeBlessing | 通知祝福「送上祝福」（每用户一条可改可撤；后端表 `notice_blessings`，V224） | ✅ 已落地（ADR-025；bless 模式，含 sender_name 快照） |
| Suggestion | 员工建议 | ✅ 已落地（前端模型 + 后端表 `suggestions`，V96） |
| SuggestionReply | 建议回复 | ✅ 已落地（后端表 `suggestion_replies`，V96；回复接口可顺带推进状态） |
| SuggestionLike | 建议点赞（每用户一票，复合主键） | ✅ 已落地（后端表 `suggestion_likes`，V96） |

> 建议列表返回 1-based `PageResponse`，按 `submitted_at DESC,id DESC` 稳定分页；列表 DTO 用
> `replyCount` 表示回复数而不加载完整回复，页内点赞态/点赞数/回复数批量聚合。建议状态只允许
> `submitted → reviewing → resolved/rejected`；点赞与回复都锁定 Suggestion 主行后变更。

### 2.6 实验室（lab）

| 实体 | 中文 | 状态 |
|---|---|---|
| LabTest | 检测记录 | ⏳ 待补 |
| LabSample | 检测样品 | ⏳ 待补 |
| LabReport | 检测报告 | ⏳ 待补 |
| LabEquipment | 实验室设备 | ⏳ 待补 |

### 2.7 设备控制（hvac）

| 实体 | 中文 | 状态 |
|---|---|---|
| HvacDevice | 空调设备 | ⏳ 待补 |
| HvacCommand | 控制指令 | ⏳ 待补 |
| Building | 楼栋 | ⏳ 待补 |
| Floor | 楼层 | ⏳ 待补 |

### 2.8 生产（production）

| 实体 / 表 | 中文与关键口径 | 状态 |
|---|---|---|
| `ProductionMaterialAnalysis` / `production_material_analyses` | 正式计划前分析聚合；目标仓、版本、指纹及 ACTIVE/PARTIALLY_PLANNED/COMPLETED/CANCELLED | 🟡 V234 已迁移；目标库历史对账与岗位 UAT 待完成 |
| `ProductionMaterialAnalysisItem` / `production_material_analysis_items` | 销售/手工/系统 MAKE_COMPONENT 需求行；requested/submitted/approved 与 ready_start/finish/ship 守恒 | 🟡 V234 已在本地原库；V247 仅隔离克隆验证，正式计划上限取 ready_finish |
| `ProductionMaterialAnalysisMaterial` / `production_material_analysis_materials` | 全树平铺节点、路径/父子归属、路线、阶段/包装边和三个齐套字段的分配快照 | 🟡 V234 已在本地原库；V247 仅隔离克隆验证；当前 ship 是等于 finish 的参考投影，不是独立出货门禁或物理预留，非线性边不得按平均单耗还原 |
| `PreplanSupplyAction` / `preplan_supply_actions` | BUY/MAKE/SUBCONTRACT 计划前备料动作，真实下游引用 | 🟡 V234 已迁移；V250 来源守卫仅在隔离克隆验证；通知不是权威且不自动生成商业订货 |
| `PreplanSupplyActionAllocation` / `preplan_supply_action_allocations` | action 到分析节点的精确数量分摊；总量守恒 | 🟡 V234 已迁移；业务对账待完成 |
| `ProductionMaterialAnalysisCommand` / `production_material_analysis_commands` | PREVIEW/ROUTE/NOTIFY/GENERATE_PLAN 幂等结果账 | 🟡 V234 已迁移；岗位 UAT 待完成 |
| `ProductionMaterialAnalysisPlanLink` / `production_material_analysis_plan_links` | 分批 SUBMITTED/APPROVED/RELEASED/REVERSED 计划来源 | 🟡 V234 已迁移；普通 generate 零预留，岗位 UAT 待完成 |
| `ProductionPlan` / `production_plans` | 父生产计划；真实自制件子计划也各自是一张计划 | ✅ 已落地 |
| `ProductionPlanItem` / `production_plan_items` | 计划产品明细及数量累计；`fqty` 是已审核报工量，`iqty` 是已审核 `FINISHED_IN` 入库量，二者不得互相代替 | ✅ 已落地；计划结清按 `qty-iqty<=0` 派生，不以报工完成代替入库完成 |
| `ProductionPlanningPackage` / `production_planning_packages` | 一次 V1 排产确认的指纹、版本和幂等身份 | ✅ V150 |
| `ProductionPlanningDraft` / `production_planning_drafts` | V191 审前不可变预排建议；每计划最多一个 `ACTIVE`，只可转 `APPLIED/SUPERSEDED`；审核同事务下达后关联正式计划包，草稿本身不是库存/采购/委外/DRAW/执行事实 | 🟡 V191 已迁移；目标库历史对账与岗位 UAT 待完成 |
| `ProductionGoodsWorkshopPreference` / `production_goods_workshop_preferences` | V192 每产品一条可修改的未来默认车间建议，含选择次数、最后选择人/时间；首次成功下达或执行段后续成功 assign 时学习，多车间/空车间/replay 不学习；不替代执行分段历史 | 🟡 V192 已迁移；岗位 UAT 待完成 |
| `SubplanLink` / `subplan_links` | 父计划到另一张自制件子计划的谱系；不是执行分段 | ✅ V126/V176 |
| `ProductionExecutionSegment` / `production_execution_segments` | 父计划明细按齐套、车间和日期拆出的执行从属；V249 显式冻结 DEMANDED/ZERO_MATERIAL | 🟡 V155/V194 已迁移；V249 仅隔离克隆验证；新分析链只生成 READY，AUTO_WAIT/DEFERRED 属历史兼容 |
| `PlanProgressRow`（只读投影） | `reportedQty=Σfqty`、`inboundQty=Σiqty`；物料状态只从 `CONFIRMED` 包和正式执行段投影，`NOT_PLANNED/LEGACY_UNSUPPORTED/DATA_ERROR/WAITING/PARTIAL/READY` 与报工、入库进度分列 | ✅ 当前查询已分离；不得用分析快照、未来供给或 `fqty` 伪造正式齐套/入库进度 |
| 计划详情 `trace`（只读投影） | 当前只含销售订单来源、DRAW/FINISHED_IN 库存单、采购申请和委外申请 | 🟡 **部分溯源**；不含采购/委外订单、收货、IQC 批次、报工、发运的统一时间线，禁止标注“全链路已完整” |
| `ProductionMaterialDemand` / `production_material_demands` | 执行分段逐物料基本单位需求；V248 区分 LINEAR 与不可变 EXACT_SNAPSHOT | 🟡 V150 已迁移；V248 仅隔离克隆验证，非线性包装需求不能由六位平均率重建 |
| `ProductionMaterialSupplyPeg` / `production_material_supply_pegs` | 采购/委外未来供给挂接；V194 增加 MAKE 直接层子计划明细到父需求的精确挂接；不等于当前现货或质量合格 | 🟡 V150/V154/V158/V194 已迁移；真实供给 UAT 待完成 |
| `ProductionMaterialMakeReceiptAllocation` / `production_material_make_receipt_allocations` | V194 的不可变 FINISHED_IN 回供来源、预约与 DRAW 分摊；红冲追加对称事实 | 🟡 V194 已迁移且初始 0 行；真实回供/红冲 UAT 待完成 |
| `ExecutionSegmentSalesAllocation` / `execution_segment_sales_allocations` | 执行段到销售订单行的冻结数量归属 | ✅ V157 |
| `ProductionMaterialStockEvent/Posting` | 追加式领料/良品退料事实与逐需求分摊 | ✅ V152/V161 |
| `ProductionMaterialSettlementEvent/Posting` | 追加式消耗、损耗、报废、合法在制清料事实 | ✅ V152/V161 |
| `ProductionLine` / `ProductionShift` | 产线、班次和有限产能主数据 | ⏳ 目标，尚未落地 |
| `SalesShipment.warehouse_work_*` | 销售出货的单据级待拣、拣货中、已拣、异常、交接出库及作业人/时间 | 🟡 V187 最小状态已落；不是独立 WMS 数量台账 |
| `WarehousePickLine` / `QualityDisposition` | 库位/批次/序列号/实拣/复核/装箱，以及待检/合格/隔离处置 | ⏳ P0 目标，尚未落地 |
| `SupplierHeldStock` / `SubcontractWip` | 供应商处我方物料、耗用、损耗、余料和期末结存 | ⏳ 委外 P0 目标，尚未落地 |

V191–V195 对老数据零回填：不从历史计划明细 `department_id` 猜测车间，不为历史已审/草稿/终态计划补造预排草案、计划包、MAKE peg、收货分摊或人工放行事件。V193/V195 只重跑审计覆盖 sweep，不补历史业务行或审计行。历史事实原样保留、只读回查；新表和新状态只由迁移后用户主动预排、正式下达、人工放行或后续派工生成。

MAKE 子计划只聚合本计划包当前直接 BOM 层缺口。A→B→D 时 A 只建 B，进入 B 计划再建 D；`subplan_links` 不表示父包已递归生成全部层级。

### 2.8.1 计划需求 → 采购/委外 → 财务 → 仓库（V196–V202 已迁移，待生产验收）

| 实体 / 表或视图 | 中文与关键口径 | 状态 |
|---|---|---|
| `PurchaseRequest/Item` / `purchase_requests/items` | 物料分析 `notify` 按用户选中 BUY 缺口形成的采购需求；采购端只读，不含供应商、价格、币种和结算事实 | 🟡 既有表 + V196–V200 权限/流程已迁移；后续由采购人员从任务中心人工分解商业订单，岗位 UAT 待完成 |
| `SubcontractApplication/Item` / `subcontract_applications/items` | 物料分析 `notify` 按用户选中 SUBCONTRACT 缺口形成的委外需求；委外端只读，不允许普通 CRUD/审核/红冲 | 🟡 既有表 + V196–V200 权限/流程已迁移；后续由委外人员从任务中心人工分解商业订单，岗位 UAT 待完成 |
| `v_procurement_decomposition_tasks` | 只显示 `open_qty>0` 的申请明细；待财务审批订单也先占来源容量 | 🟡 V199 已迁移；真实任务量对账待完成 |
| `WorkflowResponsibilityAssignment` / `workflow_responsibility_assignments` | **V229 已删除**（见 ADR-027） | 审批人改由 `finance_order_approval:review` + 财务部门资格决定；`procurement_order_approval_cases.assignee_*`/`procurement_arrival_exceptions.finance_assignee_*` 保留为历史快照，V229 起可空、新行为 NULL |
| `ProcurementOrderApprovalCase/Event` / `procurement_order_approval_cases/events` | 审核组资格审批实例与追加式历史（V229 起 assignee 列可空）；财务通过才把采购/委外订单 `status=1` | ✅ V196 已迁移；审核人只在精确 PENDING 任务上临时读取对象隐藏的详情，决定与响应同事务，结束后恢复普通对象范围 |
| `InboundExpectation/Item` / `inbound_expectations/items` | 财务批准订单后形成的未来到货及行快照 | ✅ V196 已迁移；不是实物、库存、AP 或质检结论 |
| `ProcurementArrivalException/Event` / `procurement_arrival_exceptions/events` | 超财务批准余量的隔离、决定与追加式事件（V229 起 finance_assignee 列可空） | ✅ V201 已迁移；实物 UAT 待完成 |
| `SupplierReturnTask` / `supplier_return_tasks` | 财务未批准数量交原下单人确认退回供应商 | ✅ V201 已迁移；不能改财务决定或扩大入库量 |
| `ProcurementInspectionItem` / `procurement_inspection_items` | V222 采购/委外共用待检 sidecar；按 receipt 类型/头/行记录收货基本量、PASS/FAIL 累计与 `PENDING/PARTIAL/RESOLVED/REVERSED` | 🟡 最小 IQC 已落；每次部分 PASS 即时写合格 `DIR_IN` 并刷新活动分析，整单终态才正式推进累计供给；不是完整 QMS |
| `ProcurementInspectionEvent` / `procurement_inspection_events` | `RECEIVED/PASS/FAIL/PRODUCTION_WOKEN/RECEIPT_REVERSED` 追加式质量/推进事实与幂等来源 | ✅ V222 append-only；禁止 UPDATE/DELETE，FAIL 不写库存，红冲只反向已 PASS 量 |

关键状态不可混用：

```text
申请 status=1（计划已下达）
  → 订货草稿 status=0 + 审批 PENDING
    → 财务 APPROVED 后订货 status=1 + 预计到货 OPEN
      → 超量时异常 PENDING_FINANCE，库存/AP 仍不变
```

历史 `status=1` 订货不补造审批记录、不重放生效副作用；V202 重新扫描全部 `public` 业务表的审计触发器契约，不是只针对上述三张异常表，也不补历史审计。V196–V202 已包含在开发原库 `uten_imp` V244 和公司目标库 V238；不得因迁移已部署就写成岗位/实物/IQC 已生产验收。

### 2.9 库存（stock / warehouse）

| 实体 / 表 | 中文与关键口径 | 状态 |
|---|---|---|
| `Goods` / `goods` | 货品主档；库存统一使用货品 UUID，不再使用旧草案 `Material`。`auto_created=true` 仅为历史 FK 身份锚，选择器/BOM/MRP 不得作为正常货品使用 | ✅ 已落地（V32/V177/V181） |
| `Warehouse` / `warehouses` | 仓库主档；参与核算与不良品仓口径见即时库存文档 | ✅ 已落地 |
| `StockBalance` / `stock_balances` | 当前余额；唯一粒度“仓库 + 货品 + 颜色”，含数量、余额金额、重量、最后变动时间 | ✅ 已落地 |
| `StockMovement` / `stock_movements` | 追加式库存流水；方向 +1/-1，来源单头/明细可回查 | ✅ 已落地 |
| `StockDocument` / `stock_documents` | 统一仓库单据头；`doc_type=CHECK` 承载普通盘点与授权余额调整 | ✅ 已落地 |
| `StockDocumentItem` / `stock_document_items` | 单据明细；盘点用 `qty`、`count_qty`、`surplus_qty` 保留前值、目标值和差额 | ✅ 已落地 |
| `StockReservation` / `stock_reservations` | 销售审核后的订单有效预留或 READY 生产物料占用；减少 ATP/自由库存但不改变在手，销售 `warehouse_id=NULL` 表示尚未定发货仓；部分交接只把本次消耗拆成仓级已消费行，未发余量仍全局；余额修正不回写预占。V298 起新增 `PREPLAN_ANALYSIS/PREPLAN_MATERIAL` 归属形态：分析来源备料 IQC PASS 入库即绑定来源分析（统一可用量口径剔出公共现货，归属分析服务层加回，下达计划包时转移给正式需求，红冲/取消/撤回对称释放，历史不回填，见 ADR-039） | ✅ 已落地 |

授权余额调整不是第二套库存实体：服务端以
`source_doc_no=AUTHORIZED_BALANCE_ADJUSTMENT:<idempotencyKey>` 标识一张自动审核的 `CHECK`，
并在同一事务写 9/10 流水和余额。物理结构以 [仓库设计](../数据迁移/17-仓库管理-新库与迁移.md)
为准，权限、API、审计和历史边界见[盘点修正文档](../数据迁移/50-仓库盘点修正与历史单据处理.md)。

### 2.9.1 财务资产与待摊（finance asset，V123/V140 + V183）

| 实体 / 表 | 中文与关键口径 | 状态 |
|---|---|---|
| `FixedAsset` / `fixed_assets` | 固定资产主档；V183 增类别、来源、关键日期、四类科目快照、归属/责任、会计/运行状态和乐观版本。提交后会计字段冻结 | 🟡 专业安全骨架已落地；激活/处置受默认门禁保护 |
| `DeferredExpense` / `deferred_expenses` | 长期待摊主档；与供应商预付款分域，只有未来受益已经成立才可进入待摊流程 | 🟡 专业安全骨架已落地；初始确认/终止受默认门禁保护 |
| `FinanceAssetCategory` / `finance_asset_categories` | 固定资产/待摊分类型类别与政策，保存生效日、方法、期限、残值、四类总账科目和必备资料代码 | 🟡 已落地；企业政策仍须财务批准 |
| `FinanceAssetBook` / `finance_asset_books` | 固定资产账簿；隔离 `CORPORATE` / `TAX` 金额、寿命、方法、科目与政策快照 | 🟡 CORPORATE 结构与月度路径已落；TAX 仅结构预留，无可用 API/GL |
| `FinanceDeferralScheduleVersion` / `finance_deferral_schedule_versions` | 待摊计划版本；未来变更建新版本，不改写已确认历史 | 🟡 已落地 |
| `FinanceDeferralScheduleLine` / `finance_deferral_schedule_lines` | 逐期摊销计划行；版本金额守恒，最后一期吸收尾差，已过账行只可反向 | 🟡 已落地 |
| `FinanceAssetApprovalStep` / `finance_asset_approval_steps` | 资产领域专用、只追加的制单/复核证据 | 🟡 已落地；**不是**跨模块通用 `ApprovalRecord` |
| `FinanceAssetEvent` / `finance_asset_events` | 调拨、运行状态、处置/终止等只追加业务事实 | 🟡 事件骨架已落；初始确认/处置/终止专用反冲未交付 |
| `FinanceAssetAccountingPeriod` / `finance_asset_accounting_periods` | 资产子账开放/关闭及受控重开原因 | 🟡 已落地 |
| `FinanceAssetPostingRun` / `finance_asset_posting_runs` | 月度运行头：类型、期间、输入指纹、算法版本、状态、职责人、凭证和反冲引用 | 🟡 CORPORATE 折旧/摊销与正常批次反冲已落地 |
| `FinanceAssetPostingLine` / `finance_asset_posting_lines` | 逐资产账簿/计划行计算快照，含期初/本期/期末、科目和部门/成本中心快照 | 🟡 已落地；GL 尚无权威部门/成本中心维度 |
| `FaDepreciationLog` / `fa_depreciation_log`、`DaAmortizationLog` / `da_amortization_log` | V123 报表兼容事实表；V183 关联正式批次并以“未反冲有效行”约束唯一性 | 🟡 已落地；禁止删除重建 |

类别的四类科目引用 `payment_styles`；正式月度运行通过 `finance_asset_posting_runs.gl_voucher_id`
追溯 `gl_vouchers/gl_entries`。已过账运行、逐项快照、日志和正式凭证不可原地修改或删除，错误必须
写关联原批次的反向事实。当前没有老库资产业务数据，历史期初/累计折旧摊销/剩余期限能力也未交付，
不得用 legacy destructive 脚本或手工 SQL 回填。

完整模型、状态机和生产阻断见
[51 · 资产与待摊专业化全链路](../数据迁移/51-资产与待摊专业化全链路.md)及
[ADR-018](../99-决策记录-ADR/ADR-018-资产与待摊专业子账及不可变过账.md)。

### 2.10 系统（system）

| 实体 | 中文 | 状态 |
|---|---|---|
| AuditLog | 审计日志 | 🟡 主体及 V183–V185 已迁移；历史全量对账、留存与生产运行验收待完成 |
| SystemConfig | 系统配置 | ⏳ 待补 |
| Attachment | 附件（通用） | 🟡 附件结构增量到 V255；共享工作树总目录已到 V289/270，开发原库 `uten_imp` 到 V244、一次性隔离克隆到 V250，公司目标库仍为 V238 既有证据（V240 表与权限；V243 `storage_key` 唯一/正大小；V244 保存服务端实际校验对象的 OSS `versionId`/ETag；V255 增隔离上传、扫描、删除 Outbox 与对账状态；上传授权绑定用户+业务对象+key+类型+大小，确认时服务端校验内容并计算 SHA-256；各业务域执行对象级查看/管理权限，下载按点击时授权并固定精确版本）；V280/V281 只治理授权和附件元数据，V282/V284/V286/V287 也没有对附件二进制内容加密，V288/V289 与附件二进制保护无关；真实 OSS、恶意内容扫描和目标部署仍为生产 NO-GO |

`attachments.storage_version` 在 V244 中为兼容本地存储和迁移前数据而保持可空；这不表示生产 OSS 可以读取 latest。`prod`/`cloud` profile 已 fail-fast 要求 `provider=oss`、`requireVersioning=true` 和 HTTPS endpoint，且校验、下载、删除缺少有效 `versionId` 时拒绝。切换真实 OSS/云端前，目标库必须满足：

```sql
SELECT count(*) FROM attachments WHERE storage_version IS NULL;
-- 发布门禁：结果必须为 0；否则隔离，或人工核验对象精确版本与服务端哈希后回填。
```

生产仍为 **NO-GO**：OSS PostPolicy/等价上传大小硬门禁、staging/final、AV 隔离、删除 outbox、孤儿上传配额/清理/对账，以及真实 OSS/CORS/RAM/恢复验收尚未完成。

### 2.11 审批流程（approval，跨模块通用）

> 本节是员工报销 / 工资条的**可配置多级审批目标模型**，当前后端尚未落地。V133 使用各业务表上的
> 显式状态、操作人和时间戳实现固定流程，不等于通用 `ApprovalNode/ApprovalRecord` 已完成。设计决策见
> [全局机制 §三](../05-架构/全局机制.md#三审批流建模可配置多级)。

| 实体 | 中文 | 状态 |
|---|---|---|
| ApprovalNode | 审批节点定义（可配置流程） | 🧭 仅目标设计，未建表/未实现 |
| ApprovalRecord | 审批记录（轨迹的一格） | 🧭 仅目标设计，未建表/未实现 |

---

### 2.12 基础资料（master，Phase 2+ 后端已落地）

| 实体 | 中文 | 状态 |
|---|---|---|
| ClientCategory | 客户分类树 | ✅ V35；老库业务树 10 根/40 节点/最大 level 2，V272 候选追加受保护系统“未分类”根后为 11 根/41 节点 |
| Client | 客户主档 | ✅ V36，老库 B_Client（260 条，34 字段）；V121 +credit_floor；V272 候选将 6 条未分组客户及后续官网/财务兜底绑定系统根并令 `category_id NOT NULL` |
| SupplierCategory | 供应商分类树 | ✅ V37；老库 15 个扁平业务根，V272 候选追加系统“未分类”根后为 16 根 |
| Supplier | 供应商主档 | ✅ V38，老库 B_Provider（386 条，29 字段，全有业务分类）；V120 +is_internal_workshop；V272 候选令 `category_id NOT NULL`，财务占位绑定系统根 |
| MaterialCategory | 货品分类树 | ✅ V31（同构，未单列详情） |
| Goods | 货品主档 | ✅ V32，老库 B_Goods；V177 标记 31 个 `auto_created` 历史锚，V181 禁止其进入活动 BOM/MRP；当前列表/详情以 `color_id/unit_id` 直接返回关系 UUID，legacy 列仅溯源 |
| GoodsBomItem | 当前货品 BOM 父件→组件关系 | 🟡 V79 已落地；有效源迁移 198,022，V181 后活动 198,025（含新系统手工 3）、stub 端点 0；源端 reject 20,798 仍待治理 |
| MouldCategory | 模具分类树 | ✅ V33；老库 65 个扁平业务根，V272 候选追加系统“未分类”根后为 66 根 |
| Mould | 模具主档 | ✅ V34，老库 B_Mould（1605 条）；V272 候选将 19 条悬空分类模具绑定系统根并令 `category_id NOT NULL` |
| Color | 颜色主档 | ✅ V39，老库 B_Color（151 条，**扁平**非树）；货品当前关系为 `goods.color_id -> colors.id`，`color_legacy_id` 只作迁移影子 |
| Unit | 基本单位主档 | ✅ V40，老库 B_Unit（66 条，扁平）；货品当前关系为 `goods.unit_id -> units.id`，`unit_legacy_id` 只作迁移影子 |
| SettlementMethod | 销售/采购/委外/AR-AP 结帐方式 UUID 主档 | ✅ V273 源码候选；老库 `B_PStyle` 8 条固定种子，Number 重复不作键 |
| FinancePaymentMethod | 钱流收付款方式 UUID 主档 | ✅ V273 源码候选；精确 `RecStyle.Name` 需从 legacy CSV 确认后才可正常选择 |
| MasterCodeReservation / Member | 业务主档编号终身占用与历史成员 | ✅ V276 建立同域保留；V279 将该成员证据原样纳入跨文档/主档的全局完整标识保留 |
| BusinessIdentifier Registry | 全局命名空间、前缀 token、日流水、完整标识和冲突证据 | ✅ V279 源码候选；只追加终身保留，历史冲突不改码，新写由数据库守卫 fail-closed |
| SystemPostingStyleRole | 总账系统过账角色 UUID 映射 | ✅ V278 源码候选；7 个角色身份固定，映射至启用且大类匹配的 `payment_styles.id`，缺失映射时过账失败关闭 |

> 基础资料货品/模具/客户/供应商为「分类树 + 主档」双表同构（邻接表 parent_id + 物化路径 path 触发器 + legacy_id 溯源）；V272 候选为模具/客户/供应商各建立 `legacy_id=-1` 的真实系统“未分类”根，根本身不可改、移动或删除，但仍可作为父级新增普通子类。**颜色/单位为扁平字典**（无分类树，仅 编号/名称/状态 + legacy_id）。
> 前端入口均为 `/basicinfo`；货品表「主颜色/单位」由后端直接返回 `colorId/unitId` 与名称。legacy 值只允许历史只读显示和受控迁移，不能在表单或单据选择器中反向生成 UUID 关系。
> 客户/供应商字段级映射见 [数据迁移/06-09](../数据迁移/)；货品/模具见 02-05；颜色/单位见 10-13。

## 三、已细化的实体（按页面填充）

> 以下实体随页面设计逐步细化。空着 = 还没设计到对应页面。

### ApprovalNode（审批节点定义 / Approval Node）

**用途**：定义某个审批流程由哪些节点、按什么顺序组成（可配置）。
**所属模块**：approval（跨模块通用）
**双写标记**：视具体业务（报销/工资条）而定

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| flowType | enum(`expense`/`payroll`) | 所属流程 |
| order | int | 节点顺序，从 1 开始 |
| name | String | 节点名（如「直属主管审批」）|
| approverPermissionCode | String? | 审批权限点（如 `expense:approve`），与固定审批人策略组合使用 |
| approverId | String? | 可选固定审批人（指向 Employee）|

**业务规则**：
- 同一 `flowType` 下 `order` 唯一且连续
- 默认节点必须由业务审批制度确认，不能从旧角色名推导
- 流程可按部门/金额配置不同节点数（如 >1万 加一级）

---

<a id="approvalrecord"></a>

### ApprovalRecord（审批记录 / Approval Record）

**用途**：某条业务单据（报销单/工资条）审批轨迹的一格，记录谁、在哪个节点、做了什么决定。
**所属模块**：approval（跨模块通用）
**双写标记**：审批结果属敏感管理数据，按需双写

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| entityType | enum(`expense`/`payroll`) | 关联的业务类型 |
| entityId | String | 关联的业务单据 ID（报销单/工资条批次）|
| nodeOrder | int | 第几节点（对应 ApprovalNode.order）|
| approverId | String? | 实际审批人（指向 Employee）|
| decision | enum(`pending`/`approved`/`rejected`) | 决定 |
| comment | String? | 审批意见 |
| actedAt | DateTime? | 审批时间（UTC）|

**关联**：
- 多条 ApprovalRecord 属于一条 ExpenseClaim / PayrollBatch（按 entityType + entityId）
- ApprovalRecord → Employee（审批人，多对一）

**业务规则**：
- 目标通用审批模型可按 `nodeOrder` 形成时间线，但当前没有 `UtenTimeline`，也没有落地通用
  `ApprovalNode/ApprovalRecord` 表；工资和报销仍使用各自固定状态及操作时间字段。
- 当前节点 decision=pending 表示「待审批」，高亮
- 驳回是终态还是允许修订重提必须由流程模板显式定义
- 末节点 approved 只推进为业务“已批准”；报销付款和工资发布仍是独立权限与独立动作
- 状态变更必须使用数据库行锁/版本条件之一，并由集成测试证明并发安全

**待定问题**：
- [ ] 审批人离职/调岗时，pending 节点如何转交（代理审批）
- [ ] 是否支持加签/会签（多人同节点）—— Phase 2 先不支持

---

### ProfileChangeRequest（个人信息修改申请 / Profile Change Request）

**用途**：员工自助修改个人信息时提交的待审申请。一次提交可包含多个字段，共用 `batch_id`；HR 整批批准 / 驳回。
**所属模块**：profile（跨 employee / auth 边界）
**双写标记**：否（首次引入；未来接老系统影子双写时再加）

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| employee_id | UUID (FK→employees) | 目标员工 |
| batch_id | UUID | 同批次多字段共用 |
| field_code | VARCHAR(64) | 机器码（phone / fullName / emergencyContact.0.phone…） |
| field_label | VARCHAR(128) | i18n 标签快照（HR 端直接展示，不依赖客户端语言） |
| field_group | VARCHAR(32) | identity / contact / address / emergency / compensation / org |
| old_value_enc | TEXT | 旧值快照；存储形态由 `value_encoding` 权威标记，不按列名或冒号猜测 |
| new_value_enc | TEXT | 新值快照（必填）；敏感字段必须为带领域标记的版本化 pgcrypto 密文 |
| value_encoding | VARCHAR(24) | `PLAIN` / `PGCRYPTO_V1`；`LEGACY_UNKNOWN` 仅允许存在于 V284 启动回填完成前，未完成时读取、审批和状态更新失败关闭 |
| status | VARCHAR(16) | pending / approved / rejected / cancelled / applied |
| submitted_by | UUID (FK→employees) | 提交人 = 员工本人 |
| submitted_at | TIMESTAMPTZ | 提交时间（UTC） |
| reviewed_by | UUID (FK→employees) | 审批人 |
| reviewed_at | TIMESTAMPTZ | 审批时间 |
| review_comment | TEXT | 审批意见（驳回必填） |
| employee_version | INT | 提交时 employees.version 快照；审批时校验 |
| idem_key | VARCHAR(64) UNIQUE | 客户端幂等键，防重复提交 |

**关联**：
- N → 1 Employee
- 1 个 batch 由 N 条记录共用 batch_id

**业务规则**：
- 字段策略分三档（`ProfileFieldPolicy`）：
  - **directEdit**：民族 / 政治面貌 / 婚姻 / 现住址 / 办公电话 / 邮箱 / 工位 —— 提交即生效
  - **requiresReview**：姓名 / 户籍地址 / 手机 / 紧急联系人 —— HR 通过后合并
  - **hrOnly**：性别 / 出生日期 / 工作地 / 薪资 / 银行 / 身份证 —— 员工不可自助申请
- 乐观锁：审批时校验 `employee.version == row.employee_version`，不一致 → 409
- 24h 内同字段 `pending / approved / applied` 记录数 > 0 → 拒绝重复提交
- 审批人为本人 → 403
- 审批快照保护策略与审核策略分离：手机、户籍地址和全部 `emergencyContact.*` old/new 快照走带领域标记的版本化 pgcrypto 密文；姓名虽需审核但仍为 `PLAIN`。
- V284 新写由统一 codec 编码/解码，敏感 INSERT/UPDATE 还必须绑定 `app.profile_change_snapshot_codec=v1` 事务级能力，数据库因此拒绝旧实例/旧 JAR 的状态变更；历史敏感行先标记 `LEGACY_UNKNOWN`，逐行验证/加密成功后才转 `PGCRYPTO_V1`。带版本密文按当前/历史 keyring 选 key；无版本旧 pgcrypto 只尝试当前 key且不猜测旧 key，无法判定或解密时失败关闭并保留原值。发布必须停写、排空旧实例后单版本切换；禁止新旧滚动并存及旧 JAR 回滚，失败只做前向修复或恢复与旧版本完整一致的隔离验证数据库备份。
- 通用审计不复制 old/new 密文或明文；V284 定向清除历史 `profile_change_requests` / `profileChangeRequest` 审计行中的 old/new 快照键和 `review_comment`，保留对象、状态、操作者和编码状态等业务元数据。

**状态机**：
```
pending  ──approve──>  applied    (字段已合并到 employees / employee_sensitive / emergency_contacts)
pending  ──reject ──>  rejected
pending  ──cancel ──>  cancelled   (员工主动撤销)
applied  ──（终态）──>  applied
```

**待定问题**：
- [ ] 是否需要"修改后再改"二次自动确认
- [ ] HR 驳回后是否允许员工一键"复用旧值 + 仅修改一项"快速重提
- [ ] 通用 ApprovalRecord 接入（多级审批）留待 Phase 7+

---

**用途**：公司全员档案，人事、工资、组织的数据根。
**所属模块**：organization（employee）
**双写标记**：是（老系统核心实体，→ 见 [数据迁移](../数据迁移/README.md)）

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| code | String | 工号（唯一，如 E001）|
| fullName | String | 姓名 |
| gender | enum(`male`/`female`) | 性别 |
| birthDate | DateTime? | 出生日期 |
| idType / idNumber | String | 证件类型/号码；号码按 `employee:pii:view` 明文或脱敏 |
| phone | String | 手机（脱敏 `138****1234`）|
| email | String? | 邮箱 |
| departmentId | UUID | 所属部门 |
| positionId | UUID? | 岗位 |
| supervisorId | UUID? | 直属主管（指向 Employee）|
| hireDate | DateTime | 入职日期 |
| status | enum(`active`/`probation`/`onLeave`/`resigned`) | 员工状态 |
| employmentType | enum(`regular`/`dispatch`/`intern`/`outsource`) | 用工性质 |
| contractStart/End | DateTime? | 合同起止 |
| baseSalary | num? | 基本工资；按 `employee:compensation:view` 读取 |
| bankAccount | String? | 银行账号；按 `employee:pii:view` 读取 |
| userId | UUID? | 关联系统账号 User |

**关联**：属于一个 Department；担任一个 Position；有直属主管 Employee；一个 User 账号；多条 EmploymentHistory。

**业务规则**：
- 工号 `code` 全公司唯一；入职时只由服务端分配，请求中的旧 `code` 不参与写入
- 身份证号唯一 + 格式校验
- 敏感读取由 `employee:pii:view` / `employee:compensation:view` 决定，不按角色名。
- V141 起敏感写入另需 `employee:pii:edit` / `employee:compensation:edit`；普通
  `employee:create/edit` 不能授权手工请求修改证件、联系方式、银行或薪酬字段。
- 状态机：入职 → `probation`/`active` → `onLeave` ↔ `active` → `resigned`；复职复用原员工档案，写入 `rehire` 任职事件并恢复账号，不新建员工记录。
- 普通更新禁止改变部门；跨部门必须走调岗。调岗/离职日期不得晚于今天、早于入职日或早于最新任职事件；只有已离职员工可归档。

**待定问题**：
- [ ] 一人能否兼多部门（兼任）—— Phase 2 不支持，单部门
- [x] 重新入职复用旧档案，并追加 `rehire` 事件

---

### Department（部门 / Department）

**用途**：公司组织架构树，员工归属、生成范围、通知范围的基础。
**所属模块**：organization（department）
**双写标记**：是

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| code | String | 编码（如 DEPT-PROD）|
| name | String | 部门名 |
| parentId | UUID? | 上级部门（顶级为 null）|
| managerId | UUID? | 负责人（指向 Employee）|
| sortOrder | int | 排序 |
| headcount | int | 历史冗余字段；页面和人员概况不得作为权威人数，使用未软删在册员工实时聚合 |

**关联**：父子 Department（自引用）；多个 Employee 属于它；一个负责人 Employee。

**业务规则**：
- 树结构，`parentId` 不能成环（不能选自己/子孙为上级）
- 删除：仅叶子部门且无员工可删，否则提示先转移员工
- 负责人必须是本部门直属且状态为 `active/probation/onLeave` 的员工，并支持显式清空
- 负责人调岗、离职或档案归档时自动解除，避免悬空引用
- 管理中心可承载直属员工、岗位和负责人，但父级关系仍是不可移动的结构基线
- 管理中心负责人可在有限成员权限面板管理本中心和子孙部门；普通部门负责人仅本部门。唯一授权事实是 `managerId`，不从岗位名称推导

**待定问题**：
- [ ] 多厂区场景 Building ↔ Department 怎么关联（见 ER 待定）

---

### Position（岗位 / Position）

**用途**：轻量的部门岗位目录，挂在可用人组织下，员工可选择担任。它不是独立定编席位，当前不支持多任职、缺编或代岗。
**所属模块**：organization

**关键字段**：id / code / name / departmentId / level（`领导层` / `班组管理` / `员工` / HR 自定义）/ sortOrder / isDeleted。
`level` 只用于分类、展示和列表排序；负责人标记来自 `departments.manager_id`，任何岗位名称/职级都不自动授权。入职选自定义名称时，后端在相同组织内按规范化名称锁定、复用或创建。
**关联**：属于一个 Department；多个 Employee 担任。

---

### EmploymentHistory（任职记录 / Employment History）

**用途**：员工职业轨迹事件（入职/调岗/离职/复职），员工详情时间线和人员流动统计的数据源。
**所属模块**：organization
**双写标记**：是

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| employeeId | UUID | 员工 |
| eventType | enum(`onboard`/`transfer`/`resign`/`rehire`) | 入职/调岗/离职/复职 |
| fromDeptId/fromPositionId | UUID? | 调岗前（transfer 用）|
| toDeptId/toPositionId | UUID? | 调岗后 / 入职部门岗位 |
| eventDate | DateTime | 事件日期 |
| remark | String? | 备注（离职原因、调岗说明）|

**关联**：属于一个 Employee。
**业务规则**：按 eventDate 倒序展示；入职、调岗、离职和复职命令自动写事件。
事件日期必须按员工生命周期单调前进；缺失事件会使人员概况的历史覆盖标记失败并隐藏离职率。

---

> Production/Inventory 的当前实体以本字典、Flyway 迁移和对应业务页为准。Lab/HVAC 尚无生产实体、API 或设备通道；原纯 Mock 模型已于 2026-08-09 删除，不作为数据模型。

---

### Notice（通知公告 + 工作通知 / Notice）

**用途**：员工端统一收件箱的一行——既承载公司广播（公告/制度/福利/系统/紧急），也承载工作平台事件（任务下发/审批结果/上游完成）。
**所属模块**：communication（通知）
**前端模型**：`lib/features/notice/models/notice.dart`
**后端落地**：表 `notices`（V92 建表、V94 补审计列、V149 增加指定接收范围）+ `server .../features/notice/`（Notice / NoticeService / NoticeController / NoticeAudienceService）；接口 `/api/notices`

**关键字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String (UUID) | 主键 |
| title | String | 标题 |
| content | String | 正文（多行） |
| type | enum(`announcement`/`policy`/`benefit`/`system`/`urgent`/`task`/`approval`/`workflow`) | 类型；后 3 种为**工作类**（`type.isWork`） |
| priority | enum(`normal`/`important`/`urgent`) | 重要度，默认 normal；驱动卡片样式与到达弹出通道 |
| publisher | String | 发布人姓名快照（后端取当前登录员工 `employees.full_name`，发布后改名不回溯） |
| publishedAt | DateTime | 发布时间 |
| isRead / readAt | bool / DateTime? | 已读状态与阅读时间（**每用户**，存 `notice_user_states`） |
| topPriority | bool | 是否置顶 |
| attachments | List\<String\> | 附件文件名数组（`notices.attachments` JSONB；通用二进制附件见 Attachment（V240）） |
| audienceScope | enum(`all`/`selected`) | 全员动态广播 / 指定范围接收人快照 |
| audienceSummary / audienceCount | String / int? | 发布时生成的范围摘要和指定范围去重后人数；全员人数为 null |
| targetDepartmentIds | JSONB UUID[] | 发布时直接选择的部门 id，解析包含后代部门 |
| targetEmployeeIds | JSONB UUID[] | 发布时直接选择的员工 id |

**每用户状态（notice_user_states）**：复合主键 (notice_id, user_id)；`read_at` 已读时间、`deleted_at` 删除时间。全员通知无记录 = 未读未删；指定范围通知在发布时预创建空状态行，该行同时表示接收资格。删除是「从自己列表移除」语义（微信式），不影响其他收件人。

**V224 增量（庆典互动，ADR-025）**：
- `type` 枚举新增 4 类庆典：`birthday`/`anniversary`/`wedding`/`newborn`（`isCelebratory`）。互动模式 `interactionMode` 按类型派生：庆典→`bless`、公告广播(announcement/policy/benefit/system/urgent)→`acknowledge`、工作类→`none`。
- 新增列：`interaction_mode`、`subject_employee_id`(FK employees SET NULL)、`subject_name`/`event_label`(快照)、`blessing_templates`(JSONB，发布者勾选的祝福模板，仅 `{name}` 占位符)。
- 新增表 `notice_acknowledgments(notice_id,user_id,acked_at)` 复合主键幂等（ack「点击收到」）；`notice_blessings(id,notice_id,user_id,sender_name,content,审计列,UNIQUE(notice_id,user_id))`（bless「送上祝福」，一人一条可改可撤）。两表 `ON DELETE CASCADE` 跟随 notices。
- 庆典类强制 audience=all（全员可祝福）；`CelebrationScheduler` 每日 08:00 按 `birth_date`/`hire_date` 月日自动发布（toggle 在 system_settings，birth_date PII 仅服务端读）。

**业务规则**：
- 工作类（task/approval/workflow）由业务系统产生（任务下发、审批流转、上游节点完成），不走人事发布页；公告类由 [通知发布页](../03-页面/通知发布页.md) 人工发布（需 `notice:publish`）
- 发布页类型→重要度映射：紧急类型=urgent、置顶=important、其余=normal
- 指定范围可组合多个部门与人员；服务端递归展开部门子树、合并有有效账号的人员并去重，发布后调岗不回溯历史可见性
- 列表排序：置顶优先，其余按发布时间倒序；可见性、未读过滤和计数在数据库执行，列表当前最多返回 500 条；全部已读走集合更新，单次批量删除最多 500 条
- 到达提醒按 priority 分派（`lib/features/notice/providers/notice_arrival.dart`）：urgent/important → 屏幕正中弹窗（UtenNotify.alert），normal → 顶部弹条（UtenNotify.banner）；接收端触发待推送/WebSocket 通道

---

## 四、双写标记汇总

> 老系统已有的实体（需双写）。详见 [../数据迁移/README.md](../数据迁移/README.md)

| 实体 | 老系统对应 | 双写状态 |
|---|---|---|
| ⏳ | ⏳ | 待老系统调研 |

---

## 五、命名约定（数据层）

- 实体名：UpperCamelCase 单数（如 `Employee`，不是 `Employees`）
- 字段名：lowerCamelCase（Dart 风格）
- 枚举：lowerCamelCase（如 `status.active`）
- 时间字段：UTC 存储，字段名用 `xxxAt`（如 `createdAt`、`approvedAt`）
- 布尔字段：`is`/`has` 前缀（如 `isActive`、`hasPermission`）
- 外键：`<实体名>Id`（如 `departmentId`）
- 主键：统一 `id`（UUID），老系统兼容字段用 `legacyId`

---

## 六、通用字段（每个业务实体都该有）

```dart
class BaseEntity {
  final String id;              // UUID
  final DateTime createdAt;     // 创建时间（UTC）
  final DateTime? updatedAt;    // 更新时间（UTC）
  final String? createdBy;      // 创建人 ID
  final String? updatedBy;      // 更新人 ID
  final bool isDeleted;         // 软删除标记
  final String? legacyId;       // 老系统 ID（如有双写）
}
```

---

## 七、Phase 2 后端落地实体（Spring Boot + PostgreSQL，已建表）

> 以下实体随"人事模块端到端"在 `server/` 用 Flyway 建表落地（UUID 主键、TIMESTAMPTZ UTC、软删除、审计列）。机密字段 pgcrypto 字段级加密，DTO 按 `employee:pii:view` / `employee:compensation:view` 等权限点脱敏，不按角色名判断。

### User（登录账号 / 与 Employee 1:1）
`id, employee_id(unique FK), login_account(unique, 默认=手机号), password_hash(Argon2id), must_change_password(首登强制改), status(active/locked/disabled), is_super_admin, auth_version, failed_attempts, locked_until, last_login_at, last_password_changed_at, remote_access, temp_password_expires_at(V297)`
> 登录不开放注册；账号随受 `employee:create` 与敏感写权限保护的入职流程创建，或由 `account:support`
> 持有人在员工详情页/权限管理页补开（账号=手机号、初始密码=证件号后 6 位、首登强制改）。
> 账号状态以 `users.status` 为准。`temp_password_expires_at` 仅由管理员「设置临时密码」写入
> （now+72h，到期拒登），改密成功清空；入职/补开初始密码不写该列。

### Role / Permission / RolePermission（历史兼容 + 权限目录）
`Role(id, code[employee/hr/finance/lab/production/manager/admin], name, is_system)` · `Permission(id, code[如 employee:view], name, category)` · `RolePermission(role_id, permission_id)` M:N。
> V29 起角色不再参与有效权限合成。现行权限 = 全员基础 ∪ 部门配置（含上级）± 个人覆盖；
> 只有 `superAdmin` 享有全量。角色表仅作历史兼容。

### UserRole（用户↔角色 M:N，历史兼容）
`user_id, role_id`。V29 起不决定有效权限，仅保留历史数据/兼容 claim。

### DepartmentPermission / UserPermissionOverride / UserDataScope（现行授权配置）

- `department_permissions(department_id, permission_id, created_at, created_by)`：部门直接权限；员工读取所属
  部门及祖先部门配置的并集。`audit_log:view/export` 禁止部门配置。15 个业务部门的默认权限由
  `V212__department_permission_matrix.sql` 作为单一权威来源种子化（受控重置：先 DELETE 再 INSERT，
  `ON CONFLICT DO NOTHING`），明细见 [部门默认权限矩阵](../数据迁移/54-部门默认权限矩阵.md)；
  3 个管理中心与公司节点保持空白，避免向下越权继承。
- `user_permission_overrides(user_id, permission_id, effect)`：`grant` 个人加授，`revoke` 从合成结果回收。
- `user_data_scopes(user_id, scope, owner_employee_id, created_at, created_by)`：`scope` 为
  `goods/client/sales`（owner 列 `owner_employee_id`）或 `purchase/subcontract/production_plan/stock_doc`
  （V233；owner 列为各单据表的 `maker_id`），表达除本人和公共数据外可额外查看+可编辑的归属人。
  采购/委外/生产/库存单据的对象级归属列 = `maker_id`（create 即填当前员工）；老数据 maker_id 为空 =
  公共可读、普通不可写。`*:view:all`（`purchase/subcontract/production_plan/stock_doc`）持有人或超管看全部。
- `authorization_state(singleton_id=1, epoch, updated_at)`：共享授权版本；与 `users.auth_version` 一起写入
  staff access JWT，并在每次请求复查。个人授权变化提升用户版本，部门树/部门权限等共享变化提升全局 epoch。
- `users.remote_access`（V241）：员工是否允许经云端入口访问，默认 `false`；它是账号部署边界，**不属于**
  `permissions` 目录，也不参与上面的部门/个人权限集合运算。cloud 在员工登录签发前、refresh 轮换前和
  每个已认证业务请求三处复核，未授权返回 `403 REMOTE_ACCESS_DENIED`；visitor 是独立公网 OTP 例外。
  仅超管且持 `authorization:manage` 可调用 `PUT /api/admin/users/{id}/remote-access`。授权和撤权两个方向
  只要值发生变化，都会 bump `auth_version`、撤销该用户全部 refresh token并记审计，要求重新登录；
  撤权后旧 access 先因版本不匹配返回 401，旧 refresh 在 cloud 返回 403。本地入口另由公司 CIDR 门禁
  保护，不能把“local 实例”本身当作可信来源。

最终有效权限 = 全员基础 ∪ 所属部门及祖先部门配置 ∪ 个人 grant − 个人 revoke；超级管理员直接拥有
完整权限目录。以上写接口同时要求 `authorization:manage` 和 `principal.superAdmin`。

### RefreshToken（不透明刷新令牌）
`id, user_id, token_hash(sha256, unique), device_info, issued_at, expires_at(7d), revoked_at, replaced_by`。轮换 + 重用检测。

### PasswordHistory（改密防重用）
`id, user_id, password_hash, changed_at`。改密时校验最近 5 条。

### AuditLog（审计日志，bigserial）
`id, actor_id, actor_account, action, target_type, target_id, before(jsonb), after(jsonb), ip, user_agent,
result, request_id, event_source, http_method, http_path, status_code, duration_ms, risk_level(生成列),
event_category(生成列), client_event_id, device_installation_id, device_name, device_manufacturer,
device_model, device_platform, device_os_version, app_version, app_build, device_form_factor,
device_browser, device_locale, device_time_zone, device_time_zone_offset_minutes, device_is_physical,
client_event_at, device_capture_status, device_profile_hash, created_at`。
> 进入应用的 GET/POST/PUT/PATCH/DELETE/HEAD `/api/**` 由请求过滤器与 MVC 拦截器覆盖；匿名认证、
> CORS 非法来源 403、401、404/405 和 MVC 前异常也由外层兜底。JWT 完整校验成功后将 actor 快照绑定
> 到本次 request，使安全上下文清理后的 404/405 仍能归属账号；无效或过期令牌不会绑定身份。
> 登录、刷新、访问拒绝等由 `AuditService` 显式写；数据变更由 DB 触发器写。
> `request_id` 串联请求和触发器明细。V185 后由通用/高敏函数新写的 before/after 会移除迁移列明的凭证、
> PII、薪资、迁移列明的原因备注类敏感文本和大型嵌套快照字段；业务名称、编号等调查必需字段仍保留。
> V185 不重写旧行，历史记录仍须受限访问并专项扫描。
> V172 的设备字段是服务端收到并清洗后的**客户端声明快照**：`device_installation_id` 是应用安装实例
> 首次运行生成的随机 UUID，不是 IMEI、MAC、硬件序列号；`device_profile_hash` 只用于核对同一份
> 清洗后快照，不构成物理设备认证。`device_capture_status` 取
> `present/partial/invalid/missing/legacy`，Web 或旧版本拿不到的字段必须留空并标明状态，不得猜测。
>
> `audit_log` 与冷归档 `audit_log_archive` 都只允许系统写入。产品不提供人工修改或删除 API；
> 在线记录只由留存任务先归档后删除，归档记录只在总期限到达后由同一任务永久删除。

### LocalAuditReceipt / DeviceAuditProfile（客户端本机证据，非数据库实体）

`DeviceAuditProfile` 保存当前安装实例的随机安装标识、设备名称/厂商/型号（平台可提供时）、平台、
系统版本、应用版本/构建号、设备形态、浏览器、语言、时区和是否物理设备。`LocalAuditReceipt`
按 `client_event_id` 保存请求方法、去除查询值后的路径、开始/完成时间、结果、状态码、
`server_request_id`、重试轨迹和当时的设备快照；不保存请求正文、查询参数值、令牌或业务数据。

- 本机最多保留最近 300 条，并按服务器公开的“在线期 + 追加归档期”总月数自动清理；
- 产品界面和业务 API 不提供手动清除、修改或批量枚举能力，只能凭完整 `client_event_id` 精确核查；
- 精确核查目标回执前必须在线调用服务端授权接口；授权成功并写入 `verify_local_audit_receipt` 后才读取
  本机回执，无权限、离线或授权失败时不读取；
- 本地完整性标记可发现常见的内容损坏或改写，但密钥仍位于本机，不是远程证明或不可抵赖签名；
- 卸载应用、清除站点/应用数据、系统存储清理、设备重置或安全存储异常都可能使本机证据消失；
- 因此本机回执只能与服务器时间、Request ID、安装标识和设备快照交叉佐证，不能单独证明实际操作者
  或宣称“绝对真实”。

审计日志详情和本机回执入口仅向持 `audit_log:view` 的指定核查人员开放；普通员工与访客没有入口，
深链和后端接口均须拒绝。任何审计数据导出还必须同时持有 `audit_log:export`。

### Employee 拆分（七子实体，最小化加密面）
- **employees**（核心身份+组织+用工，**不含加密 PII**）：code(工号,unique), fullName, gender, idType, birthDate, ethnicity, politicalStatus, maritalStatus, hujiAddress, residenceAddress, departmentId, positionId, supervisorId(自引用), hireDate, confirmedAt(转正日期≠试用期结束), status, employmentType, workLocation, seatNo, attendanceGroup, officePhone, email, paperArchiveNo。
- **employee_sensitive**（0..1:1，pgcrypto）：id_card_enc, id_card_last4, id_card_hash, phone_enc, phone_hash, bank_account_enc, bank_branch_enc，以及 V282 的 huji_address_enc, residence_address_enc, email_enc, birth_date_enc, marital_status_enc, political_status_enc, office_phone_enc。V286 后 bootstrap/legacy 员工可只有实际存在的扩展密文，`id_card_enc/phone_enc` 可为空；V287 要求主密文为空时对应 HMAC/last4 也为空。无任何敏感值可无行。正常入职/补开账号仍要求证件号和主手机号齐备；读取明文要求 `employee:pii:view`。
- **employee_compensation**（1:1，pgcrypto）：base_salary_enc, perf_salary_enc, social_insurance_base_enc, housing_fund_base_enc, allowance_standard_enc, social_insurance_location。读取要求 `employee:compensation:view`。
- **emergency_contacts**（1:N）：name, phone_enc, relationship。
- **employee_vehicles**（1:N，V211/ADR-021）：plateNo, plateNorm(大写去空白,唯一/搜索索引), vehicleType, brandModel, color, remark。**全部非必填**；`employee:view` 可见，员工列表搜索按车牌命中返回 `matchedPlates`（按车牌找人）。
- **employee_phones**（1:N，V211/ADR-021）：label, phone_enc, phone_hash。备用手机号（不参与登录）；读取明文要求 `employee:pii:view`，写入要求 `employee:pii:edit`；员工自助 `/api/profile/me/phones` 直改。
- **employee_contracts**（1:N，驱动续签次数=count(*)）：contractType, startDate, endDate, probationMonths, signOrder, remark。
- **employee_education**（1:N, 可选）、**employee_credentials**（1:N, 可选）。
- **employment_history**：eventType(onboard/transfer/resign/rehire/**confirm**——V210 新增转正事件), from/to 部门岗位, eventDate, remark。

### HrTaskClaim（HR 任务软认领，V210/ADR-021）
`task_type(confirm/birthday/anniversary/newhire), employee_id, claimed_by, claimed_at, lease_until(24h), released_at`。
部分唯一索引保证同一任务同一时间至多一条未释放认领；租约过期读取时惰性失效；任务本体仍动态计算不落库。

### TaskClaim（统一任务软认领，V215/ADR-023；2026-08-07 守卫接入）
`target_type(EXPENSE_APPROVE/PURCHASE_DECOMPOSE/SALES_ORDER_APPROVE/FULFILLMENT_TASK), target_key(目标 id 串), claimed_by, claimed_at, lease_until(按类型 15~120 分), last_heartbeat, released_at, released_by, release_reason`。
与 HrTaskClaim 同范式（部分唯一索引 + 租约 + 惰性失效），泛化到池化审批/分解场景，show-as-locked：他人认领显示「XX 处理中」、动作端点 `requireNoActiveClaimByOther` 兜底 409。认领权限按类型对齐动作端点真实权限（`sales_order:edit`/`purchase_order:edit`/`expense:approve`/`stock_doc:edit`，2026-08-07 修正错配）。

### 主档乐观锁版本列（V231，2026-08-07）
`clients/goods/suppliers` 各加 `version BIGINT NOT NULL DEFAULT 0`，实体 JPA `@Version`。编辑 DTO 回传 `version`、`update` 显式 `OptimisticLocks.requireUpToDate` 校验 + flush 自动兜底；不符 → 409「该记录已被他人修改」。简单查照类主档按同模式按需追加。

### Department（增 level + 物化路径）
`id, code(unique), name, parent_id(自引用), level(公司/决策层/管理中心/一级部门/二级班组/三级科室), manager_id, sort_order, path(物化路径,触发器维护), headcount`。子树查询走递归 CTE 或 `path LIKE`；`headcount` 不作为页面统计权威，负责人规则同上。

### WorkforceOverview（组织人员概况查询读模型，非数据库表）

由 `GET /api/org/departments/{id}/workforce-overview` 返回，范围包含所选组织及全部下级：

- 标识与期间：`organizationId/name/level`、`asOf`、`periodStart`、`periodMonths`；
- 当前人数：`directCurrentEmployees/currentEmployees/activeEmployees/probationEmployees/onLeaveEmployees`；
- 流动：`hiredEmployees/rehiredEmployees/departedEmployees/transferInEmployees/transferOutEmployees`、
  `openingHeadcount/averageHeadcount/turnoverRatePct/netChange`；
- 提醒：合同、试用期的逾期与未来 30 天到期数量；
- 质量：`historyCoverageComplete/missingHistoryRecords/dataQualityNote`。

离职率按期初与期末平均在册人数估算；历史缺失或流量守恒异常时 `turnoverRatePct=null`，界面显示 `—`。历史组织范围按当前部门树重述，不是组织结构的历史快照。

### BusinessIdentifier 注册表（V279 当前候选；V76 为历史取号基线）
> 销售、采购、仓库、委外、财务/资产、生产和研发共 37 个文档命名空间由后端 `DocNumberService.nextNumber(...)` 权威生成，**不接受客户端传入**。另有 `VISITOR_ACCOUNT=V` 与 `PRODUCTION_EXECUTION_SEGMENT=ZX` 两个 8 位流水系统号；完整映射见 [ADR-036](../99-决策记录-ADR/ADR-036-全局业务标识与单据号命名空间.md)。

**关键字段**：
| 表 / 字段 | 类型 | 说明 |
|---|---|---|
| `business_identifier_namespaces.namespace_key` | TEXT PK | 不可变的业务命名空间；服务按枚举名查找，不把前缀硬编码到 Flutter |
| `fixed_prefix` | TEXT | 全局终身保留的固定前缀；归一后不重复 |
| `business_document_sequences.sequence_date` | DATE | PostgreSQL `Asia/Shanghai` 业务日 |
| `last_seq` | BIGINT | 本 `(namespace_key, sequence_date)` 已发最大顺序，1–999999 |
| `production_product_no_sequences.plan_id` | UUID PK/FK | 计划行自动编号的稳定所有者与并发锁键；后缀 1–999999，不承载计划行关系 |
| `business_identifier_reservations.normalized_identifier` | TEXT PK | 文档号、系统号和主档编号的全局终身占用 |

**现行业务规则**：
- 格式 `PREFIXYYYYMMDDNNNNNN`，例如 `CD20260814000001`；制单人工号/姓名不入号码，人员以 UUID 关系和审计单独保存。
- 取号用原子 UPSERT，事务回滚或旧号冲突可跳号；不保证无缝连号。
- `create()` 由服务端生成；`update()` 不重取号。Flutter 只读显示“保存后自动生成”，不 peek 下一号。
- 访客号和生产执行分段号由 `MasterCodeService` 按固定前缀原子分配；8 位是硬上限，耗尽时失败关闭。
- 计划行 `product_no` 可承载用户业务语义，但以 `plan_id` 为终身所有者：显式值继续使用；留空时服务端调用数据库函数生成 `<计划单号>-NNN`，销售订单导入、物料分析、报工补产和 MRP 自制子计划均不自行拼号；同计划草稿重建可恢复，不同计划禁止复用。
- 旧 `PREFIXYYMMNNNN` 与其它历史格式原样保留，归一后登记为占用/冲突证据，新格式只用于切换后新建。
- `CJ` 继续属于采购收货，仓库产成品进仓为 `CR`；销售订单为 `XD`，`XS` 终身属于财务收款。

### SalesOrder / SalesShipment 发运控制（V187）

| 实体 | 字段 | 权威语义 |
|---|---|---|
| `SalesOrder` | `shipmentPolicy` | `LEGACY_UNSPECIFIED / ALLOW_PARTIAL / REQUIRE_COMPLETE / CUSTOMER_CONFIRM`；历史只标兼容，不补造选择 |
| `SalesOrder` | `partialShipmentConfirmedAt/By/Reason` | 客户对当前订单分批的确认事实；改量、取消和订单红冲失效并清空 |
| `SalesShipment` | `warehouseWorkStatus` | 新流程 `PENDING_PICK → PICKING → PICKED → SHIPPED`；`EXCEPTION → PENDING_PICK` 需要原因；历史草稿 `LEGACY_PENDING` |
| `SalesShipment` | `pickingStartedAt/By`、`pickedAt/By`、`handedOverAt/By` | 记录单据级仓库动作；只要状态为 `SHIPPED` 就禁止普通红冲直接增加库存，历史缺 `handedOverAt` 仅表示时间未知 |
| `SalesShipment` | `warehouseWorkUpdatedAt/By`、`warehouseExceptionReason` | 最近作业责任人与异常事实；数据库触发器和请求审计共同留痕 |

V187 没有新增独立可变 `warehouse_work` 数量表，意味着状态仍以一张出货单为粒度。2026-08-09 的口径规定并已在订单服务源码对齐：销售订单只以 `amount_original=qty×price×discount` 为新单金额事实，`exchange_rate` 与 `amount_local` 仅作历史兼容，保存和销售审核（当前无独立提交态）不依赖汇率；出货从来源订单重建客户、币种、税率、付款方式、销售员、价格、折扣、单位换算及本次原币金额，在 `SHIPPED` 锁定财务汇率后才计算发运本币并形成 AR 开账快照。客户端汇率、金额或成本不是会计事实。本地定向回归已通过，目标环境全链、数据与约束核验及岗位 UAT 仍未完成；财务已审单仍须先反审才可编辑、删除或驳回，无价格权响应由服务端清空商业字段。完整折扣/税额结算和分批舍入尾差引擎尚未落地。

### 销售出货 → 应收 → 收款 → 总账实体链（V236–V238 / ADR-030）

| 实体 / 表 | 关键事实 | 权威边界 |
|---|---|---|
| `ar_ap_ledger` | 应收币种、原币/本币开账额、开账汇率、已收、费用冲销、未收原币、到期日和结清状态 | 销售订单审核不写；仓库 `SHIPPED` 与库存/预留/订单回写同事务创建正式 AR |
| `ar_ap_source_refs` | AR 对应的发运、一个或多个销售订单及本次原/本币来源快照 | 追加来源，不按单号、客户或日期猜历史关联；合并发运不得压成单个订单号 |
| `finance_receipts` / `finance_receipt_lines` | 收款客户/账户、AR 引用、应收币种、到账原币、到账汇率、到账本币、费用冲销、账面冲减、汇兑差和核销前后原币余额 | 应收币种只读；每行显式填正到账汇率，服务端重算本币/汇兑；审核强制制单人与审核人分离 |
| `finance_reconciliations` 与账户余额字段 | 收款审核后的真实账户流水和余额变化 | 与 AR 核销同事务更新；收款红冲对称恢复，不由页面直接改余额 |
| `gl_vouchers` / `gl_entries` | `AR_POST/RECEIPT/COST_CARRY` 等 AUTO 总账投影 | 不在 `SHIPPED` 或收款审核时由同一服务即时生成；财务按期间生成/重建并校验借贷平衡，业务红冲后须重跑相关期间 |

订单待收计划只是从已审核销售订单读取的经营视图，不属于上述正式会计实体链。历史空币种、无原币余额或不可信汇率必须失败关闭并人工确认，不得用默认汇率 `1` 合成新事实。

### SalesShipment 仓库事件账（V188）

| 实体 / 表 | 关键字段 | 权威语义 |
|---|---|---|
| `SalesShipmentWarehouseEvent` / `sales_shipment_warehouse_events` | `shipment_id/from_status/to_status/reason/actor_employee_id/occurred_at` | 创建、仓库转换、异常/恢复、驳回、兼容审核和受控反向的追加式时间线；数据库拒绝 UPDATE/DELETE |

V187 当前字段是快速读取投影，V188 是不可变证据。V188 不补造历史状态时间线；一单多库、行级实拣差异、批次/库位/序列号、复核、装箱和承运件级事件仍须由后续 WMS 子账承载。

### 委外发料数量权威边界（2026-08-01）

| 表 / 字段 | 当前用途 | 是否可驱动新业务 |
|---|---|---|
| `subcontract_order_items.issued_qty` | 老库 `OQTY` 迁移与历史查询兼容 | 否；新发料不回写，前端不得据此算剩余发料量 |
| `subcontract_order_items.material_returned_qty` | 老库累计量兼容 | 否；新材料退不回写 |
| `subcontract_material_issue_items.qty` | 已审核历史发料子件的实际发出量 | 仅用于历史下游来源和红冲，不足以替代订货时冻结的 BOM 快照 |
| `subcontract_material_issue_items.returned_qty/wasted_qty` | 来源发料子件已退、已损耗累计 | 是；材料退/损耗必须引用已审发料子件，并满足两者合计不超 `qty` |

当前委外订货没有订单版本冻结的 BOM 快照，也没有完整子件发料台账。因此新增发料可以保存草稿，
但前端禁用上游数量引入和审核，服务端审核以 409 失败关闭。历史已审核发料仍可只读查看、红冲，
并作为材料退/损耗来源；不得重算旧 BOM 或批量回填伪造快照。

### 关联单据累计数量约束（V132）

采购、销售以及委外仍保留的历史来源字段包含 `received_qty`、`shipped_qty`、`issued_qty`、
`returned_qty`、`material_returned_qty` 和 `wasted_qty`。应用层检查之外，V132 在数据库
增加 12 个 `BEFORE INSERT/UPDATE` 触发器，关闭两个并发事务同时通过“剩余量检查”后导致
超收、超发、超退的竞态。触发器存在不代表所有历史累计列仍是当前业务权威字段：

- 任何新的累计负数都拒绝；
- 累计值增加时不得超过来源数量（需要时加上已退数量额度）；
- 退回/损耗不得超过已处理数量，退回撤销后也不能让已处理量重新变成超量；
- 委外发料的退料量与损耗量合计不得超过发料量；
- 委外订货的 `issued_qty/material_returned_qty` 仅作历史兼容，新服务不再写入；
- 历史库已接受的超量异常不在 V132 中自动改写，只允许朝合法方向减少，必须另做异常盘点和业务纠偏。

> V132 的“退料 + 损耗不超发料”只是局部上限，不是完整委外守恒。完整目标还必须满足
> `累计发出 = 合格产出对应耗用 + 良/不良退回 + 审批损耗 + 供应商期末结存`，并经过回厂质量结论；
> 当前供应商持有库存/在制实体尚未落地。

这些触发器保护的是数据库不变量，不替代服务端状态机、权限和幂等校验；应用还必须把约束异常
转换为可理解的冲突响应，并在并发集成测试中验证。

### 销售退货质量冻结（V189）

- `sales_return_quality_items`：一条销售退货明细对应一条质量冻结当前投影，保存仓库、货品、颜色、单位换算率、收货基本单位量以及良品释放/报废/返工累计量。该表不属于 `stock_balances`，冻结数量不能参与 ATP、预留或拣货。
- `sales_return_quality_events`：收货、良品释放、报废、返工、未处置收货撤销的追加式证据账；数据库拒绝 UPDATE/DELETE。
- 数量不变量：`released_base_qty + scrapped_base_qty + rework_base_qty <= received_base_qty`；投影状态仅允许 `PENDING/PARTIAL/DISPOSED/REVERSED`，且状态必须与数量一致。
- 历史兼容：V189 不回填历史退货。没有质量冻结行的历史已审核退货仍按原库存流水红冲，不能由系统伪造过去的质检结论。
- `StockMovement` 边界：新退货 `RECEIVED` 不写可售库存流水；只有 `GOOD_RELEASE` 以该处置事件为来源写入库。未处置冻结可整单撤销；发生任一处置后禁止退货单直接红冲。
- 订单需求边界：审核仍回写 `sales_order_items.returned_qty`，按替换需求语义重算 outstanding/chain 并清空旧分批确认，但不直接增加 `reserved_qty`；替换须重新预留/排产。V220 已落 `sales_returns.customer_disposition`、客户处置审批和追加式 `sales_return_disposition_events`，支持退款结案/换货/补发/维修后返还；真实岗位 UAT、历史对账和处置级纠错/补偿命令仍未闭环。
- 处置纠错边界：发生任一 `GOOD_RELEASE/SCRAP/REWORK` 后整单退货红冲被阻断；当前没有处置级受控反向/补偿 action。事件仍禁止 UPDATE/DELETE，误判须停下调查，待双人复核或追加补偿事件能力落地。
- 对象范围：普通 `sales_return_quality:view` 按退货 owner/委派范围读取；现有单退货质检 GET/POST 在对象策略调用层可使用专用 `sales_return_quality:handle` 操作旁路，且该权限不扩大退货编辑/红冲范围。当前尚无独立跨 owner 任务发现投影或工作台，方法权限组合、handle-only 负向和完整页面入口仍须收口，不能把直接接口能力写成已完成的 PMC 任务闭环。

> 双写标记：见 [../数据迁移/README.md](../数据迁移/README.md)（迁移落地后填充）。
> 当前销售—仓库—计划—生产/采购/委外边界及发布结论见[2026-08-01 全链路安全复核报告](../99-项目治理/2026-08-01-销售仓库生产采购委外全链路安全复核报告.md)。

### rd_tasks（工程研发部任务，V204 落地）

**用途**：接收其它部门（当前主要是生产计划）转交的工程类任务，是工程研发部工作台「任务中心」的权威投影。详见 [工程研发部任务中心.md](../03-页面/工程研发部任务中心.md)。
**所属模块**：eng（工程研发部），与生产 `production_plans` 通过 `source_doc_*` + `order_item_id` 软关联。
**实现范式**：Pattern B 纯 JdbcTemplate（无 JPA Entity），`fn_audit()` 触发器写入 `audit_log`。

**关键字段**：
| 字段 | 类型 | 说明 |
|---|---|---|
| `task_no` | TEXT | 系统生成单号 `[RD][YYYYMMDD][6位日流水]`，`DocNumberPrefix.RD_TASK`（如 `RD20260814000001`）；旧 `RD26080001` 仅作历史格式原样保留 |
| `title` | TEXT | 任务标题 |
| `category` | TEXT CHECK | `BOM` / `DESIGN` / `SAMPLE` / `TRIAL` / `ECN` / `OTHER` |
| `status` | TEXT CHECK | `OPEN` / `IN_PROGRESS` / `DONE` / `CANCELED` |
| `priority` | — | 优先级 |
| `goods_id` | UUID | 关联货品（BOM 类必填，驱动自动完成） |
| `order_item_id` | — | 来源单据明细（生产转发时回填） |
| `source_doc_*` | — | 来源单据类型/单号/明细等冗余字段，便于研发下钻 |
| `assignee_employee_id` | UUID | 责任工程师 |
| `reporter_employee_id` | UUID | 转发/报告人（完成回执通知对象） |
| `due_date` / `completed_at` | — | 截止与完成时间 |
| `close_note` | TEXT | 完成/取消备注 |
| `row_version` | INT | 乐观锁（resolve 必须携带） |

**约束与幂等**：
- 部分唯一索引 `uq_rd_tasks_open_bom (order_item_id, goods_id) WHERE category='BOM' AND status IN (OPEN, IN_PROGRESS)`：同一来源行 + 货品在未完成态下不重复建任务，重复转发由数据库 fail-closed。
- `fn_audit()` AFTER INSERT/UPDATE/DELETE 触发器写 `audit_log`，任务创建/分配/完成/取消均可追溯。

**业务规则**：
- **自动完成链路**：研发保存货品 BOM（`GoodsBomService.recalcSourceE` 在 create/update/delete 三处共用）→ 发 outbox `GOODS_BOM_UPDATED` → `ChainNoticeService.notifyBomUpdated`（校验 `hasReadyBom` 才完成，避免删空 BOM 误完成）→ `RdTaskService.resolveOpenBomTasksForGoods` 自动 DONE + 通知生产转发人。
- **手动完成**：研发任务中心「标记完成」发 `RD_TASK_RESOLVED` → 通知转发人。
- **转发通知**：生产转发发 `RD_TASK_FORWARDED` → 通知 `DEPT_ENG`（`departmentUserIds`）。全走 `business_outbox` 可靠旁路。
- **权限种子**：`rd_task:view/edit/resolve` 授 `DEPT_ENG`；`production_plan:forward_rd` 授 `DEPT_PROD` / `SUB_PLAN`。超管经 `PermissionResolver` 全集自然可见。

**关联**：
- 经 `goods_id` 关联 `goods` 主档；经 `order_item_id` + `source_doc_*` 软关联生产计划明细。
- 经 `assignee_employee_id` / `reporter_employee_id` 关联 `employees`。

**待定问题**：
- 任务取消/重开与已完成任务的受控反向链路尚未细化（当前 DONE/CANCELED 进入只读「已完成」Tab）。
- `DESIGN/SAMPLE/TRIAL/ECN/OTHER` 类别的来源单据字段语义当前与 `BOM` 同构，未来各品类可能需要专用字段。

---


**最后更新**：2026-08-14 · **后端落地边界**：PostgreSQL + Flyway，共享工作树目录最高 V289/270，270 个版本号无重复；V279 是全局业务标识注册，V280/V281 是人员与附件权限/元数据治理，V282 是员工扩展 PII 局部加密候选，V283 是生产关联采购/委外订单审批快照锁守卫修正，V284 是个人信息变更敏感快照与历史审计副本保护，V285 收紧客户默认结算方式 UUID，V286 允许无主身份的扩展-only 员工敏感行，V287 保证其派生身份一致。V288/V289 创建现货借用结构、终态守卫并刷新/验证全 `public` 审计覆盖；在线 create/revoke、持久化、双趟生效计算与 Flutter UI 已形成源码候选。开发原库 `uten_imp` 为 V244/225，一次性隔离克隆为 V250/231，公司目标库保留 V238 既有只读证据；实际版本只以各库 `flyway_schema_history` 为准。V190/V193/V195/V252/V285/V289 等审计 sweep 通常只保护迁移后的未来写入，不补造历史事实；V284 仅对 profile-change 两种 target type 的已知敏感键做定向历史清洗，不把它扩大描述成全库历史审计已脱敏。V234 同样不为历史计划或 WAITING 伪造 analysis/item/action/link。借用专项已通过，但目标库迁移、真实权限/岗位/实物 UAT、完整 HTTP 收货/IQC/反向、QMS、WMS、主数据治理、备份恢复和发布签字尚未闭环，生产仍为 **NO-GO**。

> **V196–V202 后置覆盖**：新增计划需求分解、早期单点财务负责人审批（V229/ADR-027 起改为财务审核组）、预计到货、超量到货数量授权和原下单人退回任务；V197/V202 均为新增表后的全 `public` 业务表 fail-closed 审计 sweep，只保护未来写入。上述迁移已在开发原库 V244 和公司目标库 V238 以内；历史对账、岗位/实物 UAT、完整 IQC 和发布签字未完成，生产 **NO-GO**。
