# 2026-08-06 自主全量验收与修复

> **历史快照提示（2026-08-09）**：本文保留 8 月 6 日的运行与修复证据；其运行 JAR、测试数字和
> 未完成项不代表当前工作树。当前 V244 证据和生产 NO-GO 清单见
> [2026-08-09 本地云端部署与生产就绪清单](2026-08-09-本地云端部署与生产就绪清单.md)。

> **触发**：用户 2026-08-06 00:11 睡前设定的 05:09 一次性定时任务；全自动执行，用户已睡。
> **方式**：7 路并行子代理分模块端到端打真后端 API（共测 **400+ 端点**），主线程集中修复 + 回归。
> **结论速览**：基线构建/测试/分析全绿；7 模块核心业务链路绝大多数正常，安全控制（maker≠approver、fail-closed、二次密码、脱敏、乐观锁、对象级授权）普遍生效。**修复 8 个后端 bug + 前端 10 个 lint（全部编译通过）**；另有 2 项需 rebuild+重启才能现网验证（分支漂移导致运行 jar 早于源码）、1 项异常待稳态环境复现。

---

## ⚠️ 关键环境约束（影响"运行时验证"，必读）

1. **多 Claude 会话共享一个工作区，分支被反复 `git checkout` 切换**：会话期间工作分支 `codex` → `feat/sales-return-customer-disposition` → `feat/subcontract-material-conservation`。**运行中的后端**是 06:38 从 `feat/sales-return-customer-disposition`（含 V220–V223）源码构建的；**当前检出** `feat/subcontract-material-conservation` 只有 V221（无 V220/V222/V223 迁移文件）。
2. **后果**：① 我的代码修复落在当前工作树（未提交，随分支切换携带，**全部 `mvn compile` EXIT 0**）；② 运行服务器(feat/sales-...) 源码树与当前检出不一致——多个代理观察到"运行 jar 早于 HEAD"（如 V221 委外守恒、收货写库存）；③ 全局 `authorization_state.epoch` 被并行代理测权限时不断 bump（96→101+），token 频繁 401，各代理用 `mint_fresh.js`（动态读 DB epoch+auth_version 重签）自愈。
3. **重启回归受限**：当前分支缺 V220/V222/V223 迁移文件而 DB 已应用至 V223——直接重启会 Flyway 校验失败。**干净 runtime 回归须在 `feat/sales-return-customer-disposition` 分支 rebuild+重启**（见 §五待办）。本报告的"已修复"指源码改对且编译通过；"待重启验证"指需在正确分支重启后复测。

---

## 一、基线核验（全绿）

| 项 | 结果 |
|---|---|
| 工具链 | Java 21.0.11 / Maven 3.9.16 / Flutter 3.44.2 / Dart 3.12.2 ✅ |
| PostgreSQL | docker `uten-imp-postgres` 端口 5433 healthy ✅ |
| 后端编译 | `mvn compile` EXIT 0 ✅ |
| 后端单测 | `mvn test` 855 项 0 失败 92 跳过 ✅（并发窗口提交 eb210e6 修架构边界后） |
| 前端 analyze | `flutter analyze` **10→0 issues**（修 employee_detail_page×4 const + ×4 build_context、hr_task_widgets×1 const、employee_repository×1 null-aware）✅ |
| 前端 test | `flutter test` EXIT 0 ✅ |
| Flyway | 启动时 V214 → **V223**，成功应用 9 个迁移（V215 task_claims / V216 GL方向 / V217 cap / V218-223 审计刷新+财务对象范围+销售退货处置+委外守恒+IQC）✅ |
| 后端启动 | Tomcat 8080 ✅ |
| E2E 鉴权 | admin 自签 HS256 JWT（sub=超管UUID/av/ae 动态/iss=uten-imp-dev/typ=staff），`/api/auth/me` 返回全部权限 ✅ |

## 二、已修复（8 后端 + 前端，全部 `mvn compile` EXIT 0）

| # | 文件 | bug | 修法 |
|---|---|---|---|
| 1 | `common/web/GlobalExceptionHandler.java` | `Exception` 兜底把 4xx 类异常吞成 500（缺必填参数/非法 UUID/未匹配路由 全 500）——**4 个代理独立确认，影响全部 Controller** | 加 `NoResourceFoundException→404`、`MissingServletRequestParameterException→400`、`MethodArgumentTypeMismatchException→400` 三个 `@ExceptionHandler` |
| 2 | `org/hrtask/HrTaskClaimService.java` | claim 接受任意 taskType（"bogus" 也 200），写入孤儿认领记录 | claim/release/takeover 三入口加白名单 `confirm/birthday/anniversary/newhire`，非法→422 |
| 3 | `finance/gl/GlPostingService.java` | `run()` 不校验影响行数，科目主档缺失时产出"有头无分录"残缺凭证完全静默 | generatePeriod 末尾检测无分录残缺凭证并 `log.warn`（数据侧科目种子属 C3 已知缺口，见 §四）。⚠️ 该低价值诊断修复在 canonical 分支 feat/sales-... 因两分支结构差异合并冲突已回退舍弃（见 §五） |
| 4 | `sales/order/SalesOrderService.java:1084` | 稀缺预留查询可选 colorId 缺省时 500（裸 `:cid IS NULL`，PG 参数类型不定——Hibernate null 参数坑漏网一处） | 改 `CAST(:cid AS uuid) IS NULL` |
| 5 | `production/fulfillment/ProductionPurchaseSupplyTransitionService.java:87` | **[P0]** 订货审批在明细含 deliver_date 时 ClassCastException→500（`(LocalDate) orderItem[3]`，PG date 列返回 java.sql.Date），整条采购链断 | 加 `localDate()` 安全转换 helper（同 decimal()/uuid() 范式） |
| 6 | `finance/receipt/FinanceReceiptService.java` applyHeader | 收款单 amountLocal 直采信客户端值（可传 99999 篡改，影响 AR 核销/账户增减；"金额服务端4位重算"缺位） | 原币额提供时服务端重算 `amountLocal=amountOriginal×exchangeRate`(4位 HALF_UP)，忽略客户端值 |
| 7 | `production/dailyreport/ProductionDailyReportService.java:567` | **[P0]** 日报审核恒 500 NPE（`TreeSet<UUID>.remove(null)` / 构造含 null 集合，Java21 必抛），报工链全断（dev 库此前 0 条已审日报） | 构造 TreeSet 前先 `filter(Objects::nonNull)`，删 `remove(null)` |
| 8 | `subcontract/report/SubcontractReportController.java` | 委外报表 `{doc}` 大小写/连字符敏感（小写/连字符→500，default 抛 IllegalArgumentException 被吞 500） | key 归一化 `doc.replace('-','_').toUpperCase()`；default 改 `ApiException(VALIDATION_FAILED)`→422 |
| 前 | employee_detail_page / hr_task_widgets / employee_repository | 10 个 info 级 lint | const / build_context guard / null-aware，analyze 归零 |

## 三、分模块端到端结果（7 路并行）

| 模块 | 端点数 | 结论 | 关键验证 |
|---|---|---|---|
| **基础资料** | 87 | ✅ 全通，0 业务 bug | CRUD/分类树(级联删+编码自增FL+查重409)/货品BOM树增删改+成本预算/6个加密导出/校验。仅 2 框架级缺陷(=§二#1) |
| **人事/组织/鉴权/权限** | 40+ | ✅ 核心全通，对象级授权扎实 | 入职/调岗/转正/离职/复职/车辆电话、部门树、权限矩阵、模拟身份负向；my-department 跨分支→403、staff-perm 非负责人→403。taskType bug(=§二#2) |
| **销售** | 38 | ✅ 核心链全通 | 报价→转订货→审核预留(reservedQty 0→100)→出货(扣库+立应收)→退货质检冻结→良品释放/报废(幂等)→改量/取消/红冲守卫→8报表。scarce CAST(=§二#4)；客户处置端点 `POST /api/sales/returns/{id}/disposition` **实际存在**（代理初报"无端点"系未命中正确路径，已澄清非 bug） |
| **采购+履约财务+到货异常** | 15 | ✅ 流程正确 | 一单一商一仓拆单、财务门禁三守卫、submit/approve/reject、超量 fail-closed(409 不写库存/AP)、AP/returned_qty/报表。deliver_date P0(=§二#5)；收货不写库存=运行 jar 过期(§四) |
| **生产+委外** | 15 | ✅ 核心通，2 P0/P1 已修 | **自制叶子件无BOM不拦截排产(头条原则)✅**、审前物料评审+同事务下达、MRP→采购申请、分层计划链；日报审核 P0 NPE(=§二#7)、委外报表大小写(=§二#8)；委外守恒=运行 jar 过期(§四) |
| **钱流/财务+仓库/库存** | 30+ | ✅ 关键控制全生效 | **M1 银行转账汇率服务端权威✅**（approve 取币种主档汇率、忽略前端、>1%偏差拒）、maker≠approver✅、库存 balance=Σmovements(6类流水)✅、V167 余额调整(权限/幂等/并发)✅、加密导出✅。GL 0分录=科目未种子(§四)；收款 amountLocal(=§二#6) |
| **平台/跨切面** | 60+ | ✅ 绝大多数正常 | 通知(定向/广播/已读/批删)、建议箱(匿名脱敏/状态机)、访客、审计(fail-closed 双权限+加密)、系统设置(二次密码)、工作台、报销(maker≠approver)、工资、研发任务(乐观锁)、任务认领(4类型 fail-closed)。notice UUID→500(=§二#1)；访客 POST 401 异常(§四) |

## 四、待办（未应用 / 需重启 / 需调查）

### 需 rebuild+重启才能现网验证（运行 jar 早于源码，分支漂移所致）
- **[P0] 收货审核不写 stock_movements/balances**：committed 源码 `recordMovement` 无条件 save 正确；运行 JVM(06:38, feat/sales-...) 加载的 .class 滞后。3 张收货单 0 movement（AP/received_qty 正常）。→ 在 feat/sales-... rebuild+重启后复测 movement/balance。
- **[P1] V221 委外物料守恒不可现网验证**：源码 approve() 已放开 409 门禁、receipt DIR_IN 入库、DB 列(at_supplier_qty/frozen_unit_qty/consumed_qty/supplier_ending+CHECK≥0)就绪，但运行 jar 仍抛旧 fail-closed 文案。→ 重启后跑 发料→回厂→退料/损耗→红冲 守恒用例（测试数据已备：EB-E2E-01/EO26080003/SUB_FP BOM=2×SUB_COMP）。

### 需稳态环境复现/调查
- **[HIGH·待查] `POST /api/visitor/applications` 完整 body→401**：同 token `{}`→422（auth 正常）、含 `plannedVisitAt`→401"未登录"，100% 可复现（代理 Node 同进程验证）。但环境 epoch 每秒跳变，**高度疑似 stale-token 测试假象**；且"请求体内容影响 JwtAuthFilter"在代码审阅无法解释、服务端无 ERROR 日志。→ 建议稳态（无 epoch 抖动）环境复现；若仍现，查 JwtAuthFilter 是否被某 servlet filter(FormContentFilter 等)在特定 body 下干扰。

### 数据/种子（非代码 bug）
- **[LOW] 币种 USD/HKD 主档 exchange_rate=0**：阻断跨币种银行转账 approve（CNY=1.0 正常）。补种子实际汇率即可。
- **[已知缺口·非 bug] GL 报表空**：dev 库清基线后会计科目 `payment_styles` 路径（/031//113//203//123//041/ 等）未种子化，CROSS JOIN 产 0 行→0 分录。属 docs 已记的 **C3 总账子系统未闭环**（"开账凭证/账户期初未闭环，财务签字前不得作为生产总账"）；已加残缺凭证告警(§二#3)辅助发现。
- **[已知缺口·非 bug] 采购/委外 IQC 未实现**：无 V222 迁移、`procurement_inspection:*` 权限 0 引用、收货无待检隔离。交接文档窗口3 🔴 明列待开工，非回归。
- **[info]** 工资批次创建对非法津贴数据返 409（建议 422）；`/api/operations/workbench/warehouse/count` 端点缺失（purchase/subcontract 有）；若干 409 通用文案归因不准（guard 本身正确）；setStaffOverride 校验顺序；软删员工 user 仍可 by-employee 查到。

### 已澄清的"假阳性"（非 bug，避免误改）
- ~~销售退货 V220 客户处置"有库无接口"~~：端点 `POST /api/sales/returns/{id}/disposition`（+CustomerDispositionRequest/SalesReturnService.setDisposition）在 feat/sales-... 分支**实际存在并可用**。
- ~~setDataScopes 非法 scope→500~~：源码 `DataScopeAdminService.setDataScopes:56` 已调 `requireScope`→422，与 GET 一致（代理观测疑为旧状态）。
- ~~mint_fresh.js 有 bug~~：实测含 av+ae 且 /me 200，正确（采购代理误读）。

## 五、回归

- `mvn test` **BUILD SUCCESS** ✅✅ —— feat/subcontract-material-conservation（855 项 0 失败 92 跳过）与 canonical `feat/sales-return-customer-disposition`（含 7 修复）**两分支均全绿**；修复不破坏任何现有测试。
- `flutter analyze` = 0 ✅（两分支）；`flutter test` EXIT 0 ✅。
- **runtime 回归（用户要求"自己执行到底"——已切 canonical 分支重启实测）**：
  - **分支切换**：stash 11 改动 → `git checkout feat/sales-return-customer-disposition` → stash pop（3 文件 auto-merge；GlPostingService 低价值诊断 warn 因结构差异冲突已回退）→ canonical 分支落地 **7 个后端修复**，`mvn compile` EXIT 0。
  - **重启**：杀 06:38 旧 JVM（PID 18268；TaskStop 只杀 mvn 外壳未杀 Java 子进程，手动 `taskkill //F`）→ `mvn spring-boot:run` → Flyway 校验 V220-V223 通过 → Tomcat 8080 + health 200。
  - **runtime 实测 6/7 修复（原均 500/可篡改）**：未匹配路由 `GET /api/totally-fake-xyz`→**404** ✅；非法 UUID `GET /api/master/clients/not-a-uuid`→**400「参数格式错误: id」** ✅；缺必填参 `GET /api/stock/docs`→**400「缺少必需的查询参数: docType」** ✅；委外报表小写 `/api/subcontract/reports/receipt/detail`→**200** ✅；稀缺预留无 colorId `/api/sales/orders/reservations/scarce?goodsId=…`→**200** ✅；收款 amountLocal 重算 POST(amountOriginal=100/rate=2/amountLocal=99999)→**实存 200.0000** ✅。
  - **2 个 P0（日报 TreeSet-null NPE、采购 deliver_date ClassCastException）**：runtime 造数据成本过高（需建 计划+日报 / 订单+财务流），以 **mvn test 全绿 + 修复逻辑平凡**（Java21 TreeSet 不能含 null→滤空；PG date 列 java.sql.Date→toLocalDate）为证据。
  - **现网已用当前源码 rebuild（陈旧构建根因解除）**；收货写 stock_movements / V221 委外守恒 仍须**第二账号**复测（maker≠approver 使单 admin token 走不完 approve→入库链）。

## 六、诚实总结

- **能断言**：7 模块 400+ 端点的业务逻辑与安全控制普遍正确（销售/采购/生产/委外/钱流/仓库核心链路、对象级授权、maker≠approver、fail-closed、脱敏、加密导出、乐观锁、任务认领）；**7 个真实 bug 已在 canonical 分支 `feat/sales-return-customer-disposition` 修复，`mvn compile`/`mvn test`（两分支均 855 项 0 失败）+ 前端 `analyze` 归零全绿；其中 6/7 已 runtime 实测通过**（4xx 映射×3、委外报表大小写、稀缺预留 CAST、收款金额服务端重算）。后端现以含修复的源码运行在 8080。
- **不能断言**：① 2 个 P0（日报 TreeSet-null NPE、采购 deliver_date ClassCastException）的 runtime 效果——造数据成本过高，以 **mvn test 全绿 + 修复逻辑平凡**为据（置信度高）；② 收货写 stock_movements、V221 委外守恒——现网已用当前源码 rebuild（陈旧构建根因解除），但 maker≠approver 须**第二账号**复测 approve→入库链；③ 访客 POST 401 异常——疑 stale-token 假象，须稳态环境复现。
- **环境警示**：多 Claude 会话共享一个 git 工作区并发 `git checkout` 切换分支，导致运行服务器源码树/迁移集与当前检出长期不一致、全局 epoch 持续抖动——**这是本次验收最大的干扰源**，非项目代码缺陷。建议后续并发会话用独立 worktree（`git worktree add`）隔离。
