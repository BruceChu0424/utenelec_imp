# 2026-08-16 白天三项 UI 债收口报告（fontSize token 化 / 弹窗三档断点 / V-token 残留）

> **性质**：凌晨复查收口报告 §五 新发现债的三项全量收口（fontSize 硬编码、720–840
> 固定宽弹窗、5 文件分号 V-token），并同步规范文档。
> **Owner**：白天收口会话 | **时间**：2026-08-16 白天 | **分支**：`uimp/chore/full-integration-20260815`
> **开工状态**：工作区干净（凌晨四轮已全部本地提交）；收口期间另有**两个并行会话**同分支工作：
> 前端会话抽共享 `UtenDocLinkPickerSheet`（已提交 ab0487d9，五个右滑选单面板收编）、
> 后端会话在途改生产/采购/委外 facade + V292 迁移（本报告收口时仍未提交）。

## 一、fontSize 硬编码收敛（真实债 ~111 处盘点 → 75 处转 token / 其余豁免）

**口径**（已固化进 `docs/00-项目准则/04-字体与字号可调.md` §3.1）：token =
`theme.textTheme.<role>`，按磅值对号入座（48/36/28=display，24/20/18=headline，
16/15=titleLarge/bodyLarge，14 按 w400/w600=bodyMedium/titleMedium/labelLarge，
13=bodySmall/titleSmall，12=labelMedium，11=labelSmall；微字号 10 用既有惯例
`labelSmall?.copyWith(fontSize: 10)`）。

- **转 token 75 处 / 56 文件**：components（导出错误行/导入按钮/AppBar 标题走
  `appBarTheme.titleTextStyle`/可编辑表格行按钮/节标题/分段筛选）、shared（任务认领
  徽标/供应商单元格）、basic_data（4 张分类页子计数、树视图、货品/客户选择器副行）、
  department（树视图层级角标/节点行/选择器徽标）、admin、dashboard、production
  （看板 6 处 + `_chip` 去死参数、计划详情 4 处、报表筛选 chip、结清面板副标题）、
  sales（进度抽屉 7、批量发货 7、详情 2、报表、仓储）、purchase/subcontract、
  warehouse、finance、payroll、expense（含 30px 大额数字 → displaySmall）、notice
  4 个标签徽章、suggestion、profile/employee、entry、胶囊导航（**量宽与渲染改同源
  token**：`calcItemWidth` 增加 style 参数，两侧同用 titleMedium）。
- **豁免 36 处**（全部有据，规范 §3.1 落档）：`pw.TextStyle`（货品 BOM 预览 ×2、
  组织架构图 PDF ×6）；纸面复刻（BOM 预览 `_paperCell` ×2）；图形绑定注字
  （ProgressRing 参数 ×3、头像首字 ×2）；尺寸变体体系（UtenButton/ClickGuard/
  UtenStatusBadge/UtenNotificationBadge/待排产徽标）；护栏特例（字号档选择器
  `_Label` 固定 13——防五档并排溢出，且显式 style 会覆盖 SegmentedButton 选中态配色）。
- **连带修正**：`floating_capsule_nav_bar` 量宽样式与渲染样式此前各写一份
  `fontSize:14/w600`，现由调用方传入同一 `titleMedium`，消除双源漂移风险。

## 二、固定宽弹窗/侧板三档断点显式适配（原 15 处全数收口）

新增 `lib/core/responsive/dialog_size.dart`（`utenDialogWidth` + 配套
`utenDialogInsetPadding`）：居中弹窗 expanded=设计宽全量 / medium=min(设计宽, 屏宽−48) /
compact=屏宽−24 近全屏，边距 40/24/12 配套；**只改宽度不改 insetPadding 会被
Material 默认 40 边距钳回窄条**（规范 §3.5 已注明）。侧板统一走
`showUtenAdaptivePanel`（compact 底部弹层降级 + 宽屏右滑入），不再手搓。

| 处置 | 明细 |
|---|---|
| 并行前端会话收编（ab0487d9） | 采购/销售/委外 doc-link 选择器 ×3、ar_ap 选择器 840、可报工计划行选择器 840 |
| 本轮侧板 → `showUtenAdaptivePanel` | 货品选择器 720、材料结清面板 840（顺带把裸 `<600` 判断换成断点系统、保留 elevation 16/300ms/黑38 遮罩差异）、批量发货面板 840 |
| 本轮居中弹窗 → 三档宽度 | 通知详情 760、where-used 货品详情 800、生产下达结果 720、物料需求评审 760 |
| 非弹窗假阳性（维持现状） | 入口页卡片 720 / profile 信息行 720 / 财务资产横幅文本 760（内容限宽）；计划向导纸面 794 与汇总表 794（A4 纸面复刻，固定磅值豁免） |

## 三、5 文件分号格式 V-token 残留清理（12 处）

`sales_repository`（`；V96/V100×2/V178×3` 六处）、`stock_doc_repository`（V97×2）、
`stock_doc_edit_page`、`stock_doc_list_page`、`warehouse_report_table_page`（各 V97）
——只删版本号 token，语义（仅 shipment/order/DRAW 可用、0/1/2 进度含义）全保留，
对齐 4933de44 首轮清理后的标准样式（`；仅 order 类型可用`）。
`sales_doc_config.dart` 的 `V51__sales_documents.sql` 是**迁移文件名 seed 对齐指针**
（护栏类，按适中策略保留）；`stock_doc_detail_page` 复查无残留。

## 四、验证记录

- `flutter analyze` 0 issues（每批后跑，累计 6 次）；`dart format` 本轮 56 个改动
  文件 0 未格式化残留；**`flutter test --no-pub` 733/733 全绿两遍**（fontSize 收口后、
  弹窗收口后各一遍；733 = 729 + 并行会话新增 4）。
- 后端：收口时并行后端会话在途（facade 重构中，曾出现缺 import/文件移动的瞬时不编译态），
  `mvn compile + UTEN_RUN_DB_TESTS=true mvn test` 在其稳定后补跑，结果见本文件末尾
  「五、后端门禁补录」。

## 五、后端门禁补录

（后端会话稳定后回填）

## 六、提交边界说明（并行会话共存）

本会话提交**只含自己改动的 57 个前端文件 + 4 个文档**；以下工作区改动归属并行
会话、不在本提交内：`server/**`（后端 facade 重构 + V292，收口时仍在途、曾出现
编译中间态）、`purchase_doc_config.dart` 与 `purchase_doc_edit_page.dart`
（订货单免仓库 hasWarehouse 功能；后者文件内含本会话 1 处 fontSize 收敛
「他人正在分解此单」提示行，为保持本提交可独立编译而**随其功能一并落地**，
不单独拆出）。
