# 2026-09-05 物料分析准备与生产链本地验收记录

> **历史证据范围**：本文保留上轮V476源码候选及209项等验收。文中“默认委外、强制原因、移除记忆、本轮无新迁移”等为该轮时点行为；本次V477/V478已调整，现行规则及待最终回填结果见[顶层路线记忆与确认修复记录](2026-09-05-顶层路线记忆与确认修复验收记录.md)。历史数字不能代替本次完整验证。

- 范围：物料分析准备页、三类下达入口、主仓范围/实际叶仓、路线草稿和选择、子件可见性、库存分配准确性、下层待料与车间执行链。
- 状态：本地源码候选。本文记录已完成的本地证据与待补项，不是部署、目标库升级或真实岗位UAT证明。
- 用户边界：不上传GitHub、不执行远端CI；本轮未commit/push，未写现有业务数据库，未部署。
- 现行说明：[生产物料分析页](../03-页面/生产物料分析页.md)、[ADR-070](../99-决策记录-ADR/ADR-070-物料分析分桶详情页与三处学习记忆及超量下单.md)、[V476](../数据迁移/92-V476仓库主子层级.md)。

## 一、交付行为

1. 准备任务按“下达采购 / 下达委外 / 下达车间”组织，详情分未下达、已下达、需处理，保持真实任务和来源进度。
2. 主表八列、复选、按产品/按物料汇总，搜索/表头设置/全屏共用工具条；详情承载完整数据。选中名称、路线和层级白字绿底，浅色未选中黑字。
3. 未确认且未下达的路线默认委外草稿，已确认历史值保留。下拉不请求保存，点击创建所选路线才提交；偏离主档必须填写本批原因。旧右键即时保存及自动路线学习预填退役，历史接口和数据保留。
4. 顶部主仓只作范围，最多展开100个所属可用叶仓；实际领料仓仍是叶仓UUID。其它仓库存只作调拨参考，旧分析保留原仓事实。
5. 已委托child的真实物料继续可见、可确认路线且参与汇总；原父树已转交零需求不重复计算。子件投影的最终Flutter验证列于第四节。
6. 直接层完整套料和深层诊断不能重复分配同一公共库存；exact归属继续按节点隔离。深层遍历使用稳定优先队列，同批委外BOM分类只查询一次。
7. 合法子件可在下层不足时先下达车间WAITING；完整齐套才生成正式预留和DRAW；全部真实发料后才开工。没有放开部分发料、待检或未入目标仓的开工门禁。

## 二、后端定向证据

以下为不同执行范围，存在重叠测试，不相加成一个“全量通过数”。

最终物料分析算法版本于2026-09-05 07:55–07:57 (America/Denver, UTC-06:00)完成复跑。一次本地Maven命令执行下述六类分析回归和四类隔离PostgreSQL测试，退出码0；报告仍分别列为分析89项和数据库32项，不与初次103项重复加总。

```powershell
$env:UTEN_RUN_DB_TESTS = 'true'
mvn -o -q '-Dtest=ProductionExecutionSegmentPostgresTest,ProductionExecutionSegmentOperationsPostgresTest,ProductionMakeSupplyLifecyclePostgresTest,ProductionMaterialAnalysisPersistencePostgresTest,MaterialAnalysisServiceBehaviorTest,MaterialAnalysisUuidFallbackContractTest,MaterialAnalysisSafetyReplenishmentProjectionTest,MaterialAnalysisPublicSafetyWorkflowContractTest,ProductionMaterialAnalysisWorkflowContractTest,MaterialAnalysisCommandBatchLookupTest' test
```

本地完整日志：`server/target/material-analysis-final-postgres-20260905.log`；逐类XML：`server/target/surefire-reports/TEST-*.xml`。日志确认4次完整应用438个迁移至V476。该命令没有连接现有业务库；后续独立修复的车间查询SQL不冒用本节测试作为其验收。

### 2.1 生产链路定向回归

本轮初次链路核查执行14类、103项，0失败、0错误、0跳过。覆盖：

| 类别 | 测试类及项数 |
|---|---|
| 车间与领料通知 | ChainNoticeProductionWorkshopScopeTest 4；ChainNoticeReadyEventTest 1；ProductionDrawNoticeTest 5；ProductionWorkshopMaterialNoticeContractTest 2 |
| 分析唤醒 | MaterialAnalysisSupplyWakeupServiceTest 9；MaterialAnalysisSupplyWakeupHookContractTest 5 |
| 数量和分配 | MaterialConsumptionMathTest 5；PriorityCompleteKitAllocatorTest 6；CompleteKitAllocatorTest 13 |
| 分析/计划合同 | ProductionMaterialAnalysisWorkflowContractTest 24；ProductionExecutionPackageCommandStatusTest 3；ProductionExecutionPlanningRequestedStatusTest 1；ProductionPlanningRequestValidatorTest 8 |
| 开工与幂等 | ProductionExecutionSegmentServiceTest 17 |

关键事实：WAITING可先分配车间；齐套后仓库/车间通知精确到执行段；未发料或只发部分需求拒绝START；批量START先预检全部行、按UUID稳定锁序，旧版本和同键异payload拒绝，已成功请求可重放。

本次执行为本地Maven定向测试，不是GitHub CI。完整源代码测试入口位于 `server/src/test/java/com/uten/imp/features/production` 与 `features/notice`。

### 2.2 准确性和批量查询修复后报告

最终复跑六类共89项；逐类回读 `server/target/surefire-reports/TEST-*.xml`，下列报告均为0失败/错误/跳过：

| 测试类 | 项数 | 实际覆盖 |
|---|---:|---|
| MaterialAnalysisServiceBehaviorTest | 51 | 直接层/深层共享库存守恒、父库存0/0.5/1、精确权益在两端、父件有货但其它直层缺料、重复nodeKey跨产品隔离及大树 |
| MaterialAnalysisUuidFallbackContractTest | 2 | 来源UUID兼容约束 |
| MaterialAnalysisSafetyReplenishmentProjectionTest | 5 | 安全补库投影 |
| MaterialAnalysisPublicSafetyWorkflowContractTest | 4 | 公共安全库存分账 |
| ProductionMaterialAnalysisWorkflowContractTest | 24 | 物料通知/child/计划门禁，匹配批量BOM分类实现 |
| MaterialAnalysisCommandBatchLookupTest | 3 | 500产品重复物料只查一次、空候选不查询、叶子不误判成有BOM父件 |

51项行为测试中的新复现原先出现“公共库存1被直接层和深层显示分配2”，修复后保证直接层已分配1时深层分配0。父件库存0/0.5/1只产生对应子需求；已有父件不因其它料短缺再次展开采购其子料。exact持有人与公共库存按各自账本分开，不能双扣或移给其它节点。

纯计算压力用例 `largeAnalysisSharesStockOnceAndKeepsRepeatedBomPathsScopedToEachProduct`：5000产品、10000节点、2000份共享库存，固定乱序输入并复用nodeKey，检查总分配和逐产品身份。初次Surefire样本为0.390秒，最终复跑记录 `time="0.503"`，即约0.503秒。它只测算法，不含数据库读取、HTTP、权限、网络、前端渲染；前端500来源限制没有扩大。

### 2.3 隔离PostgreSQL链路

本地Docker Testcontainers使用PostgreSQL16.15。4个一次性测试库分别从空库完整应用438个迁移至V476；现有业务库未连接或写入。

| 测试类 | 通过项数 |
|---|---:|
| ProductionMaterialAnalysisPersistencePostgresTest | 13 |
| ProductionExecutionSegmentOperationsPostgresTest | 2 |
| ProductionMakeSupplyLifecyclePostgresTest | 7 |
| ProductionExecutionSegmentPostgresTest | 10 |

最终算法版本复跑仍为32项，0失败、0错误、0跳过；4个隔离库均完整迁移至V476。关键场景：

- 计划10件、库存6套，持久化READY6+WAITING4，WAITING零预留。
- 余4分两次到货，仅能完整满足待料段时原子提升；委外回厂同样校验整套。
- EXACT_SNAPSHOT的9999/4000需求、预留与DRAW均为3，保存精确计量。
- MAKE实收入库精确peg和reservation守恒；并发分配不能超过同一入库行容量。
- 并发确认只保存一个幂等结果；取消/反向恢复未执行库存。
- 历史model_version=0仍兼容，计划提交/批准/释放守恒。
- 非生产车间或错误班组归属拒绝；数据库执行状态门禁生效。

这些既有测试验证SQL/执行约束，没有调用新的完整浏览器流程，也不能替代本次child展示和算法的界面验收。

## 三、实现及迁移范围

本轮没有新增迁移，未编辑已应用SQL或运行flyway repair。BOM批量查询复用既有goods_id索引；新算法仅改变当前分析分配计算，旧活动分析按正常授权刷新生效，不改写历史订单、领料、报工、origin或流水。

已检查生成、审核、齐套、DRAW、START、报工、FQC和实收入库的职责。仅凭通知不推进业务；仅凭齐套不等于已领料，实际完成仍以iqty为准。未实施物理合单、任意部分delegated_qty、未齐套开工或待料段任意再次拆分。

## 四、Flutter和文档最终验证

- **最终组合 Flutter 回归：209/209 通过，退出码 0**。日志 `build/material-analysis-final-combined-tests.log`。范围包括物料分析页面99项，以及本轮表格流程、三路线、子件可见性、模型/请求、生产Hub/路由权限、共用表格、仓库层级/树、车间页面、执行组面板、刷新接线、执行查询API契约与徽章6项。该209项是一次执行的实际报告，不再与前面的97/110/93等中间范围相加。
- 关键界面证据：八列/默认委外/显式创建与原因、500+1分批与超时幂等、跨页/跨搜索选择和全局清除、全屏切视图、保存中选中对比、主仓只读/刷新失败回显、子件真实需求/异常来源只读、参考节点双击、超过100产品的新child首窗可见、已有数量/车间/负责人保留、正式生成后的execution tick、徽章身份/权限/乱序保护。浅色/深色/390px窄屏使用项目主题和本地字体检查，无布局异常。
- **最终静态检查：No issues found，退出码0**。命令范围为 `flutter analyze lib/features/production lib/features/basic_data/widgets/master_data_table_view.dart lib/features/dashboard/providers/workbench_refresh.dart lib/shared/providers/master_name_provider.dart lib/core/router/permission_by_path.dart test/features/production test/page_route_permission_dependency_test.dart`；日志 `build/material-analysis-final-combined-analyze.log`。修改的Dart文件经定向格式化，`git diff --check`通过。
- 文档采用现行说明替换旧六桶、处理列、无复选、自动路线学习、下层硬阻断和旧向导口径；历史测试不作为当前实现验收标准。
- 文档检查已完成：最终12份更新文档共143个相对链接全部存在；新增行无全角括号；git diff --check无错误。该检查仅验证文档结构与标点，不等于业务测试。

## 五、尚未完成的验收

本轮未做目标非空库升级、目标历史数量/权限/审计对账、浏览器真实账号全链、仓库实物收发、车间岗位UAT、真实网络/数据库负载、备份恢复或部署回读。因此不能用103项、51项、32项或0.390秒样本宣称“已上线”“真实全链全部通过”或“全接口大数据响应均达标”。

本地源码与本轮自动化界面验收已完成；上传GitHub/CI与发布须由后续明确任务触发。后端服务未重启，当前业务库未做写操作，不能把源码修复当作现场已生效。

## 六、现场“自制下达车间不见、SQL错误、无徽章”核查

### 6.1 已核实的现场错误与源码修复

只读查看本地 `server/logs/uten-imp-dev.log`，仅摘取SQL语法证据：`SQLState: 42601`、`syntax error at or near "BY"`、`TRUEORDER BY`。本文不保存账号、人员、令牌、查询参数或完整现场SQL。

`ProductionExecutionWorkbenchService.segmentPage` 将筛选末尾的 `task.reportable = TRUE` 与 ORDER BY 直接连接，形成 `TRUEORDER BY`。查询失败不能解释为“无车间任务”或静默回空。源码增加明确换行分隔。

同类源码审查另外修复关联单据的 `candidateORDER BY` 拼接、UNION派生表五列显式别名，以及根任务对象范围加括号/空格后接AND。这些是代码审查发现，不冒充日志已证实的另一现场错误。修复不改变权限、对象范围、分页合同或数据库结构，不需要迁移。

### 6.2 新增查询验证

`ProductionExecutionWorkbenchQueryPostgresTest` 5项在一次性PostgreSQL中通过真实Hibernate执行：

- 默认车间列表包含WAITING，分页及徽章计数使用同一安排范围；
- READY_TO_REPORT/READY_TO_START筛选与关键词可执行，撤销报工权限后只读能力仍收口；
- PREPARING/IN_PROGRESS及空搜索结果正常；
- 根对象范围与附加AND分离，总览和根任务工单分页正常；
- 仅有生产计划查看权限时，关联单据仍有稳定列名和可执行排序。

`ProductionExecutionWorkbenchServiceSortTest` 2项、`ProductionExecutionWorkbenchSecurityContractTest` 4项、`ProductionExecutionWorkbenchRelatedDocumentsContractTest` 1项同时通过。新增查询5项与原契约7项分别说明，共12项0失败/错误/跳过；不混入第二节32项全迁移测试。

该查询测试使用最小投影fixtures，没有执行完整Flyway迁移；第二节4个隔离库的438迁移证据独立保留。查询日志为 `server/target/production-workbench-query-postgres-20260905.log`；本地Maven退出码0。按下列类集合可复现本次专项：

```powershell
$env:UTEN_RUN_DB_TESTS = 'true'
mvn -o -q '-Dtest=ProductionExecutionWorkbenchQueryPostgresTest,ProductionExecutionWorkbenchServiceSortTest,ProductionExecutionWorkbenchSecurityContractTest,ProductionExecutionWorkbenchRelatedDocumentsContractTest' test
```

### 6.3 两阶段任务与大分析可见性

已核对 `material_analysis_bucket_detail.dart` 的新建前后UUID集合、`_buildPlanRows(preferredIds:)` 和轻量输入草稿。显式创建child后提示“已创建备料子任务，尚未下达车间”，将新增子件前置到首个100行可见窗口并预选，保留其它数量/车间/负责人输入，再由员工生成生产计划。不是把child创建伪装成正式下达。

只有通过计划生成和独立审核门禁才形成车间执行段。默认车间来自V192、默认负责人来自组织树，并要求员工核对；缺组织安排必须人工补齐。无审核权限的员工只提交草稿，不能为解决可见性自动猜负责人、自动审批或给库存放行。

### 6.4 生成后徽章与会话保护

已最终回读下列源码：

- `material_analysis_plan_actions.dart`在计划生成成功并应用服务端结果后调用`refreshAfterProductionPlanGenerated`。`production_execution_refresh.dart`发布`production:execution`列表刷新信号，并刷新已初始化的车间/待办/未读计数及仓库DRAW、工作台和通知列表；仅创建备料child不调用这个正式计划结果刷新。
- `production_workshop_tasks_page.dart`监听执行刷新信号和返回页面事件，重新加载当前筛选；加载代次只允许最新请求更新列表，错误保持可见重试入口，不能吞成空任务。
- `production_execution_group_panel.dart`监听同一执行刷新信号，未处于打开/报工动作时重载进度详情；`dashboard/providers/workbench_refresh.dart`的全局徽章刷新也包含车间任务计数。
- `production_workshop_task_count_provider.dart`按会话状态、有效账号、actor、代操作只读状态和查看权限变化重建计数器，初始清零；异步响应同时检查最新请求代次、stop和mounted，旧账号/旧权限响应不得晚回写。
- 仅同一账号上下文的瞬时网络失败保留最后确认值，不跨账号保留。徽章来自真实执行段计数，不在创建child后人工加1。

独立`production_workshop_task_count_provider_test.dart`6项已通过：手动刷新胜过迟到轮询、换账号清零并拒旧响应、撤权不发请求且重新授权恢复、actor变化清零、stop拦晚响应和后续请求、同账号断网保留最后确认数。该6项是provider专项，不能代替现场账号或完整页面交接验证。

现场交接续作集成测试另110/110通过，退出码0，日志 `build/material-workshop-followup-tests.log`：物料页99项(原97项加新child首窗与输入保留2项)、刷新helper/页面2项及其余车间/进度/执行查询接口默认筛选契约。该110项与第四节页面范围重叠，不相加为新的全量总数；主任务第四节仍负责最后的完整范围和静态分析结论。

### 6.5 本地候选与现场版本

SQL修复和前端交互属于本地源码候选。现场server进程尚未重启，没有取得真实账号使用修复版本后列表/徽章成功回读证据；不能把源码测试写成已安装、实际用户任务已恢复或真实岗位UAT通过。本文没有执行现有业务库写入、迁移、服务部署或GitHub CI。

现场补充文档完成后再次检查8份业务文档：118个相对链接全部存在，新增全角括号0，git diff --check无错误；该检查不包含真实服务版本验收。
