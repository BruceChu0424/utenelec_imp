# 61 - V417 财务迁移异常队列、付款幂等与 50 年账本证据

> 记录时间：2026-08-28（America/Denver）  
> 性质：当前共享工作树的本地候选证据；不是公司目标库迁移、岗位 UAT、恢复演练或部署证明

## 一、对应源码字节

| 文件 | SHA-256 |
|---|---|
| `V416__finance_migration_exception_queues.sql` | `6036189170472362D29A18BE26AB405B675DC19DEE9377C62B2246192231ECAA` |
| `V417__finance_payment_command_idempotency.sql` | `822601204A82BE8319022443F6A3271E07F5A66AA9C1F98D1D7B2A57314E0DF9` |
| `AccountStatementScalePostgresTest.java` | `3D7387FD7825FD0A453D84E2C4E7D285E8D4A62E7EEE869350A98E3AFA81602E` |

V407/V408 已被本机 Flyway 执行，未修改其字节。新增规则只通过 V416/V417 前向迁移落地。

## 二、执行命令与环境

```powershell
$env:UTEN_RUN_DB_TESTS='true'
mvn -q "-Dtest=AccountStatementScalePostgresTest" test
```

- PostgreSQL：Testcontainers `postgres:16-alpine`，服务端版本 16.15；
- Flyway：在最终兼容性重跑中校验并从空 schema 应用 381 个唯一迁移，最终版本 V419；财务迁移字节仍止于 V417；
- 迁移结果：`Successfully applied 381 migrations ... now at version v419`；
- 测试报告：6 项、0 failure、0 error、0 skipped，总耗时 35.79 秒。

## 三、数据规模与查询计划

- 5 个人民币账户；
- 每账户 50,000 条，总计 250,000 条追加式流水；
- 1976–2026 共 50 年分布；
- Java/JDBC 记录墙钟：20 ms；
- PostgreSQL Planning Time：5.561 ms；
- PostgreSQL Execution Time：1.059 ms。

`EXPLAIN (ANALYZE, BUFFERS)` 的完整月期初使用
`idx_account_flow_monthly_account_month` Index Only Scan，起始月尾段和查询窗口使用
`idx_frec_account_date_stable` Index Only Scan；没有
`Seq Scan on finance_reconciliations`。V418/V419 是后续生产/仓库候选；本次通过只证明财务 V416/V417 在该结构上兼容，不等于 V418/V419 功能回归通过。这些数字只属于本次容器、机器和样本分布，不外推为目标库 P95/P99。

## 四、同轮正确性与失败关闭证据

同一测试类确认：

1. 账户缓存余额与 `init + Σ(in-out)` 原账重建差异为 0；
2. 月汇总与不可变原账逐月差异为 0，增量上海月份归属和按账户重建均正确；
3. 规模样本在 `v_account_flow_migration_exceptions` 中为 0；
4. 相邻分页按 `bill_date,posting_seq` 稳定且无重叠；
5. 无 `dateFrom` 时仍从期初滚动全部原始事实；
6. 新流水缺来源 UUID，或人民币账户原币金额与本位币快照不一致，均被 SQLSTATE `23514` 拒绝；
7. V1 收款缺银行流水号或 maker 自审均被 SQLSTATE `23514` 拒绝；
8. 直接 SQL 只把 V1 收款草稿改成终态但不写真实账户流水，在提交时被
   `receipt_flow_terminal_guard` 拒绝，单头仍为草稿。

付款幂等另由 `FinancePaymentSettlementTest` 覆盖同键同请求重放、同键异内容冲突和陈旧
`expectedVersion` 拒绝；`FinancePaymentSettlementPostgresTest` 在 V417/379 空库上 5/5、跳过 0。

## 五、当前共享工作树全量回归边界

- 非清理 `mvn -q -DskipTests compile` 通过；本轮财务核心服务/权限/GL/迁移合同定向 74/74 通过。
- 完整 `mvn -q test` 在 V418/V419 并发工作期间不可作为接受证据：一次运行中 `target/classes` 与迁移资源被并发清理，最终为 2495 项、12 failure、44 error、325 skipped；另有可独立识别的 V418/V419 仓库夹具/审计覆盖失败。Windows 文件锁又阻止 `mvn clean`，因此没有把这次结果写成财务失败或全量通过。
- 财务 Flutter 收款、付款、客户预收、账户流水契约 29/29 通过；完整 `flutter analyze` 0 issue。
- 完整 `flutter test` 当次运行 1135 项、2 项失败：一个财务标签预期已修复并在财务 29/29 中复验；另一个是并发 V419 仓库到货登记导航夹具，仍不属于财务接受证据。

所以当前可以接受的是“财务 V416/V417 源码候选及其定向/数据库证据”，不能接受“V419 全项目回归完成”。

## 六、仍需目标环境完成

- 对目标 `flyway_schema_history` 做精确文件/校验和盘点并先取得可恢复备份；
- 查询并签收 `v_receipt_v0_settlement_exceptions`、`v_receipt_v0_gl_reconciliation`、
  `v_account_flow_migration_exceptions`、账户缓存/流水、AR/AP 与 GL 差异；
- 以目标真实分布重跑首页、深页、大日期区间、关键字和全量重建的 EXPLAIN/P95/P99；
- 完成财务/销售/采购/委外/仓库多岗位 UAT、银行/代理回单核对、恢复演练和签名发布。

任一项目未完成时，生产继续 **NO-GO**。
