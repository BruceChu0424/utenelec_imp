# 2026-07-31 生产全链路实现与复核报告

> **2026-08-11 后置覆盖**：本文保留 7 月 31 日历史证据，不是当前页面、迁移或全链路验收结论。当前单入口、多产品 BOM、V192 默认车间、部分 IQC PASS 和“仅部分溯源投影”以[2026-08-11 文档同步报告](2026-08-11-生产调度物料分析与溯源文档同步报告.md)为准；生产仍为 **NO-GO**。

> **历史快照提示（2026-08-09）**：本文业务链证据按原日期保留；其中 Flyway、测试和部署状态已
> 发生变化。当前平台部署结论、真实验证和待办以
> [2026-08-09 本地云端部署与生产就绪清单](2026-08-09-本地云端部署与生产就绪清单.md)为准。

> **结论**：销售→生产→仓库→采购→报工→成品入库→余料退回→销售通知的 V1 核心源码与定向自动化证据已经建立，但当前生产发布结论仍为 **NO-GO**。
>
> **原因**：源码可执行不等于现网数据可信、岗位实物流程可用或运维可恢复。历史数据迁移/零差异对账、人工 UAT、生产规模压测、Outbox 运维、备份恢复、权限矩阵和若干业务能力仍未过门禁。
>
> **复核范围**：README、`docs/` 业务/架构/数据迁移/治理文档、V149–V165 迁移（生产履约主体为 V150–V164）+ V176（subplan_links 归属列，本轮新增）、Spring Boot 服务和测试、Flutter 排产/工作台/报工/审计页面与测试。
>
> **已验证发布基线**：提交 `5097f8cc77bda2e84c04e1b80d893c5392bf8a55`，分支 `codex/modular-architecture`，草稿 [PR #2](https://github.com/BruceChu0424/uten_imp/pull/2)。该基线明确不包含工作区中 V166 及之后仍在开发/复核的迁移、Dashboard、库存余额调整、采购单位治理、审计/人事增强和 `website/`。
>
> **2026-08-01 后续证据（不改写上述 7 月 31 日发布基线）**：V181 已在本地目标库成功应用，活动
> BOM 为 198,025 条且 stub 端点为 0；81 条误接边与严格证明安全的 2 条 DRAW 明细、1 张空采购申请链
> 已软删，31 个历史货品锚及历史成本/余额/单据引用保留。源端 20,798 条 reject 仍未完成业务认定，
> 因而本报告的 NO-GO 结论不变。
>
> **当前口径提示**：本文是 2026-07-31 历史基线及 8 月 1 日早期补充。V187–V190 落地后的销售发运、
> 仓库事件、退货冻结、当前全量自动化证据和剩余 NO-GO，以
> [2026-08-01 全链路安全复核报告](2026-08-01-销售仓库生产采购委外全链路安全复核报告.md)为当前权威；不要从本文旧红冲或旧缺口清单推导现行规则。

---

## 一、这次真正交付了什么

| 能力 | 当前实现 | 权威数据/主要证据 | 发布判断 |
|---|---|---|---|
| 当前 BOM 输入门禁（后续补证） | V181 隔离 `auto_created` 误接边，并由数据库触发器及 `GoodsBomService` 禁止占位/已删除货品成为活动 BOM 端点；历史快照不回写 | V181、`GoodsBomStubIsolationTest`、目标库活动 stub BOM/DRAW/采购申请均为 0 | 运行时污染已关闭；20,798 条源端 reject 仍阻断发布 |
| 排产详情 | 产品 BOM 默认全部展开；不再先选车间；“一键生成子计划”先预览，进入物料需求评审对话框（充足/采购缺料/自制缺料/委外缺料 四分类（2026-08-05 起：无 BOM 不再阻断，自制叶子件缺料派生「造 N 个」子计划，对话框不再转发研发），勾选生成采购申请，段值采用预览建议）；精细编辑车间/班组/日期仍走执行段编辑面板 | `material_review_dialog.dart`、`execution_segment_planning_sheet.dart` 静态复核、`production_execution_planning_model_test.dart`、Flutter 全量测试 | 源码已实现，需岗位与无障碍 UAT |
| 完整套料拆分 | 按所有必需物料的最小支持套数拆 READY/WAITING；WAITING 不占零散现货、不发 DRAW | `CompleteKitAllocator`、V155、`ProductionExecutionSegmentPostgresTest` | 核心可执行 |
| 统一原料履约账 | 需求用 `production_material_demands`；物理占用复用 `stock_reservations`；未来供给用 `production_material_supply_pegs` | V150、`ProductionMaterialFulfillmentPostgresTest` | 源码已实现，历史数据对账未完成 |
| 可用库存 | V150 用 LATERAL 重建 `v_stock_available`，避免仓级+全局预留重复 JOIN；安全库存继续从可排量扣除 | V150、库存兼容/并发 PG 测试 | 源码已修复，现网全量对账仍阻断 |
| 可靠通知旁路 | 业务事务同写 `business_outbox`；真实 WAITING→READY 追加 `PRODUCTION_SEGMENT_READY`，按执行段+收货去重并按 `SUB_PLAN`/`DEPT_PROD` 部门权限树投递 | `ChainNoticeReadyEventTest` 的 publishOnce/dedupe payload、PG 状态迁移证据、`ChainNoticeService` 部门映射源码 | 核心可执行，部署监控/重放/对象范围黑盒未完成 |
| 领料/良品退料/结清 | DRAW/WDRAW 负责库存，posting 精确记录单据行→需求→占用；显式消耗、审批损耗、合法在制参与结清；重复和红冲沿原来源对称 | V152、`ProductionMaterialIssueReturnPostgresTest` | 核心可执行；不良品实物处置、超领/补料、成本未闭环 |
| 部门任务工作台 | 仓库、采购、委外三张同源工作台；状态和计数来自履约视图与真实单据；可跳回来源单；零计数卡片、刷新后状态消失及未知状态均保持合法筛选 | V153、工作台 Java/Flutter 测试、三个部门零计数/刷新回归 Widget 测试 | 核心查询可用，Dropdown 崩溃已修复；独立备料交接状态缺失 |
| 采购请求→到料 | BUY 缺口形成采购申请；订单和分批收货逐笔迁移 peg；整套齐备才原子 WAITING→READY+DRAW | V154、`ProductionPurchaseSupplyTransitionPostgresTest` | 核心可执行；跨申请合单/IQC 未闭环 |
| 正式执行段 | `production_execution_segments` 是计划明细的执行批次，不是自制件子计划本身；V1 禁止再走旧派生写入口，但通过专用干净内核 `generateSelfMadeSubplansForPackage` 派生自制件子计划（`subplan_links.source='EXECUTION_V1'`） | V155、规划/生命周期测试 | 核心可执行；历史 V0/V1 切换需对账 |
| 派工/开工/报工/入库/结案 | 执行段分配、派工、开工、分批报工、精确成品入库；完成需成品数量及物料结清同时满足 | V156、`ProductionExecutionSegmentOperationsPostgresTest` | 核心可执行；质量附件/工时/补产未闭环 |
| 销售精确分摊 | 每个执行段冻结到具体 `plan_order_item_link`/销售订单行；报工和成品入库携带分摊 ID 并按其容量校验；销售订单行显示自身执行段进度 | V157、`ExecutionSegmentSalesAllocationPostgresTest`、销售进度 Flutter UI | 已纳入组合回归证据；历史来源迁移仍待完成 |
| 委外供给回写 | SUBCONTRACT 缺口→委外申请→订单→分批回厂逐需求迁移 peg；整套齐备才 WAITING→READY + reservation/DRAW；未开工可精确反向 | V158、供给合同/迁移测试、`ProductionExecutionSegmentPostgresTest` | 供给 V1 可执行；委外发料/在制/余料清账未实现 |
| 完成后红冲 | 已完成段先显式 `REOPEN_COMPLETION`，原子重开全部关联段；再允许入库与报工按顺序反向 | V159、`ProductionCompletionReversePostgresTest` | 已纳入最终组合回归；岗位反向 UAT 未完成 |
| 待料临时库存覆盖 | 采购工作台按仓货色、需求日、稳定优先级用自由库存抵减建议；不落占用，库存变化可重新暴露缺口 | V160、`FulfillmentWorkbenchProvisionalStockPostgresTest` | 核心查询可用 |
| 物料台账 append-only | 已接受的领退/消耗/损耗事件和 posting 禁止 UPDATE/DELETE；纠错只能追加对称反向记录 | V161、`ProductionMaterialAppendOnlyLedgerMigrationTest`、`ProductionMaterialAppendOnlyLedgerPostgresTest` | 数据库守卫已建立；历史异常仍须对账 |
| 采购/委外来源与守恒 | 生产 peg 引用的申请/订单来源行不可被通用 CRUD 换料、改量或删除；需求身份、peg 生命周期、委外转换/回厂、reservation/DRAW 来源和容量联动校验 | V162、`ProductionSupplySourceGuardMigrationTest`、`ProductionSupplySourceGuardTest` | 核心 fail-closed；完整岗位反向 UAT 未完成 |
| 采购收货来源追溯 | 每条有效收货分摊贯穿收货头/行、采购订单行、需求、reservation、DRAW 和执行段，物料/仓库/数量不一致即拒绝；已消费不能直接反向 | V163、`ProductionPurchaseReceiptProvenanceMigrationTest`、`ProductionPurchaseReceiptProvenancePostgresTest` | 核心来源链已加固；应付/质量联动仍需验收 |
| 生产关联库存单据保护 | 以计划包、`plan_draw_links`、执行段/销售分摊外键识别生产单据；通用库存 CRUD 不可改头、删单或替换明细，专用审核/分批出库/反向仍可用 | V164、`ProductionLinkedStockDocumentGuardMigrationTest`、`ProductionLinkedStockDocumentGuardPostgresTest` | 核心防篡改已建立；真实角色/API 黑盒仍需 UAT |
| 审计详情 | 系统管理审计列表可打开桌面侧滑/移动底部详情，查看脱敏元数据与格式化 before/after | `AuditQueryService`、Flutter 审计详情 Widget 测试 | 源码已实现；语义覆盖率/保留运维待验收 |

V157 精确销售分摊与 V161–V164 已纳入最终组合回归：后端 `mvn test` 共 343 项、0 failure/error、48 skipped（真实 PG 默认跳过）；真实 PostgreSQL 16 个测试类共 44 项、0 failure/error/skip；空库从 V1 回放 146 个迁移到 V165。V148 已按已部署校验和 `1590936016` 锁定，V147→V165 两阶段升级测试通过。上述证据仍不替代历史数据迁移、对象范围黑盒和人工 UAT；V158 也不能推导为委外发料、在制、质量回厂和余料核销已经完成。

---

## 二、端到端走查：数据应当怎样变化

### 2.1 销售审核与生产待排

1. 销售订单审核后，生产待排列表按交期和急单规则读取审核有效订单。
2. 未排量必须由订单有效数量、已发/退、销售成品预留和有效计划关联聚合计算；页面缓存不是权威值。
3. 销售只能看获授权订单，成本、物料价格、内部质量责任和绩效字段不得进入销售 API。

**失败语义**：订单来源、单位换算或累计为负时 fail-closed，不允许调度用手工数字覆盖错账。

### 2.2 预览与确认

1. 打开排产详情时，全部产品 BOM 直接显示；系统计算单台用量、总需求、现货、其他单据占用、安全库存、未来供给和缺口。
2. 点击“一键生成子计划”只生成预览，不写计划包、执行段、库存占用、采购申请或领料单。
3. 用户在编辑面板调整后确认；服务端重新锁定订单/计划/库存，校验预览指纹、版本和稳定幂等键。
4. 示例：产品需求 10 件，物料 A 可支持 10、B 可支持 6，结果必须是 READY 6 + WAITING 4。READY 为 A6/B6 建完整物理占用和 DRAW；WAITING 的 A4/B4 只保留需求，不占 A 的零散现货。
5. 若计划明细合并多个销售订单行，确认时还必须把每个执行段按 link 剩余容量做不可变精确分摊；每段分摊合计等于段计划量，不能把一个段 JOIN 到所有订单行形成笛卡尔重复。
6. 确认事务还会对自制组件（有 BOM 且净需求>0）派生子生产计划（`subplan_links.source='EXECUTION_V1'`，V176）；成品未维护 BOM 的计划行通过 `noBomPlanItemIds` 返回，前端引导补 BOM，不再整批失败。

**为什么这样算**：如果 WAITING 把已有的 A4 先锁住，它可能长期饿死更早能凑整套的任务；如果不做临时覆盖，采购端又会把 A4 错算成采购缺口。V160 因而只在采购建议中按需求优先级临时抵减，不生成占用；正式转 READY 时再次原子抢整套。

### 2.3 缺料、采购与到料

> 本节是 2026-07-31 历史快照。V196+ / ADR-019 已后置替代第 2–3 点的入口和审批责任：申请由计划链下达且业务端只读；任务中心可跨申请选行但一供应商/委外商、一仓库一单；订货送财务审批审核组（V229/ADR-027：财务部门持 `finance_order_approval:review` 的审核组），通过后才生效。历史数量守恒结论继续保留。

1. BUY 路线的核验缺口随计划包生成采购申请和逐需求 peg；如果用户要求不生成采购申请但仍有 BUY 缺口，确认整体失败。
2. 采购工作台可按状态/逾期/异常筛选，并多选同一张已审申请的明细转采购订单。当前不支持跨多张申请自动合并，避免无供应商/币种/税率/MOQ 规则时错误拼单。
3. 采购订单审核把申请 peg 一对一迁移为订单 peg。
4. 分批收货只把实际审核收货量转为目标仓库存及收货供给事实；部分到货不足整套时，WAITING 不提前转 READY。
5. 当所有必需物料可一次覆盖整段时，事务内稳定锁定仓货色、创建全套 `stock_reservations`、生成精确 DRAW，并把段转 READY；只有真实迁移成功才同事务写 READY Outbox 事件，按执行段+触发收货去重。
6. V162/V163 要求申请/订单来源、收货头/行、订单 peg、生产需求、reservation、DRAW 和执行段同源；采购订单、收货或申请反向时，若对应数量已被生产占用/消耗，必须先解开精确下游，不能用 `GREATEST(...,0)` 静默吞差异。

### 2.4 委外供给（不是委外执行闭环）

> 同样地，以下“委外申请草稿/委外订单审核”是历史实现描述；V196+ 当前口径为计划下达只读申请、委外分解订货、精确财务审批。发料、供应商在制与 IQC 未完成边界仍然有效。

1. 物料主数据明确选择 SUBCONTRACT 时，确认生成委外申请草稿并为每个生产需求建立 peg；未知来源和 MAKE 继续 fail-closed。
2. 委外订单审核把申请 peg 等量迁移到订单 peg；回厂单审核按 receipt item 分配并可分批累计，重复回调不重复挂接。
3. 只有 WAITING 段的全部必需物料都可覆盖时，才整体转 READY 并建立全套物理占用和 DRAW；单项提前回厂不会假开工。
4. 未开工的订单/回厂反向会整套撤回 READY 分配；已有消费时数据库和服务共同阻断直接反向。
5. 当前不把委外发料单、委外在制消耗、余料退回/损耗自动挂到该生产需求；这些仍须后续统一清账。

### 2.5 仓库备料与领料

1. 仓库工作台只显示后端同源 DRAW 任务；当前把未审核 DRAW 归为“待备料/待领取”，部分审核累计为“部分领取”，全部为“已领取”。
2. 仓库审核发料才产生库存流水，并按 posting 精确消费原 `stock_reservations`；重复审核或相同幂等请求不得二次扣料。
3. 取消/红冲必须沿原 posting 恢复同一需求、同一占用和同一库存方向。
4. 生产关联 DRAW/FINISHED_IN 的通用库存编辑、删行和删单由 V164 阻断；审核、分批出库、完成重开和专用反向必须走对应业务入口。

**当前边界**：尚无独立的“待备料→备料中→待双方交接→已交接/生产在制”状态机；因此现有卡片不能假装已经记录实物交接时间、拣货人和双方签收。

### 2.6 派工、开工与分批报工

1. 调度可在执行段上更新车间、班组、负责人和日期，然后派工；状态版本必须匹配。
2. 开工后进入生产中；报工页面从执行段入口带入精确身份，不再只按货品猜计划。
3. 同一执行段允许多次报工；审核累计合格量不得超过执行段计划量，跨计划/跨段身份直接拒绝。
4. 草稿报工不改变生产进度；审核后才更新段累计和销售/看板旁路事件。

**当前边界**：不良原因、照片、完整参与人员/实做工时、返工/补产幂等、阈值通知尚未全部实现，不得标为“质量报工完整闭环”。

### 2.7 成品入库、物料清账与结案

1. 成品入库行必须指向正确执行段；审核数量不得超过该段已审核合格且尚未入库的数量。
2. 首次或部分入库只更新入库累计；不能因为报工达到计划量就提前把成品视作可发库存。
3. 物料实际消耗、良品退料、审批损耗/报废和合法在制必须与累计领用对平。
4. 良品退料先生成 WDRAW，仓库审核点收后才增加原料仓可用库存；申请草稿不增库存。
5. 只有成品入库数量达标且物料结清，执行段才允许 COMPLETED。

### 2.8 完成后的反向

1. COMPLETED 段不允许直接红冲报工，防止出现“成品仍在库但合格报工已减少”的倒挂。
2. 先对已审核成品入库发起完成重开：锁单据和全部关联段，校验版本、累计和下游，写唯一 `REOPEN_COMPLETION` 事件，段从 COMPLETED 回到 IN_PROGRESS。
3. 然后红冲成品入库，最后才允许红冲原报工。
4. 一个入库单关联多个执行段时必须全有或全无；任一段失败，重开事件、段状态和库存单据都回滚。
5. 完成时已释放的未用物料不会因重开自动恢复占用；若需继续生产，应重新走可用量校验和领料，不得凭历史占用复活。

### 2.9 销售同步、部门任务和审计

1. 排产、缺料、开工、部分完成、完成、延期等事件与主事务一起写 Outbox。
2. 消费者按 `dedupe_key` 幂等投递；READY 使用执行段+触发收货去重，失败回滚本次消费并重试，达到阈值进入人工处理。
3. 销售订单详情按订单行读取 V157 精确分摊，只显示该行的执行段编号、计划量、状态、车间、日期、已报工和已入库；不通过货品 JOIN 猜归属。
4. 仓库/采购/委外工作台读取同一履约读模型；数字变化必须能追到需求、执行段、来源单据和审核状态；READY 待办按 `SUB_PLAN`/`DEPT_PROD` 部门及有效子部门解析接收人，不依赖已下线角色。
5. 系统管理审计详情显示脱敏的 before/after；业务数量变更还应带来源、幂等键、操作者、原因和前后累计。

### 2.10 任务工作台筛选稳定性

1. 仓库、采购、委外的状态统计卡使用固定业务定义，即使数量为 0 也允许点击；这表示“用该条件确认当前无任务”，不是无效状态。
2. 旧实现只用后端非零 `statusCounts` 生成下拉选项，点击零计数卡或刷新后当前状态消失时，选中值在 `DropdownMenuItem` 中匹配 0 项，因而触发 Flutter 断言。
3. 修复后先清理空选项、按值去重；当前选中值缺失时补入一个由统一标签解析器生成的临时选项，并用包含当前值的 `ValueKey` 同步外部筛选状态。
4. 任何时刻都必须保证：空筛选只匹配一个“全部”项，非空筛选在下拉项中恰好匹配一次。未知后端编码保留原值，不能为了避免异常而静默清空筛选。
5. 回归覆盖仓库 `READY_TO_PICK`、采购/委外 `UNPEGGED` 的零计数卡、零逾期卡，以及刷新后当前后端状态从选项中消失的场景。

详细交互和部门动作边界见[生产履约任务工作台](../03-页面/生产履约任务工作台.md)。

---

## 三、不会让数据乱掉的关键算法

### 3.1 可排完整套数

```text
物料 i 可支持套数
= floor((合格自由库存_i - 安全库存_i) / 单台基本用量_i)

立即可生产量
= min(订单未排量, 所有必需物料可支持套数)
```

未来采购/委外供给只用于预计齐套与净需求，不直接进入立即可生产量。

### 3.2 占用和清料

```text
占用有效量 = qty - consumed_qty - released_qty >= 0

累计审核领用
= 已确认耗用
+ 已验收良品退料
+ 已验收不良/报废
+ 审批损耗
+ 合法在制结转
```

任何红冲必须按原 posting 反向；不能靠重算当前 BOM 猜原来领了什么。

V161 进一步把四张生产物料事件/posting 表设为 append-only：已接受事实不能原地改或删，错误只能新增对应反向事实，避免审计、幂等和清料恒等式被历史重写破坏。

### 3.3 V160 临时覆盖

```text
自由库存池 = 账面可用 - 已生效占用 - 安全库存
按 仓库/货品/颜色 分区
按 需求日、计划、需求稳定排序依次抵减
采购建议 = max(需求未覆盖量 - 本需求取得的临时覆盖, 0)
```

临时覆盖只影响工作台建议，不是库存承诺；正式 READY 仍必须重新取锁并一次占齐。

### 3.4 并发与幂等

- 单据表头先 `PESSIMISTIC_WRITE`，上游和需求按 UUID 稳定排序。
- 库存按仓库/货品/颜色取得事务级 advisory lock。
- 相同幂等键 + 相同请求哈希返回同结果；相同键 + 不同载荷返回冲突。
- 任何中途失败整事务回滚，Outbox、库存、累计和审计不能各自成功一半。

### 3.5 销售分摊守恒

```text
Σ(某执行段的销售分摊量) = 执行段计划量
Σ(某计划销售 link 被各执行段冻结的分摊量) <= link 有效分配量
报工/入库累计 <= 对应执行段销售分摊量
```

---

## 四、自动化证据与最终复核状态

本轮已分阶段建立以下针对性证据；最终合并前仍应由根任务在所有并行改动落定后串行复跑，避免 Maven `target` 并发污染：

- 统一账与完整套料：`ProductionMaterialFulfillmentPostgresTest`、`CompleteKitAllocatorTest`、`ProductionExecutionSegmentPostgresTest`。
- 采购供给：`ProductionPurchaseSupplyTransitionPostgresTest`。
- 领/退/清料：`ProductionMaterialIssueReturnPostgresTest`。
- 报工/入库/结案：`DailyReportExecutionSegmentGuardTest`、`ProductionExecutionSegmentOperationsPostgresTest`。
- 完成态反向：`ProductionCompletionReversePostgresTest`、迁移/服务契约测试。
- Outbox：`BusinessOutboxPostgresTest`。
- 委外供给：`ProductionExecutionPlanningServiceSupplyRouteTest`、`ProductionSubcontractSupplyTransitionContractTest`、`ProductionSubcontractSupplyTransitionMigrationTest`，以及执行段真实 PostgreSQL 整套唤醒场景。
- 销售精确分摊：`ExecutionSegmentSalesAllocationPostgresTest`（笛卡尔泄漏、报工/入库容量、冻结 link 删除/缩量、销售改单 409）及销售进度 UI 测试，已纳入组合回归证据。
- V160 临时覆盖：`FulfillmentWorkbenchProvisionalStockPostgresTest`。
- V161 append-only 台账：`ProductionMaterialAppendOnlyLedgerMigrationTest`、`ProductionMaterialAppendOnlyLedgerPostgresTest`。
- V162 采购/委外来源守卫：`ProductionSupplySourceGuardMigrationTest`、`ProductionSupplySourceGuardTest` 及执行段/委外定向 PG 场景。
- V163 采购收货追溯：`ProductionPurchaseReceiptProvenanceMigrationTest`、`ProductionPurchaseReceiptProvenancePostgresTest`。
- V164 生产关联库存单据：`ProductionLinkedStockDocumentGuardMigrationTest`、`ProductionLinkedStockDocumentServiceContractTest`、`ProductionLinkedStockDocumentGuardPostgresTest` 及 Flutter capability 测试。
- READY 部门通知：`ChainNoticeReadyEventTest` 覆盖 publishOnce 与 dedupe payload；PG 证据覆盖 WAITING→READY 状态迁移；`ChainNoticeService` 源码实现 `SUB_PLAN`/`DEPT_PROD` 部门映射。
- 工作台和排产 UI：7 月 31 日基线含 repository/model、任务工作台 Widget 与全量测试；2026-08-01 后续又增加 `production_execution_segments_card_test.dart`、`production_board_subplan_navigation_test.dart` 和 `sales_plan_progress_sheet_test.dart`，覆盖执行分段详情/权限/防叠窗、真实子计划跳转及销售深链。
- 审计详情：`AuditQueryServiceTest` 与 Flutter 审计详情 Widget 测试。
- 架构/场景守卫：`ArchitectureBoundaryTest`、`BusinessChainScenarioMatrixTest`。

最终组合回归使用以下口径，不能用“曾经单独通过”代替：

```powershell
cd server
mvn.cmd test

$env:UTEN_RUN_DB_TESTS='true'
mvn.cmd -q -Dtest=InventoryTransactionalIntegrityPostgresTest,StockReservationSalesCompatibilityPostgresTest,ProcurementDocumentStateLockPostgresTest,ReportablePlanLinePostgresTest,ProductionMaterialFulfillmentPostgresTest,ProductionExecutionSegmentPostgresTest,ExecutionSegmentSalesAllocationPostgresTest,ProductionPurchaseSupplyTransitionPostgresTest,ProductionMaterialIssueReturnPostgresTest,ProductionExecutionSegmentOperationsPostgresTest,ProductionCompletionReversePostgresTest,BusinessOutboxPostgresTest,FulfillmentWorkbenchProvisionalStockPostgresTest,ProductionMaterialAppendOnlyLedgerPostgresTest,ProductionPurchaseReceiptProvenancePostgresTest,ProductionLinkedStockDocumentGuardPostgresTest test

cd ..
flutter test --no-pub
flutter analyze --no-pub
git diff --check
```

| 最终检查 | 当前记录 |
|---|---|
| 后端全量 | `mvn test`：343 tests，0 failure/error，48 skipped（PG 默认跳过） |
| PostgreSQL 核心链 | `UTEN_RUN_DB_TESTS=true`：16 classes / 44 tests，0 failure/error/skip |
| 已验证发布基线 | `5097f8cc77bda2e84c04e1b80d893c5392bf8a55`；`codex/modular-architecture`；草稿 PR #2 |
| V158–V164 定向证据 | 委外供给、完成反向、临时覆盖、append-only、来源/receipt/库存单据守卫均已纳入组合回归 |
| Flutter 全量 | 157/157 通过 |
| Flutter Web Release | `flutter build web --release --no-pub --no-wasm-dry-run` 构建成功 |
| Flutter analyze | `flutter analyze --no-pub`：no issues |
| Flyway 空库 V1–V165 | 146 个迁移成功回放到 V165（V166–V176 为基线后新增，含本轮 V176 subplan_links 归属列，待纳入下次基线复核） |
| Flyway V148 升级兼容 | 已部署 checksum `1590936016` 保持不变；V147→V165 两阶段迁移及别名/空来源边界测试通过 |
| `git diff --check` | 通过 |
| 人工端到端/实物交接 | 未执行，生产发布阻断 |
| 压测/恢复/告警/回滚 | 未执行，生产发布阻断 |

---

## 五、明确未完成、不能对外承诺的能力

1. 历史数据迁移、异常隔离、开放业务补链以及库存/计划/领退耗/财务零差异对账。
2. 有限产能 APS、冻结期、急单插单影响清单、设备/工序/换线/技能优化。
3. 替代料主数据、换算、生效范围、审批版本和成本差异。
4. IQC/IPQC/OQC、不良品隔离、照片、完整人员工时、返工、补产、不良阈值通知。
5. 超领/补料审批、不同质量余料、不良品实物处置，以及报废/在制的成本过账完整闭环。
6. 仓库独立拣货、备料中、双方交接和生产在制状态机。
7. 跨采购申请按供应商/币种/税率/MOQ 自动合并，以及主动低库存补货、阈值回滞、上限与人工确认。
8. 委外发料、委外在制、回厂质量、余料退回/损耗与逐需求核销。
9. 全量对象级权限矩阵、语义审计覆盖率、销售敏感字段黑盒测试。
10. 生产规模性能、死锁压力、Outbox 积压/重放、监控告警、备份恢复、回滚和灰度演练。

---

## 六、生产放行清单

只有以下项目全部有负责人、日期、证据和签字后，才能把 README 的 NO-GO 改为 GO：

- [ ] 历史迁移脚本可重复执行，异常单独隔离，前后快照与对账为零差异。
- [ ] 核心 PostgreSQL 并发、反向、幂等和 Flyway 回放在候选版本全部通过。
- [ ] 销售、生产、仓库、采购、委外、质量、系统管理完成正向和反向 UAT。
- [ ] 实物发料/退料/成品入库与系统数量逐笔抽盘一致。
- [ ] 订单级分批/齐套/客户确认策略与仓库绑定、拣货、交接状态通过销售/PMC 联合验收。
- [ ] 采购/委外到货待检、合格转可用和不合格反向通过质量岗位验收。
- [ ] 权限对象范围、批量全有或全无、成本/隐私脱敏黑盒验收通过。
- [ ] 容量、锁等待、死锁、Outbox 积压和重放达到签字 SLO。
- [ ] 备份恢复、故障切换、回滚、审计归档和告警值班演练通过。
- [ ] 未实现能力保持功能开关关闭或 fail-closed，前端不展示误导性“已完成”。

---

## 六补、2026-08-01 生产子计划交互与全流程复核补充

### 6A. 本次修复事实

- `subplan_links` 中的真实自制件子计划现在从父计划详情和生产进度页进入 childPlanId 自己的计划详情；父计划表头、展开控件和子计划行是独立点击域。
- `production_execution_segments` 仍是父计划执行从属；点击打开自适应详情，只读用户可查看，调整/派工/开工/报工按状态与权限显示。
- 确认结果和销售订单生产进度可通过 `/production/plans/{parentId}?executionSegmentId={segmentId}` 定位目标分段；自动打开一次且重复点击不叠窗。
- MRP 预览与真实子计划列表失败隔离；`MrpService` 使用安全日期转换兼容 JDBC 日期对象/文本，避免子计划列表强制转换 500。
- 空计划 ID、自引用、无权限、目标不存在或路由失败均有明确反馈，不再保留“可点但没反应”的交互。

涉及源码：`production_execution_segments_card.dart`、`production_plan_detail_page.dart`、`production_routes.dart`、`production_board_page.dart`、`sales_plan_progress_sheet.dart` 和 `MrpService.java`。

### 6B. 当时验证证据（后续已由当前候选全量证据覆盖）

- 相关回归 33 项通过；其中新增三份 Widget 测试分别覆盖执行分段、生产看板真实子计划和销售深链。
- 目标 Dart analyze 已输出 “No issues found”；命令随后仅因沙箱无法写 Flutter telemetry 时间戳返回非零。
- 未执行登录态浏览器人工 UAT；未以本次 Java 日期兼容改动执行 Maven 全量构建。
- 对整个生产目录的 analyze 仍被并行工作区中**无关且未跟踪**的 `where_used_product_detail_dialog.dart:537`（无效 `const Semantics`）阻断；该文件不属于本次修复，未被改写。

因此本次证据只能支持“点击/深链源码与定向回归已修复”，不能替换 7 月 31 日发布基线，也不能推导生产可上线。

### 6C. V187 落地前确认的 P0 快照

1. 销售订单缺少 `ALLOW_PARTIAL / REQUIRE_COMPLETE / CUSTOMER_CONFIRM` 权威策略，尚不能系统决定“一个现货、其余一个月后完成”是先发还是齐套。
2. 订单有效预留减少 ATP 但不扣在手；发运窗口绑定仓库后仍缺独立分配、拣货、交接状态，不能把“可发货”当作“仓库已备好”。
3. 采购/委外回厂没有完整待检隔离和合格转可用门，供给 peg/收货回写不等于质量合格。
4. 委外缺供应商处我方库存、在制耗用、回厂质量、余料/损耗和期末结存守恒账。
5. V176 已创建 MAKE 自制子计划谱系，但须补集成证据证明子计划完工/入库对父物料需求 supply peg 的唤醒，以及取消、改量、反向守恒。
6. 历史开放订单、V90 预留、旧 MRP、BOM/单位、库存和委外发料仍须可重复迁移、异常隔离与零差异对账；不得回写伪造历史事实。

**复核结论不变：生产发布仍为 NO-GO。**

> 后续 V187/V188 已把上述第 1、2 项推进为“订单级三策略 + 单据级仓库状态 + 不可变事件”的最小闭环，
> 但完整 WMS 数量态仍未落地；第 3–6 项仍是当前阻断。最新细分与验证数字以当前全链路安全复核报告为准。

---

## 七、关联文档

- [生产订单排产与执行全链路需求](../07-业务链路/04-生产订单排产与执行全链路需求.md)
- [销售订货到发货全链路 SOP](../07-业务链路/01-销售订货到发货全链路-SOP.md)
- [生产履约任务工作台（仓库 / 采购 / 委外）](../03-页面/生产履约任务工作台.md)
- [生产调度工作台](../03-页面/生产调度工作台.md)
- [生产订单排产页](../03-页面/生产订单排产页.md)
- [生产执行分段与报工页](../03-页面/生产执行分段与报工页.md)
- [生产主管看板页](../03-页面/生产主管看板页.md)
- [销售订单生产进度](../03-页面/销售订单生产进度.md)
- [销售现货预留生命周期与稀缺仲裁](../07-业务链路/05-销售现货预留生命周期与稀缺仲裁.md)
- [生产履约 V1 实体关系与数量权威](../04-数据模型/生产履约V1实体关系.md)
- [业务链场景矩阵](../../server/src/test/resources/business-chain-scenario-matrix.md)
- [ADR-017：模块化单体与异步旁路](../99-决策记录-ADR/ADR-017-模块化单体与异步旁路.md)
- [2026-07-30 生产就绪审计报告](2026-07-30-生产就绪审计报告.md)
- [2026-08-01 销售—仓库—计划—生产/采购/委外全链路安全复核报告](2026-08-01-销售仓库生产采购委外全链路安全复核报告.md)
