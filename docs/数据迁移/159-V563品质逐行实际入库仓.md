# V563 品质逐行实际入库仓

新增前向迁移 `V563__iqc_stock_in_actual_warehouse.sql`，当前目录头 V563/521。设计见 [ADR-079](../99-决策记录-ADR/ADR-079-品质放行逐行实际入库仓与委外成本来源.md)。

- 实际入库项的 `warehouse_id` 来自仓库逐行确认，必须是有效核算叶仓及有效祖先链。
- 原质检/收货仓保留历史参考含义；PASS、原始来源和合格数量不变。
- 新版逐行显式传仓；旧客户端缺仓字段仅继承已有放行切片仓并执行同一叶仓校验，不从商业单据或其它仓补猜。旧成功幂等回执继续可重放。
- 实际库存流水与入库项同仓，来源权益和按仓收货数量基于真实入库记录；不以表头仓代替多个实际仓。
- 委外正常损耗成本使用稳定实际产出对象，处理可空订单仓；该部分复用现有成本引擎，无额外迁移或历史回填。
- 反向按原价值转移恢复原登记池，实际仓库存按原入库流水扣回。

不新增业务表，CLEAR 273 / PRESERVE 96 不变；迁移头、运行脚本完整目录白名单及相应合同同步至 V563/521。本轮只维护脚本和源码，未执行业务数据清理。旧已应用迁移文件保留原字节，目标安装需使用同版本前后端和迁移制品。
