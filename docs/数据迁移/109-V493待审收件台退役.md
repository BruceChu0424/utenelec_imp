# V493 待审收件台退役

迁移文件：`V493__retire_review_inbox.sql`。

用户要求移除常用功能中的待审收件台，任务直接在各业务入口办理并显示徽章。本次前向迁移将
`review_inbox:view` 设为 `active=false/assignable=false`，停用 `reviews.inbox` 并移除其活动面关联。
原授权和通知历史保留供审计，既有 Flyway 文件不变，不改业务单据、库存、应收应付及通知办结事实。

前端旧深链只回 `/dashboard`，原 Flutter 页面/仓储代码和后端聚合 API 删除。
销售订单修改复审复用已有财审 surface 及权限，不产生额外默认授权。

迁移后应核对活动权限目录不返回旧码，旧 surface 无法授权，部门/员工有效权限不含旧码，
既有通知仍可查、各业务任务徽章和部门定向弹窗继续工作。此文件说明源码候选行为，
不代表目标数据库已执行迁移或生产验收通过。
