# 2026-08-16 全链路溯源联动与计算准确性审计修复报告

> **性质**：五路深审（销售↔计划、采购、委外、生产执行↔仓库、前端溯源跳转）+ 计算准确性
> 复核 + 全部修复落地。审计结论先行、关键 bug 由第二人（主会话）逐行核验后才动手修。
> **Owner**：全链 | **日期**：2026-08-16 | **分支**：`uimp/chore/full-integration-20260815`

## 一、审计方法

1. 五个并行深审代理分别扫：销售↔计划/排产链、采购全链、委外全链、生产执行↔仓库链、
   前端溯源跳转与数量/进度口径；覆盖后端 80+ Controller、Flutter 全部相关页面、241 篇文档。
2. 所有疑似 bug 由主会话**逐行核验**（不轻信代理结论）：委外重复消费、催交报表口径、
   前端剩余量口径、ATP/预留/出货回写等「被判正确」的计算也抽查了源码。
3. 修复后：`mvn compile` + **全量后端测试套件**（`UTEN_RUN_DB_TESTS=true mvn test`，含
   Testcontainers 全部 DB/HTTP 测试）+ `flutter analyze`（0 issues）+ `dart format`（CI 门禁口径）。

> 测试过程本身证明有价值：E2E/HTTP 契约/迁移计数守卫先后抓出新代码三处缺陷并已修复——
> ①`employees` 姓名列实为 `full_name`；②原生查询 DATE 列的 `java.sql.Date` 安全转换；
> ③PostgreSQL 无 `min(uuid)` 聚合（改为 `MIN(warehouse_id::text)::uuid`）。同期为五个 mock 型
> 单测补齐新查询的桩、迁移计数守卫 289/270 → 290/271。

## 二、计算准确性结论（审计）

**核心计算全部正确**（多层防线：悲观锁+advisory 锁+CAS 守卫 `updated!=1` 即抛、
分配器先全量校验后写、DB DEFERRED 触发器守恒）：ATP（每仓扣安全库存、预留只扣一次）、
订单行五数量链、部分发货预留拆行守恒、超发双闸、合并排产分摊（Σlink.allocated=计划量）、
执行分段分摊（DB 级守恒触发器 V157/V217）、IQC 两阶段推进（口径分离不重复、幂等唯一索引）、
采购超批准量（declared=accepted+unaccepted 守恒）、报工/入库（防虚入）、SHIPPED 原子事务
与 AR 防重复、汇率口径（订单不换汇、SHIPPED 才锁）、库存余额同事务 upsert。

**实锤 bug（本次修复）**：

| # | 问题 | 位置 | 修复 |
|---|---|---|---|
| 1 | **委外回厂重复消费**：同一订货明细下同一子件分多行/多张发料单时，回厂对每行都全额消费 `回厂量×冻结单耗` → 消耗翻倍/误 409；红冲用 `GREATEST(...,0)` 钳位静默吞错账 | `SubcontractReceiptService.consumeIssuedMaterials` | 按（货品+颜色）分组一次计算总量，组内 FIFO 分摊、逐行 CAS；红冲 LIFO 精确回退 + 不足即 409 人工核销（fail-closed）；同组冻结单耗版本分叉 → 409 不猜版本 |
| 2 | **催交报表漏退回回补**：`unreceivedQty = qty−received` 且 WHERE 同漏 `+returned`；退货后催交对象虚多、退完的行仍催 | `PurchaseReportService` | 列与过滤均改为 `qty−received+returned`（与任务中心/结案重算/在途供给三处权威口径一致） |
| 3 | **前端引入面板剩余量口径不一致**：收货←订货 `q−received` 漏 `+returned`，退货回补后少显示可收量 | `doc_link_picker._remainQty` | 改为 `q−received+returned` |
| 4 | 退料/损耗 CAS 上限漏扣已消费量（报错口径错，DB CHECK 兜底） | `SubcontractMaterialReturnService` / `SubcontractWasteService` | 上限改为 `at_supplier−consumed ≥ returned+wasted+q`（V221 守恒口径，友好 409） |
| 5 | **无余额↔流水对账机制**：漂移（迁移误差/手工改库/未来 bug）永远发现不了 | — | 新增 `StockReconciliationService/Scheduler`（每日 08:51 只读对账、只告警不改账）+ `GET /api/stock/reconciliation`（`stock:balance:adjust` 权限） |

**降级修正（非错账）**：采购退货应用层无「退量≤已收−已退」校验——V132 DB 触发器已兜底
（returned≤received / ≤收货行 qty），不会错账，只是超退报 500 而非业务提示；保持现状，待后续补友好校验。

## 三、溯源联动修复（全部落地）

### 3.1 后端
- **销售单号谱系打通采购链**：申请行生成时沿 `plan_order_item_links`/分析行回溯写入
  `sales_order_no`（多单去重、「等 N 张」截断）；订货行继承申请行谱系（sourceDocNo/
  productionPlanNo/salesOrderNo/需求日）；RequestItemDto/OrderItemDto 透出。
- **IQC 全局待检单列表** `GET /procurement/inspection/pending-receipts` + 明细投影补
  货品/颜色/来源订货单号/仓库（此前前端只能显示 UUID）。
- **计划溯源增强**：`PlanTraceLink` 增客户/业务员维度；新增**已审核报工单**溯源组
  （traceDailyReports）。
- **仓库单据溯源**：StockDocDetail 增 `sourcePlanId`（plan_draw_links 反查，供跳计划详情）。
- **四类详情头来源引用**：采购订货→申请、采购收货→订货、委外订货→申请、委外进仓→订货
  （全部明细同源时下发 id+billNo）。
- **物流单号体系（V290 迁移）**：`sales_shipments.logistics_no` + 出货保存/详情透出 +
  **订单详情出货单聚合**（OrderShipmentRefDto：单号/日期/状态/物流单号/仓库作业状态/交接时间）。
- **ShipmentDetail.priceMasked** 标记（与订单同口径，无权限渲染 *** 而非 —）。

### 3.2 前端（统一 `SourceDocLink` 组件：编号必显示、有权威 id 必可跳）
- 采购：订货详情头「来源申请」跳申请详情；收货/退货「来源订货单」跳订货详情；
  申请/订货明细表新增「生产计划/销售订单」谱系列；行金额改用服务端权威金额。
- 委外：订货→申请、进仓/发料→订货跳转；来源单据文本兜底显示；发料明细新增
  **供应商子账列**（在供应商处/已消费/供应商结存/冻结单耗）。
- 销售：出货/其它出货「来源订单」跳订单详情；退货「来源出货单」跳出货详情；
  出货编辑页新增**物流单号**字段；订单详情新增**出货与物流聚合卡**（全部出货单可点跳）；
  出货价格脱敏标记生效。
- 仓库：单据详情显示**来源生产计划**（可跳计划详情）与**来源报工**（FINISHED_IN 可跳日报）；
  加载失败从「toast+空白」改为页内错误态。
- **出入库流水全类型跳转**：11 种 sourceDocType + STOCK_DOC 按 movement_type（V59 字典）
  推导，覆盖采购/销售/委外/仓库全部单据（此前仅盘点单可跳、其余静默）。
- **计划溯源卡**：销售订单 chip 带客户·业务员副标题；新增「报工单（已审核）」组。
- **IQC 待检处置页**（`/warehouse/inspections`，新路由+权限+入口）：待检单聚合→明细→
  PASS（部分放行）/FAIL 处置，幂等键客户端生成。
- **委外发料前端门禁解除**：`approvalBlockedReason` 移除（后端 V221 早已放开）；从上游引入
  按「订货数−已回厂+成品退回」给出可发料产量上限（此前防御性返回 0 导致 UI 全链卡死）。
- 一致性：采购报表行点击 Edit→Detail（与销售/委外/仓库统一，只读用户不再撞权限墙）；
  「本月完成」卡钻取带月初过滤（卡数与列表数一致）。

## 四、验证

- 后端 `mvn compile` 绿；定向单测 5/5 绿（SubcontractMaterialIssueSafetyGate 等）；
- **FullChainEndToEndTest 54/54 全绿**（Testcontainers PG16 + 全部 271 个迁移含新增 V290、
  UTEN_RUN_DB_TESTS=true、BUILD SUCCESS）：销售下单→计划→MRP→采购→收货 IQC→完工入库→出货→
  库存/金额/守恒→权限/并发/安全全链实测，覆盖本次重写的委外回厂消费、谱系继承、出货聚合、
  IQC 投影与 trace 增强。开发过程中 E2E 曾先后抓出两处新代码缺陷并已修复：
  ①`employees` 表姓名列实为 `full_name`（seller_name 连接列名错）；②原生查询 DATE 列返回
  `java.sql.Date` 需安全转换（订货行谱系继承 deliverDate）——均已加入防御式转换。
- Flutter `flutter analyze` 0 issues；`dart format lib` 合规（13 文件重排）。
- 新增迁移仅 V290（可空列，不回填、无触发器依赖）；无既有迁移改动。

## 五、遗留（明确不做/待决策）

1. 委外订货/回厂的 `order_no` 历史自由文本字段仍为空——权威来源已由 `sourceOrderId/
   sourceOrderNo` 头级引用替代，legacy 列不再补数。
2. 销售退货、报价等其余详情页的来源引用字段后端暂未下发（模型已就绪，后续按需补）。
3. 前端 `>1 → /100` 进度归一启发式 4 处重复——当前所有环值均为服务端 0-1 clamp 后下发，
   实际无风险；建议后续收敛到模型层一处。
   **（已于同日凌晨复查轮完成：收敛至 `lib/shared/models/progress_ratio.dart`
   `normalizeProgressRatio` 唯一入口，4 处改为调用，见《2026-08-16-凌晨全项目复查收口报告.md》。）**
4. 采购退货应用层友好容量校验（DB 已兜底不产生错账）。
5. 完整 WMS 物流件级明细、特采、批次责任等仍未落地（与 SOP NO-GO 边界一致）。
