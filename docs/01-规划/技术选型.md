# 技术选型

> 本档记录 Uten IMP 的核心技术选型及理由。详细决策过程见 [99-决策记录-ADR/](../99-决策记录-ADR/)。
>
> 📍 **现状（2026-07-31）**：前后端技术栈已落地，业务客户端统一使用 Dio；工资与员工报销活动 Mock 已移除，V133 表、真实 API 与固定状态机已有源码，但业务口径和生产 E2E 仍待验收。实验室及业务设备控制待接入；审计设备声明与本机回执已接入，但不是硬件认证。安全机制尚未通过生产门禁，详见[生产就绪审计报告](../99-项目治理/2026-07-30-生产就绪审计报告.md)。

---

## 一、技术栈总览

| 层 | 选型 | 版本 | 选型理由 |
|---|---|---|---|
| 框架 | Flutter | `>=3.27.0`；CI 固定 3.44.2 | 一套代码六端（iOS/Android/Web/Windows/macOS/Linux），制造业 IT 团队维护成本低 |
| 语言 | Dart | SDK `^3.12.2` | 强类型、空安全、AOT 编译性能好 |
| 状态管理 | Riverpod | 2.x | 类型安全、可测试、无 BuildContext 依赖、社区主流 |
| 路由 | go_router | 最新 | 官方推荐、声明式、Web 深链友好、支持守卫 |
| 设计系统 | Material 3 | 内置 | Flutter 官方支持，主题 token 化好 |
| 国际化 | intl + gen_l10n | 官方 | 不引入额外依赖，arb 文件可协作 |
| 偏好存储 | shared_preferences | 最新 | 轻量偏好（主题/语言/字号/性能档/工作台布局缓存） |
| 敏感存储 | flutter_secure_storage | 最新 | token / refresh（iOS Keychain / Android Keystore，已启用） |
| 设备检测 | device_info_plus | 最新 | 用于性能分级和生成经清洗的审计设备声明；安装标识是应用随机 UUID，不采集 IMEI、MAC 或硬件序列号，也不构成可信设备认证 |
| 网络层 | dio | 最新 | 拦截器机制好；已接真实后端，AuthInterceptor 自动加 token + 401 静默刷新 |
| Lint | very_good_analysis | 最新 | 严格规则，团队风格统一 |
| 测试 | flutter_test；integration_test 依赖已声明 | 内置 | 当前有单元/Widget 测试，但尚无 `integration_test/` 用例，不能宣称集成测试已落地 |

---

## 二、为什么选 Flutter（而不是 React Native / 原生 / Web）

| 候选 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| **Flutter** | 一套代码六端、UI 一致、性能好、Dart 强类型 | 桌面端生态不如原生（但企业后台够用） | ✅ 选定 |
| React Native | Web 生态成熟 | 桌面端支持差（RN 桌面不成熟）、双端 UI 不一致 | ❌ |
| 原生（各端分别做） | 体验最佳 | 5 端 × N 个模块开发量爆炸 | ❌ |
| 纯 Web | 桌面/移动浏览器都跑 | 移动端体验差、离线弱、无法上架 | ❌ |

详见 [../99-决策记录-ADR/ADR-001-技术栈选型.md](../99-决策记录-ADR/ADR-001-技术栈选型.md)。

---

## 三、为什么选 Riverpod（而不是 Provider / Bloc / GetX）

| 候选 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| **Riverpod 2.x** | 类型安全、可测试、无 context 依赖、AutoDispose、AsyncNotifier 一站式 | 学习曲线略陡 | ✅ 选定 |
| Provider（官方旧） | 简单 | 无类型、依赖 BuildContext、复杂状态难管理 | ❌ 已被 Riverpod 超越 |
| Bloc | 强约束、事件流清晰 | 样板代码多、对中小项目过重 | ❌ |
| GetX | 简单 | 架构混乱、反模式多、社区争议大 | ❌ |

详见 [../99-决策记录-ADR/ADR-002-状态管理选Riverpod.md](../99-决策记录-ADR/ADR-002-状态管理选Riverpod.md)。

---

## 四、为什么选 go_router

- Flutter 官方 package（first-party）
- 声明式路由，配置集中
- 支持 Web 深链和浏览器地址栏
- 内置 redirect 钩子，做路由守卫（未登录跳登录页）简单
- 支持嵌套导航（ShellRoute 外壳 + 子页面）

---

## 五、为什么用 Material 3（而不是自建设计系统）

- Flutter 官方支持，ThemeData 直接配
- ColorScheme / TextTheme / ComponentTheme 全套 token
- 响应式断点有官方推荐（WindowSizeClass）
- 团队学习成本低（设计师也熟悉 Material）
- 不选自定义系统：避免重复造轮子，企业后台不需要"惊艳到独一无二"

在 Material 3 基础上用品牌色（深绿+青绿）覆盖默认蓝色。

---

## 六、现状：核心栈已落地，仍有上线阻断

> 后端 Spring Boot 3.5.16 + PostgreSQL + Flyway（见 [ADR-006](../99-决策记录-ADR/ADR-006-后端栈选型SpringBoot+Postgres.md)）。
> 销售/采购/委外/仓库/生产/钱流/基础资料/HR/鉴权/访客/通知/建议使用真实后端。
> 工资与员工报销前端已切 Dio，V133 表、后端 API 与固定状态机已有源码；是否完整仍以权限、
> 审批/薪酬口径、会计副作用和生产 E2E 为准。实验室检测 / 空调控制等设备能力待后续接入。

| 维度 | 实现 |
|---|---|
| 数据 | PostgreSQL（pgcrypto 字段加密）+ Flyway 当前源码最高 V173；PostgreSQL 16 空库 154 个迁移已完成审计功能定向验证，目标库版本仍必须查询 `flyway_schema_history`，历史数据仍有 BOM/委外/总账阻断 |
| 网络 | dio + Repository 接口；员工、工资、员工报销旧 Mock 已删除，后端不可用时不得回退假数据 |
| 认证 | Argon2id + JWT（access 源码默认 15min + refresh 7 天，TTL 可配 + 不透明轮换 + 重用检测）+ V135 授权版本逐请求校验 + 首登强制改密 |
| 存储 | 偏好 shared_preferences；令牌 flutter_secure_storage（禁止混用） |
| 加密 | 身份证/手机/银行/薪资 pgcrypto 字段级加密（密钥版本化 `v<version>:`）+ DTO 按权限点脱敏 + 导出 Agile AES-256 |
| 权限 | **部门配置（含上级部门）+ 个人覆盖 + 按权限点脱敏**（角色体系 2026-07-24 下线）；V135 `auth_version`/授权 `epoch` 使旧 staff access 版本不匹配时立即 401。审计查看/导出是例外：除超管固有全权限外只能个人点名授权、禁止部门继承 |
| 数据导出 | 独立 `*:export` 权限 + Bucket4j 限流 + Agile AES-256（密码入 body）+ 行数上限 + 审计；审计导出必须同时持 `audit_log:view` 与 `audit_log:export`（[ADR-012](../99-决策记录-ADR/ADR-012-报表加密导出与列排序与行跳源头.md)） |
| 系统设置 | 运行时策略阈值可视化 + 二次密码确认 + 审计；审计在线期 1–120 月、追加归档期 0–240 月（默认 6+30 月）（[ADR-013](../99-决策记录-ADR/ADR-013-系统设置与安全策略可视化.md)） |
| 审计 | 请求、显式事件、公开业务表变化三层留痕；风险统计、四页详情、设备证据、本机在线授权核对、加密导出和自动留存见[审计中心](../03-页面/审计日志页.md) |
| 错误处理 | 统一错误码（ApiException），不泄露后端细节 |
| 架构 | Repository 接口模式，业务代码与数据实现解耦；报表统一走 `ReportTableResponse` 范式 |
| 交付门禁 | GitHub Actions：Gitleaks 全历史、Maven verify、Flutter format/analyze/test/Web build、OSV 周期扫描；远端强制门禁和 E2E 仍待验收 |

---

## 七、当前前端依赖基线

| 类别 | 当前约束 |
|---|---|
| 路由 / 状态 | `go_router ^14.6.2`；`flutter_riverpod/riverpod_annotation ^2.6.1` |
| 网络 / 存储 | `dio ^5.7.0`；`shared_preferences ^2.3.3`；`flutter_secure_storage ^9.2.2` |
| 国际化 / 设备 | `intl ^0.20.2`；`device_info_plus ^11.2.0` |
| 文件 / 打印 | `path_provider ^2.1.5`；`pdf ^3.11.3`；`printing ^5.14.2` |
| 扫码 / QR | `mobile_scanner ^7.0.0`；`qr_flutter ^4.1.0` |
| 代码质量 | `flutter_lints ^6.0.0`；`custom_lint ^0.7.0`；`riverpod_lint ^2.6.3` |

完整直接依赖看仓库根 [`pubspec.yaml`](../../pubspec.yaml)，最终解析版本看 `pubspec.lock`；两者都应
进入 OSV/SBOM 扫描。不要在本文复制完整 lockfile。

---

## 八、版本与升级策略

- **Flutter SDK**：跟随 stable 频道，不追 beta
- **依赖**：每季度 review 一次，major 升级单独安排
- **Dart**：随 Flutter，不单独升级
- **lint 规则**：升级 very_good_analysis 后统一修复

---

## 九、待定项

| 项 | 状态 | 决策时机 |
|---|---|---|
| 图表库（fl_chart vs syncfusion_flutter_charts） | 待定（报表统一表格 `MasterDataTableView` 暂不依赖） | Dashboard 数据可视化阶段 |
| 是否引入 sentry / firebase_crashlytics | 待定 | 部署运维阶段 |
| 是否引入 flutter_dotenv（环境配置） | 倾向是（目前走 `server/.env`） | 多环境部署时定 |
| CI/CD 平台与工作流 | GitHub Actions 源码已加入；分支保护、环境、部署和产物签名待定 | 上线准备阶段 |
| prod HTTPS 终结（反向代理） | 方案边界已定：prod `native` + `UTEN_TRUSTED_PROXY_REGEX`，8080 仅代理可达；具体代理/证书仍待定 | 部署阶段完成网络、伪造代理头、证书续期与故障演练 |
| 导出限流多实例共享（Redis） | 待定 | 多实例部署时（单实例 Bucket4j 已够用） |

---

**最后更新**：2026-07-30（校准 V141 员工敏感写权限、工资/员工报销/访客/财务资产真实分页与长期索引、依赖与交付安全门禁）
