# 全平台整改 交接总览(先读这份)

> 状态日期: 2026-09-24。第一阶段(第一波+第一波 b)完成并合并到 main(7e0ad86f), 已发 **v2.0.0** 并激活到公司内网测试服务器(数据库 V640→V687, 数据保留); 第二阶段起按 `03-后续工作流任务书.md` 继续。
> 本目录四份文件:
> - `01-交接总览.md`(本文): 背景、用户原话与拍板、做了什么、为什么、还差什么、下一步怎么开工。
> - `02-审计发现全集.md`: 239 条审计发现全文(证据/根因/修法/验证), 后续所有工作的"原料"。
> - `03-后续工作流任务书.md`: 剩余每个工作流的派工单(负责发现 ID、文件边界、目标、做法), 可原样交给实现者。
> - `04-编排方法与验收.md`: 工作区隔离、实现者铁律、合并与迁移头同步、验收命令、性能证明方法、AI 编排脚本模板、踩坑。
> - `05-重构后验证与注意事项.md`: 每个工作流/每一波/最终验收怎么验证(四道门、全量门禁、12 条业务链矩阵、性能与数据不变量核对)以及各区域最容易出错的地方。

## 一、用户要什么(原话要点, 2026-09-22 晚 ~ 09-23)

1. 优化、提高整个项目的稳定性和效率: 页面切换流畅, 每个按钮反应快, "点下一步要加载半天"的地方都要快; 但必须保证正确性, 特别是数据计算正确。
2. 减少重复代码: 能提取就提取、能公共就公共、能组件就组件。
3. 问题一定定位到根因, 不要打补丁。
4. 权限: 每个页面的权限 + 工作台权限管理的总权限设计要加好、改好。
5. 审计日志: 该记录的都要能追溯; 数据会很大, 要有合理保留期并能自动清理, 且确保真的能清。
6. 系统设置: 该加的加, 改值后要真的生效。
7. 删掉老的没用的代码、备份、日志、无用文件; 代码结构要优秀; 大文件该拆就拆、模块化。
8. 数据大了响应也要快; 数据库可以改、可以重构、可以合并表, 但数据迁移代码与文件(老库导入)要对应修改, 适配以前的数据。
9. 文档: 老旧的删、该合并的合并, 完成后更新文档。
10. 安全。
11. 平台还没上线, "该重构就重构", 不用担心兼容。
12. 完整完成后合并代码(并行会话都已结束), 最后全量检查, 保证每条业务链都正确、能正常走完、考虑到各种情况, 并给总结。
13. 要考虑公司内部服务器的性能, 系统操作不吃性能、撑得住。
14. 迁移压缩时, 内网测试服务器原有数据要保留。
15. (09-23 午)周额度只剩约 10%: 完成这一阶段后收尾, 写详细交接文档, 下周继续(可能由其他人继续)。

**用户四项拍板(09-23)**: ① 整改最后把 Flyway 迁移压成 V1 基线(但服务器数据要保留 → 用"只改 flyway 历史"的重基线脚本, 见 03 文档 squash 节); ② 删除旧发布链, 打 tag 存档, 修订 ADR-060 §5; ③ 老库备份/职工信息表/product lists 迁出仓库到 `D:/uten_legacy_inputs`(脚本改读环境变量 `UTEN_LEGACY_INPUT_DIR`), 开发库 dump 与 1.37GB 备份移到隔离目录由用户自己删; ④ 库存价值账本轮只降开销(批量写/去派生审计/触发器收窄), 另写 ADR 查同行 ERP 提简化方案, 用户确认后再改模型。

## 二、为什么慢/为什么乱: 审计得出的共同根因

审计(13 个只读维度 + 完整性评审, 全文见 02 文档)用 dev 库真实请求耗时(audit_log 的 duration_ms)和代码/SQL 证据定位到:

1. **预锁足迹重复发现+整行 md5 复核**: 每条写命令"预读依赖图→加锁→锁后整图重读比 md5", 且每层嵌套服务各跑一遍(质检链一次 11 遍)。→ 第一阶段已根治(并发核心)。
2. **仓库/质检事务里同步整张重算物料分析**, 并拿全公司唯一的主仓 advisory 锁, 所有库存写被串成一条队。→ 第二波 maengine 根治(第一阶段已把主仓锁改为 3 秒限时尝试+只读预览用共享锁)。
3. **写放大**: 404 张表行级审计(含派生/计算表, 每行整行 JSON)、285 个延迟约束触发器每行每事件提交时重跑、52 张表语句级单位锁触发器、101 个冗余索引。→ 审计部分第一阶段已根治; 触发器/索引部分由 dbhyg 工作流处理(见第三节状态)。
4. **空写**: 采购订单头 767 次更新里 706 次值没变, 分析明细大量只改 updated_at。→ 第二波 whq/maengine。
5. **回滚式预览**: 物料分析"下达预览"真跑一遍下达再回滚, 持独占锁, 预览之间排队到 22 秒; 前端为掩盖慢又复刻一套估算算法(第二套口径)。→ 第二波 maengine。
6. **前端请求编排**: 工作台约 40 个互不协调的 60s 定时器各打计数端点, 每页 AppBar 现拉权限能力, 详情页 5 步串行请求, 返回即刷新不看数据变没变。→ 第一阶段已根治。
7. **同一个数算多遍**: 可用量 3 套公式、剩余未排量 6 份实现、订单行预留计数器跨单位舍入、金额舍入四套口径。→ 金额第一阶段处理; 其余第三波 truth。

实测基线(dev audit_log, 2026-09-18~23): 采购来料检验批量判定 p50 6.0s/最慢 31s; 物料分析下达预览 p50 2.5s/最慢 22s; 下达 p50 2.9s/最慢 20s; 日报审核 p50 2.2s/最慢 15s; 批量领料 p50 1.7s/最慢 9s; 委外发料 1.3-5.6s。公司服务器(4GB 堆)上 100 来源下达 19.4s、500 来源 55s。

## 三、第一阶段做了什么(已合并到 main)

集成分支 `wf/platform-overhaul-20260922`, 每个工作流一个分支 `ov/<key>`, 实现→对抗评审→修复→主线程合并。

| 工作流 | ADR / 迁移 | 结果 | 未完成部分(已排进后续任务书) |
|---|---|---|---|
| 纳入并行会话遗留改动 | — | 取消分析/撤回原因改为选填(空白记"未填写原因", 填写至少 2 字), 表格表头与配色微调; 9/9 + 181/181 用例 | — |
| frontperf 前端性能与徽章 | ADR-108 | 服务端 `GET /api/workbench/badges` 一次算全部红/黄数(复用原端点权限), 前端只剩一个 60s 轮询(隐藏暂停/单飞/登出即停), 回工作台 34 个 GET→1; 工作台概览待办改由同一徽章端口派生; `/auth/me` 带会话快照(可委派页面/单据范围/偏好), 删每页 capability 与偏好整表请求; 返回即刷新按需且在转场后; Web/桌面 150ms 淡入; 网络层慢不等于断、恢复不重建 Dio、令牌临期提前刷新; 通知 2s 轮询→20s 游标; 主档字典会话级单份缓存; 详情首屏只等详情; 表格列宽缓存与单行重建; LAN nginx HTTP/2。flutter 全量 3029 通过 | SSE 推送通道(capacity); 编辑表格表体虚拟化(uikit); 部分详情缺服务端名称字段(uikit); 约 28 个已无前端调用的 /count 端点删除(cleanup) |
| master 主档完整性 | ADR-111, V683-V684 | 事故根治: 删除按引用目录全覆盖拦截(登记 305 个引用主档的列, 224 引用+81 豁免, 覆盖测试防遗漏), 中文说明被谁使用; 数据库兜底触发器; 分类删除与归类并发闸; 物料分析 BOM 校验指明具体行与原因; 组装粘贴/添加、批量启停/删除改服务端单事务命令; 组装页签 18/76→4 条 SQL; 分类主档页明细区、选择器、树组件去重 | 其余业务批量循环(sales/hr/仓库, 归 whq/uikit); 批量新建货品命令 |
| audit 审计 | ADR-105, V670-V673 | 行级审计三清单(FULL 180/COLUMN_SCOPED 4/NONE 218, 各写理由, 新表不登记即红); 只存变化键、无变化不写; 分类只算一次; 成功 GET 默认不记、每个成功写请求自动生成语义事件; 匿名/会话固定窗口节流; 业务不读审计(审核/驳回/红冲时间落单据列); 审计表按月分区只追加、运行账号不能改删, 留存 DETACH/DROP 并有 6 个月法定下限, 每次运行写完成事件、服务器状态页 26 小时无成功告警; 账号只存后 4 位; production_plan_costs 去分区; 删第二套清理脚本。实测审计行 -75%~-89%, JSON 字节 -83%~-93% | 权限差异事件(perm 已做)、账号状态事件(security) |
| concurrency 并发核心 | ADR-107 | 发现以事务为单位只做一次, 锁后复核改比 (id, xmin), 删全部 md5(to_jsonb) 快照; 声明 id 覆盖缺口不可重跑并记 ERROR, 自动重跑 10 秒预算(排队不计)+命令超 30 秒不再重跑; 主仓协调锁保持阻塞排队(等待上限=lock_timeout, 超时转可重跑 409), 只读预览用共享锁; 测试环境默认开嵌套足迹诊断开关; createdHere 只看 xmin; 事务开始自动绑定审计操作人, tx.bind 同事务只一次; BaseEntity 实现 Persistable(新建只一条 INSERT); 服务端截止时间(lock_timeout 10s / statement_timeout 60s / idle_in_tx 120s / 事务默认 40s, 55P03/57014/40P01 映射可重跑 409)。实测: 直送审核 491→385 条语句(md5 114→0)、批量出库 708→544(md5 156→0, set_config 16→1)、计划审核 180→125 | 去掉库存写主仓锁与缩小锁集合(maengine/whq); 删 530 处手写 tx.bind(cleanup) |
| perm 权限目录 | ADR-109, V677-V678 | 授权策略单一事实源 permissions.grant_policy(替换 5 份互相矛盾的名单, 服务端+数据库同口径); 删 23 个停用/仅前端码与死端点(目录 467→449); 动作拆分与命名统一(出货财审 view/approve/reject/reverse、仓库销售出库 view/execute); 三条卡死链定默认岗位并用契约测试锁住; 角色体系删除, 全员基础包可编辑, 通知按部门+权限码; 五个授权入口差量保存+一条带 added/removed 的事件(最大矩阵原样保存 140 语句/278 审计→3 次只读/0 审计); hub/工作台守卫=子卡并集; 空负责人单据不再公共可读, 计划子资源统一对象范围; 清空业务数据要超管本人+专属码+本次密码; 页面本地拼权限设基线只减不增 | 按钮显隐全面改服务端 allowedActions(uikit/truth 分模块推进) |
| dbhyg 触发器与索引 | ADR-106, V674-V676 | 热表约束/守卫触发器按"相关列真变化"起跳(相关列由校验函数调用闭包递归展开, 宁多勿少), 删 3 条被包含的重复校验, 单号触发器只挂 INSERT+列级守卫; 删 52 张表 104 个语句级单位锁触发器与 goods.quantity_unit_locked 列, 改单位时按需判定; 删 96 冗余索引、补 74 热点索引(契约测试锁住); 收付款类别引用改共享锁。实测: 直送审核触发器调用 600→416; 14 行 IQC 确认入库触发器 2729→2191、JDBC 7.5-9.2s→2.5-4.6s; decide-batch JDBC 约 2.0s→0.9-1.5s | 延迟校验"每键只校验一次"、同表合并、估值写入一次成形(归 whq/prodwrite, 见 ADR-106 §2.1) |
| security 认证安全与设置 | ADR-110, V680-V682 | 服务端会话 auth_sessions(吊销/空闲/绝对期限一律 401, 登出/改密/锁定/离职/改手机号即失效; 自动轮询不续期); 统一再认证 step-up(5 分钟一次性凭证, 挂在超管/授权/重置密码/模拟身份/系统设置/清空业务数据/改手机号姓名/补开账号/一键授权等); 登录锁定期对错同码; 临时密码 20 位随机限时不可自定, 高危权限(permissions.high_risk)账号只超管可重置, 本人与超管双提醒; 密码哈希全局并发闸门; 系统设置 SystemSettingKey 枚举单一登记, 公共设置下发密码长度/附件上限/角标间隔; 敏感检索走请求体 | 列表页交货期预警天数接入设置; 角标间隔接入徽章轮询; nginx 限流/日志格式(capacity) |
| money 金额口径 | ADR-112, V685-V687 | 唯一金额口径 MoneyPolicy(精确乘积; 分摊=累计份额−已分摊, 末批取余; 架构测试禁止 MoneyPolicy 外 HALF_UP); IQC 一张收货只切一次(不合格+合格全退恰好冲平应付); 保存请求不带金额(带了 400), 金额只由服务端派生; 出货 3 件 100 元分三次=33.3333/33.3334/33.3333; 资金过账单一入口 AccountFlowLedgerService(7 类单据审核+红冲余额复原真库实测); 总账附表人工/折旧按绑定表取事实(未配置明确标注) | 全站 JSON 金额改字符串; 工资计提凭证; 库存价值列精确化(随价值账模型, 用户待拍板) |

另外:
- 界面(用户 09-23 追加): 工作台与各模块 hub 卡片的红/黄计数徽章从右上角挪到"图标(+名字)那一行最右边"与图标垂直居中, 放大更醒目(工作台 1.25 倍、hub 卡 1.4 倍, UtenBadgeScale); 字号档放大时徽章同步放大(手机端药丸随文字倍数放大, 不被数字撑破)。
- 本机仓库根清理: 日志、target-* 目录、代理草稿、失控日志等 27GB 移到 `D:/uten_cleanup_quarantine_20260923`(可审阅后自行删除; 里面有 MANIFEST.json 记录来源); 老库备份 YTDQ_2023-202607、职工信息表.xls、product lists 移到 `D:/uten_legacy_inputs`; `.mimosa`(工具状态)未动。
- 货品事故恢复(用户 09-23 反映"长名称成品在物料分析准备页报错"): 根因是 09-22 删除了仍被 V4ZJ005「V9开关面框组件(铂银)」BOM 引用的组件 V40001-dy, 以及删了成品 280135075 的 7 行 BOM; 已按 audit_log 删除前镜像在 dev 库与克隆库恢复(审计标记 overhaul-restore-20260923), 全库 BOM 引用扫描 0 异常; 代码根因由 master 工作流修复。
- 两条整改前就存在的失败用例已修: 通知横幅看不见时到上限不收(真 bug: 不出帧时收起动画不推进)、取消原因契约断言。

## 四、第一阶段收尾状态与验证结果

- 集成分支 `wf/platform-overhaul-20260922` 合并了全部 8 个工作流 + 徽章界面改动 + 基线修复, 共 25 个提交, 1335 个文件 +53936/-25333 行; 新 ADR-105~112。
- 09-23 晚发现主工作区还有并行会话未提交的成果(V646 委外子件精确库存交接、V647 追加并入增长事件, 其中 V646/V647 已应用到用户 dev 库), 为不改动已应用的编号, 把整改迁移整体顺延到 **V670-V687**(头 V687 / 616 张, V648-V669 跳号), 再在 `wf/platform-overhaul-integ2` 上合并该快照(提交 a916d5ee, 16 处冲突), 并行会话的 ADR-105 改号为 **ADR-113**。该分支即 main 现头 7e0ad86f。
- 迁移在真实数据上验证: 克隆库(dev 库副本, 含 18 万行审计与全部现有业务数据)由 V645 升到 V687 共 18 个迁移, 8.4 秒完成, 应用正常启动; 18 个关键接口冒烟全部 200(工作台徽章、仪表盘、物料分析准备与开始分析——含 09-22 出事的 V9 长名称成品、执行工作台、车间任务、销售/采购/库存/货品列表、权限目录与基础包、系统设置、审计查询与汇总、服务器状态、采购工作台)。
- 最终门禁(提交 7e0ad86f): flutter format/analyze 干净, flutter test 全量 3111 通过/11 跳过/0 失败; 后端快道 `mvn verify` BUILD SUCCESS; 合并时后端真库用例 545 个(含 FullChainEndToEndTest 160/160)通过; 部署契约(角色加固)15/15。
  CI(GitHub Quality Gate)在 7e0ad86f 上快道红 2 个类 14 个用例: 未设 UTEN_PROFILE 时按 prod 启动, 而 prod 要求显式内网网段(ADR-110 security-17), CI 没有 server/.env → 上下文起不来; 本机有 .env(dev)所以没暴露。已在这两个用例里给出网段修复(不放宽生产校验), 见 main 上随后的提交。
  **教训**: 本机验证要和 CI 同口径, 跑后端门禁前先把 `server/.env` 挪开(或在隔离 worktree 里不拷 .env), 否则 dev profile 会掩盖 prod 口径的问题。
- 合并过程中发现并修复的问题(均有测试): V682 引用已被 V677 删除的列(迁移顺序下会失败)→改写; 审计契约对"基线之后新建/删除的表"判定互相矛盾→修正; 清空业务数据两套密码校验→统一为再认证; 一键授权/基础包保存可绕过再认证→补上; 6 处测试断言随设计变化过期→按新口径校正并写明理由; 通知横幅应用不可见时到上限不收起(真 bug)→修复。

### v2.0.0 发布与公司服务器部署记录(2026-09-24)

- 发布: main 快进到 7e0ad86f 后 `gh workflow run simple-release.yml --ref main -f version=v2.0.0 -f allow_running_checks=true`, 构建+签名+上传约 3 分钟(OSS 传输加速)。发布前本机先跑通 format/analyze/flutter 全量/后端快道。
- 迁移演练(本机): dev 库副本 V647→V687, 18 个迁移 34 秒, 应用正常启动, 18 个关键接口冒烟全 200(含 09-22 出事的 V9 长名称成品物料分析预览)。
- 服务器(`utenelec@100.109.196.17`, v1.4.0, 库 V640/592): `sudo uten-imp-updater check` 暂存 v2.0.0 → `activate v2.0.0` 激活成功, 健康检查通过, `/version.json` = v2.0.0 / 7e0ad86f; 库现为 V687 / 616; 审计表已按月分区(5 个分区, 历史 148,885 行全部保留); 关键表行数迁移前后逐表一致(用户 7、货品 138、BOM 行 217、员工 143、客户 260、供应商 386 等)。
- **部署事故(已处置, 务必知悉)**: 我想在服务器上用暂存版 migrator 对一个副本库演练, 但 `UtenImpMigrator` 设计上写死连 `127.0.0.1:5432/uten_imp`、不认 `UTEN_DB_URL`, 于是 08:57:05 把**正式库**直接从 V640 迁到 V687(24 个迁移 17.8 秒, 成功), 旧 v1.4.0 代码对着新结构跑了约 30 秒, 08:57:37 随即 activate 收口。该窗口 journal 无 ERROR。迁移前 08:50 的完整备份: `/var/backups/uten-imp/uten_imp_pre_v2.0.0_V640_20260924-0850.dump`(另有副本库 `uten_imp_rehearsal_v2` 为迁移前 V640 状态, 确认无需回滚后可 `dropdb`); activate 自己也备份了一份 `uten_imp-v2.0.0-20260924-085737.*.dump`。**以后迁移演练只能在本机克隆库做, 服务器上只用 updater activate。**
- 服务器冒烟(自签临时会话, 用完已吊销): /auth/me、工作台徽章、仪表盘、物料分析候选与预览、执行工作台、车间任务、销售/采购/库存/货品列表、权限目录与基础包、系统设置、服务器状态、采购工作台、单据状态计数全部 200, 且都在 10-350ms。
- nginx: 服务器配置是手工维护的实例(与模板有差异), 本次只把 `listen 443 ssl;` 改为 `listen 443 ssl http2;`(备份 `/etc/nginx/sites-available/uten-imp.bak-20260924-v1.4.0`, `nginx -t` 通过后 reload, 实测 HTTP/2 200)。模板里的"带指纹静态资源长缓存"没有搬(Flutter 产物目前不带指纹, 搬了也不生效), 留给 capacity 工作流。
- `server.env` 的 `UTEN_SERVER_STATUS_APPLICATION_VERSION` 已手工改为 v2.0.0(备份 `server.env.bak-v1.4.0`)。

### 用户需要知道的行为变化(请转告使用者)
1. 发版后所有人需要重新登录一次(旧的登录保持全部作废, 改为服务端会话)。
2. 登出后原登录立即失效; 长时间不操作会被服务端判定超时(以系统设置"空闲超时"为准), 自动刷新的角标轮询不算操作。
3. 高危操作(设/撤超管、改权限与数据范围、重置密码、模拟身份、改系统设置、清空业务数据、改本人手机号/姓名、补开账号、一键授权)点确认时要再输一次本人密码(5 分钟内有效一次)。
4. 重置密码不能自定临时密码, 系统生成 20 位随机密码只显示一次且限时; 带高危权限(付款/收款/转账/费用审批、报销付款、工资审核发布等)的员工账号, 只有超管能重置或补开; 复职员工原密码作废, 要由账号支持重新发放。
5. 权限: 退回供应商任务默认给采购与综合营销事业部; IQC 不合格退回/贷项默认给仓储部(登记退回)与财务部(确认贷项/关闭/看金额); 官网询盘给综合营销事业部; 结算方式查看给财务部; 模具分类维护给工程研发部。可在权限管理页调整。
6. 权限码改名: 出货财务审核拆成 查看/通过/退回/反审 四个, 仓库销售出库拆成 查看/执行 两个; 角色(planner/buyer 等)已删除, 全员基础权限在权限管理页"基础包"里改。
7. 货品删除: 仍被组装清单或未结单据使用的货品/颜色/单位等不能删除, 系统会说明被谁使用。
8. 货品"基本单位能否修改"的口径变为"此刻是否还有数量记录引用"(原来是"用过一次就永久锁定"); 软删、取消、红冲的历史行与组装清单仍算引用。若业务要坚持"终身锁定", 需要另行设计。
9. 金额: 保存单据时不再接受前端算的金额, 一律由服务端按 数量×单价×汇率 精确算; 分批出货/退货金额按"累计份额、末批取余"保证合计等于原单。
10. 新通知到达最多约 20 秒延迟(原来 2 秒一次高频轮询), 下一阶段做服务端推送(SSE)后恢复实时。
11. 卡片徽章位置与大小改变(见上)。

## 五、下一步怎么开工(给下周的自己或接手的人)

**续做第零步(必做, 先处理主工作区遗留)**: 截至 2026-09-24, 本机主工作区 `D:/Projects/uten_imp` 的 main 分支停在 e5916b69(徽章挪位先行移植), **远端 origin/main 已是 7e0ad86f**; 主工作区里还有已结束会话留下的未提交改动(约 140 个文件), 分两批:
   - 已被 7e0ad86f 吸收的快照(a916d5ee, 与工作区逐文件一致的 98 个);
   - 快照之后新增的「委外损耗独立结清不改订货量」: `V648__subcontract_loss_fulfillment_without_order_revision.sql`、`ADR-106-委外损耗独立结清不改订货量.md`、`SubcontractLossSettlementSql.java`、`233-V648委外损耗独立履约结清.md`、`SubcontractOrderInProgressFilterTest.java`、`subcontract_business_categories_test.dart` 以及对 34 个已有文件的修改(含 reset_business_data.sql 与两个清库契约测试); 另有「物料分析准备页面表头」会话的取消原因选填、表格选中色、联动滚动门等 lib/test/docs 改动。
   处理法: ① 在 7e0ad86f 上开 worktree, 把上述新增改动(以主工作区文件为准)搬过去; ② **V648 改号 V688, ADR-106 改号 ADR-114**(服务器已在 V687, V648 会被 Flyway 判乱序拒绝); ③ 按 04 文档做迁移头同步(头变 V688 / 617)与审计三清单/清库分类/主档引用登记; ④ 跑全量门禁后合并, 再把主工作区 `git checkout -f`/清理后快进到新 main。**在此之前不要从主工作区重启 8080**: 启动时 Flyway 会把 V648 应用到 dev 库, 之后改号要手工修 flyway 历史。

**续做第一步(必做, 约半小时)**:
1. `git log --oneline -15 main` 确认第一阶段合并提交在 main 上; `git worktree list` 查看遗留 worktree(`.claude/worktrees/ov-*` 第一阶段分支已合并, 可 `git worktree remove` 清理; `overhaul` 是集成 worktree)。
2. 读本目录 04 文档第二、三、四节(工作区、铁律、合并与迁移头同步)。
3. 基线验证: `flutter analyze`、`cd server && mvn -o -q compile test-compile`; 需要时跑第四节列出的全量命令确认仍全绿。
4. dev 库: 用户自己的 8080 实例启动时 Flyway 会把 dev 库迁到第一阶段迁移头; 克隆库 `uten_imp_overhaul` 已迁好, 供复现与压测。

**然后按波次做**(03 文档): 第二波 maengine / whq / prodwrite / readpath / capacity 五个工作流可并行(每个独立 worktree, 文件边界已划好), 每个都要"实现→对抗评审→修复"; 合并后统一改迁移头、跑全量验收; 再开第三波 truth / cleanup / uikit; 第四波 tables / splitbe / splitfe; 第五波 docs / squash / final。

**优先级建议**(若资源有限): maengine > whq > prodwrite(用户最痛的慢按钮) > truth(数据正确) > capacity(服务器撑得住) > readpath > cleanup > tables > uikit > splits > docs > squash > final。final 的"老库全量引导+大数据压测+逐链多账号 E2E"不能省。

**不要重复做的调研**: 所有根因都有证据在 02 文档; 用 ID 定位, 开工时只需 grep 当前代码核对行号(已漂移)。第一阶段各工作流的详细报告(实测、改过的契约断言及理由)在各 ADR-105~112 与对应迁移说明里。

## 六、风险与注意

- 第一阶段改了锁协议、审计模型、权限目录、会话与再认证等全局机制; 后续工作流必须在它们之上做, 不要回退(例如不要恢复 md5 快照、不要让业务代码读 audit_log、不要再加手写权限名单)。
- 新增表必须同时登记: 审计三清单、主档引用目录(若引用主档)、reset_business_data.sql 表分类与数据库孪生函数、权限(若有新端点, grant_policy 与链路可达契约)。
- 价值账模型、多执行段分摊规则、直送单据粒度这三处改的是业务口径, 要写 ADR 并让用户确认。
- 公司服务器已部署第一阶段(v2.0.0, 见第四节部署记录)。下次发版照 `deploy/simple/RUNBOOK.zh-CN.md` 与记忆里的发布配方: 本机克隆库演练 → 发布 → `updater check` → 改 server.env 版本 → `updater activate`; 服务器上不要直接跑 migrator。
