# 93 - V477 物料路线原因选填

- 更新：2026-09-05。
- 迁移文件：[V477__material_analysis_optional_route_reason.sql](../../server/src/main/resources/db/migration/V477__material_analysis_optional_route_reason.sql)。
- 状态：前向源码候选；V477独立隔离测试已有报告，现有业务库未执行。V478及合并后的本轮最终结果见[验收记录](../99-项目治理/2026-09-05-顶层路线记忆与确认修复验收记录.md)，不能混用上轮结果。

## 一、修复依据

现场日志 `server/logs/uten-imp-dev.log` 于2026-09-05 08:19:25、08:19:34 (UTC-06:00)记录SQLSTATE `23514`，命中 `production_material_analysis_material_route_reason_chk`。失败行的路线由BUY改为SUBCONTRACT，原因只有1个字符；V234既有约束要求trim后至少2个字符，故不能通过数据库终审。

本次业务规则明确改为“原因选填，空可直接确认”。只改前端提示或服务端校验仍会被旧数据库CHECK拒绝，必须以前向迁移同步约束；V234已应用字节不改。

## 二、约束变化

V477仅替换并验证 `production_material_analysis_material_route_reason_chk`，不新增表、列、权限或默认授权，不改任何历史原因文本。

| 状态 | 新约束 |
|---|---|
| 未确认 | confirmed_route、route_reason、route_confirmed_by、route_confirmed_at均为NULL |
| 已确认 | 路线只能BUY/MAKE/SUBCONTRACT；确认人和时间非NULL |
| 已确认原因 | 可为NULL；非NULL时trim长度1..1000 |

应用将空白输入归一为NULL。非空原因保留员工实际输入，不自动拼接“系统代填原因”。单字原因可保存；1001字符、非法路线或缺确认身份仍应拒绝。新增约束先NOT VALID再VALIDATE，并更新列注释，未削弱确认身份与合法路线门禁。

## 三、界面与权限

“确认路线”直接保存所选各自实际路线，原因可空。旁边“原因(选填)”只编辑草稿，可填、清空或取消；编辑原因本身不提交PUT。通用原因对话框默认仍必填，借料等其它业务不随本次修改放宽。

继续使用 `production_material_analysis:view/route`、服务端CONFIRM_ROUTES、原分析对象范围、版本/指纹和幂等键。根ROOT_SUPPLY也用同一确认协议，不新增一组权限。

## 四、兼容与验证

V477独立轮次报告为87/87，包含 `MaterialAnalysisOptionalRouteReasonTest` 3项、`OptionalRouteReasonPostgresTest` 5项及原行为/工作流/迁移合同。5项PostgreSQL16.15测试含V476历史确认行升级后保留，以及真实saveRoutes/刷新不丢原因和确认审计。日志：`server/target/material-analysis-optional-route-reason-20260905.log`。

87项包含这5项，不能再相加；它不是V478或本轮最终全链结果。目标库非空升级、现场岗位和恢复验收尚未执行。合并后的最终命令、测试范围和状态见[本轮记录](../99-项目治理/2026-09-05-顶层路线记忆与确认修复验收记录.md)。

升级前核对目标 `flyway_schema_history` 与源码字节，按正式发布/备份方案处理。不得改V234、执行flyway repair或手工改历史route_reason补齐字数来掩盖失败。程序回退也须确认与新增空原因数据兼容，不能把旧JAR等同数据库恢复。
