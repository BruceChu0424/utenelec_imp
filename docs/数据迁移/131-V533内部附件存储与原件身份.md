# V533：内部附件存储与原件身份

> 状态：源码候选，真实数据库验证进行中。不得据此认定服务器已经升级。

迁移文件：`server/src/main/resources/db/migration/V533__attachment_internal_storage_identity.sql`。

四张现有附件表新增 `storage_provider`，明确每条记录所在的存储后端。附件表新增 `stored_size_bytes/storage_encoding`，上传会话新增 `final_stored_size_bytes/final_storage_encoding`，区分原始字节和落盘表示。原始 `size_bytes/sha256` 含义不变，实际文件大小包含内部存储封装头。

迁移前只有 OSS 返回对象版本号，故非空版本可确定归类为 OSS；其余记录保留 `legacy_unknown`，不猜成新配置的内部服务器。后续新增行必须显式写入来源。原文件及业务归属不改，历史扫描和对账证据不伪造。

删除队列的去重键加来源前缀；孤儿对象唯一键包含来源，防止相同对象名跨后端误删。新增数据库守卫固定已确认原件、精确版本和物理表示，并阻止通过退回未验证状态绕过保护。正常头像标记、删除意图、删除失败与重试不改内容身份。

不新增业务表，清库 CLEAR/PRESERVE 分类保持原样；正式目录计数从 V532/491 增至 V533/492。既有迁移字节保留，不做 Flyway repair，不在迁移时扫描、压缩或搬运外部文件。

权限、文件链路、容量与恢复要求以 [ADR-074](../99-决策记录-ADR/ADR-074-公司内部附件存储与原件保留.md) 为准。真实 PostgreSQL 回归为 `AttachmentStorageIdentityMigrationPostgresTest`，从包含附件、会话、删除意图与孤儿证据的 V532 数据向前迁移。
