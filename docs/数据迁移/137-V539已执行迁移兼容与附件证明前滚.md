# V539 已执行迁移兼容与附件证明前滚

当前迁移目录为 **V539 / 498 条迁移**。不新增业务表、权限或金额字段；清理分类仍为 CLEAR 266 张、PRESERVE 96 张。

## 启动失败与修正

本机数据库已经执行的 V537 校验值为 `-290329290`，开发期间在同一 SQL 文件内追加两项附件检查后，源码校验值变为 `-2018214118`，Flyway 因历史文件不一致拒绝启动。这是已执行迁移文件被改动导致的兼容问题，不是业务记录损坏。

V537 已恢复首次执行内容和原校验值；新增 `V539__attachment_reset_completion_guards_forward.sql` 以 `CREATE OR REPLACE FUNCTION` 更新统一附件完成证明。现有库正常前滚，全新库依次执行原 V537、V538、V539，最终函数一致。禁止手工改 `flyway_schema_history`、运行 `repair` 或删除迁移记录来掩盖问题。

## 前向检查

- 临时文件删除证明除提供方、键和成功时间外，若上传会话记有 `staging_version`，删除队列的版本必须一致。历史会话未记录版本时保持原兼容规则。
- 删除队列即使标为 `SUCCEEDED`，缺少 `completed_at` 仍属于未完成，不能据此清空测试业务。

V539 仅替换 `fn_business_attachment_reset_blockers()`；V537 已完成的运行时函数挂接不重复执行，不改旧单据、附件元数据、文件本体、删除队列或审计记录。实际版本身份、原件删除和凭证到期的其他检查保持。

## 实现与验证

迁移位于 `server/src/main/resources/db/migration/`。`server/ops/reset_business_data.sql` 增加 V539/498 白名单，`MigrationRehearsalSupport` 与两个清理脚本契约同步。完整附件操作说明见 [V537](135-V537业务附件清理完成证明.md)；主仓数量和实际子仓成本规则继续见 [V538](136-V538当前主仓统一预算说明.md)。

本次将发生启动错误的实际 V537 数据库备份恢复到隔离新库，正常应用 V538、V539 后 `migrate` 和 `validate` 均通过，361 张非 Flyway 表的完整行摘要一致，业务、附件及审计记录保持原样。空库累计 498 条迁移验证通过，最终完成证明函数与实际旧库前滚结果一致；没有使用 `repair`。备份和具体执行证据由统一发布记录保存；源码修正及隔离测试不等于公司服务器已完成升级。
